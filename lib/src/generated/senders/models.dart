// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `POST /senders` object of the Brevo API.
final class CreateSenderResponse {
  /// Creates a CreateSenderResponse from already-decoded fields.
  const CreateSenderResponse({
    this.dkimError,
    this.id,
    this.spfError,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateSenderResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateSenderResponse.fromJson(Map<String, Object?> json) =>
      CreateSenderResponse(
        dkimError: json.optBool('dkimError'),
        id: json.optInt('id'),
        spfError: json.optBool('spfError'),
        raw: json,
      );

  /// Status of DKIM configuration for the sender (true = DKIM not
  /// well configured, false = DKIM well configured)
  final bool? dkimError;

  /// ID of the Sender created
  final int? id;

  /// Status of SPF configuration for the sender (true = SPF not
  /// well configured, false = SPF well configured)
  final bool? spfError;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (dkimError != null) 'dkimError': dkimError!,
        if (id != null) 'id': id!,
        if (spfError != null) 'spfError': spfError!,
      };
}

/// The `GET /senders/{senderId}/ips` object of the Brevo API.
final class GetIpsFromSenderResponse {
  /// Creates a GetIpsFromSenderResponse from already-decoded fields.
  const GetIpsFromSenderResponse({
    this.ips = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetIpsFromSenderResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetIpsFromSenderResponse.fromJson(Map<String, Object?> json) =>
      GetIpsFromSenderResponse(
        ips: json.optObjectList('ips', GetIpsFromSenderResponseIpsItem.fromJson,
            objectName: 'GetIpsFromSenderResponse'),
        raw: json,
      );

  /// Dedicated IP(s) linked to a sender. Returns empty array for standard accounts
  /// without dedicated IPs.
  final List<GetIpsFromSenderResponseIpsItem> ips;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (ips.isNotEmpty) 'ips': [for (final item in ips) item.toJson()],
      };
}

/// The `GetIpsFromSenderResponse.ipsItem` object of the Brevo API.
final class GetIpsFromSenderResponseIpsItem {
  /// Creates a GetIpsFromSenderResponseIpsItem from already-decoded fields.
  const GetIpsFromSenderResponseIpsItem({
    this.domain,
    this.id,
    this.ip,
    this.weight,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetIpsFromSenderResponseIpsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetIpsFromSenderResponseIpsItem.fromJson(Map<String, Object?> json) =>
      GetIpsFromSenderResponseIpsItem(
        domain: json.optString('domain'),
        id: json.optInt('id'),
        ip: json.optString('ip'),
        weight: json.optInt('weight'),
        raw: json,
      );

  /// Domain associated to the IP
  final String? domain;

  /// ID of the dedicated IP
  final int? id;

  /// Dedicated IP address
  final String? ip;

  /// Weight of the IP for this sender
  final int? weight;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (domain != null) 'domain': domain!,
        if (id != null) 'id': id!,
        if (ip != null) 'ip': ip!,
        if (weight != null) 'weight': weight!,
      };
}

/// The `GET /senders/ips` object of the Brevo API.
final class GetIpsResponse {
  /// Creates a GetIpsResponse from already-decoded fields.
  const GetIpsResponse({
    this.ips = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetIpsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetIpsResponse.fromJson(Map<String, Object?> json) => GetIpsResponse(
        ips: json.optObjectList('ips', GetIpsResponseIpsItem.fromJson,
            objectName: 'GetIpsResponse'),
        raw: json,
      );

  /// Dedicated IP(s) available on your account. Returns empty array
  /// for standard accounts without dedicated IPs.
  final List<GetIpsResponseIpsItem> ips;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (ips.isNotEmpty) 'ips': [for (final item in ips) item.toJson()],
      };
}

/// The `GetIpsResponse.ipsItem` object of the Brevo API.
final class GetIpsResponseIpsItem {
  /// Creates a GetIpsResponseIpsItem from already-decoded fields.
  const GetIpsResponseIpsItem({
    this.active,
    this.domain,
    this.id,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetIpsResponseIpsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetIpsResponseIpsItem.fromJson(Map<String, Object?> json) =>
      GetIpsResponseIpsItem(
        active: json.optBool('active'),
        domain: json.optString('domain'),
        id: json.optInt('id'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// Status of the IP (true=active, false=inactive)
  final bool? active;

  /// Domain associated to the IP
  final String? domain;

  /// ID of the dedicated IP
  final int? id;

  /// Dedicated IP address
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (active != null) 'active': active!,
        if (domain != null) 'domain': domain!,
        if (id != null) 'id': id!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `GET /senders` object of the Brevo API.
final class GetSendersResponse {
  /// Creates a GetSendersResponse from already-decoded fields.
  const GetSendersResponse({
    this.senders = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSendersResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSendersResponse.fromJson(Map<String, Object?> json) =>
      GetSendersResponse(
        senders: json.optObjectList(
            'senders', GetSendersResponseSendersItem.fromJson,
            objectName: 'GetSendersResponse'),
        raw: json,
      );

  /// List of the senders available in your account
  final List<GetSendersResponseSendersItem> senders;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (senders.isNotEmpty)
          'senders': [for (final item in senders) item.toJson()],
      };
}

/// The `GetSendersResponse.sendersItem` object of the Brevo API.
final class GetSendersResponseSendersItem {
  /// Creates a GetSendersResponseSendersItem from already-decoded fields.
  const GetSendersResponseSendersItem({
    this.active,
    this.email,
    this.id,
    this.ips = const [],
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSendersResponseSendersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSendersResponseSendersItem.fromJson(Map<String, Object?> json) =>
      GetSendersResponseSendersItem(
        active: json.optBool('active'),
        email: json.optString('email'),
        id: json.optInt('id'),
        ips: json.optObjectList(
            'ips', GetSendersResponseSendersItemIpsItem.fromJson,
            objectName: 'GetSendersResponseSendersItem'),
        name: json.optString('name'),
        raw: json,
      );

  /// Status of sender (true=activated, false=deactivated)
  final bool? active;

  /// From Email associated to the sender
  final String? email;

  /// Id of the sender
  final int? id;

  /// List of dedicated IP(s) available in the account. This
  /// data is displayed only for dedicated IPs. For standard accounts,
  /// this will be an empty array.
  final List<GetSendersResponseSendersItemIpsItem> ips;

  /// From Name associated to the sender
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (active != null) 'active': active!,
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (ips.isNotEmpty) 'ips': [for (final item in ips) item.toJson()],
        if (name != null) 'name': name!,
      };
}

/// The `GetSendersResponseSendersItem.ipsItem` object of the Brevo API.
final class GetSendersResponseSendersItemIpsItem {
  /// Creates a GetSendersResponseSendersItemIpsItem from already-decoded fields.
  const GetSendersResponseSendersItemIpsItem({
    this.domain,
    this.ip,
    this.weight,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSendersResponseSendersItemIpsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSendersResponseSendersItemIpsItem.fromJson(
          Map<String, Object?> json) =>
      GetSendersResponseSendersItemIpsItem(
        domain: json.optString('domain'),
        ip: json.optString('ip'),
        weight: json.optInt('weight'),
        raw: json,
      );

  /// Domain of the IP
  final String? domain;

  /// Dedicated IP available in your account
  final String? ip;

  /// Weight of the IP for this sender
  final int? weight;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (domain != null) 'domain': domain!,
        if (ip != null) 'ip': ip!,
        if (weight != null) 'weight': weight!,
      };
}
