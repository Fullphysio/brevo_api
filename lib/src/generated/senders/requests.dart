// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The request body of `POST /senders`.
final class CreateSenderRequest {
  /// Creates the request body.
  const CreateSenderRequest({
    required this.email,
    this.ips,
    required this.name,
  });

  /// Decodes a JSON object into a CreateSenderRequest. Optional fields absent from [json] stay null.
  factory CreateSenderRequest.fromJson(Map<String, Object?> json) =>
      CreateSenderRequest(
        email: json.requireString('email', 'CreateSenderRequest'),
        ips: json.containsKey('ips')
            ? json.optObjectList('ips', CreateSenderRequestIpsItem.fromJson,
                objectName: 'CreateSenderRequest')
            : null,
        name: json.requireString('name', 'CreateSenderRequest'),
      );

  /// From email to use for the sender. A verification email will be
  /// sent to this address.
  final String email;

  /// **Mandatory in case of dedicated IP**. IPs to associate to the
  /// sender. Not required for standard accounts.
  final List<CreateSenderRequestIpsItem>? ips;

  /// From Name to use for the sender
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'email': email,
        if (ips != null) 'ips': [for (final item in ips!) item.toJson()],
        'name': name,
      };
}

/// The request body of `CreateSenderRequest.ipsItem`.
final class CreateSenderRequestIpsItem {
  /// Creates the request body.
  const CreateSenderRequestIpsItem({
    required this.domain,
    required this.ip,
    this.weight,
  });

  /// Decodes a JSON object into a CreateSenderRequestIpsItem. Optional fields absent from [json] stay null.
  factory CreateSenderRequestIpsItem.fromJson(Map<String, Object?> json) =>
      CreateSenderRequestIpsItem(
        domain: json.requireString('domain', 'CreateSenderRequestIpsItem'),
        ip: json.requireString('ip', 'CreateSenderRequestIpsItem'),
        weight: json.optInt('weight'),
      );

  /// Domain of the IP
  final String domain;

  /// Dedicated IP available in your account
  final String ip;

  /// Weight to apply to the IP. Sum of all IP weights must be
  /// 100. Should be passed for either ALL or NONE of the IPs.
  /// If it's not passed, the sending will be equally balanced
  /// on all IPs.
  final int? weight;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'domain': domain,
        'ip': ip,
        if (weight != null) 'weight': weight!,
      };
}

/// The request body of `PUT /senders/{senderId}`.
final class UpdateSenderRequest {
  /// Creates the request body.
  const UpdateSenderRequest({
    this.email,
    this.ips,
    this.name,
  });

  /// Decodes a JSON object into a UpdateSenderRequest. Optional fields absent from [json] stay null.
  factory UpdateSenderRequest.fromJson(Map<String, Object?> json) =>
      UpdateSenderRequest(
        email: json.optString('email'),
        ips: json.containsKey('ips')
            ? json.optObjectList('ips', UpdateSenderRequestIpsItem.fromJson,
                objectName: 'UpdateSenderRequest')
            : null,
        name: json.optString('name'),
      );

  /// From Email to update the sender
  final String? email;

  /// **Only in case of dedicated IP**. IPs to associate to the
  /// sender. If passed, will replace all the existing IPs. Not required for standard accounts.
  final List<UpdateSenderRequestIpsItem>? ips;

  /// From Name to update the sender
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (ips != null) 'ips': [for (final item in ips!) item.toJson()],
        if (name != null) 'name': name!,
      };
}

/// The request body of `UpdateSenderRequest.ipsItem`.
final class UpdateSenderRequestIpsItem {
  /// Creates the request body.
  const UpdateSenderRequestIpsItem({
    required this.domain,
    required this.ip,
    this.weight,
  });

  /// Decodes a JSON object into a UpdateSenderRequestIpsItem. Optional fields absent from [json] stay null.
  factory UpdateSenderRequestIpsItem.fromJson(Map<String, Object?> json) =>
      UpdateSenderRequestIpsItem(
        domain: json.requireString('domain', 'UpdateSenderRequestIpsItem'),
        ip: json.requireString('ip', 'UpdateSenderRequestIpsItem'),
        weight: json.optInt('weight'),
      );

  /// Domain of the IP
  final String domain;

  /// Dedicated IP available in your account
  final String ip;

  /// Weight to apply to the IP. Sum of all IP weights must be
  /// 100. Should be passed for either ALL or NONE of the IPs.
  /// If it's not passed, the sending will be equally balanced
  /// on all IPs.
  final int? weight;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'domain': domain,
        'ip': ip,
        if (weight != null) 'weight': weight!,
      };
}

/// The request body of `PUT /senders/{senderId}/validate`.
final class ValidateSenderByOtpRequest {
  /// Creates the request body.
  const ValidateSenderByOtpRequest({
    required this.otp,
  });

  /// Decodes a JSON object into a ValidateSenderByOtpRequest. Optional fields absent from [json] stay null.
  factory ValidateSenderByOtpRequest.fromJson(Map<String, Object?> json) =>
      ValidateSenderByOtpRequest(
        otp: json.requireInt('otp', 'ValidateSenderByOtpRequest'),
      );

  /// 6 digit OTP received on email
  final int otp;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'otp': otp,
      };
}
