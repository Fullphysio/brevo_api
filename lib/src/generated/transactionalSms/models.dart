// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /transactionalSMS/statistics/events` object of the Brevo API.
final class GetSmsEventsResponse {
  /// Creates a GetSmsEventsResponse from already-decoded fields.
  const GetSmsEventsResponse({
    this.events = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsEventsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsEventsResponse.fromJson(Map<String, Object?> json) =>
      GetSmsEventsResponse(
        events: json.optObjectList(
            'events', GetSmsEventsResponseEventsItem.fromJson,
            objectName: 'GetSmsEventsResponse'),
        raw: json,
      );

  /// The `events` field.
  final List<GetSmsEventsResponseEventsItem> events;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
      };
}

/// The `GetSmsEventsResponse.eventsItem` object of the Brevo API.
final class GetSmsEventsResponseEventsItem {
  /// Creates a GetSmsEventsResponseEventsItem from already-decoded fields.
  const GetSmsEventsResponseEventsItem({
    this.date,
    this.event,
    this.messageId,
    this.phoneNumber,
    this.reason,
    this.reply,
    this.tag,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsEventsResponseEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsEventsResponseEventsItem.fromJson(Map<String, Object?> json) =>
      GetSmsEventsResponseEventsItem(
        date: json.optString('date'),
        event:
            json.optEnum('event', GetSmsEventsResponseEventsItemEvent.fromWire),
        messageId: json.optString('messageId'),
        phoneNumber: json.optString('phoneNumber'),
        reason: json.optString('reason'),
        reply: json.optString('reply'),
        tag: json.optString('tag'),
        raw: json,
      );

  /// UTC date-time on which the event has been generated
  final String? date;

  /// Event which occurred
  final GetSmsEventsResponseEventsItemEvent? event;

  /// Message ID which generated the event
  final String? messageId;

  /// Phone number which has generated the event
  final String? phoneNumber;

  /// Reason of bounce (only available if the event is hardbounce or softbounce)
  final String? reason;

  /// The `reply` field.
  final String? reply;

  /// Tag of the SMS which generated the event
  final String? tag;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (date != null) 'date': date!,
        if (event != null) 'event': event!.value,
        if (messageId != null) 'messageId': messageId!,
        if (phoneNumber != null) 'phoneNumber': phoneNumber!,
        if (reason != null) 'reason': reason!,
        if (reply != null) 'reply': reply!,
        if (tag != null) 'tag': tag!,
      };
}

/// The `GET /transactionalSMS/statistics/aggregatedReport` object of the Brevo API.
final class GetTransacAggregatedSmsReportResponse {
  /// Creates a GetTransacAggregatedSmsReportResponse from already-decoded fields.
  const GetTransacAggregatedSmsReportResponse({
    this.accepted,
    this.blocked,
    this.delivered,
    this.hardBounces,
    this.range,
    this.rejected,
    this.replied,
    this.requests,
    this.skipped,
    this.softBounces,
    this.unsubscribed,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacAggregatedSmsReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacAggregatedSmsReportResponse.fromJson(
          Map<String, Object?> json) =>
      GetTransacAggregatedSmsReportResponse(
        accepted: json.optInt('accepted'),
        blocked: json.optInt('blocked'),
        delivered: json.optInt('delivered'),
        hardBounces: json.optInt('hardBounces'),
        range: json.optString('range'),
        rejected: json.optInt('rejected'),
        replied: json.optInt('replied'),
        requests: json.optInt('requests'),
        skipped: json.optInt('skipped'),
        softBounces: json.optInt('softBounces'),
        unsubscribed: json.optInt('unsubscribed'),
        raw: json,
      );

  /// Number of accepted SMS for the timeframe
  final int? accepted;

  /// Number of blocked contact for the timeframe
  final int? blocked;

  /// Number of delivered SMS for the timeframe
  final int? delivered;

  /// Number of hardbounces for the timeframe
  final int? hardBounces;

  /// Time frame of the report
  final String? range;

  /// Number of rejected SMS for the timeframe
  final int? rejected;

  /// Number of answered SMS for the timeframe
  final int? replied;

  /// Number of requests for the timeframe
  final int? requests;

  /// Number of skipped SMS for the timeframe
  final int? skipped;

  /// Number of softbounces for the timeframe
  final int? softBounces;

  /// Number of unsubscription for the timeframe
  final int? unsubscribed;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (accepted != null) 'accepted': accepted!,
        if (blocked != null) 'blocked': blocked!,
        if (delivered != null) 'delivered': delivered!,
        if (hardBounces != null) 'hardBounces': hardBounces!,
        if (range != null) 'range': range!,
        if (rejected != null) 'rejected': rejected!,
        if (replied != null) 'replied': replied!,
        if (requests != null) 'requests': requests!,
        if (skipped != null) 'skipped': skipped!,
        if (softBounces != null) 'softBounces': softBounces!,
        if (unsubscribed != null) 'unsubscribed': unsubscribed!,
      };
}

/// The `GET /transactionalSMS/statistics/reports` object of the Brevo API.
final class GetTransacSmsReportResponse {
  /// Creates a GetTransacSmsReportResponse from already-decoded fields.
  const GetTransacSmsReportResponse({
    this.reports = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacSmsReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacSmsReportResponse.fromJson(Map<String, Object?> json) =>
      GetTransacSmsReportResponse(
        reports: json.optObjectList(
            'reports', GetTransacSmsReportResponseReportsItem.fromJson,
            objectName: 'GetTransacSmsReportResponse'),
        raw: json,
      );

  /// The `reports` field.
  final List<GetTransacSmsReportResponseReportsItem> reports;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (reports.isNotEmpty)
          'reports': [for (final item in reports) item.toJson()],
      };
}

/// The `GetTransacSmsReportResponse.reportsItem` object of the Brevo API.
final class GetTransacSmsReportResponseReportsItem {
  /// Creates a GetTransacSmsReportResponseReportsItem from already-decoded fields.
  const GetTransacSmsReportResponseReportsItem({
    this.accepted,
    this.blocked,
    this.date,
    this.delivered,
    this.hardBounces,
    this.rejected,
    this.replied,
    this.requests,
    this.skipped,
    this.softBounces,
    this.unsubscribed,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacSmsReportResponseReportsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacSmsReportResponseReportsItem.fromJson(
          Map<String, Object?> json) =>
      GetTransacSmsReportResponseReportsItem(
        accepted: json.optInt('accepted'),
        blocked: json.optInt('blocked'),
        date: json.optString('date'),
        delivered: json.optInt('delivered'),
        hardBounces: json.optInt('hardBounces'),
        rejected: json.optInt('rejected'),
        replied: json.optInt('replied'),
        requests: json.optInt('requests'),
        skipped: json.optInt('skipped'),
        softBounces: json.optInt('softBounces'),
        unsubscribed: json.optInt('unsubscribed'),
        raw: json,
      );

  /// Number of accepted SMS for the date
  final int? accepted;

  /// Number of blocked contact for the date
  final int? blocked;

  /// Date for which statistics are retrieved
  final String? date;

  /// [date] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get dateDate => date == null ? null : DateTime.tryParse(date!);

  /// Number of delivered SMS for the date
  final int? delivered;

  /// Number of hardbounces for the date
  final int? hardBounces;

  /// Number of rejected SMS for the date
  final int? rejected;

  /// Number of answered SMS for the date
  final int? replied;

  /// Number of requests for the date
  final int? requests;

  /// Number of skipped SMS for the date
  final int? skipped;

  /// Number of softbounces for the date
  final int? softBounces;

  /// Number of unsubscription for the date
  final int? unsubscribed;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (accepted != null) 'accepted': accepted!,
        if (blocked != null) 'blocked': blocked!,
        if (date != null) 'date': date!,
        if (delivered != null) 'delivered': delivered!,
        if (hardBounces != null) 'hardBounces': hardBounces!,
        if (rejected != null) 'rejected': rejected!,
        if (replied != null) 'replied': replied!,
        if (requests != null) 'requests': requests!,
        if (skipped != null) 'skipped': skipped!,
        if (softBounces != null) 'softBounces': softBounces!,
        if (unsubscribed != null) 'unsubscribed': unsubscribed!,
      };
}

/// The `POST /transactionalSMS/send` object of the Brevo API.
final class SendAsyncTransactionalSmsResponse {
  /// Creates a SendAsyncTransactionalSmsResponse from already-decoded fields.
  const SendAsyncTransactionalSmsResponse({
    this.messageId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SendAsyncTransactionalSmsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SendAsyncTransactionalSmsResponse.fromJson(
          Map<String, Object?> json) =>
      SendAsyncTransactionalSmsResponse(
        messageId: json.optInt('messageId'),
        raw: json,
      );

  /// The `messageId` field.
  final int? messageId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (messageId != null) 'messageId': messageId!,
      };
}

/// The `POST /transactionalSMS/sms` object of the Brevo API.
final class SendTransacSmsResponse {
  /// Creates a SendTransacSmsResponse from already-decoded fields.
  const SendTransacSmsResponse({
    this.messageId,
    this.reference,
    this.remainingCredits,
    this.smsCount,
    this.usedCredits,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SendTransacSmsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SendTransacSmsResponse.fromJson(Map<String, Object?> json) =>
      SendTransacSmsResponse(
        messageId: json.optInt('messageId'),
        reference: json.optString('reference'),
        remainingCredits: json.optDouble('remainingCredits'),
        smsCount: json.optInt('smsCount'),
        usedCredits: json.optDouble('usedCredits'),
        raw: json,
      );

  /// The `messageId` field.
  final int? messageId;

  /// The `reference` field.
  final String? reference;

  /// Remaining SMS credits of the user
  final double? remainingCredits;

  /// Count of SMS's to send multiple text messages
  final int? smsCount;

  /// SMS credits used per text message
  final double? usedCredits;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (messageId != null) 'messageId': messageId!,
        if (reference != null) 'reference': reference!,
        if (remainingCredits != null) 'remainingCredits': remainingCredits!,
        if (smsCount != null) 'smsCount': smsCount!,
        if (usedCredits != null) 'usedCredits': usedCredits!,
      };
}
