// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /whatsappCampaigns/{campaignId}` object of the Brevo API.
final class GetWhatsAppCampaignResponse {
  /// Creates a GetWhatsAppCampaignResponse from already-decoded fields.
  const GetWhatsAppCampaignResponse({
    this.campaignName,
    this.campaignStatus,
    this.createdAt,
    this.id,
    this.modifiedAt,
    this.recipients,
    this.scheduledAt,
    this.senderNumber,
    this.stats,
    this.template,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignResponse.fromJson(Map<String, Object?> json) =>
      GetWhatsAppCampaignResponse(
        campaignName: json.optString('campaignName'),
        campaignStatus: json.optEnum('campaignStatus',
            GetWhatsAppCampaignResponseCampaignStatus.fromWire),
        createdAt: json.optString('createdAt'),
        id: json.optInt('id'),
        modifiedAt: json.optString('modifiedAt'),
        recipients: json.optNested(
            'recipients', GetWhatsAppCampaignResponseRecipients.fromJson),
        scheduledAt: json.optString('scheduledAt'),
        senderNumber: json.optString('senderNumber'),
        stats: json.optNested('stats', WhatsappCampStats.fromJson),
        template: json.optNested(
            'template', GetWhatsAppCampaignResponseTemplate.fromJson),
        raw: json,
      );

  /// Name of the WhatsApp Campaign
  final String? campaignName;

  /// Status of the WhatsApp Campaign
  final GetWhatsAppCampaignResponseCampaignStatus? campaignStatus;

  /// Creation UTC date-time of the WhatsApp campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// ID of the WhatsApp Campaign
  final int? id;

  /// UTC date-time of last modification of the WhatsApp campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Recipients of the WhatsApp Campaign
  final GetWhatsAppCampaignResponseRecipients? recipients;

  /// UTC date-time on which WhatsApp campaign is scheduled. Should be in YYYY-MM-DDTHH:mm:ss.SSSZ format
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Sender of the WhatsApp Campaign
  final String? senderNumber;

  /// The `stats` field.
  final WhatsappCampStats? stats;

  /// The `template` field.
  final GetWhatsAppCampaignResponseTemplate? template;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignName != null) 'campaignName': campaignName!,
        if (campaignStatus != null) 'campaignStatus': campaignStatus!.value,
        if (createdAt != null) 'createdAt': createdAt!,
        if (id != null) 'id': id!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (senderNumber != null) 'senderNumber': senderNumber!,
        if (stats != null) 'stats': stats!.toJson(),
        if (template != null) 'template': template!.toJson(),
      };
}

/// Recipients of the WhatsApp Campaign
final class GetWhatsAppCampaignResponseRecipients {
  /// Creates a GetWhatsAppCampaignResponseRecipients from already-decoded fields.
  const GetWhatsAppCampaignResponseRecipients({
    this.excludedLists = const [],
    this.includedLists = const [],
    this.segments = const [],
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignResponseRecipients. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignResponseRecipients.fromJson(
          Map<String, Object?> json) =>
      GetWhatsAppCampaignResponseRecipients(
        excludedLists: json.optIntList('excludedLists'),
        includedLists: json.optIntList('includedLists'),
        segments: json.optIntList('segments'),
        type: json.optString('type'),
        raw: json,
      );

  /// List of excluded list IDs
  final List<int> excludedLists;

  /// List of included list IDs
  final List<int> includedLists;

  /// List of segment IDs
  final List<int> segments;

  /// Type of recipients (list or segment)
  final String? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (excludedLists.isNotEmpty) 'excludedLists': excludedLists,
        if (includedLists.isNotEmpty) 'includedLists': includedLists,
        if (segments.isNotEmpty) 'segments': segments,
        if (type != null) 'type': type!,
      };
}

/// The `GetWhatsAppCampaignResponse.template` object of the Brevo API.
final class GetWhatsAppCampaignResponseTemplate {
  /// Creates a GetWhatsAppCampaignResponseTemplate from already-decoded fields.
  const GetWhatsAppCampaignResponseTemplate({
    this.bodyVariables = const [],
    this.buttonType,
    this.category,
    this.components = const [],
    this.containsButton,
    this.displayHeader,
    this.headerType,
    this.headerVariables = const [],
    this.hideFooter,
    this.language,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignResponseTemplate. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignResponseTemplate.fromJson(
          Map<String, Object?> json) =>
      GetWhatsAppCampaignResponseTemplate(
        bodyVariables: json.optObjectList(
            'body_variables', VariablesItems.fromJson,
            objectName: 'GetWhatsAppCampaignResponseTemplate'),
        buttonType: json.optString('button_type'),
        category: json.optString('category'),
        components: json.optObjectList('components',
            GetWhatsAppCampaignResponseTemplateComponentsItem.fromJson,
            objectName: 'GetWhatsAppCampaignResponseTemplate'),
        containsButton: json.optBool('contains_button'),
        displayHeader: json.optBool('display_header'),
        headerType: json.optString('header_type'),
        headerVariables: json.optObjectList(
            'header_variables', VariablesItems.fromJson,
            objectName: 'GetWhatsAppCampaignResponseTemplate'),
        hideFooter: json.optBool('hide_footer'),
        language: json.optString('language'),
        name: json.optString('name'),
        raw: json,
      );

  /// array of variables item variables
  final List<VariablesItems> bodyVariables;

  /// The `button_type` field.
  final String? buttonType;

  /// description of the template
  final String? category;

  /// array of component item objects
  final List<GetWhatsAppCampaignResponseTemplateComponentsItem> components;

  /// The `contains_button` field.
  final bool? containsButton;

  /// The `display_header` field.
  final bool? displayHeader;

  /// type of header
  final String? headerType;

  /// array of variables item object
  final List<VariablesItems> headerVariables;

  /// The `hide_footer` field.
  final bool? hideFooter;

  /// language of the template
  final String? language;

  /// name of the template
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (bodyVariables.isNotEmpty)
          'body_variables': [for (final item in bodyVariables) item.toJson()],
        if (buttonType != null) 'button_type': buttonType!,
        if (category != null) 'category': category!,
        if (components.isNotEmpty)
          'components': [for (final item in components) item.toJson()],
        if (containsButton != null) 'contains_button': containsButton!,
        if (displayHeader != null) 'display_header': displayHeader!,
        if (headerType != null) 'header_type': headerType!,
        if (headerVariables.isNotEmpty)
          'header_variables': [
            for (final item in headerVariables) item.toJson()
          ],
        if (hideFooter != null) 'hide_footer': hideFooter!,
        if (language != null) 'language': language!,
        if (name != null) 'name': name!,
      };
}

/// The `GetWhatsAppCampaignResponseTemplate.componentsItem` object of the Brevo API.
final class GetWhatsAppCampaignResponseTemplateComponentsItem {
  /// Creates a GetWhatsAppCampaignResponseTemplateComponentsItem from already-decoded fields.
  const GetWhatsAppCampaignResponseTemplateComponentsItem({
    this.text,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignResponseTemplateComponentsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignResponseTemplateComponentsItem.fromJson(
          Map<String, Object?> json) =>
      GetWhatsAppCampaignResponseTemplateComponentsItem(
        text: json.optString('text'),
        type: json.optString('type'),
        raw: json,
      );

  /// The `text` field.
  final String? text;

  /// The `type` field.
  final String? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (text != null) 'text': text!,
        if (type != null) 'type': type!,
      };
}

/// The `GET /whatsappCampaigns` object of the Brevo API.
final class GetWhatsAppCampaignsResponse {
  /// Creates a GetWhatsAppCampaignsResponse from already-decoded fields.
  const GetWhatsAppCampaignsResponse({
    this.campaigns = const [],
    this.count,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignsResponse.fromJson(Map<String, Object?> json) =>
      GetWhatsAppCampaignsResponse(
        campaigns: json.optObjectList(
            'campaigns', GetWhatsAppCampaignsResponseCampaignsItem.fromJson,
            objectName: 'GetWhatsAppCampaignsResponse'),
        count: json.optInt('count'),
        raw: json,
      );

  /// The `campaigns` field.
  final List<GetWhatsAppCampaignsResponseCampaignsItem> campaigns;

  /// Number of WhatsApp campaigns retrieved
  final int? count;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaigns.isNotEmpty)
          'campaigns': [for (final item in campaigns) item.toJson()],
        if (count != null) 'count': count!,
      };
}

/// The `GetWhatsAppCampaignsResponse.campaignsItem` object of the Brevo API.
final class GetWhatsAppCampaignsResponseCampaignsItem {
  /// Creates a GetWhatsAppCampaignsResponseCampaignsItem from already-decoded fields.
  const GetWhatsAppCampaignsResponseCampaignsItem({
    this.campaignName,
    this.campaignStatus,
    this.createdAt,
    this.errorReason,
    this.id,
    this.invalidatedContacts,
    this.modifiedAt,
    this.readPercentage,
    this.scheduledAt,
    this.stats,
    this.templateId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppCampaignsResponseCampaignsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppCampaignsResponseCampaignsItem.fromJson(
          Map<String, Object?> json) =>
      GetWhatsAppCampaignsResponseCampaignsItem(
        campaignName: json.optString('campaignName'),
        campaignStatus: json.optEnum('campaignStatus',
            GetWhatsAppCampaignsResponseCampaignsItemCampaignStatus.fromWire),
        createdAt: json.optString('createdAt'),
        errorReason: json.optString('errorReason'),
        id: json.optInt('id'),
        invalidatedContacts: json.optInt('invalidatedContacts'),
        modifiedAt: json.optString('modifiedAt'),
        readPercentage: json.optDouble('readPercentage'),
        scheduledAt: json.optString('scheduledAt'),
        stats: json.optNested('stats', WhatsappCampStats.fromJson),
        templateId: json.optString('templateId'),
        raw: json,
      );

  /// Name of the WhatsApp Campaign
  final String? campaignName;

  /// Status of the Whatsapp Campaign
  final GetWhatsAppCampaignsResponseCampaignsItemCampaignStatus? campaignStatus;

  /// Creation UTC date-time of the WhatsApp campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Error Reason associated with the WhatsApp campaign sending
  final String? errorReason;

  /// ID of the WhatsApp Campaign
  final int? id;

  /// Count of invalidated contacts
  final int? invalidatedContacts;

  /// UTC date-time of last modification of the WhatsApp campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Read percentage of the the whatsapp campaign created
  final double? readPercentage;

  /// UTC date-time on which WhatsApp campaign is scheduled. Should be in YYYY-MM-DDTHH:mm:ss.SSSZ format
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// The `stats` field.
  final WhatsappCampStats? stats;

  /// Id of the WhatsApp template
  final String? templateId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignName != null) 'campaignName': campaignName!,
        if (campaignStatus != null) 'campaignStatus': campaignStatus!.value,
        if (createdAt != null) 'createdAt': createdAt!,
        if (errorReason != null) 'errorReason': errorReason!,
        if (id != null) 'id': id!,
        if (invalidatedContacts != null)
          'invalidatedContacts': invalidatedContacts!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (readPercentage != null) 'readPercentage': readPercentage!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (stats != null) 'stats': stats!.toJson(),
        if (templateId != null) 'templateId': templateId!,
      };
}

/// The `GET /whatsappCampaigns/config` object of the Brevo API.
final class GetWhatsAppConfigResponse {
  /// Creates a GetWhatsAppConfigResponse from already-decoded fields.
  const GetWhatsAppConfigResponse({
    this.businessStatus,
    this.phoneNumberNameStatus,
    this.phoneNumberQuality,
    this.sendingLimit,
    this.whatsappBusinessAccountId,
    this.whatsappBusinessAccountStatus,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppConfigResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppConfigResponse.fromJson(Map<String, Object?> json) =>
      GetWhatsAppConfigResponse(
        businessStatus: json.optString('businessStatus'),
        phoneNumberNameStatus: json.optEnum('phoneNumberNameStatus',
            GetWhatsAppConfigResponsePhoneNumberNameStatus.fromWire),
        phoneNumberQuality: json.optEnum('phoneNumberQuality',
            GetWhatsAppConfigResponsePhoneNumberQuality.fromWire),
        sendingLimit: json.optString('sendingLimit'),
        whatsappBusinessAccountId: json.optString('whatsappBusinessAccountId'),
        whatsappBusinessAccountStatus: json.optEnum(
            'whatsappBusinessAccountStatus',
            GetWhatsAppConfigResponseWhatsappBusinessAccountStatus.fromWire),
        raw: json,
      );

  /// Verification status information of the Business account
  final String? businessStatus;

  /// Status of the name associated with WhatsApp Phone number
  final GetWhatsAppConfigResponsePhoneNumberNameStatus? phoneNumberNameStatus;

  /// Quality status of phone number associated with WhatsApp account. There are three quality ratings. example - **High (GREEN) , Medium (YELLOW) and Low(RED)**
  final GetWhatsAppConfigResponsePhoneNumberQuality? phoneNumberQuality;

  /// Sending limit of the WhatsApp account
  final String? sendingLimit;

  /// Id of the WhatsApp business account
  final String? whatsappBusinessAccountId;

  /// Status information related to WhatsApp Api account
  final GetWhatsAppConfigResponseWhatsappBusinessAccountStatus?
      whatsappBusinessAccountStatus;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (businessStatus != null) 'businessStatus': businessStatus!,
        if (phoneNumberNameStatus != null)
          'phoneNumberNameStatus': phoneNumberNameStatus!.value,
        if (phoneNumberQuality != null)
          'phoneNumberQuality': phoneNumberQuality!.value,
        if (sendingLimit != null) 'sendingLimit': sendingLimit!,
        if (whatsappBusinessAccountId != null)
          'whatsappBusinessAccountId': whatsappBusinessAccountId!,
        if (whatsappBusinessAccountStatus != null)
          'whatsappBusinessAccountStatus': whatsappBusinessAccountStatus!.value,
      };
}

/// The `GET /whatsappCampaigns/template-list` object of the Brevo API.
final class GetWhatsAppTemplatesResponse {
  /// Creates a GetWhatsAppTemplatesResponse from already-decoded fields.
  const GetWhatsAppTemplatesResponse({
    this.count,
    this.templates = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppTemplatesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppTemplatesResponse.fromJson(Map<String, Object?> json) =>
      GetWhatsAppTemplatesResponse(
        count: json.optInt('count'),
        templates: json.optObjectList(
            'templates', GetWhatsAppTemplatesResponseTemplatesItem.fromJson,
            objectName: 'GetWhatsAppTemplatesResponse'),
        raw: json,
      );

  /// Number of whatsApp templates retrieved
  final int? count;

  /// The `templates` field.
  final List<GetWhatsAppTemplatesResponseTemplatesItem> templates;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (templates.isNotEmpty)
          'templates': [for (final item in templates) item.toJson()],
      };
}

/// The `GetWhatsAppTemplatesResponse.templatesItem` object of the Brevo API.
final class GetWhatsAppTemplatesResponseTemplatesItem {
  /// Creates a GetWhatsAppTemplatesResponseTemplatesItem from already-decoded fields.
  const GetWhatsAppTemplatesResponseTemplatesItem({
    this.category,
    this.createdAt,
    this.errorReason,
    this.id,
    this.language,
    this.modifiedAt,
    this.name,
    this.status,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsAppTemplatesResponseTemplatesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsAppTemplatesResponseTemplatesItem.fromJson(
          Map<String, Object?> json) =>
      GetWhatsAppTemplatesResponseTemplatesItem(
        category: json.optString('category'),
        createdAt: json.optString('createdAt'),
        errorReason: json.optString('errorReason'),
        id: json.optInt('id'),
        language: json.optString('language'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        status: json.optString('status'),
        type: json.optString('type'),
        raw: json,
      );

  /// category of the template
  final String? category;

  /// Creation UTC date-time of the whatsApp template (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Error reason in the template creation
  final String? errorReason;

  /// ID of the whatsApp template
  final int? id;

  /// Language in which template exists
  final String? language;

  /// UTC date-time of last modification of the whatsApp template (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the whatsApp template
  final String? name;

  /// Status of the whatsApp template
  final String? status;

  /// Type of the whatsApp template
  final String? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (category != null) 'category': category!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (errorReason != null) 'errorReason': errorReason!,
        if (id != null) 'id': id!,
        if (language != null) 'language': language!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
      };
}

/// The `components/schemas/variablesItems` object of the Brevo API.
final class VariablesItems {
  /// Creates a VariablesItems from already-decoded fields.
  const VariablesItems({
    this.datatype,
    this.defaultValue,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a VariablesItems. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory VariablesItems.fromJson(Map<String, Object?> json) => VariablesItems(
        datatype: json.optString('datatype'),
        defaultValue: json.optString('default'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `datatype` field.
  final String? datatype;

  /// The `default` field.
  final String? defaultValue;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (datatype != null) 'datatype': datatype!,
        if (defaultValue != null) 'default': defaultValue!,
        if (name != null) 'name': name!,
      };
}

/// The `components/schemas/WhatsappCampStats` object of the Brevo API.
final class WhatsappCampStats {
  /// Creates a WhatsappCampStats from already-decoded fields.
  const WhatsappCampStats({
    this.delivered,
    this.notSent,
    this.read,
    this.sent,
    this.unsubscribe,
    this.raw = const {},
  });

  /// Decodes a JSON object into a WhatsappCampStats. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory WhatsappCampStats.fromJson(Map<String, Object?> json) =>
      WhatsappCampStats(
        delivered: json.optInt('delivered'),
        notSent: json.optInt('notSent'),
        read: json.optInt('read'),
        sent: json.optInt('sent'),
        unsubscribe: json.optInt('unsubscribe'),
        raw: json,
      );

  /// The `delivered` field.
  final int? delivered;

  /// The `notSent` field.
  final int? notSent;

  /// The `read` field.
  final int? read;

  /// The `sent` field.
  final int? sent;

  /// The `unsubscribe` field.
  final int? unsubscribe;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (delivered != null) 'delivered': delivered!,
        if (notSent != null) 'notSent': notSent!,
        if (read != null) 'read': read!,
        if (sent != null) 'sent': sent!,
        if (unsubscribe != null) 'unsubscribe': unsubscribe!,
      };
}
