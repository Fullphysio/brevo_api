// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /whatsappCampaigns`.
final class CreateWhatsAppCampaignRequest {
  /// Creates the request body.
  const CreateWhatsAppCampaignRequest({
    required this.name,
    required this.recipients,
    required this.scheduledAt,
    required this.templateId,
  });

  /// Decodes a JSON object into a CreateWhatsAppCampaignRequest. Optional fields absent from [json] stay null.
  factory CreateWhatsAppCampaignRequest.fromJson(Map<String, Object?> json) =>
      CreateWhatsAppCampaignRequest(
        name: json.requireString('name', 'CreateWhatsAppCampaignRequest'),
        recipients: json.optNested('recipients',
                CreateWhatsAppCampaignRequestRecipients.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateWhatsAppCampaignRequest',
                key: 'recipients')),
        scheduledAt:
            json.requireString('scheduledAt', 'CreateWhatsAppCampaignRequest'),
        templateId:
            json.requireInt('templateId', 'CreateWhatsAppCampaignRequest'),
      );

  /// Name of the WhatsApp campaign creation
  final String name;

  /// Segment ids and List ids to include/exclude from campaign
  final CreateWhatsAppCampaignRequestRecipients recipients;

  /// Sending UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ). **Prefer to pass your timezone in date-time format for accurate result.For example: **2017-06-01T12:30:00+02:00**
  final String scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate => DateTime.tryParse(scheduledAt);

  /// Id of the WhatsApp template in **approved** state
  final int templateId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'name': name,
        'recipients': recipients.toJson(),
        'scheduledAt': scheduledAt,
        'templateId': templateId,
      };
}

/// Segment ids and List ids to include/exclude from campaign
final class CreateWhatsAppCampaignRequestRecipients {
  /// Creates the request body.
  const CreateWhatsAppCampaignRequestRecipients({
    this.excludedListIds,
    this.listIds,
    this.segments,
  });

  /// Decodes a JSON object into a CreateWhatsAppCampaignRequestRecipients. Optional fields absent from [json] stay null.
  factory CreateWhatsAppCampaignRequestRecipients.fromJson(
          Map<String, Object?> json) =>
      CreateWhatsAppCampaignRequestRecipients(
        excludedListIds: json.containsKey('excludedListIds')
            ? json.optIntList('excludedListIds')
            : null,
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        segments:
            json.containsKey('segments') ? json.optIntList('segments') : null,
      );

  /// List ids to exclude from the campaign
  final List<int>? excludedListIds;

  /// **Mandatory if scheduledAt is not empty**. List Ids to send the campaign to
  final List<int>? listIds;

  /// **Mandatory if listIds are not used**. Segment ids to send the campaign to.
  final List<int>? segments;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (excludedListIds != null) 'excludedListIds': excludedListIds!,
        if (listIds != null) 'listIds': listIds!,
        if (segments != null) 'segments': segments!,
      };
}

/// The request body of `POST /whatsappCampaigns/template`.
final class CreateWhatsAppTemplateRequest {
  /// Creates the request body.
  const CreateWhatsAppTemplateRequest({
    required this.bodyText,
    required this.category,
    this.headerText,
    required this.language,
    this.mediaUrl,
    required this.name,
    this.source,
  });

  /// Decodes a JSON object into a CreateWhatsAppTemplateRequest. Optional fields absent from [json] stay null.
  factory CreateWhatsAppTemplateRequest.fromJson(Map<String, Object?> json) =>
      CreateWhatsAppTemplateRequest(
        bodyText:
            json.requireString('bodyText', 'CreateWhatsAppTemplateRequest'),
        category: json.optEnum(
                'category', CreateWhatsAppTemplateRequestCategory.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateWhatsAppTemplateRequest', key: 'category')),
        headerText: json.optString('headerText'),
        language:
            json.requireString('language', 'CreateWhatsAppTemplateRequest'),
        mediaUrl: json.optString('mediaUrl'),
        name: json.requireString('name', 'CreateWhatsAppTemplateRequest'),
        source: json.optEnum(
            'source', CreateWhatsAppTemplateRequestSource.fromWire),
      );

  /// Body of the template. **Maximum allowed characters are 1024**
  final String bodyText;

  /// Category of the template
  final CreateWhatsAppTemplateRequestCategory category;

  /// Text content of the header in the template.  **Maximum allowed characters are 45** **Use this field to add text content in template header and if mediaUrl is empty**
  final String? headerText;

  /// Language of the template. For Example :
  /// **en** for English
  final String language;

  /// Absolute url of the media file **(no local file)** for the header. **Use this field in you want to add media in Template header and headerText is empty** Allowed extensions for media files are: #### jpeg | png | mp4 | pdf
  final String? mediaUrl;

  /// Name of the template
  final String name;

  /// source of the template
  final CreateWhatsAppTemplateRequestSource? source;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'bodyText': bodyText,
        'category': category.value,
        if (headerText != null) 'headerText': headerText!,
        'language': language,
        if (mediaUrl != null) 'mediaUrl': mediaUrl!,
        'name': name,
        if (source != null) 'source': source!.value,
      };
}

/// The request body of `PUT /whatsappCampaigns/{campaignId}`.
final class UpdateWhatsAppCampaignRequest {
  /// Creates the request body.
  const UpdateWhatsAppCampaignRequest({
    this.campaignName,
    this.campaignStatus,
    this.recipients,
    this.rescheduleFor,
  });

  /// Decodes a JSON object into a UpdateWhatsAppCampaignRequest. Optional fields absent from [json] stay null.
  factory UpdateWhatsAppCampaignRequest.fromJson(Map<String, Object?> json) =>
      UpdateWhatsAppCampaignRequest(
        campaignName: json.optString('campaignName'),
        campaignStatus: json.optEnum('campaignStatus',
            UpdateWhatsAppCampaignRequestCampaignStatus.fromWire),
        recipients: json.optNested(
            'recipients', UpdateWhatsAppCampaignRequestRecipients.fromJson),
        rescheduleFor: json.optString('rescheduleFor'),
      );

  /// Name of the campaign
  final String? campaignName;

  /// Status of the campaign
  final UpdateWhatsAppCampaignRequestCampaignStatus? campaignStatus;

  /// Segment ids and List ids to include/exclude from campaign
  final UpdateWhatsAppCampaignRequestRecipients? recipients;

  /// Reschedule the sending UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) of campaign. **Prefer to pass your timezone in date-time format for accurate result.For example: **2017-06-01T12:30:00+02:00** Use this field to update the scheduledAt of any existing draft or scheduled WhatsApp campaign.
  final String? rescheduleFor;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (campaignName != null) 'campaignName': campaignName!,
        if (campaignStatus != null) 'campaignStatus': campaignStatus!.value,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (rescheduleFor != null) 'rescheduleFor': rescheduleFor!,
      };
}

/// Segment ids and List ids to include/exclude from campaign
final class UpdateWhatsAppCampaignRequestRecipients {
  /// Creates the request body.
  const UpdateWhatsAppCampaignRequestRecipients({
    this.excludedListIds,
    this.listIds,
    this.segments,
  });

  /// Decodes a JSON object into a UpdateWhatsAppCampaignRequestRecipients. Optional fields absent from [json] stay null.
  factory UpdateWhatsAppCampaignRequestRecipients.fromJson(
          Map<String, Object?> json) =>
      UpdateWhatsAppCampaignRequestRecipients(
        excludedListIds: json.containsKey('excludedListIds')
            ? json.optIntList('excludedListIds')
            : null,
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        segments:
            json.containsKey('segments') ? json.optIntList('segments') : null,
      );

  /// List ids to exclude from the campaign
  final List<int>? excludedListIds;

  /// **Mandatory if scheduledAt is not empty**. List Ids to send the campaign to
  final List<int>? listIds;

  /// **Mandatory if listIds are not used**. Segment ids to send the campaign to.
  final List<int>? segments;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (excludedListIds != null) 'excludedListIds': excludedListIds!,
        if (listIds != null) 'listIds': listIds!,
        if (segments != null) 'segments': segments!,
      };
}
