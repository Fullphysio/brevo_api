// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';

/// The request body of `POST /smtp/blockedDomains`.
final class BlockNewDomainRequest {
  /// Creates the request body.
  const BlockNewDomainRequest({
    required this.domain,
  });

  /// Decodes a JSON object into a BlockNewDomainRequest. Optional fields absent from [json] stay null.
  factory BlockNewDomainRequest.fromJson(Map<String, Object?> json) =>
      BlockNewDomainRequest(
        domain: json.requireString('domain', 'BlockNewDomainRequest'),
      );

  /// name of the domain to be blocked
  final String domain;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'domain': domain,
      };
}

/// The request body of `POST /smtp/templates`.
final class CreateSmtpTemplateRequest {
  /// Creates the request body.
  const CreateSmtpTemplateRequest({
    this.attachmentUrl,
    this.htmlContent,
    this.htmlUrl,
    this.isActive,
    this.replyTo,
    required this.sender,
    required this.subject,
    this.tag,
    required this.templateName,
    this.toField,
  });

  /// Decodes a JSON object into a CreateSmtpTemplateRequest. Optional fields absent from [json] stay null.
  factory CreateSmtpTemplateRequest.fromJson(Map<String, Object?> json) =>
      CreateSmtpTemplateRequest(
        attachmentUrl: json.optString('attachmentUrl'),
        htmlContent: json.optString('htmlContent'),
        htmlUrl: json.optString('htmlUrl'),
        isActive: json.optBool('isActive'),
        replyTo: json.optString('replyTo'),
        sender: json.optNested(
                'sender', CreateSmtpTemplateRequestSender.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateSmtpTemplateRequest', key: 'sender')),
        subject: json.requireString('subject', 'CreateSmtpTemplateRequest'),
        tag: json.optString('tag'),
        templateName:
            json.requireString('templateName', 'CreateSmtpTemplateRequest'),
        toField: json.optString('toField'),
      );

  /// Absolute url of the attachment (**no local file**). Extension allowed: #### xlsx, xls, ods, docx, docm, doc, csv, pdf, txt, gif, jpg, jpeg, png, tif, tiff, rtf, bmp, cgm, css, shtml, html, htm, zip, xml, ppt, pptx, tar, ez, ics, mobi, msg, pub and eps'
  final String? attachmentUrl;

  /// Body of the message (HTML version). The field must have more than 10 characters. **REQUIRED if htmlUrl is empty**
  final String? htmlContent;

  /// Url which contents the body of the email message. REQUIRED if htmlContent is empty
  final String? htmlUrl;

  /// Status of template. isActive = true means template is active and isActive = false means template is inactive
  final bool? isActive;

  /// Email on which campaign recipients will be able to reply to
  final String? replyTo;

  /// Sender details including id or email and name (_optional_). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
  final CreateSmtpTemplateRequestSender sender;

  /// Subject of the template
  final String subject;

  /// Tag of the template
  final String? tag;

  /// Name of the template
  final String templateName;

  /// To personalize the **To** Field. If you want to include the first name and last name of your recipient, add **{FNAME} {LNAME}**. These contact attributes must already exist in your Brevo account. If input parameter **params** used please use **{{contact.FNAME}} {{contact.LNAME}}** for personalization
  final String? toField;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attachmentUrl != null) 'attachmentUrl': attachmentUrl!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (htmlUrl != null) 'htmlUrl': htmlUrl!,
        if (isActive != null) 'isActive': isActive!,
        if (replyTo != null) 'replyTo': replyTo!,
        'sender': sender.toJson(),
        'subject': subject,
        if (tag != null) 'tag': tag!,
        'templateName': templateName,
        if (toField != null) 'toField': toField!,
      };
}

/// Sender details including id or email and name (_optional_). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
final class CreateSmtpTemplateRequestSender {
  /// Creates the request body.
  const CreateSmtpTemplateRequestSender({
    this.email,
    this.id,
    this.name,
  });

  /// Decodes a JSON object into a CreateSmtpTemplateRequestSender. Optional fields absent from [json] stay null.
  factory CreateSmtpTemplateRequestSender.fromJson(Map<String, Object?> json) =>
      CreateSmtpTemplateRequestSender(
        email: json.optString('email'),
        id: json.optInt('id'),
        name: json.optString('name'),
      );

  /// Email of the sender
  final String? email;

  /// Select the sender for the template on the basis of sender id. _In order to select a sender with specific pool of IP’s, dedicated ip users shall pass id (instead of email)_.
  final int? id;

  /// Name of the sender. **If not passed, will be set to default**
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `POST /smtp/deleteHardbounces`.
final class DeleteHardbouncesRequest {
  /// Creates the request body.
  const DeleteHardbouncesRequest({
    this.contactEmail,
    this.endDate,
    this.startDate,
  });

  /// Decodes a JSON object into a DeleteHardbouncesRequest. Optional fields absent from [json] stay null.
  factory DeleteHardbouncesRequest.fromJson(Map<String, Object?> json) =>
      DeleteHardbouncesRequest(
        contactEmail: json.optString('contactEmail'),
        endDate: json.optString('endDate'),
        startDate: json.optString('startDate'),
      );

  /// Target a specific email address
  final String? contactEmail;

  /// Ending date (YYYY-MM-DD) of the time period for deletion. The hardbounces until this date will be deleted. Must be greater than or equal to the startDate
  final String? endDate;

  /// [endDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get endDateDate =>
      endDate == null ? null : DateTime.tryParse(endDate!);

  /// Starting date (YYYY-MM-DD) of the time period for deletion. The hardbounces occurred after this date will be deleted. Must be less than or equal to the endDate
  final String? startDate;

  /// [startDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get startDateDate =>
      startDate == null ? null : DateTime.tryParse(startDate!);

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactEmail != null) 'contactEmail': contactEmail!,
        if (endDate != null) 'endDate': endDate!,
        if (startDate != null) 'startDate': startDate!,
      };
}

/// The request body of `POST /smtp/template/preview`.
final class PostPreviewSmtpEmailTemplatesRequest {
  /// Creates the request body.
  const PostPreviewSmtpEmailTemplatesRequest({
    this.email,
    this.params,
    required this.templateId,
  });

  /// Decodes a JSON object into a PostPreviewSmtpEmailTemplatesRequest. Optional fields absent from [json] stay null.
  factory PostPreviewSmtpEmailTemplatesRequest.fromJson(
          Map<String, Object?> json) =>
      PostPreviewSmtpEmailTemplatesRequest(
        email: json.optString('email'),
        params: json.containsKey('params') ? json.optObjectMap('params') : null,
        templateId: json.requireInt(
            'templateId', 'PostPreviewSmtpEmailTemplatesRequest'),
      );

  /// Email of the contact.(Required if params not provided)
  final String? email;

  /// Key-value pairs of dynamic parameters for template rendering.(Required if email not provided) For example: **{"Firstname":"John", "Lastname":"Doe"}**
  final Map<String, Object?>? params;

  /// Id of the template.
  final int templateId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (params != null) 'params': params!,
        'templateId': templateId,
      };
}

/// The request body of `POST /smtp/email`.
final class SendTransacEmailRequest {
  /// Creates the request body.
  const SendTransacEmailRequest({
    this.attachment,
    this.batchId,
    this.bcc,
    this.cc,
    this.headers,
    this.htmlContent,
    this.messageVersions,
    this.params,
    this.replyTo,
    this.scheduledAt,
    this.sender,
    this.subject,
    this.tags,
    this.templateId,
    this.textContent,
    this.to,
  });

  /// Decodes a JSON object into a SendTransacEmailRequest. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequest.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequest(
        attachment: json.containsKey('attachment')
            ? json.optObjectList(
                'attachment', SendTransacEmailRequestAttachmentItem.fromJson,
                objectName: 'SendTransacEmailRequest')
            : null,
        batchId: json.optString('batchId'),
        bcc: json.containsKey('bcc')
            ? json.optObjectList('bcc', SendTransacEmailRequestBccItem.fromJson,
                objectName: 'SendTransacEmailRequest')
            : null,
        cc: json.containsKey('cc')
            ? json.optObjectList('cc', SendTransacEmailRequestCcItem.fromJson,
                objectName: 'SendTransacEmailRequest')
            : null,
        headers:
            json.containsKey('headers') ? json.optObjectMap('headers') : null,
        htmlContent: json.optString('htmlContent'),
        messageVersions: json.containsKey('messageVersions')
            ? json.optObjectList('messageVersions',
                SendTransacEmailRequestMessageVersionsItem.fromJson,
                objectName: 'SendTransacEmailRequest')
            : null,
        params: json.containsKey('params') ? json.optObjectMap('params') : null,
        replyTo:
            json.optNested('replyTo', SendTransacEmailRequestReplyTo.fromJson),
        scheduledAt: json.optString('scheduledAt'),
        sender:
            json.optNested('sender', SendTransacEmailRequestSender.fromJson),
        subject: json.optString('subject'),
        tags: json.containsKey('tags') ? json.optStringList('tags') : null,
        templateId: json.optInt('templateId'),
        textContent: json.optString('textContent'),
        to: json.containsKey('to')
            ? json.optObjectList('to', SendTransacEmailRequestToItem.fromJson,
                objectName: 'SendTransacEmailRequest')
            : null,
      );

  /// Array of attachment objects. Each attachment must include either an absolute URL (no local file paths) or base64-encoded content, along with the attachment filename. The `name` field is required when `content` is provided. Supported file extensions: xlsx, xls, ods, docx, docm, doc, csv, pdf, txt, gif, jpg, jpeg, png, tif, tiff, rtf, bmp, cgm, css, shtml, html, htm, zip, xml, ppt, pptx, tar, ez, ics, mobi, msg, pub, eps, odt, mp3, m4a, m4v, wma, ogg, flac, wav, aif, aifc, aiff, mp4, mov, avi, mkv, mpeg, mpg, wmv, pkpass, xlsm. When `templateId` is specified: if the template uses the New Template Language format, both `url` and `content` attachment types are supported; if the template uses the Old Template Language format, the `attachment` parameter is ignored.
  final List<SendTransacEmailRequestAttachmentItem>? attachment;

  /// UUIDv4 identifier for the scheduled batch of transactional emails. If omitted, a valid UUIDv4 batch identifier is automatically generated.
  final String? batchId;

  /// Array of BCC recipient objects. Each object contains an email address and an optional name.
  final List<SendTransacEmailRequestBccItem>? bcc;

  /// Array of CC recipient objects. Each object contains an email address and an optional name.
  final List<SendTransacEmailRequestCcItem>? cc;

  /// Custom email headers (non-standard headers) to include in the email. The `sender.ip` header can be set to specify the IP address used for sending transactional emails (dedicated IP users only). Header names must use Title-Case-Format (words separated by hyphens with the first letter of each word capitalized). Headers not in this format are automatically converted. Standard email headers are not supported. Example: `{"sender.ip":"1.2.3.4", "X-Mailin-custom":"some_custom_value", "Idempotency-Key":"abc-123"}`
  final Map<String, Object?>? headers;

  /// HTML body content of the email. Required when `templateId` is not provided. Ignored when `templateId` is provided.
  final String? htmlContent;

  /// Array of message version objects for sending customized email variants. The `templateId` can be customized per version only if a global `templateId` is provided. The `htmlContent` and `textContent` can be customized per version only if at least one of these is present in the global parameters. Global parameters such as `to` (required), `bcc`, `cc`, `replyTo`, and `subject` can be customized per version. Maximum total recipients per API request is 2000. Maximum recipients per message version is 99. Individual `params` objects must not exceed 100 KB. Cumulative `params` across all versions must not exceed 1000 KB. See https://developers.brevo.com/docs/batch-send-transactional-emails for detailed usage instructions.
  final List<SendTransacEmailRequestMessageVersionsItem>? messageVersions;

  /// Key-value pairs for template variable substitution. Only applicable when the template uses the New Template Language format.
  final Map<String, Object?>? params;

  /// Reply-to email address (required) and optional display name. Recipients will use this address when replying to the email.
  final SendTransacEmailRequestReplyTo? replyTo;

  /// UTC date-time when the email should be sent (format: YYYY-MM-DDTHH:mm:ss.SSSZ). Include timezone information in the date-time value. Scheduled emails may be delayed by up to 5 minutes.
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Sender information. Required when `templateId` is not provided. Specify either an email address (with optional name) or a sender ID. The `name` field is ignored when `id` is provided.
  final SendTransacEmailRequestSender? sender;

  /// Email subject line. Required when `templateId` is not provided.
  final String? subject;

  /// Array of tags for categorizing and filtering emails
  final List<String>? tags;

  /// Template identifier
  final int? templateId;

  /// Plain text body content of the email. Ignored when `templateId` is provided.
  final String? textContent;

  /// Array of recipient objects. Each object contains an email address and an optional display name. Required when `messageVersions` is not provided. Ignored when `messageVersions` is provided. Example: `\[{"name":"Jimmy", "email":"jimmy@example.com"}, {"name":"Joe", "email":"joe@example.com"}\]`
  final List<SendTransacEmailRequestToItem>? to;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attachment != null)
          'attachment': [for (final item in attachment!) item.toJson()],
        if (batchId != null) 'batchId': batchId!,
        if (bcc != null) 'bcc': [for (final item in bcc!) item.toJson()],
        if (cc != null) 'cc': [for (final item in cc!) item.toJson()],
        if (headers != null) 'headers': headers!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (messageVersions != null)
          'messageVersions': [
            for (final item in messageVersions!) item.toJson()
          ],
        if (params != null) 'params': params!,
        if (replyTo != null) 'replyTo': replyTo!.toJson(),
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sender != null) 'sender': sender!.toJson(),
        if (subject != null) 'subject': subject!,
        if (tags != null) 'tags': tags!,
        if (templateId != null) 'templateId': templateId!,
        if (textContent != null) 'textContent': textContent!,
        if (to != null) 'to': [for (final item in to!) item.toJson()],
      };
}

/// The request body of `SendTransacEmailRequest.attachmentItem`.
final class SendTransacEmailRequestAttachmentItem {
  /// Creates the request body.
  const SendTransacEmailRequestAttachmentItem({
    this.content,
    this.name,
    this.url,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestAttachmentItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestAttachmentItem.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestAttachmentItem(
        content: json.optString('content'),
        name: json.optString('name'),
        url: json.optString('url'),
      );

  /// Base64-encoded attachment data
  final String? content;

  /// Attachment filename. Required when `content` is provided.
  final String? name;

  /// Absolute URL of the attachment. Local file paths are not supported.
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (content != null) 'content': content!,
        if (name != null) 'name': name!,
        if (url != null) 'url': url!,
      };
}

/// The request body of `SendTransacEmailRequest.bccItem`.
final class SendTransacEmailRequestBccItem {
  /// Creates the request body.
  const SendTransacEmailRequestBccItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestBccItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestBccItem.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequestBccItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString('email', 'SendTransacEmailRequestBccItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient in bcc for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// BCC recipient email address
  final String email;

  /// Display name of the BCC recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// The request body of `SendTransacEmailRequest.ccItem`.
final class SendTransacEmailRequestCcItem {
  /// Creates the request body.
  const SendTransacEmailRequestCcItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestCcItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestCcItem.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequestCcItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString('email', 'SendTransacEmailRequestCcItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient in cc for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// CC recipient email address
  final String email;

  /// Display name of the CC recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// The request body of `SendTransacEmailRequest.messageVersionsItem`.
final class SendTransacEmailRequestMessageVersionsItem {
  /// Creates the request body.
  const SendTransacEmailRequestMessageVersionsItem({
    this.bcc,
    this.cc,
    this.htmlContent,
    this.params,
    this.replyTo,
    this.subject,
    this.textContent,
    required this.to,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestMessageVersionsItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestMessageVersionsItem.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestMessageVersionsItem(
        bcc: json.containsKey('bcc')
            ? json.optObjectList('bcc',
                SendTransacEmailRequestMessageVersionsItemBccItem.fromJson,
                objectName: 'SendTransacEmailRequestMessageVersionsItem')
            : null,
        cc: json.containsKey('cc')
            ? json.optObjectList(
                'cc', SendTransacEmailRequestMessageVersionsItemCcItem.fromJson,
                objectName: 'SendTransacEmailRequestMessageVersionsItem')
            : null,
        htmlContent: json.optString('htmlContent'),
        params: json.containsKey('params') ? json.optObjectMap('params') : null,
        replyTo: json.optNested('replyTo',
            SendTransacEmailRequestMessageVersionsItemReplyTo.fromJson),
        subject: json.optString('subject'),
        textContent: json.optString('textContent'),
        to: json.containsKey('to')
            ? json.optObjectList(
                'to', SendTransacEmailRequestMessageVersionsItemToItem.fromJson,
                objectName: 'SendTransacEmailRequestMessageVersionsItem')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'SendTransacEmailRequestMessageVersionsItem',
                key: 'to')),
      );

  /// Array of BCC recipient objects. Each object contains an email address and an optional name.
  final List<SendTransacEmailRequestMessageVersionsItemBccItem>? bcc;

  /// Array of CC recipient objects. Each object contains an email address and an optional name.
  final List<SendTransacEmailRequestMessageVersionsItemCcItem>? cc;

  /// HTML body content of the email. Required when `templateId` is not provided. Ignored when `templateId` is provided.
  final String? htmlContent;

  /// Key-value pairs for template variable substitution. Only applicable when the template uses the New Template Language format.
  final Map<String, Object?>? params;

  /// Reply-to email address (required) and optional display name. Recipients will use this address when replying to the email.
  final SendTransacEmailRequestMessageVersionsItemReplyTo? replyTo;

  /// Email subject line for this message version
  final String? subject;

  /// Plain text body content of the email. Ignored when `templateId` is provided.
  final String? textContent;

  /// Array of recipient objects. Each object contains an email address and an optional display name.
  final List<SendTransacEmailRequestMessageVersionsItemToItem> to;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (bcc != null) 'bcc': [for (final item in bcc!) item.toJson()],
        if (cc != null) 'cc': [for (final item in cc!) item.toJson()],
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (params != null) 'params': params!,
        if (replyTo != null) 'replyTo': replyTo!.toJson(),
        if (subject != null) 'subject': subject!,
        if (textContent != null) 'textContent': textContent!,
        'to': [for (final item in to) item.toJson()],
      };
}

/// The request body of `SendTransacEmailRequestMessageVersionsItem.bccItem`.
final class SendTransacEmailRequestMessageVersionsItemBccItem {
  /// Creates the request body.
  const SendTransacEmailRequestMessageVersionsItemBccItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestMessageVersionsItemBccItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestMessageVersionsItemBccItem.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestMessageVersionsItemBccItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString(
            'email', 'SendTransacEmailRequestMessageVersionsItemBccItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient in bcc for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// BCC recipient email address
  final String email;

  /// Display name of the BCC recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// The request body of `SendTransacEmailRequestMessageVersionsItem.ccItem`.
final class SendTransacEmailRequestMessageVersionsItemCcItem {
  /// Creates the request body.
  const SendTransacEmailRequestMessageVersionsItemCcItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestMessageVersionsItemCcItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestMessageVersionsItemCcItem.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestMessageVersionsItemCcItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString(
            'email', 'SendTransacEmailRequestMessageVersionsItemCcItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient in cc for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// CC recipient email address
  final String email;

  /// Display name of the CC recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// Reply-to email address (required) and optional display name. Recipients will use this address when replying to the email.
final class SendTransacEmailRequestMessageVersionsItemReplyTo {
  /// Creates the request body.
  const SendTransacEmailRequestMessageVersionsItemReplyTo({
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestMessageVersionsItemReplyTo. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestMessageVersionsItemReplyTo.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestMessageVersionsItemReplyTo(
        email: json.requireString(
            'email', 'SendTransacEmailRequestMessageVersionsItemReplyTo'),
        name: json.optString('name'),
      );

  /// Email address in reply to
  final String email;

  /// Display name for the reply-to address. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// The request body of `SendTransacEmailRequestMessageVersionsItem.toItem`.
final class SendTransacEmailRequestMessageVersionsItemToItem {
  /// Creates the request body.
  const SendTransacEmailRequestMessageVersionsItemToItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestMessageVersionsItemToItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestMessageVersionsItemToItem.fromJson(
          Map<String, Object?> json) =>
      SendTransacEmailRequestMessageVersionsItemToItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString(
            'email', 'SendTransacEmailRequestMessageVersionsItemToItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// Email address of the recipient
  final String email;

  /// Display name of the recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// Reply-to email address (required) and optional display name. Recipients will use this address when replying to the email.
final class SendTransacEmailRequestReplyTo {
  /// Creates the request body.
  const SendTransacEmailRequestReplyTo({
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestReplyTo. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestReplyTo.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequestReplyTo(
        email: json.requireString('email', 'SendTransacEmailRequestReplyTo'),
        name: json.optString('name'),
      );

  /// Email address in reply to
  final String email;

  /// Display name for the reply-to address. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// Sender information. Required when `templateId` is not provided. Specify either an email address (with optional name) or a sender ID. The `name` field is ignored when `id` is provided.
final class SendTransacEmailRequestSender {
  /// Creates the request body.
  const SendTransacEmailRequestSender({
    this.email,
    this.id,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestSender. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestSender.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequestSender(
        email: json.optString('email'),
        id: json.optInt('id'),
        name: json.optString('name'),
      );

  /// Sender email address. Required when `id` is not provided.
  final String? email;

  /// Sender identifier. Required when `email` is not provided. Use this field to select a sender with a specific IP pool (dedicated IP users only).
  final int? id;

  /// Display name of the sender. Maximum length is 70 characters. Only applicable when `email` is provided.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `SendTransacEmailRequest.toItem`.
final class SendTransacEmailRequestToItem {
  /// Creates the request body.
  const SendTransacEmailRequestToItem({
    this.contactPixelTrackingConsent,
    required this.email,
    this.name,
  });

  /// Decodes a JSON object into a SendTransacEmailRequestToItem. Optional fields absent from [json] stay null.
  factory SendTransacEmailRequestToItem.fromJson(Map<String, Object?> json) =>
      SendTransacEmailRequestToItem(
        contactPixelTrackingConsent:
            json.optBool('contactPixelTrackingConsent'),
        email: json.requireString('email', 'SendTransacEmailRequestToItem'),
        name: json.optString('name'),
      );

  /// Consent of the recipient for open (pixel) and click tracking, resolved by the sender at send time. Considered only if the per-contact pixel tracking consent feature is enabled for your account. Pass `true` if this recipient has consented to open and click tracking, in which case the open pixel and tracked links identify the recipient. Pass `false` to anonymise the open and click events (counted in aggregate statistics only). If it is not passed, the recipient is treated as unknown consent status and the email is still sent (the open and click are anonymised unless your account tracks unknown-consent contacts). A value other than `true`/`false` is rejected. Ignored when the feature is not enabled for your account.
  final bool? contactPixelTrackingConsent;

  /// Email address of the recipient
  final String email;

  /// Display name of the recipient. Maximum length is 70 characters.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactPixelTrackingConsent != null)
          'contactPixelTrackingConsent': contactPixelTrackingConsent!,
        'email': email,
        if (name != null) 'name': name!,
      };
}

/// The request body of `PUT /smtp/templates/{templateId}`.
final class UpdateSmtpTemplateRequest {
  /// Creates the request body.
  const UpdateSmtpTemplateRequest({
    this.attachmentUrl,
    this.htmlContent,
    this.htmlUrl,
    this.isActive,
    this.replyTo,
    this.sender,
    this.subject,
    this.tag,
    this.templateName,
    this.toField,
  });

  /// Decodes a JSON object into a UpdateSmtpTemplateRequest. Optional fields absent from [json] stay null.
  factory UpdateSmtpTemplateRequest.fromJson(Map<String, Object?> json) =>
      UpdateSmtpTemplateRequest(
        attachmentUrl: json.optString('attachmentUrl'),
        htmlContent: json.optString('htmlContent'),
        htmlUrl: json.optString('htmlUrl'),
        isActive: json.optBool('isActive'),
        replyTo: json.optString('replyTo'),
        sender:
            json.optNested('sender', UpdateSmtpTemplateRequestSender.fromJson),
        subject: json.optString('subject'),
        tag: json.optString('tag'),
        templateName: json.optString('templateName'),
        toField: json.optString('toField'),
      );

  /// Absolute url of the attachment (**no local file**). Extensions allowed: #### xlsx, xls, ods, docx, docm, doc, csv, pdf, txt, gif, jpg, jpeg, png, tif, tiff, rtf, bmp, cgm, css, shtml, html, htm, zip, xml, ppt, pptx, tar, ez, ics, mobi, msg, pub and eps
  final String? attachmentUrl;

  /// **Required if htmlUrl is empty**. If the template is designed using Drag & Drop editor via HTML content, then the design page will not have Drag & Drop editor access for that template. Body of the message (HTML must have more than 10 characters)
  final String? htmlContent;

  /// **Required if htmlContent is empty**. URL to the body of the email (HTML)
  final String? htmlUrl;

  /// Status of the template. isActive = false means template is inactive, isActive = true means template is active
  final bool? isActive;

  /// Email on which campaign recipients will be able to reply to
  final String? replyTo;

  /// Sender details including id or email and name (_optional_). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
  final UpdateSmtpTemplateRequestSender? sender;

  /// Subject of the email
  final String? subject;

  /// Tag of the template
  final String? tag;

  /// Name of the template
  final String? templateName;

  /// To personalize the **To** Field. If you want to include the first name and last name of your recipient, add **{FNAME} {LNAME}**. These contact attributes must already exist in your Brevo account. If input parameter **params** used please use **{{contact.FNAME}} {{contact.LNAME}}** for personalization
  final String? toField;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attachmentUrl != null) 'attachmentUrl': attachmentUrl!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (htmlUrl != null) 'htmlUrl': htmlUrl!,
        if (isActive != null) 'isActive': isActive!,
        if (replyTo != null) 'replyTo': replyTo!,
        if (sender != null) 'sender': sender!.toJson(),
        if (subject != null) 'subject': subject!,
        if (tag != null) 'tag': tag!,
        if (templateName != null) 'templateName': templateName!,
        if (toField != null) 'toField': toField!,
      };
}

/// Sender details including id or email and name (_optional_). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
final class UpdateSmtpTemplateRequestSender {
  /// Creates the request body.
  const UpdateSmtpTemplateRequestSender({
    this.email,
    this.id,
    this.name,
  });

  /// Decodes a JSON object into a UpdateSmtpTemplateRequestSender. Optional fields absent from [json] stay null.
  factory UpdateSmtpTemplateRequestSender.fromJson(Map<String, Object?> json) =>
      UpdateSmtpTemplateRequestSender(
        email: json.optString('email'),
        id: json.optInt('id'),
        name: json.optString('name'),
      );

  /// Email of the sender
  final String? email;

  /// Select the sender for the template on the basis of sender id. _In order to select a sender with specific pool of IP’s, dedicated ip users shall pass id (instead of email)_.
  final int? id;

  /// Name of the sender
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}
