// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /smtp/statistics/aggregatedReport` object of the Brevo API.
final class GetAggregatedSmtpReportResponse {
  /// Creates a GetAggregatedSmtpReportResponse from already-decoded fields.
  const GetAggregatedSmtpReportResponse({
    this.blocked,
    this.clicks,
    this.delivered,
    this.hardBounces,
    this.invalid,
    this.opens,
    this.range,
    this.requests,
    this.softBounces,
    this.spamReports,
    this.uniqueClicks,
    this.uniqueOpens,
    this.unsubscribed,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAggregatedSmtpReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAggregatedSmtpReportResponse.fromJson(Map<String, Object?> json) =>
      GetAggregatedSmtpReportResponse(
        blocked: json.optInt('blocked'),
        clicks: json.optInt('clicks'),
        delivered: json.optInt('delivered'),
        hardBounces: json.optInt('hardBounces'),
        invalid: json.optInt('invalid'),
        opens: json.optInt('opens'),
        range: json.optString('range'),
        requests: json.optInt('requests'),
        softBounces: json.optInt('softBounces'),
        spamReports: json.optInt('spamReports'),
        uniqueClicks: json.optInt('uniqueClicks'),
        uniqueOpens: json.optInt('uniqueOpens'),
        unsubscribed: json.optInt('unsubscribed'),
        raw: json,
      );

  /// Number of blocked contact emails for the timeframe
  final int? blocked;

  /// Number of clicks for the timeframe
  final int? clicks;

  /// Number of delivered emails for the timeframe
  final int? delivered;

  /// Number of hardbounces for the timeframe
  final int? hardBounces;

  /// Number of invalid emails for the timeframe
  final int? invalid;

  /// Number of openings for the timeframe
  final int? opens;

  /// Time frame of the report
  final String? range;

  /// Number of requests for the timeframe
  final int? requests;

  /// Number of softbounces for the timeframe
  final int? softBounces;

  /// Number of complaint (spam report) for the timeframe
  final int? spamReports;

  /// Number of unique clicks for the timeframe
  final int? uniqueClicks;

  /// Number of unique openings for the timeframe
  final int? uniqueOpens;

  /// Number of unsubscribed emails for the timeframe
  final int? unsubscribed;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (blocked != null) 'blocked': blocked!,
        if (clicks != null) 'clicks': clicks!,
        if (delivered != null) 'delivered': delivered!,
        if (hardBounces != null) 'hardBounces': hardBounces!,
        if (invalid != null) 'invalid': invalid!,
        if (opens != null) 'opens': opens!,
        if (range != null) 'range': range!,
        if (requests != null) 'requests': requests!,
        if (softBounces != null) 'softBounces': softBounces!,
        if (spamReports != null) 'spamReports': spamReports!,
        if (uniqueClicks != null) 'uniqueClicks': uniqueClicks!,
        if (uniqueOpens != null) 'uniqueOpens': uniqueOpens!,
        if (unsubscribed != null) 'unsubscribed': unsubscribed!,
      };
}

/// The `GET /smtp/blockedDomains` object of the Brevo API.
final class GetBlockedDomainsResponse {
  /// Creates a GetBlockedDomainsResponse from already-decoded fields.
  const GetBlockedDomainsResponse({
    this.domains = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetBlockedDomainsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetBlockedDomainsResponse.fromJson(Map<String, Object?> json) =>
      GetBlockedDomainsResponse(
        domains: json.optStringList('domains'),
        raw: json,
      );

  /// List of all blocked domains
  final List<String> domains;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (domains.isNotEmpty) 'domains': domains,
      };
}

/// The `GET /smtp/statistics/events` object of the Brevo API.
final class GetEmailEventReportResponse {
  /// Creates a GetEmailEventReportResponse from already-decoded fields.
  const GetEmailEventReportResponse({
    this.events = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetEmailEventReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetEmailEventReportResponse.fromJson(Map<String, Object?> json) =>
      GetEmailEventReportResponse(
        events: json.optObjectList(
            'events', GetEmailEventReportResponseEventsItem.fromJson,
            objectName: 'GetEmailEventReportResponse'),
        raw: json,
      );

  /// The `events` field.
  final List<GetEmailEventReportResponseEventsItem> events;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
      };
}

/// The `GetEmailEventReportResponse.eventsItem` object of the Brevo API.
final class GetEmailEventReportResponseEventsItem {
  /// Creates a GetEmailEventReportResponseEventsItem from already-decoded fields.
  const GetEmailEventReportResponseEventsItem({
    this.date,
    this.email,
    this.event,
    this.from,
    this.ip,
    this.link,
    this.messageId,
    this.reason,
    this.subject,
    this.tag,
    this.templateId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetEmailEventReportResponseEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetEmailEventReportResponseEventsItem.fromJson(
          Map<String, Object?> json) =>
      GetEmailEventReportResponseEventsItem(
        date: json.optString('date'),
        email: json.optString('email'),
        event: json.optEnum(
            'event', GetEmailEventReportResponseEventsItemEvent.fromWire),
        from: json.optString('from'),
        ip: json.optString('ip'),
        link: json.optString('link'),
        messageId: json.optString('messageId'),
        reason: json.optString('reason'),
        subject: json.optString('subject'),
        tag: json.optString('tag'),
        templateId: json.optInt('templateId'),
        raw: json,
      );

  /// UTC date-time on which the event has been generated
  final String? date;

  /// Email address which generates the event
  final String? email;

  /// Event which occurred
  final GetEmailEventReportResponseEventsItemEvent? event;

  /// Sender email from which the emails are sent
  final String? from;

  /// IP from which the user has opened the email or clicked on the link (only available if the event is opened or clicks)
  final String? ip;

  /// The link which is sent to the user (only available if the event is requests or opened or clicks)
  final String? link;

  /// Message ID which generated the event
  final String? messageId;

  /// Reason of bounce (only available if the event is hardbounce or softbounce)
  final String? reason;

  /// Subject of the event
  final String? subject;

  /// Tag of the email which generated the event
  final String? tag;

  /// ID of the template (only available if the email is template based)
  final int? templateId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (date != null) 'date': date!,
        if (email != null) 'email': email!,
        if (event != null) 'event': event!.value,
        if (from != null) 'from': from!,
        if (ip != null) 'ip': ip!,
        if (link != null) 'link': link!,
        if (messageId != null) 'messageId': messageId!,
        if (reason != null) 'reason': reason!,
        if (subject != null) 'subject': subject!,
        if (tag != null) 'tag': tag!,
        if (templateId != null) 'templateId': templateId!,
      };
}

/// The `GET /smtp/emailStatus/{identifier}` object of the Brevo API.
final class GetScheduledEmailByIdResponse {
  /// Creates a GetScheduledEmailByIdResponse from already-decoded fields.
  const GetScheduledEmailByIdResponse({
    this.batches = const [],
    this.count,
    this.createdAt,
    this.scheduledAt,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetScheduledEmailByIdResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetScheduledEmailByIdResponse.fromJson(Map<String, Object?> json) =>
      GetScheduledEmailByIdResponse(
        batches: json.optObjectList(
            'batches', GetScheduledEmailByIdResponseBatchesItem.fromJson,
            objectName: 'GetScheduledEmailByIdResponse'),
        count: json.optInt('count'),
        createdAt: json.optString('createdAt'),
        scheduledAt: json.optString('scheduledAt'),
        status: json.optEnum(
            'status', GetScheduledEmailByIdResponseStatus.fromWire),
        raw: json,
      );

  /// The `batches` field.
  final List<GetScheduledEmailByIdResponseBatchesItem> batches;

  /// Total number of batches
  final int? count;

  /// Datetime on which the email was scheduled
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Datetime for which the email was scheduled
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Current status of the scheduled email
  final GetScheduledEmailByIdResponseStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (batches.isNotEmpty)
          'batches': [for (final item in batches) item.toJson()],
        if (count != null) 'count': count!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (status != null) 'status': status!.value,
      };
}

/// The `GetScheduledEmailByIdResponse.batchesItem` object of the Brevo API.
final class GetScheduledEmailByIdResponseBatchesItem {
  /// Creates a GetScheduledEmailByIdResponseBatchesItem from already-decoded fields.
  const GetScheduledEmailByIdResponseBatchesItem({
    this.createdAt,
    this.scheduledAt,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetScheduledEmailByIdResponseBatchesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetScheduledEmailByIdResponseBatchesItem.fromJson(
          Map<String, Object?> json) =>
      GetScheduledEmailByIdResponseBatchesItem(
        createdAt: json.optString('createdAt'),
        scheduledAt: json.optString('scheduledAt'),
        status: json.optEnum(
            'status', GetScheduledEmailByIdResponseBatchesItemStatus.fromWire),
        raw: json,
      );

  /// Datetime on which the batch was scheduled
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Datetime for which the batch was scheduled
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Current status of the scheduled batch
  final GetScheduledEmailByIdResponseBatchesItemStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (status != null) 'status': status!.value,
      };
}

/// The `GET /smtp/statistics/reports` object of the Brevo API.
final class GetSmtpReportResponse {
  /// Creates a GetSmtpReportResponse from already-decoded fields.
  const GetSmtpReportResponse({
    this.reports = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmtpReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmtpReportResponse.fromJson(Map<String, Object?> json) =>
      GetSmtpReportResponse(
        reports: json.optObjectList(
            'reports', GetSmtpReportResponseReportsItem.fromJson,
            objectName: 'GetSmtpReportResponse'),
        raw: json,
      );

  /// The `reports` field.
  final List<GetSmtpReportResponseReportsItem> reports;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (reports.isNotEmpty)
          'reports': [for (final item in reports) item.toJson()],
      };
}

/// The `GetSmtpReportResponse.reportsItem` object of the Brevo API.
final class GetSmtpReportResponseReportsItem {
  /// Creates a GetSmtpReportResponseReportsItem from already-decoded fields.
  const GetSmtpReportResponseReportsItem({
    this.blocked,
    this.clicks,
    this.date,
    this.delivered,
    this.hardBounces,
    this.invalid,
    this.opens,
    this.requests,
    this.softBounces,
    this.spamReports,
    this.uniqueClicks,
    this.uniqueOpens,
    this.unsubscribed,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmtpReportResponseReportsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmtpReportResponseReportsItem.fromJson(
          Map<String, Object?> json) =>
      GetSmtpReportResponseReportsItem(
        blocked: json.optInt('blocked'),
        clicks: json.optInt('clicks'),
        date: json.optString('date'),
        delivered: json.optInt('delivered'),
        hardBounces: json.optInt('hardBounces'),
        invalid: json.optInt('invalid'),
        opens: json.optInt('opens'),
        requests: json.optInt('requests'),
        softBounces: json.optInt('softBounces'),
        spamReports: json.optInt('spamReports'),
        uniqueClicks: json.optInt('uniqueClicks'),
        uniqueOpens: json.optInt('uniqueOpens'),
        unsubscribed: json.optInt('unsubscribed'),
        raw: json,
      );

  /// Number of blocked emails for the date
  final int? blocked;

  /// Number of clicks for the date
  final int? clicks;

  /// Date of the statistics
  final String? date;

  /// [date] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get dateDate => date == null ? null : DateTime.tryParse(date!);

  /// Number of delivered emails for the date
  final int? delivered;

  /// Number of hardbounces for the date
  final int? hardBounces;

  /// Number of invalid emails for the date
  final int? invalid;

  /// Number of openings for the date
  final int? opens;

  /// Number of requests for the date
  final int? requests;

  /// Number of softbounces for the date
  final int? softBounces;

  /// Number of complaints (spam reports) for the date
  final int? spamReports;

  /// Number of unique clicks for the date
  final int? uniqueClicks;

  /// Number of unique openings for the date
  final int? uniqueOpens;

  /// Number of unsubscribed emails for the date
  final int? unsubscribed;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (blocked != null) 'blocked': blocked!,
        if (clicks != null) 'clicks': clicks!,
        if (date != null) 'date': date!,
        if (delivered != null) 'delivered': delivered!,
        if (hardBounces != null) 'hardBounces': hardBounces!,
        if (invalid != null) 'invalid': invalid!,
        if (opens != null) 'opens': opens!,
        if (requests != null) 'requests': requests!,
        if (softBounces != null) 'softBounces': softBounces!,
        if (spamReports != null) 'spamReports': spamReports!,
        if (uniqueClicks != null) 'uniqueClicks': uniqueClicks!,
        if (uniqueOpens != null) 'uniqueOpens': uniqueOpens!,
        if (unsubscribed != null) 'unsubscribed': unsubscribed!,
      };
}

/// The `components/schemas/getSmtpTemplateOverview` object of the Brevo API.
final class GetSmtpTemplateOverview {
  /// Creates a GetSmtpTemplateOverview from already-decoded fields.
  const GetSmtpTemplateOverview({
    this.createdAt,
    this.customTemplateId,
    this.doiTemplate,
    this.htmlContent,
    this.id,
    this.isActive,
    this.modifiedAt,
    this.name,
    this.replyTo,
    this.sender,
    this.subject,
    this.tag,
    this.testSent,
    this.toField,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmtpTemplateOverview. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmtpTemplateOverview.fromJson(Map<String, Object?> json) =>
      GetSmtpTemplateOverview(
        createdAt: json.optString('createdAt'),
        customTemplateId: json.optString('customTemplateId'),
        doiTemplate: json.optBool('doiTemplate'),
        htmlContent: json.optString('htmlContent'),
        id: json.optInt('id'),
        isActive: json.optBool('isActive'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        replyTo: json.optString('replyTo'),
        sender:
            json.optNested('sender', GetSmtpTemplateOverviewSender.fromJson),
        subject: json.optString('subject'),
        tag: json.optString('tag'),
        testSent: json.optBool('testSent'),
        toField: json.optString('toField'),
        raw: json,
      );

  /// Creation UTC date-time of the template (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Custom template identifier, if one was assigned during template creation. Only present when the template has a custom ID.
  final String? customTemplateId;

  /// It is true if template is a valid Double opt-in (DOI) template, otherwise it is false. This field will be available only in case of single template detail call.
  final bool? doiTemplate;

  /// HTML content of the template
  final String? htmlContent;

  /// ID of the template
  final int? id;

  /// Status of template (true=active, false=inactive)
  final bool? isActive;

  /// Last modification UTC date-time of the template (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the template
  final String? name;

  /// Email defined as the "Reply to" for the template
  final String? replyTo;

  /// The `sender` field.
  final GetSmtpTemplateOverviewSender? sender;

  /// Subject of the template
  final String? subject;

  /// Tag of the template
  final String? tag;

  /// Status of test sending for the template (true=test email has been sent, false=test email has not been sent)
  final bool? testSent;

  /// Customisation of the "to" field for the template
  final String? toField;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (customTemplateId != null) 'customTemplateId': customTemplateId!,
        if (doiTemplate != null) 'doiTemplate': doiTemplate!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (id != null) 'id': id!,
        if (isActive != null) 'isActive': isActive!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (replyTo != null) 'replyTo': replyTo!,
        if (sender != null) 'sender': sender!.toJson(),
        if (subject != null) 'subject': subject!,
        if (tag != null) 'tag': tag!,
        if (testSent != null) 'testSent': testSent!,
        if (toField != null) 'toField': toField!,
      };
}

/// The `GetSmtpTemplateOverview.sender` object of the Brevo API.
final class GetSmtpTemplateOverviewSender {
  /// Creates a GetSmtpTemplateOverviewSender from already-decoded fields.
  const GetSmtpTemplateOverviewSender({
    this.email,
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmtpTemplateOverviewSender. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmtpTemplateOverviewSender.fromJson(Map<String, Object?> json) =>
      GetSmtpTemplateOverviewSender(
        email: json.optString('email'),
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// From email for the template
  final String? email;

  /// Sender id of the template
  final String? id;

  /// Sender name for the template
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `GET /smtp/templates` object of the Brevo API.
final class GetSmtpTemplatesResponse {
  /// Creates a GetSmtpTemplatesResponse from already-decoded fields.
  const GetSmtpTemplatesResponse({
    this.count,
    this.templates = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmtpTemplatesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmtpTemplatesResponse.fromJson(Map<String, Object?> json) =>
      GetSmtpTemplatesResponse(
        count: json.optInt('count'),
        templates: json.optObjectList(
            'templates', GetSmtpTemplateOverview.fromJson,
            objectName: 'GetSmtpTemplatesResponse'),
        raw: json,
      );

  /// Count of transactional email templates
  final int? count;

  /// The `templates` field.
  final List<GetSmtpTemplateOverview> templates;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (templates.isNotEmpty)
          'templates': [for (final item in templates) item.toJson()],
      };
}

/// The `GET /smtp/blockedContacts` object of the Brevo API.
final class GetTransacBlockedContactsResponse {
  /// Creates a GetTransacBlockedContactsResponse from already-decoded fields.
  const GetTransacBlockedContactsResponse({
    this.contacts = const [],
    this.count,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacBlockedContactsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacBlockedContactsResponse.fromJson(
          Map<String, Object?> json) =>
      GetTransacBlockedContactsResponse(
        contacts: json.optObjectList(
            'contacts', GetTransacBlockedContactsResponseContactsItem.fromJson,
            objectName: 'GetTransacBlockedContactsResponse'),
        count: json.optInt('count'),
        raw: json,
      );

  /// The `contacts` field.
  final List<GetTransacBlockedContactsResponseContactsItem> contacts;

  /// Count of blocked or unsubscribed contact
  final int? count;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contacts.isNotEmpty)
          'contacts': [for (final item in contacts) item.toJson()],
        if (count != null) 'count': count!,
      };
}

/// The `GetTransacBlockedContactsResponse.contactsItem` object of the Brevo API.
final class GetTransacBlockedContactsResponseContactsItem {
  /// Creates a GetTransacBlockedContactsResponseContactsItem from already-decoded fields.
  const GetTransacBlockedContactsResponseContactsItem({
    this.blockedAt,
    this.email,
    this.reason,
    this.senderEmail,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacBlockedContactsResponseContactsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacBlockedContactsResponseContactsItem.fromJson(
          Map<String, Object?> json) =>
      GetTransacBlockedContactsResponseContactsItem(
        blockedAt: json.optString('blockedAt'),
        email: json.optString('email'),
        reason: json.optNested('reason',
            GetTransacBlockedContactsResponseContactsItemReason.fromJson),
        senderEmail: json.optString('senderEmail'),
        raw: json,
      );

  /// Date when the contact was blocked or unsubscribed on
  final String? blockedAt;

  /// [blockedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get blockedAtDate =>
      blockedAt == null ? null : DateTime.tryParse(blockedAt!);

  /// Email address of the blocked or unsubscribed contact
  final String? email;

  /// Reason for blocking / unsubscribing
  final GetTransacBlockedContactsResponseContactsItemReason? reason;

  /// Sender email address of the blocked or unsubscribed contact
  final String? senderEmail;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (blockedAt != null) 'blockedAt': blockedAt!,
        if (email != null) 'email': email!,
        if (reason != null) 'reason': reason!.toJson(),
        if (senderEmail != null) 'senderEmail': senderEmail!,
      };
}

/// Reason for blocking / unsubscribing
final class GetTransacBlockedContactsResponseContactsItemReason {
  /// Creates a GetTransacBlockedContactsResponseContactsItemReason from already-decoded fields.
  const GetTransacBlockedContactsResponseContactsItemReason({
    this.code,
    this.message,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacBlockedContactsResponseContactsItemReason. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacBlockedContactsResponseContactsItemReason.fromJson(
          Map<String, Object?> json) =>
      GetTransacBlockedContactsResponseContactsItemReason(
        code: json.optEnum('code',
            GetTransacBlockedContactsResponseContactsItemReasonCode.fromWire),
        message: json.optString('message'),
        raw: json,
      );

  /// Reason code for blocking / unsubscribing (This code is safe for comparison)
  final GetTransacBlockedContactsResponseContactsItemReasonCode? code;

  /// Reason for blocking / unsubscribing (This string is not safe for comparison)
  final String? message;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (code != null) 'code': code!.value,
        if (message != null) 'message': message!,
      };
}

/// The `GET /smtp/emails/{uuid}` object of the Brevo API.
final class GetTransacEmailContentResponse {
  /// Creates a GetTransacEmailContentResponse from already-decoded fields.
  const GetTransacEmailContentResponse({
    this.attachmentCount,
    this.body,
    this.date,
    this.email,
    this.events = const [],
    this.subject,
    this.templateId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacEmailContentResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacEmailContentResponse.fromJson(Map<String, Object?> json) =>
      GetTransacEmailContentResponse(
        attachmentCount: json.optInt('attachmentCount'),
        body: json.optString('body'),
        date: json.optString('date'),
        email: json.optString('email'),
        events: json.optObjectList(
            'events', GetTransacEmailContentResponseEventsItem.fromJson,
            objectName: 'GetTransacEmailContentResponse'),
        subject: json.optString('subject'),
        templateId: json.optInt('templateId'),
        raw: json,
      );

  /// Count of the attachments that were sent in the email
  final int? attachmentCount;

  /// Actual content of the transactional email that has been sent
  final String? body;

  /// Date on which transactional email was sent
  final String? date;

  /// Email address to which transactional email has been sent
  final String? email;

  /// Series of events which occurred on the transactional email
  final List<GetTransacEmailContentResponseEventsItem> events;

  /// Subject of the sent email
  final String? subject;

  /// Id of the template
  final int? templateId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attachmentCount != null) 'attachmentCount': attachmentCount!,
        if (body != null) 'body': body!,
        if (date != null) 'date': date!,
        if (email != null) 'email': email!,
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
        if (subject != null) 'subject': subject!,
        if (templateId != null) 'templateId': templateId!,
      };
}

/// The `GetTransacEmailContentResponse.eventsItem` object of the Brevo API.
final class GetTransacEmailContentResponseEventsItem {
  /// Creates a GetTransacEmailContentResponseEventsItem from already-decoded fields.
  const GetTransacEmailContentResponseEventsItem({
    this.name,
    this.time,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacEmailContentResponseEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacEmailContentResponseEventsItem.fromJson(
          Map<String, Object?> json) =>
      GetTransacEmailContentResponseEventsItem(
        name: json.optString('name'),
        time: json.optString('time'),
        raw: json,
      );

  /// Name of the event that occurred on the sent email
  final String? name;

  /// Time at which the event occurred
  final String? time;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (name != null) 'name': name!,
        if (time != null) 'time': time!,
      };
}

/// The `GET /smtp/emails` object of the Brevo API.
final class GetTransacEmailsListResponse {
  /// Creates a GetTransacEmailsListResponse from already-decoded fields.
  const GetTransacEmailsListResponse({
    this.count,
    this.transactionalEmails = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacEmailsListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacEmailsListResponse.fromJson(Map<String, Object?> json) =>
      GetTransacEmailsListResponse(
        count: json.optInt('count'),
        transactionalEmails: json.optObjectList('transactionalEmails',
            GetTransacEmailsListResponseTransactionalEmailsItem.fromJson,
            objectName: 'GetTransacEmailsListResponse'),
        raw: json,
      );

  /// Total number of transactional emails available on your account according to the passed filter
  final int? count;

  /// The `transactionalEmails` field.
  final List<GetTransacEmailsListResponseTransactionalEmailsItem>
      transactionalEmails;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (transactionalEmails.isNotEmpty)
          'transactionalEmails': [
            for (final item in transactionalEmails) item.toJson()
          ],
      };
}

/// The `GetTransacEmailsListResponse.transactionalEmailsItem` object of the Brevo API.
final class GetTransacEmailsListResponseTransactionalEmailsItem {
  /// Creates a GetTransacEmailsListResponseTransactionalEmailsItem from already-decoded fields.
  const GetTransacEmailsListResponseTransactionalEmailsItem({
    this.date,
    this.email,
    this.from,
    this.messageId,
    this.subject,
    this.tags = const [],
    this.templateId,
    this.uuid,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetTransacEmailsListResponseTransactionalEmailsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetTransacEmailsListResponseTransactionalEmailsItem.fromJson(
          Map<String, Object?> json) =>
      GetTransacEmailsListResponseTransactionalEmailsItem(
        date: json.optString('date'),
        email: json.optString('email'),
        from: json.optString('from'),
        messageId: json.optString('messageId'),
        subject: json.optString('subject'),
        tags: json.optStringList('tags'),
        templateId: json.optInt('templateId'),
        uuid: json.optString('uuid'),
        raw: json,
      );

  /// Date on which transactional email was sent
  final String? date;

  /// Email address to which transactional email has been sent
  final String? email;

  /// Email address of the sender from which the email was sent
  final String? from;

  /// Message Id of the sent email
  final String? messageId;

  /// Subject of the sent email
  final String? subject;

  /// Tags used for your email
  final List<String> tags;

  /// Id of the template
  final int? templateId;

  /// Unique id of the email sent to a particular contact
  final String? uuid;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (date != null) 'date': date!,
        if (email != null) 'email': email!,
        if (from != null) 'from': from!,
        if (messageId != null) 'messageId': messageId!,
        if (subject != null) 'subject': subject!,
        if (tags.isNotEmpty) 'tags': tags,
        if (templateId != null) 'templateId': templateId!,
        if (uuid != null) 'uuid': uuid!,
      };
}

/// The `POST /smtp/template/preview` object of the Brevo API.
final class PostPreviewSmtpEmailTemplatesResponse {
  /// Creates a PostPreviewSmtpEmailTemplatesResponse from already-decoded fields.
  const PostPreviewSmtpEmailTemplatesResponse({
    this.fromEmail,
    this.fromName,
    this.html,
    this.previewText,
    this.subject,
    this.usedFeedNames = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostPreviewSmtpEmailTemplatesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostPreviewSmtpEmailTemplatesResponse.fromJson(
          Map<String, Object?> json) =>
      PostPreviewSmtpEmailTemplatesResponse(
        fromEmail: json.optString('fromEmail'),
        fromName: json.optString('fromName'),
        html: json.optString('html'),
        previewText: json.optString('previewText'),
        subject: json.optString('subject'),
        usedFeedNames: json.optStringList('usedFeedNames'),
        raw: json,
      );

  /// Sender email address
  final String? fromEmail;

  /// Sender Name
  final String? fromName;

  /// Html content of the template
  final String? html;

  /// Preview text of the template
  final String? previewText;

  /// subject of the template
  final String? subject;

  /// The `usedFeedNames` field.
  final List<String> usedFeedNames;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (fromEmail != null) 'fromEmail': fromEmail!,
        if (fromName != null) 'fromName': fromName!,
        if (html != null) 'html': html!,
        if (previewText != null) 'previewText': previewText!,
        if (subject != null) 'subject': subject!,
        if (usedFeedNames.isNotEmpty) 'usedFeedNames': usedFeedNames,
      };
}

/// The `POST /smtp/email` object of the Brevo API.
final class SendTransacEmailResponse {
  /// Creates a SendTransacEmailResponse from already-decoded fields.
  const SendTransacEmailResponse({
    this.messageId,
    this.messageIds = const [],
    this.batchId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SendTransacEmailResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SendTransacEmailResponse.fromJson(Map<String, Object?> json) =>
      SendTransacEmailResponse(
        messageId: json.optString('messageId'),
        messageIds: json.optStringList('messageIds'),
        batchId: json.optString('batchId'),
        raw: json,
      );

  /// Message ID of the transactional email scheduled
  final String? messageId;

  /// The `messageIds` field.
  final List<String> messageIds;

  /// Batch identifier for the scheduled transactional email
  final String? batchId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (messageId != null) 'messageId': messageId!,
        if (messageIds.isNotEmpty) 'messageIds': messageIds,
        if (batchId != null) 'batchId': batchId!,
      };
}
