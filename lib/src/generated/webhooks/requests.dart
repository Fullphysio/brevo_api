// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /webhooks`.
final class CreateWebhookRequest {
  /// Creates the request body.
  const CreateWebhookRequest({
    this.auth,
    this.batched,
    this.channel,
    this.description,
    this.domain,
    this.events,
    this.headers,
    this.type,
    required this.url,
  });

  /// Decodes a JSON object into a CreateWebhookRequest. Optional fields absent from [json] stay null.
  factory CreateWebhookRequest.fromJson(Map<String, Object?> json) =>
      CreateWebhookRequest(
        auth: json.optNested('auth', CreateWebhookRequestAuth.fromJson),
        batched: json.optBool('batched'),
        channel: json.optEnum('channel', CreateWebhookRequestChannel.fromWire),
        description: json.optString('description'),
        domain: json.optString('domain'),
        events: json.containsKey('events')
            ? json
                .optStringList('events')
                .map(CreateWebhookRequestEventsItem.fromWire)
                .toList(growable: false)
            : null,
        headers: json.containsKey('headers')
            ? json.optObjectList(
                'headers', CreateWebhookRequestHeadersItem.fromJson,
                objectName: 'CreateWebhookRequest')
            : null,
        type: json.optEnum('type', CreateWebhookRequestType.fromWire),
        url: json.requireString('url', 'CreateWebhookRequest'),
      );

  /// Add authentication on webhook url
  final CreateWebhookRequestAuth? auth;

  /// To send batched webhooks
  final bool? batched;

  /// Channel of the webhook
  final CreateWebhookRequestChannel? channel;

  /// Description of the webhook
  final String? description;

  /// Inbound domain of webhook, required in case of event type
  /// `inbound`
  final String? domain;

  /// Events triggering the webhook. Required for transactional and
  /// marketing types, optional for inbound type (defaults to
  /// `inboundEmailProcessed`). Possible values for **Transactional**
  /// type webhook: `sent` OR `request`, `delivered`, `hardBounce`,
  /// `softBounce`, `blocked`, `spam`, `invalid`, `deferred`, `click`,
  /// `opened`, `uniqueOpened` and `unsubscribed`. Possible values for
  /// **Marketing** type webhook: `spam`, `opened`, `click`,
  /// `hardBounce`, `softBounce`, `unsubscribed`, `listAddition`,
  /// `delivered`, `contactUpdated` & `contactDeleted`. Possible values
  /// for **Inbound** type webhook: `inboundEmailProcessed`.
  final List<CreateWebhookRequestEventsItem>? events;

  /// Custom headers to be send with webhooks
  final List<CreateWebhookRequestHeadersItem>? headers;

  /// Type of the webhook
  final CreateWebhookRequestType? type;

  /// URL of the webhook
  final String url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (auth != null) 'auth': auth!.toJson(),
        if (batched != null) 'batched': batched!,
        if (channel != null) 'channel': channel!.value,
        if (description != null) 'description': description!,
        if (domain != null) 'domain': domain!,
        if (events != null) 'events': [for (final item in events!) item.value],
        if (headers != null)
          'headers': [for (final item in headers!) item.toJson()],
        if (type != null) 'type': type!.value,
        'url': url,
      };
}

/// Add authentication on webhook url
final class CreateWebhookRequestAuth {
  /// Creates the request body.
  const CreateWebhookRequestAuth({
    this.token,
    this.type,
  });

  /// Decodes a JSON object into a CreateWebhookRequestAuth. Optional fields absent from [json] stay null.
  factory CreateWebhookRequestAuth.fromJson(Map<String, Object?> json) =>
      CreateWebhookRequestAuth(
        token: json.optString('token'),
        type: json.optString('type'),
      );

  /// Webhook authentication token
  final String? token;

  /// Type of authentication
  final String? type;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (token != null) 'token': token!,
        if (type != null) 'type': type!,
      };
}

/// The request body of `CreateWebhookRequest.headersItem`.
final class CreateWebhookRequestHeadersItem {
  /// Creates the request body.
  const CreateWebhookRequestHeadersItem({
    this.key,
    this.valueValue,
  });

  /// Decodes a JSON object into a CreateWebhookRequestHeadersItem. Optional fields absent from [json] stay null.
  factory CreateWebhookRequestHeadersItem.fromJson(Map<String, Object?> json) =>
      CreateWebhookRequestHeadersItem(
        key: json.optString('key'),
        valueValue: json.optString('value'),
      );

  /// Header key name
  final String? key;

  /// Header value
  final String? valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (key != null) 'key': key!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The request body of `POST /webhooks/export`.
final class ExportWebhooksHistoryRequest {
  /// Creates the request body.
  const ExportWebhooksHistoryRequest({
    this.days,
    this.email,
    this.endDate,
    required this.event,
    this.messageId,
    required this.notifyUrl,
    this.sort,
    this.startDate,
    required this.type,
    this.webhookId,
  });

  /// Decodes a JSON object into a ExportWebhooksHistoryRequest. Optional fields absent from [json] stay null.
  factory ExportWebhooksHistoryRequest.fromJson(Map<String, Object?> json) =>
      ExportWebhooksHistoryRequest(
        days: json.optInt('days'),
        email: json.optString('email'),
        endDate: json.optString('endDate'),
        event:
            json.optEnum('event', ExportWebhooksHistoryRequestEvent.fromWire) ??
                (throw BrevoDecodeException.missingRequiredKey(
                    objectName: 'ExportWebhooksHistoryRequest', key: 'event')),
        messageId: json.optString('messageId'),
        notifyUrl:
            json.requireString('notifyURL', 'ExportWebhooksHistoryRequest'),
        sort: json.optString('sort'),
        startDate: json.optString('startDate'),
        type: json.optEnum('type', ExportWebhooksHistoryRequestType.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'ExportWebhooksHistoryRequest', key: 'type')),
        webhookId: json.optInt('webhookId'),
      );

  /// Number of days in the past including today (positive
  /// integer). _Not compatible with 'startDate' and 'endDate'_
  final int? days;

  /// Filter the history for a specific email
  final String? email;

  /// Mandatory if startDate is used. Ending date of the report
  /// (YYYY-MM-DD). Must be greater than equal to startDate
  final String? endDate;

  /// [endDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get endDateDate =>
      endDate == null ? null : DateTime.tryParse(endDate!);

  /// Filter the history for a specific event type
  final ExportWebhooksHistoryRequestEvent event;

  /// Filter the history for a specific message id. Applicable
  /// only for transactional webhooks.
  final String? messageId;

  /// Webhook URL to receive CSV file link
  final String notifyUrl;

  /// Sorting order of records (asc or desc)
  final String? sort;

  /// Mandatory if endDate is used. Starting date of the history
  /// (YYYY-MM-DD). Must be lower than equal to endDate
  final String? startDate;

  /// [startDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get startDateDate =>
      startDate == null ? null : DateTime.tryParse(startDate!);

  /// Filter the history based on webhook type
  final ExportWebhooksHistoryRequestType type;

  /// Filter the history for a specific webhook id
  final int? webhookId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (days != null) 'days': days!,
        if (email != null) 'email': email!,
        if (endDate != null) 'endDate': endDate!,
        'event': event.value,
        if (messageId != null) 'messageId': messageId!,
        'notifyURL': notifyUrl,
        if (sort != null) 'sort': sort!,
        if (startDate != null) 'startDate': startDate!,
        'type': type.value,
        if (webhookId != null) 'webhookId': webhookId!,
      };
}

/// The request body of `PUT /webhooks/{webhookId}`.
final class UpdateWebhookRequest {
  /// Creates the request body.
  const UpdateWebhookRequest({
    this.auth,
    this.batched,
    this.description,
    this.domain,
    this.events,
    this.headers,
    this.url,
  });

  /// Decodes a JSON object into a UpdateWebhookRequest. Optional fields absent from [json] stay null.
  factory UpdateWebhookRequest.fromJson(Map<String, Object?> json) =>
      UpdateWebhookRequest(
        auth: json.optNested('auth', UpdateWebhookRequestAuth.fromJson),
        batched: json.optBool('batched'),
        description: json.optString('description'),
        domain: json.optString('domain'),
        events: json.containsKey('events')
            ? json
                .optStringList('events')
                .map(UpdateWebhookRequestEventsItem.fromWire)
                .toList(growable: false)
            : null,
        headers: json.containsKey('headers')
            ? json.optObjectList(
                'headers', UpdateWebhookRequestHeadersItem.fromJson,
                objectName: 'UpdateWebhookRequest')
            : null,
        url: json.optString('url'),
      );

  /// Add authentication on webhook url
  final UpdateWebhookRequestAuth? auth;

  /// To send batched webhooks
  final bool? batched;

  /// Description of the webhook
  final String? description;

  /// Inbound domain of webhook, used in case of event type `inbound`
  final String? domain;

  /// - Events triggering the webhook. Possible values for
  /// **Transactional** type webhook: #### `sent` OR `request`,
  /// `delivered`, `hardBounce`, `softBounce`, `blocked`, `spam`,
  /// `invalid`, `deferred`, `click`, `opened`, `uniqueOpened` and
  /// `unsubscribed` - Possible values for **Marketing** type webhook:
  /// #### `spam`, `opened`, `click`, `hardBounce`, `softBounce`,
  /// `unsubscribed`, `listAddition` & `delivered` - Possible values
  /// for **Inbound** type webhook: #### `inboundEmailProcessed`
  /// #### `reply`
  final List<UpdateWebhookRequestEventsItem>? events;

  /// Custom headers to be send with webhooks
  final List<UpdateWebhookRequestHeadersItem>? headers;

  /// URL of the webhook
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (auth != null) 'auth': auth!.toJson(),
        if (batched != null) 'batched': batched!,
        if (description != null) 'description': description!,
        if (domain != null) 'domain': domain!,
        if (events != null) 'events': [for (final item in events!) item.value],
        if (headers != null)
          'headers': [for (final item in headers!) item.toJson()],
        if (url != null) 'url': url!,
      };
}

/// Add authentication on webhook url
final class UpdateWebhookRequestAuth {
  /// Creates the request body.
  const UpdateWebhookRequestAuth({
    this.token,
    this.type,
  });

  /// Decodes a JSON object into a UpdateWebhookRequestAuth. Optional fields absent from [json] stay null.
  factory UpdateWebhookRequestAuth.fromJson(Map<String, Object?> json) =>
      UpdateWebhookRequestAuth(
        token: json.optString('token'),
        type: json.optString('type'),
      );

  /// Webhook authentication token
  final String? token;

  /// Type of authentication
  final String? type;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (token != null) 'token': token!,
        if (type != null) 'type': type!,
      };
}

/// The request body of `UpdateWebhookRequest.headersItem`.
final class UpdateWebhookRequestHeadersItem {
  /// Creates the request body.
  const UpdateWebhookRequestHeadersItem({
    this.key,
    this.valueValue,
  });

  /// Decodes a JSON object into a UpdateWebhookRequestHeadersItem. Optional fields absent from [json] stay null.
  factory UpdateWebhookRequestHeadersItem.fromJson(Map<String, Object?> json) =>
      UpdateWebhookRequestHeadersItem(
        key: json.optString('key'),
        valueValue: json.optString('value'),
      );

  /// Header key name
  final String? key;

  /// Header value
  final String? valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (key != null) 'key': key!,
        if (valueValue != null) 'value': valueValue!,
      };
}
