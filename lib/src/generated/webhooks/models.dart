// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `components/schemas/getWebhook` object of the Brevo API.
final class GetWebhook {
  /// Creates a GetWebhook from already-decoded fields.
  const GetWebhook({
    this.auth = const {},
    this.batched,
    this.createdAt,
    this.description,
    this.domain,
    this.events = const [],
    this.headers = const [],
    this.id,
    this.modifiedAt,
    this.type,
    this.url,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWebhook. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWebhook.fromJson(Map<String, Object?> json) => GetWebhook(
        auth: json.optObjectMap('auth'),
        batched: json.optBool('batched'),
        createdAt: json.optString('createdAt'),
        description: json.optString('description'),
        domain: json.optString('domain'),
        events: json.optStringList('events'),
        headers: json.optList(
            'headers',
            (element) => element is Map<String, Object?>
                ? element
                : <String, Object?>{}),
        id: json.optInt('id'),
        modifiedAt: json.optString('modifiedAt'),
        type: json.optEnum('type', GetWebhookType.fromWire),
        url: json.optString('url'),
        raw: json,
      );

  /// Authentication header to be send with the webhook requests
  final Map<String, Object?> auth;

  /// Batching configuration of the webhook, we send batched webhooks if its
  /// true
  final bool? batched;

  /// Creation UTC date-time of the webhook (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Description of the webhook
  final String? description;

  /// Inbound domain of the webhook, only returned for inbound type webhooks
  final String? domain;

  /// The `events` field.
  final List<String> events;

  /// The `headers` field.
  final List<Map<String, Object?>> headers;

  /// ID of the webhook
  final int? id;

  /// Last modification UTC date-time of the webhook (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Type of webhook (marketing, transactional, or inbound)
  final GetWebhookType? type;

  /// URL of the webhook
  final String? url;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (auth.isNotEmpty) 'auth': auth,
        if (batched != null) 'batched': batched!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (description != null) 'description': description!,
        if (domain != null) 'domain': domain!,
        if (events.isNotEmpty) 'events': events,
        if (headers.isNotEmpty) 'headers': headers,
        if (id != null) 'id': id!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (type != null) 'type': type!.value,
        if (url != null) 'url': url!,
      };
}

/// The `GET /webhooks` object of the Brevo API.
final class GetWebhooksResponse {
  /// Creates a GetWebhooksResponse from already-decoded fields.
  const GetWebhooksResponse({
    this.webhooks = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWebhooksResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWebhooksResponse.fromJson(Map<String, Object?> json) =>
      GetWebhooksResponse(
        webhooks: json.optObjectList('webhooks', GetWebhook.fromJson,
            objectName: 'GetWebhooksResponse'),
        raw: json,
      );

  /// The `webhooks` field.
  final List<GetWebhook> webhooks;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (webhooks.isNotEmpty)
          'webhooks': [for (final item in webhooks) item.toJson()],
      };
}
