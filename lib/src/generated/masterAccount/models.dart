// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /corporate/group/{id}` object of the Brevo API.
final class GetCorporateGroupIdResponse {
  /// Creates a GetCorporateGroupIdResponse from already-decoded fields.
  const GetCorporateGroupIdResponse({
    this.group,
    this.subAccounts = const [],
    this.users = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateGroupIdResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateGroupIdResponse.fromJson(Map<String, Object?> json) =>
      GetCorporateGroupIdResponse(
        group:
            json.optNested('group', GetCorporateGroupIdResponseGroup.fromJson),
        subAccounts: json.optObjectList(
            'sub-accounts', GetCorporateGroupIdResponseSubAccountsItem.fromJson,
            objectName: 'GetCorporateGroupIdResponse'),
        users: json.optObjectList(
            'users', GetCorporateGroupIdResponseUsersItem.fromJson,
            objectName: 'GetCorporateGroupIdResponse'),
        raw: json,
      );

  /// The `group` field.
  final GetCorporateGroupIdResponseGroup? group;

  /// The `sub-accounts` field.
  final List<GetCorporateGroupIdResponseSubAccountsItem> subAccounts;

  /// The `users` field.
  final List<GetCorporateGroupIdResponseUsersItem> users;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (group != null) 'group': group!.toJson(),
        if (subAccounts.isNotEmpty)
          'sub-accounts': [for (final item in subAccounts) item.toJson()],
        if (users.isNotEmpty)
          'users': [for (final item in users) item.toJson()],
      };
}

/// The `GetCorporateGroupIdResponse.group` object of the Brevo API.
final class GetCorporateGroupIdResponseGroup {
  /// Creates a GetCorporateGroupIdResponseGroup from already-decoded fields.
  const GetCorporateGroupIdResponseGroup({
    this.createdAt,
    this.groupName,
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateGroupIdResponseGroup. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateGroupIdResponseGroup.fromJson(
          Map<String, Object?> json) =>
      GetCorporateGroupIdResponseGroup(
        createdAt: json.optString('createdAt'),
        groupName: json.optString('groupName'),
        id: json.optString('id'),
        raw: json,
      );

  /// Group creation date
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Name of the group
  final String? groupName;

  /// Group id
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (groupName != null) 'groupName': groupName!,
        if (id != null) 'id': id!,
      };
}

/// The `GetCorporateGroupIdResponse.sub-accountsItem` object of the Brevo API.
final class GetCorporateGroupIdResponseSubAccountsItem {
  /// Creates a GetCorporateGroupIdResponseSubAccountsItem from already-decoded fields.
  const GetCorporateGroupIdResponseSubAccountsItem({
    this.companyName,
    this.createdAt,
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateGroupIdResponseSubAccountsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateGroupIdResponseSubAccountsItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateGroupIdResponseSubAccountsItem(
        companyName: json.optString('companyName'),
        createdAt: json.optString('createdAt'),
        id: json.optInt('id'),
        raw: json,
      );

  /// Name of the sub-account organzation
  final String? companyName;

  /// Creation date of the sub-account organzation
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Id of the sub-account organzation
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (companyName != null) 'companyName': companyName!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (id != null) 'id': id!,
      };
}

/// The `GetCorporateGroupIdResponse.usersItem` object of the Brevo API.
final class GetCorporateGroupIdResponseUsersItem {
  /// Creates a GetCorporateGroupIdResponseUsersItem from already-decoded fields.
  const GetCorporateGroupIdResponseUsersItem({
    this.email,
    this.firstName,
    this.lastName,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateGroupIdResponseUsersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateGroupIdResponseUsersItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateGroupIdResponseUsersItem(
        email: json.optString('email'),
        firstName: json.optString('firstName'),
        lastName: json.optString('lastName'),
        raw: json,
      );

  /// Email address of the user
  final String? email;

  /// First name of the user
  final String? firstName;

  /// Last name of the user
  final String? lastName;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (firstName != null) 'firstName': firstName!,
        if (lastName != null) 'lastName': lastName!,
      };
}

/// The `GET /corporate/invited/users` object of the Brevo API.
final class GetCorporateInvitedUsersListResponse {
  /// Creates a GetCorporateInvitedUsersListResponse from already-decoded fields.
  const GetCorporateInvitedUsersListResponse({
    this.users = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateInvitedUsersListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateInvitedUsersListResponse.fromJson(
          Map<String, Object?> json) =>
      GetCorporateInvitedUsersListResponse(
        users: json.optObjectList(
            'users', GetCorporateInvitedUsersListResponseUsersItem.fromJson,
            objectName: 'GetCorporateInvitedUsersListResponse'),
        raw: json,
      );

  /// Get invited users list
  final List<GetCorporateInvitedUsersListResponseUsersItem> users;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (users.isNotEmpty)
          'users': [for (final item in users) item.toJson()],
      };
}

/// The `GetCorporateInvitedUsersListResponse.usersItem` object of the Brevo API.
final class GetCorporateInvitedUsersListResponseUsersItem {
  /// Creates a GetCorporateInvitedUsersListResponseUsersItem from already-decoded fields.
  const GetCorporateInvitedUsersListResponseUsersItem({
    this.email,
    this.featureAccess,
    this.groups,
    this.isOwner,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateInvitedUsersListResponseUsersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateInvitedUsersListResponseUsersItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateInvitedUsersListResponseUsersItem(
        email: json.optString('email'),
        featureAccess: json.optNested(
            'feature_access',
            GetCorporateInvitedUsersListResponseUsersItemFeatureAccess
                .fromJson),
        groups: json.optNested('groups',
            GetCorporateInvitedUsersListResponseUsersItemGroups.fromJson),
        isOwner: json.optString('is_owner'),
        status: json.optString('status'),
        raw: json,
      );

  /// Email address of the user.
  final String? email;

  /// Feature accessiblity given to the user. (Required only
  /// if status is active)
  final GetCorporateInvitedUsersListResponseUsersItemFeatureAccess?
      featureAccess;

  /// Admin user groups list
  final GetCorporateInvitedUsersListResponseUsersItemGroups? groups;

  /// Flag for indicating is user owner of the organization.
  final String? isOwner;

  /// Status of the invited user.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (featureAccess != null) 'feature_access': featureAccess!.toJson(),
        if (groups != null) 'groups': groups!.toJson(),
        if (isOwner != null) 'is_owner': isOwner!,
        if (status != null) 'status': status!,
      };
}

/// Feature accessiblity given to the user. (Required only
/// if status is active)
final class GetCorporateInvitedUsersListResponseUsersItemFeatureAccess {
  /// Creates a GetCorporateInvitedUsersListResponseUsersItemFeatureAccess from already-decoded fields.
  const GetCorporateInvitedUsersListResponseUsersItemFeatureAccess({
    this.analytics = const [],
    this.apiKeys = const [],
    this.appsManagement = const [],
    this.createSubOrganizations = const [],
    this.manageSubOrganizations = const [],
    this.myPlan = const [],
    this.subOrganizationGroups = const [],
    this.userManagement = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateInvitedUsersListResponseUsersItemFeatureAccess. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateInvitedUsersListResponseUsersItemFeatureAccess.fromJson(
          Map<String, Object?> json) =>
      GetCorporateInvitedUsersListResponseUsersItemFeatureAccess(
        analytics: json.optStringList('analytics'),
        apiKeys: json.optStringList('api_keys'),
        appsManagement: json.optStringList('apps_management'),
        createSubOrganizations: json.optStringList('create_sub_organizations'),
        manageSubOrganizations: json.optStringList('manage_sub_organizations'),
        myPlan: json.optStringList('my_plan'),
        subOrganizationGroups: json.optStringList('sub_organization_groups'),
        userManagement: json.optStringList('user_management'),
        raw: json,
      );

  /// Analytics dashboard accessibility
  final List<String> analytics;

  /// Api keys accessiblity.
  final List<String> apiKeys;

  /// Apps management accessiblity | Not available in
  /// ENTv2
  final List<String> appsManagement;

  /// Authorization to create sub-organization in the
  /// admin account. If the user creating the
  /// sub-organization, belongs to a group, the user must
  /// choose a group at the sub-organization creation.
  final List<String> createSubOrganizations;

  /// Authorization to manage and access sub-organizations
  /// in the admin account.
  final List<String> manageSubOrganizations;

  /// My plan accessiblity.
  final List<String> myPlan;

  /// Group creation, modification or deletion
  /// accessibility
  final List<String> subOrganizationGroups;

  /// User management accessiblity.
  final List<String> userManagement;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (analytics.isNotEmpty) 'analytics': analytics,
        if (apiKeys.isNotEmpty) 'api_keys': apiKeys,
        if (appsManagement.isNotEmpty) 'apps_management': appsManagement,
        if (createSubOrganizations.isNotEmpty)
          'create_sub_organizations': createSubOrganizations,
        if (manageSubOrganizations.isNotEmpty)
          'manage_sub_organizations': manageSubOrganizations,
        if (myPlan.isNotEmpty) 'my_plan': myPlan,
        if (subOrganizationGroups.isNotEmpty)
          'sub_organization_groups': subOrganizationGroups,
        if (userManagement.isNotEmpty) 'user_management': userManagement,
      };
}

/// Admin user groups list
final class GetCorporateInvitedUsersListResponseUsersItemGroups {
  /// Creates a GetCorporateInvitedUsersListResponseUsersItemGroups from already-decoded fields.
  const GetCorporateInvitedUsersListResponseUsersItemGroups({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateInvitedUsersListResponseUsersItemGroups. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateInvitedUsersListResponseUsersItemGroups.fromJson(
          Map<String, Object?> json) =>
      GetCorporateInvitedUsersListResponseUsersItemGroups(
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// group id
  final String? id;

  /// group name
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `GET /corporate/ip` object of the Brevo API.
final class GetCorporateIpResponseItem {
  /// Creates a GetCorporateIpResponseItem from already-decoded fields.
  const GetCorporateIpResponseItem({
    this.domain,
    this.ip,
    this.transactional,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateIpResponseItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateIpResponseItem.fromJson(Map<String, Object?> json) =>
      GetCorporateIpResponseItem(
        domain: json.optString('domain'),
        ip: json.optString('ip'),
        transactional: json.optBool('transactional'),
        raw: json,
      );

  /// The `domain` field.
  final String? domain;

  /// The `ip` field.
  final String? ip;

  /// The `transactional` field.
  final bool? transactional;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (domain != null) 'domain': domain!,
        if (ip != null) 'ip': ip!,
        if (transactional != null) 'transactional': transactional!,
      };
}

/// The `GET /corporate/masterAccount` object of the Brevo API.
final class GetCorporateMasterAccountResponse {
  /// Creates a GetCorporateMasterAccountResponse from already-decoded fields.
  const GetCorporateMasterAccountResponse({
    this.billingInfo,
    this.companyName,
    this.currencyCode,
    this.email,
    this.id,
    this.planInfo,
    this.timezone,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponse.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponse(
        billingInfo: json.optNested('billingInfo',
            GetCorporateMasterAccountResponseBillingInfo.fromJson),
        companyName: json.optString('companyName'),
        currencyCode: json.optString('currencyCode'),
        email: json.optString('email'),
        id: json.optInt('id'),
        planInfo: json.optNested(
            'planInfo', GetCorporateMasterAccountResponsePlanInfo.fromJson),
        timezone: json.optString('timezone'),
        raw: json,
      );

  /// Billing details of the master account organization
  final GetCorporateMasterAccountResponseBillingInfo? billingInfo;

  /// Company name of master account organization
  final String? companyName;

  /// Currency code of the master account organization
  final String? currencyCode;

  /// Email id of master account
  final String? email;

  /// Unique identifier of the master account organization
  final int? id;

  /// Plan details
  final GetCorporateMasterAccountResponsePlanInfo? planInfo;

  /// Timezone of the master account organization
  final String? timezone;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (billingInfo != null) 'billingInfo': billingInfo!.toJson(),
        if (companyName != null) 'companyName': companyName!,
        if (currencyCode != null) 'currencyCode': currencyCode!,
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (planInfo != null) 'planInfo': planInfo!.toJson(),
        if (timezone != null) 'timezone': timezone!,
      };
}

/// Billing details of the master account organization
final class GetCorporateMasterAccountResponseBillingInfo {
  /// Creates a GetCorporateMasterAccountResponseBillingInfo from already-decoded fields.
  const GetCorporateMasterAccountResponseBillingInfo({
    this.address,
    this.companyName,
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponseBillingInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponseBillingInfo.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponseBillingInfo(
        address: json.optNested('address',
            GetCorporateMasterAccountResponseBillingInfoAddress.fromJson),
        companyName: json.optString('companyName'),
        email: json.optString('email'),
        name: json.optNested(
            'name', GetCorporateMasterAccountResponseBillingInfoName.fromJson),
        raw: json,
      );

  /// Billing address of master account
  final GetCorporateMasterAccountResponseBillingInfoAddress? address;

  /// Company name of master account
  final String? companyName;

  /// Billing email id of master account
  final String? email;

  /// Billing name of master account holder
  final GetCorporateMasterAccountResponseBillingInfoName? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (address != null) 'address': address!.toJson(),
        if (companyName != null) 'companyName': companyName!,
        if (email != null) 'email': email!,
        if (name != null) 'name': name!.toJson(),
      };
}

/// Billing address of master account
final class GetCorporateMasterAccountResponseBillingInfoAddress {
  /// Creates a GetCorporateMasterAccountResponseBillingInfoAddress from already-decoded fields.
  const GetCorporateMasterAccountResponseBillingInfoAddress({
    this.countryCode,
    this.locality,
    this.postalCode,
    this.stateCode,
    this.streetAddress,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponseBillingInfoAddress. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponseBillingInfoAddress.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponseBillingInfoAddress(
        countryCode: json.optString('countryCode'),
        locality: json.optString('locality'),
        postalCode: json.optString('postalCode'),
        stateCode: json.optString('stateCode'),
        streetAddress: json.optString('streetAddress'),
        raw: json,
      );

  /// Country code
  final String? countryCode;

  /// Locality
  final String? locality;

  /// Postal code
  final String? postalCode;

  /// State code
  final String? stateCode;

  /// Street address
  final String? streetAddress;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (countryCode != null) 'countryCode': countryCode!,
        if (locality != null) 'locality': locality!,
        if (postalCode != null) 'postalCode': postalCode!,
        if (stateCode != null) 'stateCode': stateCode!,
        if (streetAddress != null) 'streetAddress': streetAddress!,
      };
}

/// Billing name of master account holder
final class GetCorporateMasterAccountResponseBillingInfoName {
  /// Creates a GetCorporateMasterAccountResponseBillingInfoName from already-decoded fields.
  const GetCorporateMasterAccountResponseBillingInfoName({
    this.familyName,
    this.givenName,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponseBillingInfoName. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponseBillingInfoName.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponseBillingInfoName(
        familyName: json.optString('familyName'),
        givenName: json.optString('givenName'),
        raw: json,
      );

  /// Last name for billing
  final String? familyName;

  /// First name for billing
  final String? givenName;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (familyName != null) 'familyName': familyName!,
        if (givenName != null) 'givenName': givenName!,
      };
}

/// Plan details
final class GetCorporateMasterAccountResponsePlanInfo {
  /// Creates a GetCorporateMasterAccountResponsePlanInfo from already-decoded fields.
  const GetCorporateMasterAccountResponsePlanInfo({
    this.currencyCode,
    this.features = const [],
    this.nextBillingAt,
    this.planPeriod,
    this.price,
    this.subAccounts,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponsePlanInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponsePlanInfo.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponsePlanInfo(
        currencyCode: json.optString('currencyCode'),
        features: json.optObjectList('features',
            GetCorporateMasterAccountResponsePlanInfoFeaturesItem.fromJson,
            objectName: 'GetCorporateMasterAccountResponsePlanInfo'),
        nextBillingAt: json.optInt('nextBillingAt'),
        planPeriod: json.optEnum('planPeriod',
            GetCorporateMasterAccountResponsePlanInfoPlanPeriod.fromWire),
        price: json.optDouble('price'),
        subAccounts: json.optInt('subAccounts'),
        raw: json,
      );

  /// Plan currency
  final String? currencyCode;

  /// List of provided features in the plan
  final List<GetCorporateMasterAccountResponsePlanInfoFeaturesItem> features;

  /// Timestamp of next billing date
  final int? nextBillingAt;

  /// Plan period type
  final GetCorporateMasterAccountResponsePlanInfoPlanPeriod? planPeriod;

  /// Plan amount
  final double? price;

  /// Number of sub-accounts
  final int? subAccounts;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (currencyCode != null) 'currencyCode': currencyCode!,
        if (features.isNotEmpty)
          'features': [for (final item in features) item.toJson()],
        if (nextBillingAt != null) 'nextBillingAt': nextBillingAt!,
        if (planPeriod != null) 'planPeriod': planPeriod!.value,
        if (price != null) 'price': price!,
        if (subAccounts != null) 'subAccounts': subAccounts!,
      };
}

/// The `GetCorporateMasterAccountResponsePlanInfo.featuresItem` object of the Brevo API.
final class GetCorporateMasterAccountResponsePlanInfoFeaturesItem {
  /// Creates a GetCorporateMasterAccountResponsePlanInfoFeaturesItem from already-decoded fields.
  const GetCorporateMasterAccountResponsePlanInfoFeaturesItem({
    this.name,
    this.quantity,
    this.quantityWithOverages,
    this.remaining,
    this.unitValue,
    this.used,
    this.usedOverages,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateMasterAccountResponsePlanInfoFeaturesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateMasterAccountResponsePlanInfoFeaturesItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateMasterAccountResponsePlanInfoFeaturesItem(
        name: json.optString('name'),
        quantity: json.optInt('quantity'),
        quantityWithOverages: json.optInt('quantityWithOverages'),
        remaining: json.optInt('remaining'),
        unitValue: json.optString('unitValue'),
        used: json.optInt('used'),
        usedOverages: json.optInt('usedOverages'),
        raw: json,
      );

  /// Name of the feature
  final String? name;

  /// Quantity provided in the plan
  final int? quantity;

  /// Quantity with overages provided in the plan (only
  /// applicable on ENTv2)
  final int? quantityWithOverages;

  /// Quantity remaining in the plan
  final int? remaining;

  /// Unit value of the feature
  final String? unitValue;

  /// Quantity consumed by master
  final int? used;

  /// Quantity consumed by sub-organizations over the
  /// admin plan limit (only applicable on ENTv2)
  final int? usedOverages;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (name != null) 'name': name!,
        if (quantity != null) 'quantity': quantity!,
        if (quantityWithOverages != null)
          'quantityWithOverages': quantityWithOverages!,
        if (remaining != null) 'remaining': remaining!,
        if (unitValue != null) 'unitValue': unitValue!,
        if (used != null) 'used': used!,
        if (usedOverages != null) 'usedOverages': usedOverages!,
      };
}

/// The `GET /corporate/subAccount/{id}` object of the Brevo API.
final class GetCorporateSubAccountIdResponse {
  /// Creates a GetCorporateSubAccountIdResponse from already-decoded fields.
  const GetCorporateSubAccountIdResponse({
    this.companyName,
    this.email,
    this.groups = const [],
    this.name,
    this.planInfo,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponse.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponse(
        companyName: json.optString('companyName'),
        email: json.optString('email'),
        groups: json.optObjectList(
            'groups', GetCorporateSubAccountIdResponseGroupsItem.fromJson,
            objectName: 'GetCorporateSubAccountIdResponse'),
        name: json.optString('name'),
        planInfo: json.optNested(
            'planInfo', GetCorporateSubAccountIdResponsePlanInfo.fromJson),
        raw: json,
      );

  /// Sub-account company name
  final String? companyName;

  /// Email id of the sub-account organization
  final String? email;

  /// List of groups associated with the sub-account
  final List<GetCorporateSubAccountIdResponseGroupsItem> groups;

  /// Name of the sub-account user
  final String? name;

  /// Sub-account plan details
  final GetCorporateSubAccountIdResponsePlanInfo? planInfo;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (companyName != null) 'companyName': companyName!,
        if (email != null) 'email': email!,
        if (groups.isNotEmpty)
          'groups': [for (final item in groups) item.toJson()],
        if (name != null) 'name': name!,
        if (planInfo != null) 'planInfo': planInfo!.toJson(),
      };
}

/// The `GetCorporateSubAccountIdResponse.groupsItem` object of the Brevo API.
final class GetCorporateSubAccountIdResponseGroupsItem {
  /// Creates a GetCorporateSubAccountIdResponseGroupsItem from already-decoded fields.
  const GetCorporateSubAccountIdResponseGroupsItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponseGroupsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponseGroupsItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponseGroupsItem(
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// Group id
  final String? id;

  /// Name of the group
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// Sub-account plan details
final class GetCorporateSubAccountIdResponsePlanInfo {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfo from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfo({
    this.credits,
    this.features,
    this.planType,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfo.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfo(
        credits: json.optNested('credits',
            GetCorporateSubAccountIdResponsePlanInfoCredits.fromJson),
        features: json.optNested('features',
            GetCorporateSubAccountIdResponsePlanInfoFeatures.fromJson),
        planType: json.optString('planType'),
        raw: json,
      );

  /// Credits quota and remaining credits on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCredits? credits;

  /// Features available on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoFeatures? features;

  /// type of the plan
  final String? planType;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (credits != null) 'credits': credits!.toJson(),
        if (features != null) 'features': features!.toJson(),
        if (planType != null) 'planType': planType!,
      };
}

/// Credits quota and remaining credits on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCredits {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCredits from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCredits({
    this.emails,
    this.externalFeeds,
    this.sms,
    this.whatsapp,
    this.wpSubscribers,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCredits. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCredits.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCredits(
        emails: json.optNested('emails',
            GetCorporateSubAccountIdResponsePlanInfoCreditsEmails.fromJson),
        externalFeeds: json.optNested(
            'externalFeeds',
            GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds
                .fromJson),
        sms: json.optNested(
            'sms', GetCorporateSubAccountIdResponsePlanInfoCreditsSms.fromJson),
        whatsapp: json.optNested('whatsapp',
            GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp.fromJson),
        wpSubscribers: json.optNested(
            'wpSubscribers',
            GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers
                .fromJson),
        raw: json,
      );

  /// Email credits remaining on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCreditsEmails? emails;

  /// externalFeeds credits remaining on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds?
      externalFeeds;

  /// SMS credits remaining on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCreditsSms? sms;

  /// Whatsapp credits remaining on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp? whatsapp;

  /// Push credits remaining on the sub-account
  final GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers?
      wpSubscribers;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (emails != null) 'emails': emails!.toJson(),
        if (externalFeeds != null) 'externalFeeds': externalFeeds!.toJson(),
        if (sms != null) 'sms': sms!.toJson(),
        if (whatsapp != null) 'whatsapp': whatsapp!.toJson(),
        if (wpSubscribers != null) 'wpSubscribers': wpSubscribers!.toJson(),
      };
}

/// Email credits remaining on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCreditsEmails {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCreditsEmails from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCreditsEmails({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCreditsEmails. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCreditsEmails.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCreditsEmails(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of email messaging limits provided
  final int? quantity;

  /// Available email messaging limits for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// externalFeeds credits remaining on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCreditsExternalFeeds(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of externalFeeds messaging limits
  /// provided
  final int? quantity;

  /// Available externalFeeds messaging limits for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// SMS credits remaining on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCreditsSms {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCreditsSms from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCreditsSms({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCreditsSms. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCreditsSms.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCreditsSms(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of SMS messaging limits provided
  final int? quantity;

  /// Available SMS messaging limits for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Whatsapp credits remaining on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCreditsWhatsapp(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of whatsapp messaging limits provided
  final int? quantity;

  /// Available whatsapp messaging limits for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Push credits remaining on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoCreditsWpSubscribers(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of Push sending limits provided
  final int? quantity;

  /// Available Push sending limits for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Features available on the sub-account
final class GetCorporateSubAccountIdResponsePlanInfoFeatures {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoFeatures from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoFeatures({
    this.inbox,
    this.landingPage,
    this.salesUsers,
    this.users,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoFeatures. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoFeatures.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoFeatures(
        inbox: json.optNested('inbox',
            GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox.fromJson),
        landingPage: json.optNested(
            'landingPage',
            GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage
                .fromJson),
        salesUsers: json.optNested(
            'salesUsers',
            GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers
                .fromJson),
        users: json.optNested('users',
            GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers.fromJson),
        raw: json,
      );

  /// Inbox details / Not applicable on ENTv2
  final GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox? inbox;

  /// Landing page details / Not applicable on ENTv2
  final GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage?
      landingPage;

  /// Sales and service users to use phone, sales and
  /// conversations
  final GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers? salesUsers;

  /// Marketing users to manage the marketing channels
  final GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers? users;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (inbox != null) 'inbox': inbox!.toJson(),
        if (landingPage != null) 'landingPage': landingPage!.toJson(),
        if (salesUsers != null) 'salesUsers': salesUsers!.toJson(),
        if (users != null) 'users': users!.toJson(),
      };
}

/// Inbox details / Not applicable on ENTv2
final class GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoFeaturesInbox(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of inbox provided
  final int? quantity;

  /// Available inboxes for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Landing page details / Not applicable on ENTv2
final class GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoFeaturesLandingPage(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of landing pages provided
  final int? quantity;

  /// Available landing pages for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Sales and service users to use phone, sales and
/// conversations
final class GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoFeaturesSalesUsers(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of sales users provided
  final int? quantity;

  /// Available sales users for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// Marketing users to manage the marketing channels
final class GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers {
  /// Creates a GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers from already-decoded fields.
  const GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers({
    this.quantity,
    this.remaining,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountIdResponsePlanInfoFeaturesUsers(
        quantity: json.optInt('quantity'),
        remaining: json.optInt('remaining'),
        raw: json,
      );

  /// Quantity of marketing users provided
  final int? quantity;

  /// Available marketing users for use
  final int? remaining;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (quantity != null) 'quantity': quantity!,
        if (remaining != null) 'remaining': remaining!,
      };
}

/// The `GET /corporate/subAccount` object of the Brevo API.
final class GetCorporateSubAccountResponse {
  /// Creates a GetCorporateSubAccountResponse from already-decoded fields.
  const GetCorporateSubAccountResponse({
    this.count,
    this.subAccounts = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountResponse.fromJson(Map<String, Object?> json) =>
      GetCorporateSubAccountResponse(
        count: json.optInt('count'),
        subAccounts: json.optObjectList('subAccounts',
            GetCorporateSubAccountResponseSubAccountsItem.fromJson,
            objectName: 'GetCorporateSubAccountResponse'),
        raw: json,
      );

  /// Total number of subaccounts
  final int? count;

  /// The `subAccounts` field.
  final List<GetCorporateSubAccountResponseSubAccountsItem> subAccounts;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (subAccounts.isNotEmpty)
          'subAccounts': [for (final item in subAccounts) item.toJson()],
      };
}

/// The `GetCorporateSubAccountResponse.subAccountsItem` object of the Brevo API.
final class GetCorporateSubAccountResponseSubAccountsItem {
  /// Creates a GetCorporateSubAccountResponseSubAccountsItem from already-decoded fields.
  const GetCorporateSubAccountResponseSubAccountsItem({
    this.active,
    this.companyName,
    this.createdAt,
    this.groups = const [],
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountResponseSubAccountsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountResponseSubAccountsItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountResponseSubAccountsItem(
        active: json.optBool('active'),
        companyName: json.optString('companyName'),
        createdAt: json.optInt('createdAt'),
        groups: json.optObjectList('groups',
            GetCorporateSubAccountResponseSubAccountsItemGroupsItem.fromJson,
            objectName: 'GetCorporateSubAccountResponseSubAccountsItem'),
        id: json.optInt('id'),
        raw: json,
      );

  /// Whether the sub-account is active or not
  final bool? active;

  /// Name of the sub-account company
  final String? companyName;

  /// Timestamp when the sub-account was created
  final int? createdAt;

  /// Group details
  final List<GetCorporateSubAccountResponseSubAccountsItemGroupsItem> groups;

  /// id of the sub-account
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (active != null) 'active': active!,
        if (companyName != null) 'companyName': companyName!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (groups.isNotEmpty)
          'groups': [for (final item in groups) item.toJson()],
        if (id != null) 'id': id!,
      };
}

/// The `GetCorporateSubAccountResponseSubAccountsItem.groupsItem` object of the Brevo API.
final class GetCorporateSubAccountResponseSubAccountsItemGroupsItem {
  /// Creates a GetCorporateSubAccountResponseSubAccountsItemGroupsItem from already-decoded fields.
  const GetCorporateSubAccountResponseSubAccountsItemGroupsItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateSubAccountResponseSubAccountsItemGroupsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateSubAccountResponseSubAccountsItemGroupsItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateSubAccountResponseSubAccountsItemGroupsItem(
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// Group identifier
  final String? id;

  /// Name of the group
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// Check admin user permissions
final class GetCorporateUserPermissionResponse {
  /// Creates a GetCorporateUserPermissionResponse from already-decoded fields.
  const GetCorporateUserPermissionResponse({
    this.email,
    this.featureAccess,
    this.groups = const [],
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateUserPermissionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateUserPermissionResponse.fromJson(
          Map<String, Object?> json) =>
      GetCorporateUserPermissionResponse(
        email: json.optString('email'),
        featureAccess: json.optNested('feature_access',
            GetCorporateUserPermissionResponseFeatureAccess.fromJson),
        groups: json.optObjectList(
            'groups', GetCorporateUserPermissionResponseGroupsItem.fromJson,
            objectName: 'GetCorporateUserPermissionResponse'),
        status: json.optString('status'),
        raw: json,
      );

  /// Email address of the user.
  final String? email;

  /// Granular feature permissions given to the user.
  final GetCorporateUserPermissionResponseFeatureAccess? featureAccess;

  /// The `groups` field.
  final List<GetCorporateUserPermissionResponseGroupsItem> groups;

  /// Status of the invited user.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (featureAccess != null) 'feature_access': featureAccess!.toJson(),
        if (groups.isNotEmpty)
          'groups': [for (final item in groups) item.toJson()],
        if (status != null) 'status': status!,
      };
}

/// Granular feature permissions given to the user.
final class GetCorporateUserPermissionResponseFeatureAccess {
  /// Creates a GetCorporateUserPermissionResponseFeatureAccess from already-decoded fields.
  const GetCorporateUserPermissionResponseFeatureAccess({
    this.analytics = const [],
    this.apiKeys = const [],
    this.appsManagement = const [],
    this.createSubOrganizations = const [],
    this.manageSubOrganizations = const [],
    this.myPlan = const [],
    this.subOrganizationGroups = const [],
    this.userManagement = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateUserPermissionResponseFeatureAccess. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateUserPermissionResponseFeatureAccess.fromJson(
          Map<String, Object?> json) =>
      GetCorporateUserPermissionResponseFeatureAccess(
        analytics: json.optStringList('analytics'),
        apiKeys: json.optStringList('api_keys'),
        appsManagement: json.optStringList('apps_management'),
        createSubOrganizations: json.optStringList('create_sub_organizations'),
        manageSubOrganizations: json.optStringList('manage_sub_organizations'),
        myPlan: json.optStringList('my_plan'),
        subOrganizationGroups: json.optStringList('sub_organization_groups'),
        userManagement: json.optStringList('user_management'),
        raw: json,
      );

  /// Permission on analytics
  final List<String> analytics;

  /// Permission on api keys
  final List<String> apiKeys;

  /// Permission on apps management
  final List<String> appsManagement;

  /// Permission on create sub-accounts
  final List<String> createSubOrganizations;

  /// Permission on manage sub-accounts
  final List<String> manageSubOrganizations;

  /// Permission on my plan
  final List<String> myPlan;

  /// Permission on groups
  final List<String> subOrganizationGroups;

  /// Permission on user management
  final List<String> userManagement;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (analytics.isNotEmpty) 'analytics': analytics,
        if (apiKeys.isNotEmpty) 'api_keys': apiKeys,
        if (appsManagement.isNotEmpty) 'apps_management': appsManagement,
        if (createSubOrganizations.isNotEmpty)
          'create_sub_organizations': createSubOrganizations,
        if (manageSubOrganizations.isNotEmpty)
          'manage_sub_organizations': manageSubOrganizations,
        if (myPlan.isNotEmpty) 'my_plan': myPlan,
        if (subOrganizationGroups.isNotEmpty)
          'sub_organization_groups': subOrganizationGroups,
        if (userManagement.isNotEmpty) 'user_management': userManagement,
      };
}

/// Groups details
final class GetCorporateUserPermissionResponseGroupsItem {
  /// Creates a GetCorporateUserPermissionResponseGroupsItem from already-decoded fields.
  const GetCorporateUserPermissionResponseGroupsItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCorporateUserPermissionResponseGroupsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCorporateUserPermissionResponseGroupsItem.fromJson(
          Map<String, Object?> json) =>
      GetCorporateUserPermissionResponseGroupsItem(
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// group identifier
  final String? id;

  /// Group name
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `components/schemas/getSsoToken` object of the Brevo API.
final class GetSsoToken {
  /// Creates a GetSsoToken from already-decoded fields.
  const GetSsoToken({
    this.token,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSsoToken. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSsoToken.fromJson(Map<String, Object?> json) => GetSsoToken(
        token: json.optString('token'),
        raw: json,
      );

  /// Session token, it will remain valid for 15 days.
  final String? token;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (token != null) 'token': token!,
      };
}

/// The `GET /corporate/groups` object of the Brevo API.
final class GetSubAccountGroupsResponseItem {
  /// Creates a GetSubAccountGroupsResponseItem from already-decoded fields.
  const GetSubAccountGroupsResponseItem({
    this.groupName,
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSubAccountGroupsResponseItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSubAccountGroupsResponseItem.fromJson(Map<String, Object?> json) =>
      GetSubAccountGroupsResponseItem(
        groupName: json.optString('groupName'),
        id: json.optString('id'),
        raw: json,
      );

  /// The name of the group of sub-accounts
  final String? groupName;

  /// Unique id of the group
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (groupName != null) 'groupName': groupName!,
        if (id != null) 'id': id!,
      };
}

/// The `POST /corporate/user/invitation/send` object of the Brevo API.
final class InviteAdminUserResponse {
  /// Creates a InviteAdminUserResponse from already-decoded fields.
  const InviteAdminUserResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a InviteAdminUserResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory InviteAdminUserResponse.fromJson(Map<String, Object?> json) =>
      InviteAdminUserResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// The `id` field.
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `POST /corporate/group` object of the Brevo API.
final class PostCorporateGroupResponse {
  /// Creates a PostCorporateGroupResponse from already-decoded fields.
  const PostCorporateGroupResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCorporateGroupResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCorporateGroupResponse.fromJson(Map<String, Object?> json) =>
      PostCorporateGroupResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// The `id` field.
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `POST /corporate/subAccount/key` object of the Brevo API.
final class PostCorporateSubAccountKeyResponse {
  /// Creates a PostCorporateSubAccountKeyResponse from already-decoded fields.
  const PostCorporateSubAccountKeyResponse({
    this.key,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCorporateSubAccountKeyResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCorporateSubAccountKeyResponse.fromJson(
          Map<String, Object?> json) =>
      PostCorporateSubAccountKeyResponse(
        key: json.optString('key'),
        status: json.optString('status'),
        raw: json,
      );

  /// API key
  final String? key;

  /// Status of the API operation.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (key != null) 'key': key!,
        if (status != null) 'status': status!,
      };
}

/// The `POST /corporate/subAccount` object of the Brevo API.
final class PostCorporateSubAccountResponse {
  /// Creates a PostCorporateSubAccountResponse from already-decoded fields.
  const PostCorporateSubAccountResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCorporateSubAccountResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCorporateSubAccountResponse.fromJson(Map<String, Object?> json) =>
      PostCorporateSubAccountResponse(
        id: json.optInt('id'),
        raw: json,
      );

  /// ID of the sub-account created
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `PUT /corporate/user/invitation/{action}/{email}` object of the Brevo API.
final class PutCorporateUserInvitationActionEmailResponse {
  /// Creates a PutCorporateUserInvitationActionEmailResponse from already-decoded fields.
  const PutCorporateUserInvitationActionEmailResponse({
    this.message,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PutCorporateUserInvitationActionEmailResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PutCorporateUserInvitationActionEmailResponse.fromJson(
          Map<String, Object?> json) =>
      PutCorporateUserInvitationActionEmailResponse(
        message: json.optString('message'),
        raw: json,
      );

  /// Action success message
  final String? message;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (message != null) 'message': message!,
      };
}

/// The `POST /corporate/subAccount/ip/associate` object of the Brevo API.
final class RecordStringUnknown {
  /// Creates a RecordStringUnknown from already-decoded fields.
  const RecordStringUnknown({
    this.raw = const {},
  });

  /// Decodes a JSON object into a RecordStringUnknown. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory RecordStringUnknown.fromJson(Map<String, Object?> json) =>
      RecordStringUnknown(
        raw: json,
      );

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => const <String, Object?>{};
}
