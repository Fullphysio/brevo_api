// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import '../shared/models.dart';
import 'enums.dart';

/// The `components/schemas/getSmsCampaignOverview` object of the Brevo API.
final class GetSmsCampaignOverview {
  /// Creates a GetSmsCampaignOverview from already-decoded fields.
  const GetSmsCampaignOverview({
    this.content,
    this.createdAt,
    this.id,
    this.modifiedAt,
    this.name,
    this.organisationPrefix,
    this.scheduledAt,
    this.sender,
    this.sentDate,
    this.status,
    this.unsubscribeInstruction,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignOverview. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignOverview.fromJson(Map<String, Object?> json) =>
      GetSmsCampaignOverview(
        content: json.optString('content'),
        createdAt: json.optString('createdAt'),
        id: json.optInt('id'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        organisationPrefix: json.optString('organisationPrefix'),
        scheduledAt: json.optString('scheduledAt'),
        sender: json.optString('sender'),
        sentDate: json.optString('sentDate'),
        status: json.optEnum('status', GetSmsCampaignOverviewStatus.fromWire),
        unsubscribeInstruction: json.optString('unsubscribeInstruction'),
        raw: json,
      );

  /// Content of the SMS Campaign
  final String? content;

  /// Creation UTC date-time of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// ID of the SMS Campaign
  final int? id;

  /// UTC date-time of last modification of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the SMS Campaign
  final String? name;

  /// A recognizable prefix added as the Brand Name before the message content. Empty string if not set.
  final String? organisationPrefix;

  /// UTC date-time on which SMS campaign is scheduled. Should be in YYYY-MM-DDTHH:mm:ss.SSSZ format. Empty string if not scheduled.
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Sender of the SMS Campaign
  final String? sender;

  /// UTC date-time on which the SMS campaign was sent (YYYY-MM-DDTHH:mm:ss.SSSZ). Only available if the campaign status is 'sent'.
  final String? sentDate;

  /// [sentDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get sentDateDate =>
      sentDate == null ? null : DateTime.tryParse(sentDate!);

  /// Status of the SMS Campaign
  final GetSmsCampaignOverviewStatus? status;

  /// Instructions to unsubscribe from future communications. Empty string if not set.
  final String? unsubscribeInstruction;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (content != null) 'content': content!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (id != null) 'id': id!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (organisationPrefix != null)
          'organisationPrefix': organisationPrefix!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sender != null) 'sender': sender!,
        if (sentDate != null) 'sentDate': sentDate!,
        if (status != null) 'status': status!.value,
        if (unsubscribeInstruction != null)
          'unsubscribeInstruction': unsubscribeInstruction!,
      };
}

/// The `GET /smsCampaigns/{campaignId}` object of the Brevo API.
final class GetSmsCampaignResponse {
  /// Creates a GetSmsCampaignResponse from already-decoded fields.
  const GetSmsCampaignResponse({
    this.content,
    this.createdAt,
    this.id,
    this.modifiedAt,
    this.name,
    this.organisationPrefix,
    this.scheduledAt,
    this.sender,
    this.sentDate,
    this.status,
    this.unsubscribeInstruction,
    this.recipients,
    this.statistics,
    this.tags = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignResponse.fromJson(Map<String, Object?> json) =>
      GetSmsCampaignResponse(
        content: json.optString('content'),
        createdAt: json.optString('createdAt'),
        id: json.optInt('id'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        organisationPrefix: json.optString('organisationPrefix'),
        scheduledAt: json.optString('scheduledAt'),
        sender: json.optString('sender'),
        sentDate: json.optString('sentDate'),
        status: json.optEnum('status', GetSmsCampaignResponseStatus.fromWire),
        unsubscribeInstruction: json.optString('unsubscribeInstruction'),
        recipients: json.optNested(
            'recipients', GetSmsCampaignResponseRecipients.fromJson),
        statistics: json.optNested('statistics', GetSmsCampaignStats.fromJson),
        tags: json.optStringList('tags'),
        raw: json,
      );

  /// Content of the SMS Campaign
  final String? content;

  /// Creation UTC date-time of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// ID of the SMS Campaign
  final int? id;

  /// UTC date-time of last modification of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the SMS Campaign
  final String? name;

  /// A recognizable prefix added as the Brand Name before the message content. Empty string if not set.
  final String? organisationPrefix;

  /// UTC date-time on which SMS campaign is scheduled. Should be in YYYY-MM-DDTHH:mm:ss.SSSZ format. Empty string if not scheduled.
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Sender of the SMS Campaign
  final String? sender;

  /// UTC date-time on which the SMS campaign was sent (YYYY-MM-DDTHH:mm:ss.SSSZ). Only available if the campaign status is 'sent'.
  final String? sentDate;

  /// [sentDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get sentDateDate =>
      sentDate == null ? null : DateTime.tryParse(sentDate!);

  /// Status of the SMS Campaign
  final GetSmsCampaignResponseStatus? status;

  /// Instructions to unsubscribe from future communications. Empty string if not set.
  final String? unsubscribeInstruction;

  /// Recipients of the SMS campaign. For a single campaign, lists and exclusion lists are returned as objects with id and name.
  final GetSmsCampaignResponseRecipients? recipients;

  /// The `statistics` field.
  final GetSmsCampaignStats? statistics;

  /// Tags (labels) associated with the SMS campaign. Only available when retrieving a single campaign.
  final List<String> tags;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (content != null) 'content': content!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (id != null) 'id': id!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (organisationPrefix != null)
          'organisationPrefix': organisationPrefix!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sender != null) 'sender': sender!,
        if (sentDate != null) 'sentDate': sentDate!,
        if (status != null) 'status': status!.value,
        if (unsubscribeInstruction != null)
          'unsubscribeInstruction': unsubscribeInstruction!,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (statistics != null) 'statistics': statistics!.toJson(),
        if (tags.isNotEmpty) 'tags': tags,
      };
}

/// Recipients of the SMS campaign. For a single campaign, lists and exclusion lists are returned as objects with id and name.
final class GetSmsCampaignResponseRecipients {
  /// Creates a GetSmsCampaignResponseRecipients from already-decoded fields.
  const GetSmsCampaignResponseRecipients({
    this.exclusionLists = const [],
    this.lists = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignResponseRecipients. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignResponseRecipients.fromJson(
          Map<String, Object?> json) =>
      GetSmsCampaignResponseRecipients(
        exclusionLists: json.optObjectList('exclusionLists',
            GetSmsCampaignResponseRecipientsExclusionListsItem.fromJson,
            objectName: 'GetSmsCampaignResponseRecipients'),
        lists: json.optObjectList(
            'lists', GetSmsCampaignResponseRecipientsListsItem.fromJson,
            objectName: 'GetSmsCampaignResponseRecipients'),
        raw: json,
      );

  /// The `exclusionLists` field.
  final List<GetSmsCampaignResponseRecipientsExclusionListsItem> exclusionLists;

  /// The `lists` field.
  final List<GetSmsCampaignResponseRecipientsListsItem> lists;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (exclusionLists.isNotEmpty)
          'exclusionLists': [for (final item in exclusionLists) item.toJson()],
        if (lists.isNotEmpty)
          'lists': [for (final item in lists) item.toJson()],
      };
}

/// The `GetSmsCampaignResponseRecipients.exclusionListsItem` object of the Brevo API.
final class GetSmsCampaignResponseRecipientsExclusionListsItem {
  /// Creates a GetSmsCampaignResponseRecipientsExclusionListsItem from already-decoded fields.
  const GetSmsCampaignResponseRecipientsExclusionListsItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignResponseRecipientsExclusionListsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignResponseRecipientsExclusionListsItem.fromJson(
          Map<String, Object?> json) =>
      GetSmsCampaignResponseRecipientsExclusionListsItem(
        id: json.optInt('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// ID of the exclusion list
  final int? id;

  /// Name of the exclusion list
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `GetSmsCampaignResponseRecipients.listsItem` object of the Brevo API.
final class GetSmsCampaignResponseRecipientsListsItem {
  /// Creates a GetSmsCampaignResponseRecipientsListsItem from already-decoded fields.
  const GetSmsCampaignResponseRecipientsListsItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignResponseRecipientsListsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignResponseRecipientsListsItem.fromJson(
          Map<String, Object?> json) =>
      GetSmsCampaignResponseRecipientsListsItem(
        id: json.optInt('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// ID of the list
  final int? id;

  /// Name of the list
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `components/schemas/getSmsCampaignStats` object of the Brevo API.
final class GetSmsCampaignStats {
  /// Creates a GetSmsCampaignStats from already-decoded fields.
  const GetSmsCampaignStats({
    this.answered,
    this.delivered,
    this.hardBounces,
    this.processing,
    this.sent,
    this.softBounces,
    this.unsubscriptions,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignStats. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignStats.fromJson(Map<String, Object?> json) =>
      GetSmsCampaignStats(
        answered: json.optInt('answered'),
        delivered: json.optInt('delivered'),
        hardBounces: json.optInt('hardBounces'),
        processing: json.optInt('processing'),
        sent: json.optInt('sent'),
        softBounces: json.optInt('softBounces'),
        unsubscriptions: json.optInt('unsubscriptions'),
        raw: json,
      );

  /// Number of replies to the SMS
  final int? answered;

  /// Number of delivered SMS
  final int? delivered;

  /// Number of hardbounced SMS
  final int? hardBounces;

  /// Number of processing SMS
  final int? processing;

  /// Number of sent SMS
  final int? sent;

  /// Number of softbounced SMS
  final int? softBounces;

  /// Number of unsubscription SMS
  final int? unsubscriptions;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (answered != null) 'answered': answered!,
        if (delivered != null) 'delivered': delivered!,
        if (hardBounces != null) 'hardBounces': hardBounces!,
        if (processing != null) 'processing': processing!,
        if (sent != null) 'sent': sent!,
        if (softBounces != null) 'softBounces': softBounces!,
        if (unsubscriptions != null) 'unsubscriptions': unsubscriptions!,
      };
}

/// The `GET /smsCampaigns` object of the Brevo API.
final class GetSmsCampaignsResponse {
  /// Creates a GetSmsCampaignsResponse from already-decoded fields.
  const GetSmsCampaignsResponse({
    this.campaigns = const [],
    this.count,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignsResponse.fromJson(Map<String, Object?> json) =>
      GetSmsCampaignsResponse(
        campaigns: json.optObjectList(
            'campaigns', GetSmsCampaignsResponseCampaignsItem.fromJson,
            objectName: 'GetSmsCampaignsResponse'),
        count: json.optInt('count'),
        raw: json,
      );

  /// The `campaigns` field.
  final List<GetSmsCampaignsResponseCampaignsItem> campaigns;

  /// Number of SMS campaigns retrieved
  final int? count;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaigns.isNotEmpty)
          'campaigns': [for (final item in campaigns) item.toJson()],
        if (count != null) 'count': count!,
      };
}

/// The `GetSmsCampaignsResponse.campaignsItem` object of the Brevo API.
final class GetSmsCampaignsResponseCampaignsItem {
  /// Creates a GetSmsCampaignsResponseCampaignsItem from already-decoded fields.
  const GetSmsCampaignsResponseCampaignsItem({
    this.content,
    this.createdAt,
    this.id,
    this.modifiedAt,
    this.name,
    this.organisationPrefix,
    this.scheduledAt,
    this.sender,
    this.sentDate,
    this.status,
    this.unsubscribeInstruction,
    this.recipients,
    this.statistics,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSmsCampaignsResponseCampaignsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSmsCampaignsResponseCampaignsItem.fromJson(
          Map<String, Object?> json) =>
      GetSmsCampaignsResponseCampaignsItem(
        content: json.optString('content'),
        createdAt: json.optString('createdAt'),
        id: json.optInt('id'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        organisationPrefix: json.optString('organisationPrefix'),
        scheduledAt: json.optString('scheduledAt'),
        sender: json.optString('sender'),
        sentDate: json.optString('sentDate'),
        status: json.optEnum(
            'status', GetSmsCampaignsResponseCampaignsItemStatus.fromWire),
        unsubscribeInstruction: json.optString('unsubscribeInstruction'),
        recipients:
            json.optNested('recipients', GetCampaignRecipients.fromJson),
        statistics: json.optNested('statistics', GetSmsCampaignStats.fromJson),
        raw: json,
      );

  /// Content of the SMS Campaign
  final String? content;

  /// Creation UTC date-time of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// ID of the SMS Campaign
  final int? id;

  /// UTC date-time of last modification of the SMS campaign (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the SMS Campaign
  final String? name;

  /// A recognizable prefix added as the Brand Name before the message content. Empty string if not set.
  final String? organisationPrefix;

  /// UTC date-time on which SMS campaign is scheduled. Should be in YYYY-MM-DDTHH:mm:ss.SSSZ format. Empty string if not scheduled.
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Sender of the SMS Campaign
  final String? sender;

  /// UTC date-time on which the SMS campaign was sent (YYYY-MM-DDTHH:mm:ss.SSSZ). Only available if the campaign status is 'sent'.
  final String? sentDate;

  /// [sentDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get sentDateDate =>
      sentDate == null ? null : DateTime.tryParse(sentDate!);

  /// Status of the SMS Campaign
  final GetSmsCampaignsResponseCampaignsItemStatus? status;

  /// Instructions to unsubscribe from future communications. Empty string if not set.
  final String? unsubscribeInstruction;

  /// The `recipients` field.
  final GetCampaignRecipients? recipients;

  /// The `statistics` field.
  final GetSmsCampaignStats? statistics;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (content != null) 'content': content!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (id != null) 'id': id!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (organisationPrefix != null)
          'organisationPrefix': organisationPrefix!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sender != null) 'sender': sender!,
        if (sentDate != null) 'sentDate': sentDate!,
        if (status != null) 'status': status!.value,
        if (unsubscribeInstruction != null)
          'unsubscribeInstruction': unsubscribeInstruction!,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (statistics != null) 'statistics': statistics!.toJson(),
      };
}
