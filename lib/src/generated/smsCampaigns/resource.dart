// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/offset_page.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import '../shared/requests.dart';
import 'enums.dart';
import 'models.dart';
import 'requests.dart';

/// The `smsCampaigns` resource of the Brevo API: `client.smsCampaigns`.
final class BrevoSmsCampaigns {
  /// Creates the resource backed by [_client].
  const BrevoSmsCampaigns(this._client);

  final BrevoClient _client;

  /// `POST /smsCampaigns`
  ///
  /// Creates an SMS campaign
  ///
  /// Create a new SMS campaign with the required name, sender, and content fields. The sender name is limited to 11 alphanumeric characters or 15 numeric characters, and the content should stay within 160 characters per SMS segment. If a scheduledAt date is provided, listIds in recipients become mandatory; accounts under validation are limited to 4 total campaigns and campaigns with more than 10 recipients will be saved as draft.
  Future<void> createSmsCampaign(CreateSmsCampaignRequest request,
      {BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/smsCampaigns',
        body: request.toJson(),
        options: options);
  }

  /// `DELETE /smsCampaigns/{campaignId}`
  ///
  /// Delete an SMS campaign
  ///
  /// Delete an SMS campaign by its campaign ID. Only campaigns that have not been scheduled or sent can be deleted; attempting to delete a campaign that is queued, in process, or has been sent with recipients will return a 403 permission denied error.
  ///
  /// \[campaignId\]: id of the SMS campaign
  Future<void> deleteSmsCampaign(
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}',
        options: options);
  }

  /// `GET /smsCampaigns/{campaignId}`
  ///
  /// Get an SMS campaign
  ///
  /// Retrieve detailed information about a specific SMS campaign by its ID, including campaign content, sender, recipients with list names, statistics (delivered, sent, bounces, unsubscriptions, answered), and tags. Unlike the list endpoint, recipients are returned as objects with id and name fields rather than plain IDs.
  ///
  /// \[campaignId\]: id of the SMS campaign
  Future<GetSmsCampaignResponse> getSmsCampaign(
      {required int campaignId, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}',
        options: options);
    return GetSmsCampaignResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetSmsCampaignResponse'));
  }

  /// `GET /smsCampaigns`
  ///
  /// Returns the information for all your created SMS campaigns
  ///
  /// Retrieve a paginated list of all your SMS campaigns with their statistics and recipient information. Results can be filtered by status and date range, with a default limit of 500 and maximum of 1000 per page. The sort order defaults to descending by creation date; date filters are only available when status is not passed or is set to sent.
  ///
  /// \[status\]: Status of campaign.
  ///
  /// \[startDate\]: **Mandatory if endDate is used.** Starting (urlencoded) UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) to filter the sent SMS campaigns. **Prefer to pass your timezone in date-time format for accurate result.** Only available if `status` is not passed or is set to `sent`. `startDate` must not be in the future.
  ///
  /// \[endDate\]: **Mandatory if startDate is used.** Ending (urlencoded) UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) to filter the sent SMS campaigns. **Prefer to pass your timezone in date-time format for accurate result.** Only available if `status` is not passed or is set to `sent`. `endDate` must not be in the future.
  ///
  /// \[limit\]: Number of documents per page
  ///
  /// \[offset\]: Beginning point in the list to retrieve from.
  ///
  /// \[sort\]: Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed
  Future<GetSmsCampaignsResponse> getSmsCampaigns(
      {GetSmsCampaignsRequestStatus? status,
      String? startDate,
      String? endDate,
      int? limit,
      int? offset,
      GetSmsCampaignsRequestSort? sort,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/smsCampaigns',
        query: <String, Object?>{
          'status': status?.value,
          'startDate': startDate,
          'endDate': endDate,
          'limit': limit,
          'offset': offset,
          'sort': sort?.value
        },
        options: options);
    return GetSmsCampaignsResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetSmsCampaignsResponse'));
  }

  /// Calls \[getSmsCampaigns\] and wraps the result in a \[BrevoOffsetPage\], so the following pages can be fetched with `nextPage()` or every item walked with `autoPaging()`.
  Future<BrevoOffsetPage<GetSmsCampaignsResponseCampaignsItem>>
      getSmsCampaignsPaged(
          {int limit = 500,
          int offset = 0,
          GetSmsCampaignsRequestStatus? status,
          String? startDate,
          String? endDate,
          GetSmsCampaignsRequestSort? sort,
          BrevoRequestOptions? options}) async {
    final page = await getSmsCampaigns(
        limit: limit,
        status: status,
        startDate: startDate,
        endDate: endDate,
        sort: sort,
        options: options,
        offset: offset);
    return BrevoOffsetPage<GetSmsCampaignsResponseCampaignsItem>(
      items: page.campaigns,
      limit: limit,
      offset: offset,
      count: page.count,
      fetchPage: (next) => getSmsCampaignsPaged(
          limit: limit,
          status: status,
          startDate: startDate,
          endDate: endDate,
          sort: sort,
          options: options,
          offset: next),
    );
  }

  /// `POST /smsCampaigns/{campaignId}/exportRecipients`
  ///
  /// Export an SMS campaign's recipients
  ///
  /// It returns the background process ID which on completion calls the notify URL that you have set in the input.
  ///
  /// \[campaignId\]: id of the campaign
  Future<void> requestSmsRecipientExport(
      RequestSmsRecipientExportRequest request,
      {required int campaignId,
      BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}/exportRecipients',
        body: request.toJson(),
        options: options);
  }

  /// `POST /smsCampaigns/{campaignId}/sendNow`
  ///
  /// Send your SMS campaign immediately
  ///
  /// Send an existing SMS campaign immediately by scheduling it for the current time. The system verifies your account''s SMS credit balance before dispatching; if credits are insufficient or the remaining credit is less than the number of recipients, a 402 error is returned. The campaign must have valid recipients and content already configured.
  ///
  /// \[campaignId\]: id of the campaign
  Future<void> sendSmsCampaignNow(
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}/sendNow',
        options: options);
  }

  /// `POST /smsCampaigns/{campaignId}/sendReport`
  ///
  /// Send an SMS campaign's report
  ///
  /// Send report of Sent and Archived campaign, to the specified email addresses, with respective data and a pdf attachment in detail.
  ///
  /// \[campaignId\]: id of the campaign
  Future<void> sendSmsReport(SendReport request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}/sendReport',
        body: request.toJson(),
        options: options);
  }

  /// `POST /smsCampaigns/{campaignId}/sendTest`
  ///
  /// Send a test SMS campaign
  ///
  /// Send a test SMS to a specified phone number to preview the campaign before sending it to all recipients. The phone number must belong to one of your existing contacts in your Brevo account and must not be blacklisted. The number should include the country code (e.g. 33689965433).
  ///
  /// \[campaignId\]: Id of the SMS campaign
  Future<void> sendTestSms(SendTestSmsRequest request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}/sendTest',
        body: request.toJson(),
        options: options);
  }

  /// `PUT /smsCampaigns/{campaignId}`
  ///
  /// Update an SMS campaign
  ///
  /// Update an existing SMS campaign''s properties such as name, sender, content, recipients, scheduled date, organisation prefix, and unsubscribe instructions. The request body must contain at least one valid field to update. The campaign must exist and must be of type SMS; if a scheduledAt is provided, valid recipients must be present either in the request or already configured on the campaign.
  ///
  /// \[campaignId\]: id of the SMS campaign
  Future<void> updateSmsCampaign(UpdateSmsCampaignRequest request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'PUT',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}',
        body: request.toJson(),
        options: options);
  }

  /// `PUT /smsCampaigns/{campaignId}/status`
  ///
  /// Update a campaign's status
  ///
  /// Update the status of an SMS campaign, such as suspending, archiving, or replicating it. Available status values include suspended, archive, darchive, sent, queued, replicate, replicateTemplate, cancel, and draft. Note that the replicateTemplate status is only available for template type campaigns.
  ///
  /// \[campaignId\]: id of the campaign
  Future<void> updateSmsCampaignStatus(UpdateCampaignStatus request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'PUT',
        path: '/smsCampaigns/${brevoPathSegment(campaignId)}/status',
        body: request.toJson(),
        options: options);
  }
}
