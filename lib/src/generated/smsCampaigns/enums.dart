// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/open_enum.dart';

/// Status of the SMS Campaign
final class GetSmsCampaignOverviewStatus extends BrevoOpenEnum {
  const GetSmsCampaignOverviewStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetSmsCampaignOverviewStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetSmsCampaignOverviewStatus._('draft');

  /// The `sent` value.
  static const sent = GetSmsCampaignOverviewStatus._('sent');

  /// The `archive` value.
  static const archive = GetSmsCampaignOverviewStatus._('archive');

  /// The `queued` value.
  static const queued = GetSmsCampaignOverviewStatus._('queued');

  /// The `suspended` value.
  static const suspended = GetSmsCampaignOverviewStatus._('suspended');

  /// The `inProcess` value.
  static const inProcess = GetSmsCampaignOverviewStatus._('inProcess');

  /// The values known when this package was generated.
  static const values = [draft, sent, archive, queued, suspended, inProcess];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetSmsCampaignOverviewStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetSmsCampaignOverviewStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Status of the SMS Campaign
final class GetSmsCampaignResponseStatus extends BrevoOpenEnum {
  const GetSmsCampaignResponseStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetSmsCampaignResponseStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetSmsCampaignResponseStatus._('draft');

  /// The `sent` value.
  static const sent = GetSmsCampaignResponseStatus._('sent');

  /// The `archive` value.
  static const archive = GetSmsCampaignResponseStatus._('archive');

  /// The `queued` value.
  static const queued = GetSmsCampaignResponseStatus._('queued');

  /// The `suspended` value.
  static const suspended = GetSmsCampaignResponseStatus._('suspended');

  /// The `inProcess` value.
  static const inProcess = GetSmsCampaignResponseStatus._('inProcess');

  /// The values known when this package was generated.
  static const values = [draft, sent, archive, queued, suspended, inProcess];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetSmsCampaignResponseStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetSmsCampaignResponseStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetSmsCampaignsRequestSort` values of the Brevo API.
final class GetSmsCampaignsRequestSort extends BrevoOpenEnum {
  const GetSmsCampaignsRequestSort._(super.value);

  /// Wraps a value this package does not know.
  const GetSmsCampaignsRequestSort.unknown(super.value);

  /// The `asc` value.
  static const asc = GetSmsCampaignsRequestSort._('asc');

  /// The `desc` value.
  static const desc = GetSmsCampaignsRequestSort._('desc');

  /// The values known when this package was generated.
  static const values = [asc, desc];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetSmsCampaignsRequestSort.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetSmsCampaignsRequestSort.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetSmsCampaignsRequestStatus` values of the Brevo API.
final class GetSmsCampaignsRequestStatus extends BrevoOpenEnum {
  const GetSmsCampaignsRequestStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetSmsCampaignsRequestStatus.unknown(super.value);

  /// The `suspended` value.
  static const suspended = GetSmsCampaignsRequestStatus._('suspended');

  /// The `archive` value.
  static const archive = GetSmsCampaignsRequestStatus._('archive');

  /// The `sent` value.
  static const sent = GetSmsCampaignsRequestStatus._('sent');

  /// The `queued` value.
  static const queued = GetSmsCampaignsRequestStatus._('queued');

  /// The `draft` value.
  static const draft = GetSmsCampaignsRequestStatus._('draft');

  /// The `inProcess` value.
  static const inProcess = GetSmsCampaignsRequestStatus._('inProcess');

  /// The values known when this package was generated.
  static const values = [suspended, archive, sent, queued, draft, inProcess];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetSmsCampaignsRequestStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetSmsCampaignsRequestStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Status of the SMS Campaign
final class GetSmsCampaignsResponseCampaignsItemStatus extends BrevoOpenEnum {
  const GetSmsCampaignsResponseCampaignsItemStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetSmsCampaignsResponseCampaignsItemStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetSmsCampaignsResponseCampaignsItemStatus._('draft');

  /// The `sent` value.
  static const sent = GetSmsCampaignsResponseCampaignsItemStatus._('sent');

  /// The `archive` value.
  static const archive =
      GetSmsCampaignsResponseCampaignsItemStatus._('archive');

  /// The `queued` value.
  static const queued = GetSmsCampaignsResponseCampaignsItemStatus._('queued');

  /// The `suspended` value.
  static const suspended =
      GetSmsCampaignsResponseCampaignsItemStatus._('suspended');

  /// The `inProcess` value.
  static const inProcess =
      GetSmsCampaignsResponseCampaignsItemStatus._('inProcess');

  /// The values known when this package was generated.
  static const values = [draft, sent, archive, queued, suspended, inProcess];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetSmsCampaignsResponseCampaignsItemStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetSmsCampaignsResponseCampaignsItemStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Filter the recipients based on how they interacted with the campaign
final class RequestSmsRecipientExportRequestRecipientsType
    extends BrevoOpenEnum {
  const RequestSmsRecipientExportRequestRecipientsType._(super.value);

  /// Wraps a value this package does not know.
  const RequestSmsRecipientExportRequestRecipientsType.unknown(super.value);

  /// The `all` value.
  static const all = RequestSmsRecipientExportRequestRecipientsType._('all');

  /// The `delivered` value.
  static const delivered =
      RequestSmsRecipientExportRequestRecipientsType._('delivered');

  /// The `answered` value.
  static const answered =
      RequestSmsRecipientExportRequestRecipientsType._('answered');

  /// The `softBounces` value.
  static const softBounces =
      RequestSmsRecipientExportRequestRecipientsType._('softBounces');

  /// The `hardBounces` value.
  static const hardBounces =
      RequestSmsRecipientExportRequestRecipientsType._('hardBounces');

  /// The `unsubscribed` value.
  static const unsubscribed =
      RequestSmsRecipientExportRequestRecipientsType._('unsubscribed');

  /// The values known when this package was generated.
  static const values = [
    all,
    delivered,
    answered,
    softBounces,
    hardBounces,
    unsubscribed
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory RequestSmsRecipientExportRequestRecipientsType.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            RequestSmsRecipientExportRequestRecipientsType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}
