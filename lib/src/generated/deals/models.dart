// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// Deal Details
final class Deal {
  /// Creates a Deal from already-decoded fields.
  const Deal({
    this.attributes = const {},
    this.id,
    this.linkedCompaniesIds = const [],
    this.linkedContactsIds = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a Deal. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Deal.fromJson(Map<String, Object?> json) => Deal(
        attributes: json.optObjectMap('attributes'),
        id: json.optString('id'),
        linkedCompaniesIds: json.optStringList('linkedCompaniesIds'),
        linkedContactsIds: json.optIntList('linkedContactsIds'),
        raw: json,
      );

  /// Deal attributes with values
  final Map<String, Object?> attributes;

  /// Unique deal id
  final String? id;

  /// Companies ids for companies linked to this deal
  final List<String> linkedCompaniesIds;

  /// Contact ids for contacts linked to this deal
  final List<int> linkedContactsIds;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (id != null) 'id': id!,
        if (linkedCompaniesIds.isNotEmpty)
          'linkedCompaniesIds': linkedCompaniesIds,
        if (linkedContactsIds.isNotEmpty)
          'linkedContactsIds': linkedContactsIds,
      };
}

/// Deal attribute details
final class GetCrmAttributesDealsResponseItem {
  /// Creates a GetCrmAttributesDealsResponseItem from already-decoded fields.
  const GetCrmAttributesDealsResponseItem({
    this.attributeOptions = const [],
    this.attributeTypeName,
    this.internalName,
    this.isRequired,
    this.isValueReadonly,
    this.label,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmAttributesDealsResponseItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmAttributesDealsResponseItem.fromJson(
          Map<String, Object?> json) =>
      GetCrmAttributesDealsResponseItem(
        attributeOptions: json.optObjectList('attributeOptions',
            GetCrmAttributesDealsResponseItemAttributeOptionsItem.fromJson,
            objectName: 'GetCrmAttributesDealsResponseItem'),
        attributeTypeName: json.optString('attributeTypeName'),
        internalName: json.optString('internalName'),
        isRequired: json.optBool('isRequired'),
        isValueReadonly: json.optBool('isValueReadonly'),
        label: json.optString('label'),
        raw: json,
      );

  /// Options for single-select or multi-choice attributes
  final List<GetCrmAttributesDealsResponseItemAttributeOptionsItem>
      attributeOptions;

  /// Type of the attribute
  final String? attributeTypeName;

  /// Internal name of the attribute
  final String? internalName;

  /// Whether this attribute is required
  final bool? isRequired;

  /// Whether this attribute value is read-only
  final bool? isValueReadonly;

  /// Display label of the attribute
  final String? label;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributeOptions.isNotEmpty)
          'attributeOptions': [
            for (final item in attributeOptions) item.toJson()
          ],
        if (attributeTypeName != null) 'attributeTypeName': attributeTypeName!,
        if (internalName != null) 'internalName': internalName!,
        if (isRequired != null) 'isRequired': isRequired!,
        if (isValueReadonly != null) 'isValueReadonly': isValueReadonly!,
        if (label != null) 'label': label!,
      };
}

/// The `GetCrmAttributesDealsResponseItem.attributeOptionsItem` object of the Brevo API.
final class GetCrmAttributesDealsResponseItemAttributeOptionsItem {
  /// Creates a GetCrmAttributesDealsResponseItemAttributeOptionsItem from already-decoded fields.
  const GetCrmAttributesDealsResponseItemAttributeOptionsItem({
    this.key,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmAttributesDealsResponseItemAttributeOptionsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmAttributesDealsResponseItemAttributeOptionsItem.fromJson(
          Map<String, Object?> json) =>
      GetCrmAttributesDealsResponseItemAttributeOptionsItem(
        key: json.optString('key'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// The `key` field.
  final String? key;

  /// The `value` field.
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (key != null) 'key': key!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// List of Deals
final class GetCrmDealsResponse {
  /// Creates a GetCrmDealsResponse from already-decoded fields.
  const GetCrmDealsResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmDealsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmDealsResponse.fromJson(Map<String, Object?> json) =>
      GetCrmDealsResponse(
        items: json.optObjectList('items', Deal.fromJson,
            objectName: 'GetCrmDealsResponse'),
        raw: json,
      );

  /// List of deals
  final List<Deal> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// Pipeline details with stages
final class Pipeline {
  /// Creates a Pipeline from already-decoded fields.
  const Pipeline({
    this.pipeline,
    this.pipelineName,
    this.stages = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a Pipeline. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Pipeline.fromJson(Map<String, Object?> json) => Pipeline(
        pipeline: json.optString('pipeline'),
        pipelineName: json.optString('pipeline_name'),
        stages: json.optObjectList('stages', PipelineStagesItem.fromJson,
            objectName: 'Pipeline'),
        raw: json,
      );

  /// Pipeline id
  final String? pipeline;

  /// Pipeline Name
  final String? pipelineName;

  /// List of pipeline stages
  final List<PipelineStagesItem> stages;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (pipeline != null) 'pipeline': pipeline!,
        if (pipelineName != null) 'pipeline_name': pipelineName!,
        if (stages.isNotEmpty)
          'stages': [for (final item in stages) item.toJson()],
      };
}

/// Stage details
final class PipelineStagesItem {
  /// Creates a PipelineStagesItem from already-decoded fields.
  const PipelineStagesItem({
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PipelineStagesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PipelineStagesItem.fromJson(Map<String, Object?> json) =>
      PipelineStagesItem(
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// Stage id
  final String? id;

  /// Stage name
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The `POST /crm/deals/import` object of the Brevo API.
final class PostCrmDealsImportResponse {
  /// Creates a PostCrmDealsImportResponse from already-decoded fields.
  const PostCrmDealsImportResponse({
    this.processId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCrmDealsImportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCrmDealsImportResponse.fromJson(Map<String, Object?> json) =>
      PostCrmDealsImportResponse(
        processId: json.optInt('processId'),
        raw: json,
      );

  /// The ID of the import process
  final int? processId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (processId != null) 'processId': processId!,
      };
}

/// Created deal id
final class PostCrmDealsResponse {
  /// Creates a PostCrmDealsResponse from already-decoded fields.
  const PostCrmDealsResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCrmDealsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCrmDealsResponse.fromJson(Map<String, Object?> json) =>
      PostCrmDealsResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// Unique deal id
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}
