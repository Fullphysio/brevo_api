// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import '../../core/multipart.dart';

/// The request body of `PATCH /crm/deals/{id}`.
final class PatchCrmDealsIdRequest {
  /// Creates the request body.
  const PatchCrmDealsIdRequest({
    this.attributes,
    this.linkedCompaniesIds,
    this.linkedContactsIds,
    this.name,
  });

  /// Decodes a JSON object into a PatchCrmDealsIdRequest. Optional fields absent from [json] stay null.
  factory PatchCrmDealsIdRequest.fromJson(Map<String, Object?> json) =>
      PatchCrmDealsIdRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        linkedCompaniesIds: json.containsKey('linkedCompaniesIds')
            ? json.optStringList('linkedCompaniesIds')
            : null,
        linkedContactsIds: json.containsKey('linkedContactsIds')
            ? json.optIntList('linkedContactsIds')
            : null,
        name: json.optString('name'),
      );

  /// Attributes for deal update To assign owner of a Deal you can send attributes.deal_owner and utilize the account email or ID. If you wish to update the pipeline of a deal you need to provide the `pipeline` and the `deal_stage` Pipeline and deal_stage are ids you can fetch using this endpoint `/crm/pipeline/details/{pipelineID}`
  final Map<String, Object?>? attributes;

  /// Warning - Using PATCH on linkedCompaniesIds replaces the list of linked companies. Omitted IDs will be removed.
  final List<String>? linkedCompaniesIds;

  /// Warning - Using PATCH on linkedContactIds replaces the list of linked contacts. Omitted IDs will be removed.
  final List<int>? linkedContactsIds;

  /// Name of deal
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (linkedCompaniesIds != null)
          'linkedCompaniesIds': linkedCompaniesIds!,
        if (linkedContactsIds != null) 'linkedContactsIds': linkedContactsIds!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `PATCH /crm/deals/link-unlink/{id}`.
final class PatchCrmDealsLinkUnlinkIdRequest {
  /// Creates the request body.
  const PatchCrmDealsLinkUnlinkIdRequest({
    this.linkCompanyIds,
    this.linkContactIds,
    this.unlinkCompanyIds,
    this.unlinkContactIds,
  });

  /// Decodes a JSON object into a PatchCrmDealsLinkUnlinkIdRequest. Optional fields absent from [json] stay null.
  factory PatchCrmDealsLinkUnlinkIdRequest.fromJson(
          Map<String, Object?> json) =>
      PatchCrmDealsLinkUnlinkIdRequest(
        linkCompanyIds: json.containsKey('linkCompanyIds')
            ? json.optStringList('linkCompanyIds')
            : null,
        linkContactIds: json.containsKey('linkContactIds')
            ? json.optIntList('linkContactIds')
            : null,
        unlinkCompanyIds: json.containsKey('unlinkCompanyIds')
            ? json.optStringList('unlinkCompanyIds')
            : null,
        unlinkContactIds: json.containsKey('unlinkContactIds')
            ? json.optIntList('unlinkContactIds')
            : null,
      );

  /// Company ids to be linked with deal
  final List<String>? linkCompanyIds;

  /// Contact ids for contacts to be linked with deal
  final List<int>? linkContactIds;

  /// Company ids to be unlinked from deal
  final List<String>? unlinkCompanyIds;

  /// Contact ids for contacts to be unlinked from deal
  final List<int>? unlinkContactIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (linkCompanyIds != null) 'linkCompanyIds': linkCompanyIds!,
        if (linkContactIds != null) 'linkContactIds': linkContactIds!,
        if (unlinkCompanyIds != null) 'unlinkCompanyIds': unlinkCompanyIds!,
        if (unlinkContactIds != null) 'unlinkContactIds': unlinkContactIds!,
      };
}

/// The request body of `POST /crm/deals/import`.
final class PostCrmDealsImportRequest {
  /// Creates the request body.
  const PostCrmDealsImportRequest({
    this.file,
    this.mapping,
  });

  /// The CSV file to upload.The file should have the first row as the mapping attribute. Some default attribute names are (a) deal_id \[brevo mongoID to update deals\] (b) associated_contact (c) associated_company (f) any other attribute with internal name
  final BrevoFile? file;

  /// The mapping options in JSON format. Here is an example of the JSON structure: ```json {
  ///   "link_entities": true, // Determines whether to link related entities during the import process
  ///   "unlink_entities": false, // Determines whether to unlink related entities during the import process
  ///   "update_existing_records": true, // Determines whether to update based on deal ID or treat every row as create
  ///   "unset_empty_attributes": false // Determines whether to unset a specific attribute during update if the values input is blank
  /// } ```
  final Map<String, Object?>? mapping;

  /// The non-file parts of the `multipart/form-data` body; a `Map` or `List` value is sent JSON-encoded.
  Map<String, Object?> toFields() => {
        if (mapping != null) 'mapping': mapping!,
      };

  /// The file parts of the `multipart/form-data` body.
  Map<String, BrevoFile> toFiles() => {
        if (file != null) 'file': file!,
      };
}

/// The request body of `POST /crm/deals`.
final class PostCrmDealsRequest {
  /// Creates the request body.
  const PostCrmDealsRequest({
    this.attributes,
    this.linkedCompaniesIds,
    this.linkedContactsIds,
    required this.name,
  });

  /// Decodes a JSON object into a PostCrmDealsRequest. Optional fields absent from [json] stay null.
  factory PostCrmDealsRequest.fromJson(Map<String, Object?> json) =>
      PostCrmDealsRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        linkedCompaniesIds: json.containsKey('linkedCompaniesIds')
            ? json.optStringList('linkedCompaniesIds')
            : null,
        linkedContactsIds: json.containsKey('linkedContactsIds')
            ? json.optIntList('linkedContactsIds')
            : null,
        name: json.requireString('name', 'PostCrmDealsRequest'),
      );

  /// Attributes for deal creation To assign owner of a Deal you can send attributes.deal_owner and utilize the account email or ID. If you want to create a deal on a specific pipeline and stage you can use the following attributes `pipeline` and `deal_stage`. Pipeline and deal_stage are ids you can fetch using this endpoint `/crm/pipeline/details/{pipelineID}`
  final Map<String, Object?>? attributes;

  /// Company ids to be linked with deal
  final List<String>? linkedCompaniesIds;

  /// Contact ids to be linked with deal
  final List<int>? linkedContactsIds;

  /// Name of deal
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (linkedCompaniesIds != null)
          'linkedCompaniesIds': linkedCompaniesIds!,
        if (linkedContactsIds != null) 'linkedContactsIds': linkedContactsIds!,
        'name': name,
      };
}
