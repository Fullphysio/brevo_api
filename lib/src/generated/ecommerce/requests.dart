// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';

/// The request body of `POST /orders/status/batch`.
final class CreateBatchOrderRequest {
  /// Creates the request body.
  const CreateBatchOrderRequest({
    this.historical,
    this.notifyUrl,
    required this.orders,
  });

  /// Decodes a JSON object into a CreateBatchOrderRequest. Optional fields absent from [json] stay null.
  factory CreateBatchOrderRequest.fromJson(Map<String, Object?> json) =>
      CreateBatchOrderRequest(
        historical: json.optBool('historical'),
        notifyUrl: json.optString('notifyUrl'),
        orders: json.containsKey('orders')
            ? json.optObjectList('orders', Order.fromJson,
                objectName: 'CreateBatchOrderRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateBatchOrderRequest', key: 'orders')),
      );

  /// Defines whether you want your orders to be considered as live data or as historical data (import of past data, synchronising data). True: orders will not trigger any automation workflows. False: orders will trigger workflows as usual.
  final bool? historical;

  /// Webhook URL to receive the status of the batch request
  final String? notifyUrl;

  /// array of order objects
  final List<Order> orders;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (historical != null) 'historical': historical!,
        if (notifyUrl != null) 'notifyUrl': notifyUrl!,
        'orders': [for (final item in orders) item.toJson()],
      };
}

/// The request body of `POST /products/{id}/alerts/{type}`.
final class CreateProductAlertRequest {
  /// Creates the request body.
  const CreateProductAlertRequest({
    this.contactIdentifiers,
  });

  /// Decodes a JSON object into a CreateProductAlertRequest. Optional fields absent from [json] stay null.
  factory CreateProductAlertRequest.fromJson(Map<String, Object?> json) =>
      CreateProductAlertRequest(
        contactIdentifiers: json.optNested('contactIdentifiers',
            CreateProductAlertRequestContactIdentifiers.fromJson),
      );

  /// The `contactIdentifiers` field.
  final CreateProductAlertRequestContactIdentifiers? contactIdentifiers;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactIdentifiers != null)
          'contactIdentifiers': contactIdentifiers!.toJson(),
      };
}

/// The request body of `CreateProductAlertRequest.contactIdentifiers`.
final class CreateProductAlertRequestContactIdentifiers {
  /// Creates the request body.
  const CreateProductAlertRequestContactIdentifiers({
    this.email,
    this.extId,
    this.sms,
  });

  /// Decodes a JSON object into a CreateProductAlertRequestContactIdentifiers. Optional fields absent from [json] stay null.
  factory CreateProductAlertRequestContactIdentifiers.fromJson(
          Map<String, Object?> json) =>
      CreateProductAlertRequestContactIdentifiers(
        email: json.optString('email'),
        extId: json.optString('ext_id'),
        sms: json.optString('sms'),
      );

  /// Email address of the contact
  final String? email;

  /// Contact ID in your system
  final String? extId;

  /// Sms of the contact
  final String? sms;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (extId != null) 'ext_id': extId!,
        if (sms != null) 'sms': sms!,
      };
}

/// The request body of `POST /categories/batch`.
final class CreateUpdateBatchCategoryRequest {
  /// Creates the request body.
  const CreateUpdateBatchCategoryRequest({
    required this.categories,
    this.updateEnabled,
  });

  /// Decodes a JSON object into a CreateUpdateBatchCategoryRequest. Optional fields absent from [json] stay null.
  factory CreateUpdateBatchCategoryRequest.fromJson(
          Map<String, Object?> json) =>
      CreateUpdateBatchCategoryRequest(
        categories: json.containsKey('categories')
            ? json.optObjectList('categories',
                CreateUpdateBatchCategoryRequestCategoriesItem.fromJson,
                objectName: 'CreateUpdateBatchCategoryRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateUpdateBatchCategoryRequest',
                key: 'categories')),
        updateEnabled: json.optBool('updateEnabled'),
      );

  /// array of categories objects
  final List<CreateUpdateBatchCategoryRequestCategoriesItem> categories;

  /// Facilitate to update the existing categories in the same request (updateEnabled = true)
  final bool? updateEnabled;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'categories': [for (final item in categories) item.toJson()],
        if (updateEnabled != null) 'updateEnabled': updateEnabled!,
      };
}

/// The request body of `CreateUpdateBatchCategoryRequest.categoriesItem`.
final class CreateUpdateBatchCategoryRequestCategoriesItem {
  /// Creates the request body.
  const CreateUpdateBatchCategoryRequestCategoriesItem({
    this.deletedAt,
    required this.id,
    this.isDeleted,
    this.name,
    this.url,
  });

  /// Decodes a JSON object into a CreateUpdateBatchCategoryRequestCategoriesItem. Optional fields absent from [json] stay null.
  factory CreateUpdateBatchCategoryRequestCategoriesItem.fromJson(
          Map<String, Object?> json) =>
      CreateUpdateBatchCategoryRequestCategoriesItem(
        deletedAt: json.optString('deletedAt'),
        id: json.requireString(
            'id', 'CreateUpdateBatchCategoryRequestCategoriesItem'),
        isDeleted: json.optBool('isDeleted'),
        name: json.optString('name'),
        url: json.optString('url'),
      );

  /// UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) of the category deleted from the shop's database
  final String? deletedAt;

  /// [deletedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get deletedAtDate =>
      deletedAt == null ? null : DateTime.tryParse(deletedAt!);

  /// Unique Category ID as saved in the shop
  final String id;

  /// category deleted from the shop's database
  final bool? isDeleted;

  /// **Mandatory in case of creation**. Name of the Category, as displayed in the shop
  final String? name;

  /// URL to the category
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (deletedAt != null) 'deletedAt': deletedAt!,
        'id': id,
        if (isDeleted != null) 'isDeleted': isDeleted!,
        if (name != null) 'name': name!,
        if (url != null) 'url': url!,
      };
}

/// The request body of `POST /products/batch`.
final class CreateUpdateBatchProductsRequest {
  /// Creates the request body.
  const CreateUpdateBatchProductsRequest({
    required this.products,
    this.updateEnabled,
  });

  /// Decodes a JSON object into a CreateUpdateBatchProductsRequest. Optional fields absent from [json] stay null.
  factory CreateUpdateBatchProductsRequest.fromJson(
          Map<String, Object?> json) =>
      CreateUpdateBatchProductsRequest(
        products: json.containsKey('products')
            ? json.optObjectList('products',
                CreateUpdateBatchProductsRequestProductsItem.fromJson,
                objectName: 'CreateUpdateBatchProductsRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateUpdateBatchProductsRequest',
                key: 'products')),
        updateEnabled: json.optBool('updateEnabled'),
      );

  /// array of products objects
  final List<CreateUpdateBatchProductsRequestProductsItem> products;

  /// Facilitate to update the existing products in the same request (updateEnabled = true)
  final bool? updateEnabled;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'products': [for (final item in products) item.toJson()],
        if (updateEnabled != null) 'updateEnabled': updateEnabled!,
      };
}

/// The request body of `CreateUpdateBatchProductsRequest.productsItem`.
final class CreateUpdateBatchProductsRequestProductsItem {
  /// Creates the request body.
  const CreateUpdateBatchProductsRequestProductsItem({
    this.alternativePrice,
    this.brand,
    this.categories,
    this.deletedAt,
    this.description,
    required this.id,
    this.imageUrl,
    this.isDeleted,
    this.metaInfo,
    required this.name,
    this.parentId,
    this.price,
    this.sku,
    this.stock,
    this.url,
  });

  /// Decodes a JSON object into a CreateUpdateBatchProductsRequestProductsItem. Optional fields absent from [json] stay null.
  factory CreateUpdateBatchProductsRequestProductsItem.fromJson(
          Map<String, Object?> json) =>
      CreateUpdateBatchProductsRequestProductsItem(
        alternativePrice: json.optDouble('alternativePrice'),
        brand: json.optString('brand'),
        categories: json.containsKey('categories')
            ? json.optStringList('categories')
            : null,
        deletedAt: json.optString('deletedAt'),
        description: json.optString('description'),
        id: json.requireString(
            'id', 'CreateUpdateBatchProductsRequestProductsItem'),
        imageUrl: json.optString('imageUrl'),
        isDeleted: json.optBool('isDeleted'),
        metaInfo:
            json.containsKey('metaInfo') ? json.optObjectMap('metaInfo') : null,
        name: json.requireString(
            'name', 'CreateUpdateBatchProductsRequestProductsItem'),
        parentId: json.optString('parentId'),
        price: json.optDouble('price'),
        sku: json.optString('sku'),
        stock: json.optDouble('stock'),
        url: json.optString('url'),
      );

  /// Alternative price of the product
  final double? alternativePrice;

  /// Brand of the product
  final String? brand;

  /// Category ID-s of the product
  final List<String>? categories;

  /// UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) of the product deleted from the shop's database
  final String? deletedAt;

  /// [deletedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get deletedAtDate =>
      deletedAt == null ? null : DateTime.tryParse(deletedAt!);

  /// Description of the product
  final String? description;

  /// Product ID for which you requested the details
  final String id;

  /// Absolute URL to the cover image of the product
  final String? imageUrl;

  /// product deleted from the shop's database
  final bool? isDeleted;

  /// Meta data of product such as description, vendor, producer, stock level. The total characters of cumulative metaInfo shall not exceed **20000 characters**.
  final Map<String, Object?>? metaInfo;

  /// **Mandatory in case of creation**. Name of the product, as displayed in the shop
  final String name;

  /// Parent product id of the product
  final String? parentId;

  /// Price of the product
  final double? price;

  /// Product identifier from the shop
  final String? sku;

  /// Current stock value of the product from the shop's database
  final double? stock;

  /// URL to the product
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (alternativePrice != null) 'alternativePrice': alternativePrice!,
        if (brand != null) 'brand': brand!,
        if (categories != null) 'categories': categories!,
        if (deletedAt != null) 'deletedAt': deletedAt!,
        if (description != null) 'description': description!,
        'id': id,
        if (imageUrl != null) 'imageUrl': imageUrl!,
        if (isDeleted != null) 'isDeleted': isDeleted!,
        if (metaInfo != null) 'metaInfo': metaInfo!,
        'name': name,
        if (parentId != null) 'parentId': parentId!,
        if (price != null) 'price': price!,
        if (sku != null) 'sku': sku!,
        if (stock != null) 'stock': stock!,
        if (url != null) 'url': url!,
      };
}

/// The request body of `POST /categories`.
final class CreateUpdateCategoryRequest {
  /// Creates the request body.
  const CreateUpdateCategoryRequest({
    this.deletedAt,
    required this.id,
    this.isDeleted,
    this.name,
    this.updateEnabled,
    this.url,
  });

  /// Decodes a JSON object into a CreateUpdateCategoryRequest. Optional fields absent from [json] stay null.
  factory CreateUpdateCategoryRequest.fromJson(Map<String, Object?> json) =>
      CreateUpdateCategoryRequest(
        deletedAt: json.optString('deletedAt'),
        id: json.requireString('id', 'CreateUpdateCategoryRequest'),
        isDeleted: json.optBool('isDeleted'),
        name: json.optString('name'),
        updateEnabled: json.optBool('updateEnabled'),
        url: json.optString('url'),
      );

  /// UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) of the category deleted from the shop's database
  final String? deletedAt;

  /// [deletedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get deletedAtDate =>
      deletedAt == null ? null : DateTime.tryParse(deletedAt!);

  /// Unique Category ID as saved in the shop
  final String id;

  /// category deleted from the shop's database
  final bool? isDeleted;

  /// **Mandatory in case of creation**. Name of the Category, as displayed in the shop
  final String? name;

  /// Facilitate to update the existing category in the same request (updateEnabled = true)
  final bool? updateEnabled;

  /// URL to the category
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (deletedAt != null) 'deletedAt': deletedAt!,
        'id': id,
        if (isDeleted != null) 'isDeleted': isDeleted!,
        if (name != null) 'name': name!,
        if (updateEnabled != null) 'updateEnabled': updateEnabled!,
        if (url != null) 'url': url!,
      };
}

/// The request body of `POST /products`.
final class CreateUpdateProductRequest {
  /// Creates the request body.
  const CreateUpdateProductRequest({
    this.alternativePrice,
    this.brand,
    this.categories,
    this.deletedAt,
    this.description,
    required this.id,
    this.imageUrl,
    this.isDeleted,
    this.metaInfo,
    required this.name,
    this.parentId,
    this.price,
    this.sku,
    this.stock,
    this.updateEnabled,
    this.url,
  });

  /// Decodes a JSON object into a CreateUpdateProductRequest. Optional fields absent from [json] stay null.
  factory CreateUpdateProductRequest.fromJson(Map<String, Object?> json) =>
      CreateUpdateProductRequest(
        alternativePrice: json.optDouble('alternativePrice'),
        brand: json.optString('brand'),
        categories: json.containsKey('categories')
            ? json.optStringList('categories')
            : null,
        deletedAt: json.optString('deletedAt'),
        description: json.optString('description'),
        id: json.requireString('id', 'CreateUpdateProductRequest'),
        imageUrl: json.optString('imageUrl'),
        isDeleted: json.optBool('isDeleted'),
        metaInfo:
            json.containsKey('metaInfo') ? json.optObjectMap('metaInfo') : null,
        name: json.requireString('name', 'CreateUpdateProductRequest'),
        parentId: json.optString('parentId'),
        price: json.optDouble('price'),
        sku: json.optString('sku'),
        stock: json.optDouble('stock'),
        updateEnabled: json.optBool('updateEnabled'),
        url: json.optString('url'),
      );

  /// Alternative price of the product
  final double? alternativePrice;

  /// Brand of the product
  final String? brand;

  /// Category ID-s of the product
  final List<String>? categories;

  /// UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) of the product deleted from the shop's database
  final String? deletedAt;

  /// [deletedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get deletedAtDate =>
      deletedAt == null ? null : DateTime.tryParse(deletedAt!);

  /// Description of the product
  final String? description;

  /// Product ID for which you requested the details
  final String id;

  /// Absolute URL to the cover image of the product
  final String? imageUrl;

  /// product deleted from the shop's database
  final bool? isDeleted;

  /// Meta data of product such as description, vendor, producer, stock level. The total characters of cumulative metaInfo shall not exceed **20000 characters**.
  final Map<String, Object?>? metaInfo;

  /// **Mandatory in case of creation**. Name of the product, as displayed in the shop
  final String name;

  /// Parent product id of the product
  final String? parentId;

  /// Price of the product
  final double? price;

  /// Product identifier from the shop
  final String? sku;

  /// Current stock value of the product from the shop's database
  final double? stock;

  /// Facilitate to update the existing product in the same request (updateEnabled = true)
  final bool? updateEnabled;

  /// URL to the product
  final String? url;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (alternativePrice != null) 'alternativePrice': alternativePrice!,
        if (brand != null) 'brand': brand!,
        if (categories != null) 'categories': categories!,
        if (deletedAt != null) 'deletedAt': deletedAt!,
        if (description != null) 'description': description!,
        'id': id,
        if (imageUrl != null) 'imageUrl': imageUrl!,
        if (isDeleted != null) 'isDeleted': isDeleted!,
        if (metaInfo != null) 'metaInfo': metaInfo!,
        'name': name,
        if (parentId != null) 'parentId': parentId!,
        if (price != null) 'price': price!,
        if (sku != null) 'sku': sku!,
        if (stock != null) 'stock': stock!,
        if (updateEnabled != null) 'updateEnabled': updateEnabled!,
        if (url != null) 'url': url!,
      };
}

/// The request body of `components/schemas/order`.
final class Order {
  /// Creates the request body.
  const Order({
    required this.amount,
    this.billing,
    this.coupons,
    required this.createdAt,
    required this.id,
    this.identifiers,
    this.metaInfo,
    required this.products,
    required this.status,
    this.storeId,
    required this.updatedAt,
  });

  /// Decodes a JSON object into a Order. Optional fields absent from [json] stay null.
  factory Order.fromJson(Map<String, Object?> json) => Order(
        amount: json.optDouble('amount') ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'Order', key: 'amount')),
        billing: json.optNested('billing', OrderBilling.fromJson),
        coupons:
            json.containsKey('coupons') ? json.optStringList('coupons') : null,
        createdAt: json.requireString('createdAt', 'Order'),
        id: json.requireString('id', 'Order'),
        identifiers: json.optNested('identifiers', OrderIdentifiers.fromJson),
        metaInfo:
            json.containsKey('metaInfo') ? json.optObjectMap('metaInfo') : null,
        products: json.containsKey('products')
            ? json.optObjectList('products', OrderProductsItem.fromJson,
                objectName: 'Order')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'Order', key: 'products')),
        status: json.requireString('status', 'Order'),
        storeId: json.optString('storeId'),
        updatedAt: json.requireString('updatedAt', 'Order'),
      );

  /// Total amount of the order, including all shipping expenses, tax and the price of items.
  final double amount;

  /// Billing details of an order.
  final OrderBilling? billing;

  /// Coupons applied to the order. Stored case insensitive.
  final List<String>? coupons;

  /// Event occurrence UTC date-time (YYYY-MM-DDTHH:mm:ssZ), when order is actually created.
  final String createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate => DateTime.tryParse(createdAt);

  /// Unique ID of the order.
  final String id;

  /// Identifies the contact associated with the order.
  final OrderIdentifiers? identifiers;

  /// Meta data of order to store additional detail such as custom message, customer type, source.
  final Map<String, Object?>? metaInfo;

  /// The `products` field.
  final List<OrderProductsItem> products;

  /// State of the order.
  final String status;

  /// ID of store where the order is placed
  final String? storeId;

  /// Event updated UTC date-time (YYYY-MM-DDTHH:mm:ssZ), when the status of the order is actually changed/updated.
  final String updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate => DateTime.tryParse(updatedAt);

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'amount': amount,
        if (billing != null) 'billing': billing!.toJson(),
        if (coupons != null) 'coupons': coupons!,
        'createdAt': createdAt,
        'id': id,
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
        if (metaInfo != null) 'metaInfo': metaInfo!,
        'products': [for (final item in products) item.toJson()],
        'status': status,
        if (storeId != null) 'storeId': storeId!,
        'updatedAt': updatedAt,
      };
}

/// Billing details of an order.
final class OrderBilling {
  /// Creates the request body.
  const OrderBilling({
    this.address,
    this.city,
    this.country,
    this.countryCode,
    this.paymentMethod,
    this.phone,
    this.postCode,
    this.region,
  });

  /// Decodes a JSON object into a OrderBilling. Optional fields absent from [json] stay null.
  factory OrderBilling.fromJson(Map<String, Object?> json) => OrderBilling(
        address: json.optString('address'),
        city: json.optString('city'),
        country: json.optString('country'),
        countryCode: json.optString('countryCode'),
        paymentMethod: json.optString('paymentMethod'),
        phone: json.optString('phone'),
        postCode: json.optString('postCode'),
        region: json.optString('region'),
      );

  /// Full billing address.
  final String? address;

  /// Exact city of the address.
  final String? city;

  /// Billing country name.
  final String? country;

  /// Billing country 2-letter ISO code.
  final String? countryCode;

  /// How the visitor will pay for the item(s), e.g. paypal, check, etc.
  final String? paymentMethod;

  /// Billing phone number.
  final String? phone;

  /// Postcode for delivery and billing.
  final String? postCode;

  /// Exact region (state/province) for delivery and billing.
  final String? region;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (address != null) 'address': address!,
        if (city != null) 'city': city!,
        if (country != null) 'country': country!,
        if (countryCode != null) 'countryCode': countryCode!,
        if (paymentMethod != null) 'paymentMethod': paymentMethod!,
        if (phone != null) 'phone': phone!,
        if (postCode != null) 'postCode': postCode!,
        if (region != null) 'region': region!,
      };
}

/// Identifies the contact associated with the order.
final class OrderIdentifiers {
  /// Creates the request body.
  const OrderIdentifiers({
    this.emailId,
    this.extId,
    this.loyaltySubscriptionId,
    this.phoneId,
  });

  /// Decodes a JSON object into a OrderIdentifiers. Optional fields absent from [json] stay null.
  factory OrderIdentifiers.fromJson(Map<String, Object?> json) =>
      OrderIdentifiers(
        emailId: json.optString('email_id'),
        extId: json.optString('ext_id'),
        loyaltySubscriptionId: json.optString('loyalty_subscription_id'),
        phoneId: json.optString('phone_id'),
      );

  /// Email of the contact associated with the order
  final String? emailId;

  /// ext_id associated with the order
  final String? extId;

  /// loyalty_subscription_id associated with the order
  final String? loyaltySubscriptionId;

  /// Phone number of the contact associated with the order
  final String? phoneId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (emailId != null) 'email_id': emailId!,
        if (extId != null) 'ext_id': extId!,
        if (loyaltySubscriptionId != null)
          'loyalty_subscription_id': loyaltySubscriptionId!,
        if (phoneId != null) 'phone_id': phoneId!,
      };
}

/// Details for the Products in an order.
final class OrderProductsItem {
  /// Creates the request body.
  const OrderProductsItem({
    required this.price,
    required this.productId,
    this.quantity,
    this.quantityFloat,
    this.variantId,
  });

  /// Decodes a JSON object into a OrderProductsItem. Optional fields absent from [json] stay null.
  factory OrderProductsItem.fromJson(Map<String, Object?> json) =>
      OrderProductsItem(
        price: json.optDouble('price') ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'OrderProductsItem', key: 'price')),
        productId: json.requireString('productId', 'OrderProductsItem'),
        quantity: json.optInt('quantity'),
        quantityFloat: json.optDouble('quantityFloat'),
        variantId: json.optString('variantId'),
      );

  /// The price of a unit of product
  final double price;

  /// ID of the product.
  final String productId;

  /// **Required if quantityFloat is empty.**
  ///
  /// Number of product units added to the cart (whole numbers only, e.g., 10)
  final int? quantity;

  /// **Required if quantity is empty.**
  ///
  /// Number of product units added to the cart(supports decimals, e.g., 20.52)
  final double? quantityFloat;

  /// Product ID of the red color shirts.
  final String? variantId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'price': price,
        'productId': productId,
        if (quantity != null) 'quantity': quantity!,
        if (quantityFloat != null) 'quantityFloat': quantityFloat!,
        if (variantId != null) 'variantId': variantId!,
      };
}

/// The request body of `POST /ecommerce/config/displayCurrency`.
final class SetConfigDisplayCurrencyRequest {
  /// Creates the request body.
  const SetConfigDisplayCurrencyRequest({
    required this.code,
  });

  /// Decodes a JSON object into a SetConfigDisplayCurrencyRequest. Optional fields absent from [json] stay null.
  factory SetConfigDisplayCurrencyRequest.fromJson(Map<String, Object?> json) =>
      SetConfigDisplayCurrencyRequest(
        code: json.requireString('code', 'SetConfigDisplayCurrencyRequest'),
      );

  /// ISO 4217 compliant display currency code
  final String code;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'code': code,
      };
}
