// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `POST /organization/user/update/permissions` object of the Brevo API.
final class EditUserPermissionResponse {
  /// Creates a EditUserPermissionResponse from already-decoded fields.
  const EditUserPermissionResponse({
    this.creditNotes = const [],
    this.invoiceId,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a EditUserPermissionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory EditUserPermissionResponse.fromJson(Map<String, Object?> json) =>
      EditUserPermissionResponse(
        creditNotes: json.optStringList('credit_notes'),
        invoiceId: json.optString('invoice_id'),
        status: json.optString('status'),
        raw: json,
      );

  /// Credit note
  final List<String> creditNotes;

  /// Invoice id
  final String? invoiceId;

  /// Status of the API operation.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (creditNotes.isNotEmpty) 'credit_notes': creditNotes,
        if (invoiceId != null) 'invoice_id': invoiceId!,
        if (status != null) 'status': status!,
      };
}

/// The `GET /organization/invited/users` object of the Brevo API.
final class GetInvitedUsersListResponse {
  /// Creates a GetInvitedUsersListResponse from already-decoded fields.
  const GetInvitedUsersListResponse({
    this.users = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInvitedUsersListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInvitedUsersListResponse.fromJson(Map<String, Object?> json) =>
      GetInvitedUsersListResponse(
        users: json.optObjectList(
            'users', GetInvitedUsersListResponseUsersItem.fromJson,
            objectName: 'GetInvitedUsersListResponse'),
        raw: json,
      );

  /// Get invited users list
  final List<GetInvitedUsersListResponseUsersItem> users;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (users.isNotEmpty)
          'users': [for (final item in users) item.toJson()],
      };
}

/// The `GetInvitedUsersListResponse.usersItem` object of the Brevo API.
final class GetInvitedUsersListResponseUsersItem {
  /// Creates a GetInvitedUsersListResponseUsersItem from already-decoded fields.
  const GetInvitedUsersListResponseUsersItem({
    this.email,
    this.featureAccess,
    this.isOwner,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInvitedUsersListResponseUsersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInvitedUsersListResponseUsersItem.fromJson(
          Map<String, Object?> json) =>
      GetInvitedUsersListResponseUsersItem(
        email: json.optString('email'),
        featureAccess: json.optNested('feature_access',
            GetInvitedUsersListResponseUsersItemFeatureAccess.fromJson),
        isOwner: json.optString('is_owner'),
        status: json.optString('status'),
        raw: json,
      );

  /// Email address of the user.
  final String? email;

  /// Feature accessiblity given to the user.
  final GetInvitedUsersListResponseUsersItemFeatureAccess? featureAccess;

  /// Flag for indicating is user owner of the organization.
  final String? isOwner;

  /// Status of the invited user.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (featureAccess != null) 'feature_access': featureAccess!.toJson(),
        if (isOwner != null) 'is_owner': isOwner!,
        if (status != null) 'status': status!,
      };
}

/// Feature accessiblity given to the user.
final class GetInvitedUsersListResponseUsersItemFeatureAccess {
  /// Creates a GetInvitedUsersListResponseUsersItemFeatureAccess from already-decoded fields.
  const GetInvitedUsersListResponseUsersItemFeatureAccess({
    this.conversations,
    this.crm,
    this.marketing,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInvitedUsersListResponseUsersItemFeatureAccess. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInvitedUsersListResponseUsersItemFeatureAccess.fromJson(
          Map<String, Object?> json) =>
      GetInvitedUsersListResponseUsersItemFeatureAccess(
        conversations: json.optString('conversations'),
        crm: json.optString('crm'),
        marketing: json.optString('marketing'),
        raw: json,
      );

  /// Conversations features accessiblity.
  final String? conversations;

  /// CRM features accessiblity.
  final String? crm;

  /// Marketing features accessiblity.
  final String? marketing;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (conversations != null) 'conversations': conversations!,
        if (crm != null) 'crm': crm!,
        if (marketing != null) 'marketing': marketing!,
      };
}

/// Check user permission
final class GetUserPermissionResponse {
  /// Creates a GetUserPermissionResponse from already-decoded fields.
  const GetUserPermissionResponse({
    this.email,
    this.privileges = const [],
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetUserPermissionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetUserPermissionResponse.fromJson(Map<String, Object?> json) =>
      GetUserPermissionResponse(
        email: json.optString('email'),
        privileges: json.optObjectList(
            'privileges', GetUserPermissionResponsePrivilegesItem.fromJson,
            objectName: 'GetUserPermissionResponse'),
        status: json.optString('status'),
        raw: json,
      );

  /// Email address of the user.
  final String? email;

  /// Granular feature permissions given to the user.
  final List<GetUserPermissionResponsePrivilegesItem> privileges;

  /// Status of the invited user.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (privileges.isNotEmpty)
          'privileges': [for (final item in privileges) item.toJson()],
        if (status != null) 'status': status!,
      };
}

/// The `GetUserPermissionResponse.privilegesItem` object of the Brevo API.
final class GetUserPermissionResponsePrivilegesItem {
  /// Creates a GetUserPermissionResponsePrivilegesItem from already-decoded fields.
  const GetUserPermissionResponsePrivilegesItem({
    this.feature,
    this.permissions = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetUserPermissionResponsePrivilegesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetUserPermissionResponsePrivilegesItem.fromJson(
          Map<String, Object?> json) =>
      GetUserPermissionResponsePrivilegesItem(
        feature: json.optString('feature'),
        permissions: json.optStringList('permissions'),
        raw: json,
      );

  /// The `feature` field.
  final String? feature;

  /// The `permissions` field.
  final List<String> permissions;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (feature != null) 'feature': feature!,
        if (permissions.isNotEmpty) 'permissions': permissions,
      };
}

/// The `POST /organization/user/invitation/send` object of the Brevo API.
final class InviteuserResponse {
  /// Creates a InviteuserResponse from already-decoded fields.
  const InviteuserResponse({
    this.invoiceId,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a InviteuserResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory InviteuserResponse.fromJson(Map<String, Object?> json) =>
      InviteuserResponse(
        invoiceId: json.optString('invoice_id'),
        status: json.optString('status'),
        raw: json,
      );

  /// Invoice id
  final String? invoiceId;

  /// Status of the API operation.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (invoiceId != null) 'invoice_id': invoiceId!,
        if (status != null) 'status': status!,
      };
}

/// The `PUT /organization/user/invitation/revoke/{email}` object of the Brevo API.
final class PutRevokeUserPermissionResponse {
  /// Creates a PutRevokeUserPermissionResponse from already-decoded fields.
  const PutRevokeUserPermissionResponse({
    this.creditNotes = const [],
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PutRevokeUserPermissionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PutRevokeUserPermissionResponse.fromJson(Map<String, Object?> json) =>
      PutRevokeUserPermissionResponse(
        creditNotes: json.optStringList('credit_notes'),
        status: json.optString('status'),
        raw: json,
      );

  /// Credit note
  final List<String> creditNotes;

  /// Status of the API operation.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (creditNotes.isNotEmpty) 'credit_notes': creditNotes,
        if (status != null) 'status': status!,
      };
}

/// The `PUT /organization/user/invitation/{action}/{email}` object of the Brevo API.
final class PutresendcancelinvitationResponse {
  /// Creates a PutresendcancelinvitationResponse from already-decoded fields.
  const PutresendcancelinvitationResponse({
    this.creditNotes = const [],
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PutresendcancelinvitationResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PutresendcancelinvitationResponse.fromJson(
          Map<String, Object?> json) =>
      PutresendcancelinvitationResponse(
        creditNotes: json.optStringList('credit_notes'),
        status: json.optString('status'),
        raw: json,
      );

  /// Credit note
  final List<String> creditNotes;

  /// Status of the API operation.
  final String? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (creditNotes.isNotEmpty) 'credit_notes': creditNotes,
        if (status != null) 'status': status!,
      };
}
