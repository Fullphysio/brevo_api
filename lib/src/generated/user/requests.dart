// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `components/schemas/inviteuser`.
final class Inviteuser {
  /// Creates the request body.
  const Inviteuser({
    required this.allFeaturesAccess,
    required this.email,
    required this.privileges,
  });

  /// Decodes a JSON object into a Inviteuser. Optional fields absent from [json] stay null.
  factory Inviteuser.fromJson(Map<String, Object?> json) => Inviteuser(
        allFeaturesAccess: json.optBool('all_features_access') ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'Inviteuser', key: 'all_features_access')),
        email: json.requireString('email', 'Inviteuser'),
        privileges: json.containsKey('privileges')
            ? json.optObjectList(
                'privileges', InviteuserPrivilegesItem.fromJson,
                objectName: 'Inviteuser')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'Inviteuser', key: 'privileges')),
      );

  /// All access to the features
  final bool allFeaturesAccess;

  /// Email address for the organization
  final String email;

  /// The `privileges` field.
  final List<InviteuserPrivilegesItem> privileges;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'all_features_access': allFeaturesAccess,
        'email': email,
        'privileges': [for (final item in privileges) item.toJson()],
      };
}

/// Privileges given to the user
final class InviteuserPrivilegesItem {
  /// Creates the request body.
  const InviteuserPrivilegesItem({
    this.feature,
    this.permissions,
  });

  /// Decodes a JSON object into a InviteuserPrivilegesItem. Optional fields absent from [json] stay null.
  factory InviteuserPrivilegesItem.fromJson(Map<String, Object?> json) =>
      InviteuserPrivilegesItem(
        feature:
            json.optEnum('feature', InviteuserPrivilegesItemFeature.fromWire),
        permissions: json.containsKey('permissions')
            ? json
                .optStringList('permissions')
                .map(InviteuserPrivilegesItemPermissionsItem.fromWire)
                .toList(growable: false)
            : null,
      );

  /// Feature name
  final InviteuserPrivilegesItemFeature? feature;

  /// Permissions for a given feature
  final List<InviteuserPrivilegesItemPermissionsItem>? permissions;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (feature != null) 'feature': feature!.value,
        if (permissions != null)
          'permissions': [for (final item in permissions!) item.value],
      };
}
