// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `PUT /senders/domains/{domainName}/authenticate` object of the Brevo API.
final class AuthenticateDomainResponse {
  /// Creates a AuthenticateDomainResponse from already-decoded fields.
  const AuthenticateDomainResponse({
    this.domainName,
    this.message,
    this.raw = const {},
  });

  /// Decodes a JSON object into a AuthenticateDomainResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory AuthenticateDomainResponse.fromJson(Map<String, Object?> json) =>
      AuthenticateDomainResponse(
        domainName: json.optString('domain_name'),
        message: json.optString('message'),
        raw: json,
      );

  /// Domain name that was authenticated
  final String? domainName;

  /// Success message confirming authentication
  final String? message;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (domainName != null) 'domain_name': domainName!,
        if (message != null) 'message': message!,
      };
}

/// The `POST /senders/domains` object of the Brevo API.
final class CreateDomainResponse {
  /// Creates a CreateDomainResponse from already-decoded fields.
  const CreateDomainResponse({
    this.dnsRecords,
    this.domainName,
    this.domainProvider,
    this.id,
    this.message,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateDomainResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateDomainResponse.fromJson(Map<String, Object?> json) =>
      CreateDomainResponse(
        dnsRecords: json.optNested(
            'dns_records', CreateDomainResponseDnsRecords.fromJson),
        domainName: json.optString('domain_name'),
        domainProvider: json.optString('domain_provider'),
        id: json.optInt('id'),
        message: json.optString('message'),
        raw: json,
      );

  /// DNS records required for domain authentication
  final CreateDomainResponseDnsRecords? dnsRecords;

  /// Domain name
  final String? domainName;

  /// Detected domain provider
  final String? domainProvider;

  /// ID of the Domain created
  final int? id;

  /// Success message with next steps
  final String? message;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (dnsRecords != null) 'dns_records': dnsRecords!.toJson(),
        if (domainName != null) 'domain_name': domainName!,
        if (domainProvider != null) 'domain_provider': domainProvider!,
        if (id != null) 'id': id!,
        if (message != null) 'message': message!,
      };
}

/// DNS records required for domain authentication
final class CreateDomainResponseDnsRecords {
  /// Creates a CreateDomainResponseDnsRecords from already-decoded fields.
  const CreateDomainResponseDnsRecords({
    this.brevoCode,
    this.dkimRecord,
    this.dmarcRecord,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateDomainResponseDnsRecords. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateDomainResponseDnsRecords.fromJson(Map<String, Object?> json) =>
      CreateDomainResponseDnsRecords(
        brevoCode: json.optNested(
            'brevo_code', CreateDomainResponseDnsRecordsBrevoCode.fromJson),
        dkimRecord: json.optNested(
            'dkim_record', CreateDomainResponseDnsRecordsDkimRecord.fromJson),
        dmarcRecord: json.optNested(
            'dmarc_record', CreateDomainResponseDnsRecordsDmarcRecord.fromJson),
        raw: json,
      );

  /// Brevo verification code record
  final CreateDomainResponseDnsRecordsBrevoCode? brevoCode;

  /// DKIM authentication record
  final CreateDomainResponseDnsRecordsDkimRecord? dkimRecord;

  /// DMARC policy record
  final CreateDomainResponseDnsRecordsDmarcRecord? dmarcRecord;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (brevoCode != null) 'brevo_code': brevoCode!.toJson(),
        if (dkimRecord != null) 'dkim_record': dkimRecord!.toJson(),
        if (dmarcRecord != null) 'dmarc_record': dmarcRecord!.toJson(),
      };
}

/// Brevo verification code record
final class CreateDomainResponseDnsRecordsBrevoCode {
  /// Creates a CreateDomainResponseDnsRecordsBrevoCode from already-decoded fields.
  const CreateDomainResponseDnsRecordsBrevoCode({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateDomainResponseDnsRecordsBrevoCode. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateDomainResponseDnsRecordsBrevoCode.fromJson(
          Map<String, Object?> json) =>
      CreateDomainResponseDnsRecordsBrevoCode(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// DKIM authentication record
final class CreateDomainResponseDnsRecordsDkimRecord {
  /// Creates a CreateDomainResponseDnsRecordsDkimRecord from already-decoded fields.
  const CreateDomainResponseDnsRecordsDkimRecord({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateDomainResponseDnsRecordsDkimRecord. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateDomainResponseDnsRecordsDkimRecord.fromJson(
          Map<String, Object?> json) =>
      CreateDomainResponseDnsRecordsDkimRecord(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// DMARC policy record
final class CreateDomainResponseDnsRecordsDmarcRecord {
  /// Creates a CreateDomainResponseDnsRecordsDmarcRecord from already-decoded fields.
  const CreateDomainResponseDnsRecordsDmarcRecord({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateDomainResponseDnsRecordsDmarcRecord. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateDomainResponseDnsRecordsDmarcRecord.fromJson(
          Map<String, Object?> json) =>
      CreateDomainResponseDnsRecordsDmarcRecord(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `GET /senders/domains/{domainName}` object of the Brevo API.
final class GetDomainConfigurationResponse {
  /// Creates a GetDomainConfigurationResponse from already-decoded fields.
  const GetDomainConfigurationResponse({
    this.authenticated,
    this.dnsRecords,
    this.domain,
    this.verified,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainConfigurationResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainConfigurationResponse.fromJson(Map<String, Object?> json) =>
      GetDomainConfigurationResponse(
        authenticated: json.optBool('authenticated'),
        dnsRecords: json.optNested(
            'dns_records', GetDomainConfigurationResponseDnsRecords.fromJson),
        domain: json.optString('domain'),
        verified: json.optBool('verified'),
        raw: json,
      );

  /// Status of domain authentication (true=authenticated, false=non
  /// authenticated)
  final bool? authenticated;

  /// DNS records configuration and status
  final GetDomainConfigurationResponseDnsRecords? dnsRecords;

  /// Domain name
  final String? domain;

  /// Status of domain verification (true=verified, false=non
  /// verified)
  final bool? verified;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authenticated != null) 'authenticated': authenticated!,
        if (dnsRecords != null) 'dns_records': dnsRecords!.toJson(),
        if (domain != null) 'domain': domain!,
        if (verified != null) 'verified': verified!,
      };
}

/// DNS records configuration and status
final class GetDomainConfigurationResponseDnsRecords {
  /// Creates a GetDomainConfigurationResponseDnsRecords from already-decoded fields.
  const GetDomainConfigurationResponseDnsRecords({
    this.brevoCode,
    this.dkimRecord,
    this.dmarcRecord,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainConfigurationResponseDnsRecords. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainConfigurationResponseDnsRecords.fromJson(
          Map<String, Object?> json) =>
      GetDomainConfigurationResponseDnsRecords(
        brevoCode: json.optNested('brevo_code',
            GetDomainConfigurationResponseDnsRecordsBrevoCode.fromJson),
        dkimRecord: json.optNested('dkim_record',
            GetDomainConfigurationResponseDnsRecordsDkimRecord.fromJson),
        dmarcRecord: json.optNested('dmarc_record',
            GetDomainConfigurationResponseDnsRecordsDmarcRecord.fromJson),
        raw: json,
      );

  /// Brevo verification code record
  final GetDomainConfigurationResponseDnsRecordsBrevoCode? brevoCode;

  /// DKIM authentication record
  final GetDomainConfigurationResponseDnsRecordsDkimRecord? dkimRecord;

  /// DMARC policy record
  final GetDomainConfigurationResponseDnsRecordsDmarcRecord? dmarcRecord;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (brevoCode != null) 'brevo_code': brevoCode!.toJson(),
        if (dkimRecord != null) 'dkim_record': dkimRecord!.toJson(),
        if (dmarcRecord != null) 'dmarc_record': dmarcRecord!.toJson(),
      };
}

/// Brevo verification code record
final class GetDomainConfigurationResponseDnsRecordsBrevoCode {
  /// Creates a GetDomainConfigurationResponseDnsRecordsBrevoCode from already-decoded fields.
  const GetDomainConfigurationResponseDnsRecordsBrevoCode({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainConfigurationResponseDnsRecordsBrevoCode. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainConfigurationResponseDnsRecordsBrevoCode.fromJson(
          Map<String, Object?> json) =>
      GetDomainConfigurationResponseDnsRecordsBrevoCode(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// DKIM authentication record
final class GetDomainConfigurationResponseDnsRecordsDkimRecord {
  /// Creates a GetDomainConfigurationResponseDnsRecordsDkimRecord from already-decoded fields.
  const GetDomainConfigurationResponseDnsRecordsDkimRecord({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainConfigurationResponseDnsRecordsDkimRecord. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainConfigurationResponseDnsRecordsDkimRecord.fromJson(
          Map<String, Object?> json) =>
      GetDomainConfigurationResponseDnsRecordsDkimRecord(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// DMARC policy record
final class GetDomainConfigurationResponseDnsRecordsDmarcRecord {
  /// Creates a GetDomainConfigurationResponseDnsRecordsDmarcRecord from already-decoded fields.
  const GetDomainConfigurationResponseDnsRecordsDmarcRecord({
    this.hostName,
    this.status,
    this.type,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainConfigurationResponseDnsRecordsDmarcRecord. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainConfigurationResponseDnsRecordsDmarcRecord.fromJson(
          Map<String, Object?> json) =>
      GetDomainConfigurationResponseDnsRecordsDmarcRecord(
        hostName: json.optString('host_name'),
        status: json.optBool('status'),
        type: json.optString('type'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// DNS record hostname
  final String? hostName;

  /// Whether the record is properly configured
  final bool? status;

  /// DNS record type
  final String? type;

  /// DNS record value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hostName != null) 'host_name': hostName!,
        if (status != null) 'status': status!,
        if (type != null) 'type': type!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `GET /senders/domains` object of the Brevo API.
final class GetDomainsResponse {
  /// Creates a GetDomainsResponse from already-decoded fields.
  const GetDomainsResponse({
    this.count,
    this.currentPage,
    this.domains = const [],
    this.totalPages,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainsResponse.fromJson(Map<String, Object?> json) =>
      GetDomainsResponse(
        count: json.optInt('count'),
        currentPage: json.optInt('current_page'),
        domains: json.optObjectList(
            'domains', GetDomainsResponseDomainsItem.fromJson,
            objectName: 'GetDomainsResponse'),
        totalPages: json.optInt('total_pages'),
        raw: json,
      );

  /// Total number of domains
  final int? count;

  /// Current page number
  final int? currentPage;

  /// List of the domains available in your account
  final List<GetDomainsResponseDomainsItem> domains;

  /// Total number of pages
  final int? totalPages;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (currentPage != null) 'current_page': currentPage!,
        if (domains.isNotEmpty)
          'domains': [for (final item in domains) item.toJson()],
        if (totalPages != null) 'total_pages': totalPages!,
      };
}

/// The `GetDomainsResponse.domainsItem` object of the Brevo API.
final class GetDomainsResponseDomainsItem {
  /// Creates a GetDomainsResponseDomainsItem from already-decoded fields.
  const GetDomainsResponseDomainsItem({
    this.authenticated,
    this.creator,
    this.domainName,
    this.id,
    this.ip,
    this.provider,
    this.verified,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainsResponseDomainsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainsResponseDomainsItem.fromJson(Map<String, Object?> json) =>
      GetDomainsResponseDomainsItem(
        authenticated: json.optBool('authenticated'),
        creator: json.optNested(
            'creator', GetDomainsResponseDomainsItemCreator.fromJson),
        domainName: json.optString('domain_name'),
        id: json.optString('id'),
        ip: json.optString('ip'),
        provider: json.optString('provider'),
        verified: json.optBool('verified'),
        raw: json,
      );

  /// Status of domain authentication (true=authenticated,
  /// false=non authenticated)
  final bool? authenticated;

  /// Information about who created the domain
  final GetDomainsResponseDomainsItemCreator? creator;

  /// Domain name
  final String? domainName;

  /// Unique identifier of the domain
  final String? id;

  /// Dedicated IP associated with domain (null if none)
  final String? ip;

  /// DNS provider for the domain
  final String? provider;

  /// Status of domain verification (true=verified, false=non
  /// verified)
  final bool? verified;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authenticated != null) 'authenticated': authenticated!,
        if (creator != null) 'creator': creator!.toJson(),
        if (domainName != null) 'domain_name': domainName!,
        if (id != null) 'id': id!,
        if (ip != null) 'ip': ip!,
        if (provider != null) 'provider': provider!,
        if (verified != null) 'verified': verified!,
      };
}

/// Information about who created the domain
final class GetDomainsResponseDomainsItemCreator {
  /// Creates a GetDomainsResponseDomainsItemCreator from already-decoded fields.
  const GetDomainsResponseDomainsItemCreator({
    this.creationDate,
    this.email,
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetDomainsResponseDomainsItemCreator. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetDomainsResponseDomainsItemCreator.fromJson(
          Map<String, Object?> json) =>
      GetDomainsResponseDomainsItemCreator(
        creationDate: json.optString('creationDate'),
        email: json.optString('email'),
        id: json.optString('id'),
        raw: json,
      );

  /// Domain creation date
  final String? creationDate;

  /// [creationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get creationDateDate =>
      creationDate == null ? null : DateTime.tryParse(creationDate!);

  /// Creator's email address
  final String? email;

  /// Creator's user ID
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (creationDate != null) 'creationDate': creationDate!,
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
      };
}
