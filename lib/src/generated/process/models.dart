// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /processes/{processId}` object of the Brevo API.
final class GetProcessResponse {
  /// Creates a GetProcessResponse from already-decoded fields.
  const GetProcessResponse({
    this.exportUrl,
    this.id,
    this.info,
    this.name,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessResponse.fromJson(Map<String, Object?> json) =>
      GetProcessResponse(
        exportUrl: json.optString('export_url'),
        id: json.optInt('id'),
        info: json.optNested('info', GetProcessResponseInfo.fromJson),
        name: json.optEnum('name', GetProcessResponseName.fromWire),
        status: json.optEnum('status', GetProcessResponseStatus.fromWire),
        raw: json,
      );

  /// Download URL for completed export processes (returned for SEARCH_EXPORT_USERS, SEARCH_EXPORT_USERS_API, CAMPAIGN_USER_DETAILS, and EXPORT_WEBHOOK process types)
  final String? exportUrl;

  /// Unique identifier of the process
  final int? id;

  /// Additional process information, only returned for completed IMPORTUSER processes. Contains URLs to CSV files with details about problematic records.
  final GetProcessResponseInfo? info;

  /// Name/type of the process
  final GetProcessResponseName? name;

  /// Current status of the process
  final GetProcessResponseStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (exportUrl != null) 'export_url': exportUrl!,
        if (id != null) 'id': id!,
        if (info != null) 'info': info!.toJson(),
        if (name != null) 'name': name!.value,
        if (status != null) 'status': status!.value,
      };
}

/// Additional process information, only returned for completed IMPORTUSER processes. Contains URLs to CSV files with details about problematic records.
final class GetProcessResponseInfo {
  /// Creates a GetProcessResponseInfo from already-decoded fields.
  const GetProcessResponseInfo({
    this.importValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessResponseInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessResponseInfo.fromJson(Map<String, Object?> json) =>
      GetProcessResponseInfo(
        importValue:
            json.optNested('import', GetProcessResponseInfoImport.fromJson),
        raw: json,
      );

  /// Import process details with URLs to CSV reports
  final GetProcessResponseInfoImport? importValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (importValue != null) 'import': importValue!.toJson(),
      };
}

/// Import process details with URLs to CSV reports
final class GetProcessResponseInfoImport {
  /// Creates a GetProcessResponseInfoImport from already-decoded fields.
  const GetProcessResponseInfoImport({
    this.duplicateContactId,
    this.duplicateEmailId,
    this.duplicateExtId,
    this.duplicateLandlineNumberId,
    this.duplicatePhoneId,
    this.duplicateWhatsappId,
    this.invalidEmails,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessResponseInfoImport. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessResponseInfoImport.fromJson(Map<String, Object?> json) =>
      GetProcessResponseInfoImport(
        duplicateContactId: json.optString('duplicate_contact_id'),
        duplicateEmailId: json.optString('duplicate_email_id'),
        duplicateExtId: json.optString('duplicate_ext_id'),
        duplicateLandlineNumberId:
            json.optString('duplicate_landline_number_id'),
        duplicatePhoneId: json.optString('duplicate_phone_id'),
        duplicateWhatsappId: json.optString('duplicate_whatsapp_id'),
        invalidEmails: json.optString('invalid_emails'),
        raw: json,
      );

  /// URL to CSV file containing duplicate contact IDs, or null if none
  final String? duplicateContactId;

  /// URL to CSV file containing duplicate email IDs, or null if none
  final String? duplicateEmailId;

  /// URL to CSV file containing duplicate external IDs, or null if none
  final String? duplicateExtId;

  /// URL to CSV file containing duplicate landline numbers, or null if none
  final String? duplicateLandlineNumberId;

  /// URL to CSV file containing duplicate phone numbers, or null if none
  final String? duplicatePhoneId;

  /// URL to CSV file containing duplicate WhatsApp numbers, or null if none
  final String? duplicateWhatsappId;

  /// URL to CSV file containing invalid email addresses, or null if none
  final String? invalidEmails;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (duplicateContactId != null)
          'duplicate_contact_id': duplicateContactId!,
        if (duplicateEmailId != null) 'duplicate_email_id': duplicateEmailId!,
        if (duplicateExtId != null) 'duplicate_ext_id': duplicateExtId!,
        if (duplicateLandlineNumberId != null)
          'duplicate_landline_number_id': duplicateLandlineNumberId!,
        if (duplicatePhoneId != null) 'duplicate_phone_id': duplicatePhoneId!,
        if (duplicateWhatsappId != null)
          'duplicate_whatsapp_id': duplicateWhatsappId!,
        if (invalidEmails != null) 'invalid_emails': invalidEmails!,
      };
}

/// The `GET /processes` object of the Brevo API.
final class GetProcessesResponse {
  /// Creates a GetProcessesResponse from already-decoded fields.
  const GetProcessesResponse({
    this.count,
    this.processes = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessesResponse.fromJson(Map<String, Object?> json) =>
      GetProcessesResponse(
        count: json.optInt('count'),
        processes: json.optObjectList(
            'processes', GetProcessesResponseProcessesItem.fromJson,
            objectName: 'GetProcessesResponse'),
        raw: json,
      );

  /// Total number of processes available on your account
  final int? count;

  /// List of background processes on your account
  final List<GetProcessesResponseProcessesItem> processes;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (processes.isNotEmpty)
          'processes': [for (final item in processes) item.toJson()],
      };
}

/// The `GetProcessesResponse.processesItem` object of the Brevo API.
final class GetProcessesResponseProcessesItem {
  /// Creates a GetProcessesResponseProcessesItem from already-decoded fields.
  const GetProcessesResponseProcessesItem({
    this.exportUrl,
    this.id,
    this.info,
    this.name,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessesResponseProcessesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessesResponseProcessesItem.fromJson(
          Map<String, Object?> json) =>
      GetProcessesResponseProcessesItem(
        exportUrl: json.optString('export_url'),
        id: json.optInt('id'),
        info: json.optNested(
            'info', GetProcessesResponseProcessesItemInfo.fromJson),
        name: json.optEnum(
            'name', GetProcessesResponseProcessesItemName.fromWire),
        status: json.optEnum(
            'status', GetProcessesResponseProcessesItemStatus.fromWire),
        raw: json,
      );

  /// Download URL for completed export processes (returned for SEARCH_EXPORT_USERS, SEARCH_EXPORT_USERS_API, CAMPAIGN_USER_DETAILS, and EXPORT_WEBHOOK process types)
  final String? exportUrl;

  /// Unique identifier of the process
  final int? id;

  /// Additional process information, only returned for completed IMPORTUSER processes. Contains URLs to CSV files with details about problematic records.
  final GetProcessesResponseProcessesItemInfo? info;

  /// Name/type of the process
  final GetProcessesResponseProcessesItemName? name;

  /// Current status of the process
  final GetProcessesResponseProcessesItemStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (exportUrl != null) 'export_url': exportUrl!,
        if (id != null) 'id': id!,
        if (info != null) 'info': info!.toJson(),
        if (name != null) 'name': name!.value,
        if (status != null) 'status': status!.value,
      };
}

/// Additional process information, only returned for completed IMPORTUSER processes. Contains URLs to CSV files with details about problematic records.
final class GetProcessesResponseProcessesItemInfo {
  /// Creates a GetProcessesResponseProcessesItemInfo from already-decoded fields.
  const GetProcessesResponseProcessesItemInfo({
    this.importValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessesResponseProcessesItemInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessesResponseProcessesItemInfo.fromJson(
          Map<String, Object?> json) =>
      GetProcessesResponseProcessesItemInfo(
        importValue: json.optNested(
            'import', GetProcessesResponseProcessesItemInfoImport.fromJson),
        raw: json,
      );

  /// Import process details with URLs to CSV reports
  final GetProcessesResponseProcessesItemInfoImport? importValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (importValue != null) 'import': importValue!.toJson(),
      };
}

/// Import process details with URLs to CSV reports
final class GetProcessesResponseProcessesItemInfoImport {
  /// Creates a GetProcessesResponseProcessesItemInfoImport from already-decoded fields.
  const GetProcessesResponseProcessesItemInfoImport({
    this.duplicateContactId,
    this.duplicateEmailId,
    this.duplicateExtId,
    this.duplicateLandlineNumberId,
    this.duplicatePhoneId,
    this.duplicateWhatsappId,
    this.invalidEmails,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetProcessesResponseProcessesItemInfoImport. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetProcessesResponseProcessesItemInfoImport.fromJson(
          Map<String, Object?> json) =>
      GetProcessesResponseProcessesItemInfoImport(
        duplicateContactId: json.optString('duplicate_contact_id'),
        duplicateEmailId: json.optString('duplicate_email_id'),
        duplicateExtId: json.optString('duplicate_ext_id'),
        duplicateLandlineNumberId:
            json.optString('duplicate_landline_number_id'),
        duplicatePhoneId: json.optString('duplicate_phone_id'),
        duplicateWhatsappId: json.optString('duplicate_whatsapp_id'),
        invalidEmails: json.optString('invalid_emails'),
        raw: json,
      );

  /// URL to CSV file containing duplicate contact IDs, or null if none
  final String? duplicateContactId;

  /// URL to CSV file containing duplicate email IDs, or null if none
  final String? duplicateEmailId;

  /// URL to CSV file containing duplicate external IDs, or null if none
  final String? duplicateExtId;

  /// URL to CSV file containing duplicate landline numbers, or null if none
  final String? duplicateLandlineNumberId;

  /// URL to CSV file containing duplicate phone numbers, or null if none
  final String? duplicatePhoneId;

  /// URL to CSV file containing duplicate WhatsApp numbers, or null if none
  final String? duplicateWhatsappId;

  /// URL to CSV file containing invalid email addresses, or null if none
  final String? invalidEmails;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (duplicateContactId != null)
          'duplicate_contact_id': duplicateContactId!,
        if (duplicateEmailId != null) 'duplicate_email_id': duplicateEmailId!,
        if (duplicateExtId != null) 'duplicate_ext_id': duplicateExtId!,
        if (duplicateLandlineNumberId != null)
          'duplicate_landline_number_id': duplicateLandlineNumberId!,
        if (duplicatePhoneId != null) 'duplicate_phone_id': duplicatePhoneId!,
        if (duplicateWhatsappId != null)
          'duplicate_whatsapp_id': duplicateWhatsappId!,
        if (invalidEmails != null) 'invalid_emails': invalidEmails!,
      };
}
