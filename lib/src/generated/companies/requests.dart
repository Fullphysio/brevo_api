// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import '../../core/multipart.dart';
import 'enums.dart';

/// The request body of `PATCH /companies/{id}`.
final class PatchCompaniesIdRequest {
  /// Creates the request body.
  const PatchCompaniesIdRequest({
    this.attributes,
    this.countryCode,
    this.linkedContactsIds,
    this.linkedDealsIds,
    this.name,
  });

  /// Decodes a JSON object into a PatchCompaniesIdRequest. Optional fields absent from [json] stay null.
  factory PatchCompaniesIdRequest.fromJson(Map<String, Object?> json) =>
      PatchCompaniesIdRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        countryCode: json.optInt('countryCode'),
        linkedContactsIds: json.containsKey('linkedContactsIds')
            ? json.optIntList('linkedContactsIds')
            : null,
        linkedDealsIds: json.containsKey('linkedDealsIds')
            ? json.optStringList('linkedDealsIds')
            : null,
        name: json.optString('name'),
      );

  /// Attributes for company update
  final Map<String, Object?>? attributes;

  /// Country code if phone_number is passed in attributes.
  final int? countryCode;

  /// Warning - Using PATCH on linkedContactIds replaces the list of linked contacts. Omitted IDs will be removed.
  final List<int>? linkedContactsIds;

  /// Warning - Using PATCH on linkedDealsIds replaces the list of linked deals. Omitted IDs will be removed.
  final List<String>? linkedDealsIds;

  /// Name of company
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (countryCode != null) 'countryCode': countryCode!,
        if (linkedContactsIds != null) 'linkedContactsIds': linkedContactsIds!,
        if (linkedDealsIds != null) 'linkedDealsIds': linkedDealsIds!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `PATCH /companies/link-unlink/{id}`.
final class PatchCompaniesLinkUnlinkIdRequest {
  /// Creates the request body.
  const PatchCompaniesLinkUnlinkIdRequest({
    this.linkContactIds,
    this.linkDealsIds,
    this.unlinkContactIds,
    this.unlinkDealsIds,
  });

  /// Decodes a JSON object into a PatchCompaniesLinkUnlinkIdRequest. Optional fields absent from [json] stay null.
  factory PatchCompaniesLinkUnlinkIdRequest.fromJson(
          Map<String, Object?> json) =>
      PatchCompaniesLinkUnlinkIdRequest(
        linkContactIds: json.containsKey('linkContactIds')
            ? json.optIntList('linkContactIds')
            : null,
        linkDealsIds: json.containsKey('linkDealsIds')
            ? json.optStringList('linkDealsIds')
            : null,
        unlinkContactIds: json.containsKey('unlinkContactIds')
            ? json.optIntList('unlinkContactIds')
            : null,
        unlinkDealsIds: json.containsKey('unlinkDealsIds')
            ? json.optStringList('unlinkDealsIds')
            : null,
      );

  /// Contact ids for contacts to be linked with company
  final List<int>? linkContactIds;

  /// Deal ids for deals to be linked with company
  final List<String>? linkDealsIds;

  /// Contact ids for contacts to be unlinked from company
  final List<int>? unlinkContactIds;

  /// Deal ids for deals to be unlinked from company
  final List<String>? unlinkDealsIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (linkContactIds != null) 'linkContactIds': linkContactIds!,
        if (linkDealsIds != null) 'linkDealsIds': linkDealsIds!,
        if (unlinkContactIds != null) 'unlinkContactIds': unlinkContactIds!,
        if (unlinkDealsIds != null) 'unlinkDealsIds': unlinkDealsIds!,
      };
}

/// The request body of `PATCH /crm/attributes/{id}`.
final class PatchCrmAttributesIdRequest {
  /// Creates the request body.
  const PatchCrmAttributesIdRequest({
    this.label,
    this.objectType,
    this.optionsLabels,
  });

  /// Decodes a JSON object into a PatchCrmAttributesIdRequest. Optional fields absent from [json] stay null.
  factory PatchCrmAttributesIdRequest.fromJson(Map<String, Object?> json) =>
      PatchCrmAttributesIdRequest(
        label: json.optString('label'),
        objectType: json.optEnum(
            'objectType', PatchCrmAttributesIdRequestObjectType.fromWire),
        optionsLabels: json.containsKey('optionsLabels')
            ? json.optObjectList('optionsLabels',
                PatchCrmAttributesIdRequestOptionsLabelsItem.fromJson,
                objectName: 'PatchCrmAttributesIdRequest')
            : null,
      );

  /// Attribute display label
  final String? label;

  /// The type of object the attribute belongs to, it cannot be updated after creation
  final PatchCrmAttributesIdRequestObjectType? objectType;

  /// Updated labels for selectable options
  final List<PatchCrmAttributesIdRequestOptionsLabelsItem>? optionsLabels;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (label != null) 'label': label!,
        if (objectType != null) 'objectType': objectType!.value,
        if (optionsLabels != null)
          'optionsLabels': [for (final item in optionsLabels!) item.toJson()],
      };
}

/// The request body of `PatchCrmAttributesIdRequest.optionsLabelsItem`.
final class PatchCrmAttributesIdRequestOptionsLabelsItem {
  /// Creates the request body.
  const PatchCrmAttributesIdRequestOptionsLabelsItem({
    required this.key,
    required this.valueValue,
  });

  /// Decodes a JSON object into a PatchCrmAttributesIdRequestOptionsLabelsItem. Optional fields absent from [json] stay null.
  factory PatchCrmAttributesIdRequestOptionsLabelsItem.fromJson(
          Map<String, Object?> json) =>
      PatchCrmAttributesIdRequestOptionsLabelsItem(
        key: json.requireString(
            'key', 'PatchCrmAttributesIdRequestOptionsLabelsItem'),
        valueValue: json.requireString(
            'value', 'PatchCrmAttributesIdRequestOptionsLabelsItem'),
      );

  /// Internal option identifier
  final String key;

  /// Display label for the option
  final String valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'key': key,
        'value': valueValue,
      };
}

/// The request body of `POST /companies/import`.
final class PostCompaniesImportRequest {
  /// Creates the request body.
  const PostCompaniesImportRequest({
    this.file,
    this.mapping,
  });

  /// The CSV file to upload.The file should have the first row as the mapping attribute. Some default attribute names are (a) company_id \[brevo mongoID to update companies\] (b) associated_contact (c) associated_deal (f) any other attribute with internal name
  final BrevoFile? file;

  /// The mapping options in JSON format. Here is an example of the JSON structure: ```json {
  ///   "link_entities": true, // Determines whether to link related entities during the import process
  ///   "unlink_entities": false, // Determines whether to unlink related entities during the import process
  ///   "update_existing_records": true, // Determines whether to update based on company ID or treat every row as create
  ///   "unset_empty_attributes": false // Determines whether to unset a specific attribute during update if the values input is blank
  /// } ```
  final Map<String, Object?>? mapping;

  /// The non-file parts of the `multipart/form-data` body; a `Map` or `List` value is sent JSON-encoded.
  Map<String, Object?> toFields() => {
        if (mapping != null) 'mapping': mapping!,
      };

  /// The file parts of the `multipart/form-data` body.
  Map<String, BrevoFile> toFiles() => {
        if (file != null) 'file': file!,
      };
}

/// The request body of `POST /companies`.
final class PostCompaniesRequest {
  /// Creates the request body.
  const PostCompaniesRequest({
    this.attributes,
    this.countryCode,
    this.linkedContactsIds,
    this.linkedDealsIds,
    required this.name,
  });

  /// Decodes a JSON object into a PostCompaniesRequest. Optional fields absent from [json] stay null.
  factory PostCompaniesRequest.fromJson(Map<String, Object?> json) =>
      PostCompaniesRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        countryCode: json.optInt('countryCode'),
        linkedContactsIds: json.containsKey('linkedContactsIds')
            ? json.optIntList('linkedContactsIds')
            : null,
        linkedDealsIds: json.containsKey('linkedDealsIds')
            ? json.optStringList('linkedDealsIds')
            : null,
        name: json.requireString('name', 'PostCompaniesRequest'),
      );

  /// Attributes for company creation
  final Map<String, Object?>? attributes;

  /// Country code if phone_number is passed in attributes.
  final int? countryCode;

  /// Contact ids to be linked with company
  final List<int>? linkedContactsIds;

  /// Deal ids to be linked with company
  final List<String>? linkedDealsIds;

  /// Name of company
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (countryCode != null) 'countryCode': countryCode!,
        if (linkedContactsIds != null) 'linkedContactsIds': linkedContactsIds!,
        if (linkedDealsIds != null) 'linkedDealsIds': linkedDealsIds!,
        'name': name,
      };
}

/// The request body of `POST /crm/attributes`.
final class PostCrmAttributesRequest {
  /// Creates the request body.
  const PostCrmAttributesRequest({
    required this.attributeType,
    this.description,
    required this.label,
    required this.objectType,
    this.optionsLabels,
  });

  /// Decodes a JSON object into a PostCrmAttributesRequest. Optional fields absent from [json] stay null.
  factory PostCrmAttributesRequest.fromJson(Map<String, Object?> json) =>
      PostCrmAttributesRequest(
        attributeType: json.optEnum('attributeType',
                PostCrmAttributesRequestAttributeType.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'PostCrmAttributesRequest', key: 'attributeType')),
        description: json.optString('description'),
        label: json.requireString('label', 'PostCrmAttributesRequest'),
        objectType: json.optEnum(
                'objectType', PostCrmAttributesRequestObjectType.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'PostCrmAttributesRequest', key: 'objectType')),
        optionsLabels: json.containsKey('optionsLabels')
            ? json.optStringList('optionsLabels')
            : null,
      );

  /// The type of attribute (must be one of the defined enums)
  final PostCrmAttributesRequestAttributeType attributeType;

  /// A description of the attribute
  final String? description;

  /// The label for the attribute (max 50 characters, cannot be empty)
  final String label;

  /// The type of object the attribute belongs to. Must be either `companies` or `deals`.
  final PostCrmAttributesRequestObjectType objectType;

  /// Options for multi-choice or single-select attributes
  final List<String>? optionsLabels;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'attributeType': attributeType.value,
        if (description != null) 'description': description!,
        'label': label,
        'objectType': objectType.value,
        if (optionsLabels != null) 'optionsLabels': optionsLabels!,
      };
}
