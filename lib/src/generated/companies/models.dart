// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// Company Details
final class Company {
  /// Creates a Company from already-decoded fields.
  const Company({
    this.attributes = const {},
    this.id,
    this.linkedContactsIds = const [],
    this.linkedDealsIds = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a Company. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Company.fromJson(Map<String, Object?> json) => Company(
        attributes: json.optObjectMap('attributes'),
        id: json.optString('id'),
        linkedContactsIds: json.optIntList('linkedContactsIds'),
        linkedDealsIds: json.optStringList('linkedDealsIds'),
        raw: json,
      );

  /// Company attributes with values
  final Map<String, Object?> attributes;

  /// Unique company id
  final String? id;

  /// Contact ids for contacts linked to this company
  final List<int> linkedContactsIds;

  /// Deal ids for deals linked to this company
  final List<String> linkedDealsIds;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (id != null) 'id': id!,
        if (linkedContactsIds.isNotEmpty)
          'linkedContactsIds': linkedContactsIds,
        if (linkedDealsIds.isNotEmpty) 'linkedDealsIds': linkedDealsIds,
      };
}

/// List of companies
final class GetCompaniesResponse {
  /// Creates a GetCompaniesResponse from already-decoded fields.
  const GetCompaniesResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCompaniesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCompaniesResponse.fromJson(Map<String, Object?> json) =>
      GetCompaniesResponse(
        items: json.optObjectList('items', Company.fromJson,
            objectName: 'GetCompaniesResponse'),
        raw: json,
      );

  /// List of companies
  final List<Company> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// Company attribute details
final class GetCrmAttributesCompaniesResponseItem {
  /// Creates a GetCrmAttributesCompaniesResponseItem from already-decoded fields.
  const GetCrmAttributesCompaniesResponseItem({
    this.attributeOptions = const [],
    this.attributeTypeName,
    this.internalName,
    this.isRequired,
    this.isValueReadonly,
    this.label,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmAttributesCompaniesResponseItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmAttributesCompaniesResponseItem.fromJson(
          Map<String, Object?> json) =>
      GetCrmAttributesCompaniesResponseItem(
        attributeOptions: json.optObjectList('attributeOptions',
            GetCrmAttributesCompaniesResponseItemAttributeOptionsItem.fromJson,
            objectName: 'GetCrmAttributesCompaniesResponseItem'),
        attributeTypeName: json.optString('attributeTypeName'),
        internalName: json.optString('internalName'),
        isRequired: json.optBool('isRequired'),
        isValueReadonly: json.optBool('isValueReadonly'),
        label: json.optString('label'),
        raw: json,
      );

  /// Options for single-select or multi-choice attributes
  final List<GetCrmAttributesCompaniesResponseItemAttributeOptionsItem>
      attributeOptions;

  /// Type of the attribute
  final String? attributeTypeName;

  /// Internal name of the attribute
  final String? internalName;

  /// Whether this attribute is required
  final bool? isRequired;

  /// Whether this attribute value is read-only
  final bool? isValueReadonly;

  /// Display label of the attribute
  final String? label;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributeOptions.isNotEmpty)
          'attributeOptions': [
            for (final item in attributeOptions) item.toJson()
          ],
        if (attributeTypeName != null) 'attributeTypeName': attributeTypeName!,
        if (internalName != null) 'internalName': internalName!,
        if (isRequired != null) 'isRequired': isRequired!,
        if (isValueReadonly != null) 'isValueReadonly': isValueReadonly!,
        if (label != null) 'label': label!,
      };
}

/// The `GetCrmAttributesCompaniesResponseItem.attributeOptionsItem` object of the Brevo API.
final class GetCrmAttributesCompaniesResponseItemAttributeOptionsItem {
  /// Creates a GetCrmAttributesCompaniesResponseItemAttributeOptionsItem from already-decoded fields.
  const GetCrmAttributesCompaniesResponseItemAttributeOptionsItem({
    this.key,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmAttributesCompaniesResponseItemAttributeOptionsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmAttributesCompaniesResponseItemAttributeOptionsItem.fromJson(
          Map<String, Object?> json) =>
      GetCrmAttributesCompaniesResponseItemAttributeOptionsItem(
        key: json.optString('key'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// The `key` field.
  final String? key;

  /// The `value` field.
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (key != null) 'key': key!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `POST /companies/import` object of the Brevo API.
final class PostCompaniesImportResponse {
  /// Creates a PostCompaniesImportResponse from already-decoded fields.
  const PostCompaniesImportResponse({
    this.processId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCompaniesImportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCompaniesImportResponse.fromJson(Map<String, Object?> json) =>
      PostCompaniesImportResponse(
        processId: json.optInt('processId'),
        raw: json,
      );

  /// The ID of the import process
  final int? processId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (processId != null) 'processId': processId!,
      };
}

/// Created company id
final class PostCompaniesResponse {
  /// Creates a PostCompaniesResponse from already-decoded fields.
  const PostCompaniesResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCompaniesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCompaniesResponse.fromJson(Map<String, Object?> json) =>
      PostCompaniesResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// Unique company id
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `POST /crm/attributes` object of the Brevo API.
final class PostCrmAttributesResponse {
  /// Creates a PostCrmAttributesResponse from already-decoded fields.
  const PostCrmAttributesResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCrmAttributesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCrmAttributesResponse.fromJson(Map<String, Object?> json) =>
      PostCrmAttributesResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// Unique ID of the created attribute
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}
