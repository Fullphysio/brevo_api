// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/open_enum.dart';

/// The `GetCrmTasksRequestFilterDate` values of the Brevo API.
final class GetCrmTasksRequestFilterDate extends BrevoOpenEnum {
  const GetCrmTasksRequestFilterDate._(super.value);

  /// Wraps a value this package does not know.
  const GetCrmTasksRequestFilterDate.unknown(super.value);

  /// The `overdue` value.
  static const overdue = GetCrmTasksRequestFilterDate._('overdue');

  /// The `today` value.
  static const today = GetCrmTasksRequestFilterDate._('today');

  /// The `tomorrow` value.
  static const tomorrow = GetCrmTasksRequestFilterDate._('tomorrow');

  /// The `week` value.
  static const week = GetCrmTasksRequestFilterDate._('week');

  /// The `range` value.
  static const range = GetCrmTasksRequestFilterDate._('range');

  /// The values known when this package was generated.
  static const values = [overdue, today, tomorrow, week, range];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetCrmTasksRequestFilterDate.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetCrmTasksRequestFilterDate.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetCrmTasksRequestFilterStatus` values of the Brevo API.
final class GetCrmTasksRequestFilterStatus extends BrevoOpenEnum {
  const GetCrmTasksRequestFilterStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetCrmTasksRequestFilterStatus.unknown(super.value);

  /// The `done` value.
  static const done = GetCrmTasksRequestFilterStatus._('done');

  /// The `undone` value.
  static const undone = GetCrmTasksRequestFilterStatus._('undone');

  /// The values known when this package was generated.
  static const values = [done, undone];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetCrmTasksRequestFilterStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetCrmTasksRequestFilterStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetCrmTasksRequestSort` values of the Brevo API.
final class GetCrmTasksRequestSort extends BrevoOpenEnum {
  const GetCrmTasksRequestSort._(super.value);

  /// Wraps a value this package does not know.
  const GetCrmTasksRequestSort.unknown(super.value);

  /// The `asc` value.
  static const asc = GetCrmTasksRequestSort._('asc');

  /// The `desc` value.
  static const desc = GetCrmTasksRequestSort._('desc');

  /// The values known when this package was generated.
  static const values = [asc, desc];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetCrmTasksRequestSort.fromWire(String value) => values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetCrmTasksRequestSort.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `TaskReminderTypesItem` values of the Brevo API.
final class TaskReminderTypesItem extends BrevoOpenEnum {
  const TaskReminderTypesItem._(super.value);

  /// Wraps a value this package does not know.
  const TaskReminderTypesItem.unknown(super.value);

  /// The `email` value.
  static const email = TaskReminderTypesItem._('email');

  /// The `push` value.
  static const push = TaskReminderTypesItem._('push');

  /// The values known when this package was generated.
  static const values = [email, push];

  /// Decodes a wire value, preserving one this package does not know.
  factory TaskReminderTypesItem.fromWire(String value) => values.firstWhere(
        (known) => known.value == value,
        orElse: () => TaskReminderTypesItem.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Unit of time before reminder is to be sent
final class TaskReminderUnit extends BrevoOpenEnum {
  const TaskReminderUnit._(super.value);

  /// Wraps a value this package does not know.
  const TaskReminderUnit.unknown(super.value);

  /// The `minutes` value.
  static const minutes = TaskReminderUnit._('minutes');

  /// The `hours` value.
  static const hours = TaskReminderUnit._('hours');

  /// The `weeks` value.
  static const weeks = TaskReminderUnit._('weeks');

  /// The `days` value.
  static const days = TaskReminderUnit._('days');

  /// The values known when this package was generated.
  static const values = [minutes, hours, weeks, days];

  /// Decodes a wire value, preserving one this package does not know.
  factory TaskReminderUnit.fromWire(String value) => values.firstWhere(
        (known) => known.value == value,
        orElse: () => TaskReminderUnit.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}
