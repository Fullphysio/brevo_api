// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// List of tasks
final class GetCrmTasksResponse {
  /// Creates a GetCrmTasksResponse from already-decoded fields.
  const GetCrmTasksResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmTasksResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmTasksResponse.fromJson(Map<String, Object?> json) =>
      GetCrmTasksResponse(
        items: json.optObjectList('items', Task.fromJson,
            objectName: 'GetCrmTasksResponse'),
        raw: json,
      );

  /// List of tasks
  final List<Task> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// The `GET /crm/tasktypes` object of the Brevo API.
final class GetCrmTasktypesResponseItem {
  /// Creates a GetCrmTasktypesResponseItem from already-decoded fields.
  const GetCrmTasktypesResponseItem({
    this.id,
    this.title,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmTasktypesResponseItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmTasktypesResponseItem.fromJson(Map<String, Object?> json) =>
      GetCrmTasktypesResponseItem(
        id: json.optString('id'),
        title: json.optString('title'),
        raw: json,
      );

  /// Id of task type
  final String? id;

  /// Title of task type
  final String? title;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (title != null) 'title': title!,
      };
}

/// Task Details
final class PostCrmTasksResponse {
  /// Creates a PostCrmTasksResponse from already-decoded fields.
  const PostCrmTasksResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCrmTasksResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCrmTasksResponse.fromJson(Map<String, Object?> json) =>
      PostCrmTasksResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// Unique task id
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// Task Details
final class Task {
  /// Creates a Task from already-decoded fields.
  const Task({
    this.assignToId,
    this.companiesIds = const [],
    this.contactsIds = const [],
    this.createdAt,
    this.date,
    this.dealsIds = const [],
    this.done,
    this.id,
    this.name,
    this.notes,
    this.taskTypeId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Task. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Task.fromJson(Map<String, Object?> json) => Task(
        assignToId: json.optString('assignToId'),
        companiesIds: json.optStringList('companiesIds'),
        contactsIds: json.optIntList('contactsIds'),
        createdAt: json.optString('createdAt'),
        date: json.optString('date'),
        dealsIds: json.optStringList('dealsIds'),
        done: json.optBool('done'),
        id: json.optString('id'),
        name: json.optString('name'),
        notes: json.optString('notes'),
        taskTypeId: json.optString('taskTypeId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Account id of the user assigned to this task
  final String? assignToId;

  /// Companies ids for companies a task is linked to
  final List<String> companiesIds;

  /// Contact ids for contacts linked to this task
  final List<int> contactsIds;

  /// Task creation date/time
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Task due date and time
  final String? date;

  /// [date] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get dateDate => date == null ? null : DateTime.tryParse(date!);

  /// Deal ids for deals a task is linked to
  final List<String> dealsIds;

  /// Whether the task is marked as done
  final bool? done;

  /// Unique task id
  final String? id;

  /// Name of task
  final String? name;

  /// Notes added to a task
  final String? notes;

  /// Id for type of task e.g Call / Email / Meeting etc.
  final String? taskTypeId;

  /// Task last update date/time
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (assignToId != null) 'assignToId': assignToId!,
        if (companiesIds.isNotEmpty) 'companiesIds': companiesIds,
        if (contactsIds.isNotEmpty) 'contactsIds': contactsIds,
        if (createdAt != null) 'createdAt': createdAt!,
        if (date != null) 'date': date!,
        if (dealsIds.isNotEmpty) 'dealsIds': dealsIds,
        if (done != null) 'done': done!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (notes != null) 'notes': notes!,
        if (taskTypeId != null) 'taskTypeId': taskTypeId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// Task reminder date/time for a task
final class TaskReminder {
  /// Creates a TaskReminder from already-decoded fields.
  const TaskReminder({
    this.types = const [],
    this.unit,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TaskReminder. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TaskReminder.fromJson(Map<String, Object?> json) => TaskReminder(
        types: json
            .optStringList('types')
            .map(TaskReminderTypesItem.fromWire)
            .toList(growable: false),
        unit: json.optEnum('unit', TaskReminderUnit.fromWire),
        valueValue: json.optInt('value'),
        raw: json,
      );

  /// Type of task reminder e.g email, push
  final List<TaskReminderTypesItem> types;

  /// Unit of time before reminder is to be sent
  final TaskReminderUnit? unit;

  /// Value of time unit before reminder is to be sent
  final int? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (types.isNotEmpty) 'types': [for (final item in types) item.value],
        if (unit != null) 'unit': unit!.value,
        if (valueValue != null) 'value': valueValue!,
      };
}
