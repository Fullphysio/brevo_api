// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import 'models.dart';
import 'requests.dart';

/// The `payments` resource of the Brevo API: `client.payments`.
final class BrevoPayments {
  /// Creates the resource backed by [_client].
  const BrevoPayments(this._client);

  final BrevoClient _client;

  /// `POST /payments/requests`
  ///
  /// Create a payment request
  ///
  /// Create a new payment request for a Brevo contact. The request requires a reference (displayed on the payment page), a contact ID, and a cart with currency and amount in cents. You can optionally configure a custom success redirect URL and enable email notifications with reminders. Returns the payment request ID and its public payment URL. A `403` error is returned if Brevo Payments is not activated or the account is not validated.
  Future<CreatePaymentRequestResponse> createPaymentRequest(
      CreatePaymentRequestRequest request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'POST',
        path: '/payments/requests',
        body: request.toJson(),
        options: options);
    return CreatePaymentRequestResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'CreatePaymentRequestResponse'));
  }

  /// `DELETE /payments/requests/{id}`
  ///
  /// Delete a payment request.
  ///
  /// Delete a payment request by its UUID. Once deleted, the payment request can no longer be accessed or paid. Returns a `404` error if no payment request matches the provided ID, and a `403` error if Brevo Payments is not activated or the account is not validated.
  ///
  /// \[id\]: ID of the payment request.
  Future<void> deletePaymentRequest(
      {required String id, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/payments/requests/${brevoPathSegment(id)}',
        options: options);
  }

  /// `GET /payments/requests/{id}`
  ///
  /// Get payment request details
  ///
  /// Retrieve the details of a specific payment request by its ID. The response includes the reference, status (created, sent, reminderSent, or paid), cart details, notification configuration, contact ID, and the number of reminders sent. Returns a `404` error if no payment request matches the provided ID.
  ///
  /// \[id\]: Id of the payment Request
  Future<GetPaymentRequestResponse> getPaymentRequest(
      {required String id, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/payments/requests/${brevoPathSegment(id)}',
        options: options);
    return GetPaymentRequestResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetPaymentRequestResponse'));
  }
}
