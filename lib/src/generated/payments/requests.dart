// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'models.dart';

/// The request body of `POST /payments/requests`.
final class CreatePaymentRequestRequest {
  /// Creates the request body.
  const CreatePaymentRequestRequest({
    required this.cart,
    this.configuration,
    required this.contactId,
    this.description,
    this.notification,
    required this.reference,
  });

  /// Decodes a JSON object into a CreatePaymentRequestRequest. Optional fields absent from [json] stay null.
  factory CreatePaymentRequestRequest.fromJson(Map<String, Object?> json) =>
      CreatePaymentRequestRequest(
        cart: json.optNested('cart', Cart.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreatePaymentRequestRequest', key: 'cart')),
        configuration: json.optNested('configuration', Configuration.fromJson),
        contactId: json.requireInt('contactId', 'CreatePaymentRequestRequest'),
        description: json.optString('description'),
        notification: json.optNested('notification', Notification.fromJson),
        reference:
            json.requireString('reference', 'CreatePaymentRequestRequest'),
      );

  /// The `cart` field.
  final Cart cart;

  /// The `configuration` field.
  final Configuration? configuration;

  /// Brevo ID of the contact requested to pay.
  final int contactId;

  /// Description of payment request.
  final String? description;

  /// The `notification` field.
  final Notification? notification;

  /// Reference of the payment request, it will appear on the payment page.
  final String reference;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'cart': cart.toJson(),
        if (configuration != null) 'configuration': configuration!.toJson(),
        'contactId': contactId,
        if (description != null) 'description': description!,
        if (notification != null) 'notification': notification!.toJson(),
        'reference': reference,
      };
}
