// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// Specify the payment currency and amount.
final class Cart {
  /// Creates a Cart from already-decoded fields.
  const Cart({
    this.currency,
    this.specificAmount,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Cart. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Cart.fromJson(Map<String, Object?> json) => Cart(
        currency: json.optString('currency'),
        specificAmount: json.optInt('specificAmount'),
        raw: json,
      );

  /// Currency code for the payment amount.
  final String? currency;

  /// Payment amount, in cents. e.g. if you want to request €12.00, then the amount in cents is 1200.
  final int? specificAmount;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (currency != null) 'currency': currency!,
        if (specificAmount != null) 'specificAmount': specificAmount!,
      };
}

/// Optional. Redirect contact to a custom success page once payment is successful. If empty the default Brevo page will be displayed once a payment is validated
final class Configuration {
  /// Creates a Configuration from already-decoded fields.
  const Configuration({
    this.customSuccessUrl,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Configuration. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Configuration.fromJson(Map<String, Object?> json) => Configuration(
        customSuccessUrl: json.optString('customSuccessUrl'),
        raw: json,
      );

  /// Absolute URL of the custom success page.
  final String? customSuccessUrl;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (customSuccessUrl != null) 'customSuccessUrl': customSuccessUrl!,
      };
}

/// The `POST /payments/requests` object of the Brevo API.
final class CreatePaymentRequestResponse {
  /// Creates a CreatePaymentRequestResponse from already-decoded fields.
  const CreatePaymentRequestResponse({
    this.id,
    this.url,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreatePaymentRequestResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreatePaymentRequestResponse.fromJson(Map<String, Object?> json) =>
      CreatePaymentRequestResponse(
        id: json.optInt('id'),
        url: json.optString('url'),
        raw: json,
      );

  /// ID of the object created
  final int? id;

  /// URL of the payment request created
  final String? url;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (url != null) 'url': url!,
      };
}

/// The `GET /payments/requests/{id}` object of the Brevo API.
final class GetPaymentRequestResponse {
  /// Creates a GetPaymentRequestResponse from already-decoded fields.
  const GetPaymentRequestResponse({
    this.cart,
    this.configuration,
    this.contactId,
    this.notification,
    this.numberOfRemindersSent,
    this.reference,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetPaymentRequestResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetPaymentRequestResponse.fromJson(Map<String, Object?> json) =>
      GetPaymentRequestResponse(
        cart: json.optNested('cart', Cart.fromJson),
        configuration: json.optNested('configuration', Configuration.fromJson),
        contactId: json.optInt('contactId'),
        notification: json.optNested('notification', Notification.fromJson),
        numberOfRemindersSent: json.optInt('numberOfRemindersSent'),
        reference: json.optString('reference'),
        status:
            json.optEnum('status', GetPaymentRequestResponseStatus.fromWire),
        raw: json,
      );

  /// The `cart` field.
  final Cart? cart;

  /// The `configuration` field.
  final Configuration? configuration;

  /// Brevo ID of the contact requested to pay.
  final int? contactId;

  /// The `notification` field.
  final Notification? notification;

  /// number of reminders sent.
  final int? numberOfRemindersSent;

  /// Reference of the payment request, it will appear on the payment page.
  final String? reference;

  /// Status of the payment request.
  final GetPaymentRequestResponseStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (cart != null) 'cart': cart!.toJson(),
        if (configuration != null) 'configuration': configuration!.toJson(),
        if (contactId != null) 'contactId': contactId!,
        if (notification != null) 'notification': notification!.toJson(),
        if (numberOfRemindersSent != null)
          'numberOfRemindersSent': numberOfRemindersSent!,
        if (reference != null) 'reference': reference!,
        if (status != null) 'status': status!.value,
      };
}

/// Optional. Use this object if you want to let Brevo send an email to the contact, with the payment request URL. If empty, no notifications (message and reminders) will be sent.
final class Notification {
  /// Creates a Notification from already-decoded fields.
  const Notification({
    this.channel,
    this.text,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Notification. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Notification.fromJson(Map<String, Object?> json) => Notification(
        channel: json.optString('channel'),
        text: json.optString('text'),
        raw: json,
      );

  /// Channel used to send the notifications.
  final String? channel;

  /// Use this field if you want to give more context to your contact about the payment request.
  final String? text;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (channel != null) 'channel': channel!,
        if (text != null) 'text': text!,
      };
}
