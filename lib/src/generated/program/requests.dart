// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';

/// The request body of `POST /loyalty/config/programs`.
final class CreateNewLpRequest {
  /// Creates the request body.
  const CreateNewLpRequest({
    this.description,
    this.documentId,
    this.meta,
    required this.name,
  });

  /// Decodes a JSON object into a CreateNewLpRequest. Optional fields absent from [json] stay null.
  factory CreateNewLpRequest.fromJson(Map<String, Object?> json) =>
      CreateNewLpRequest(
        description: json.optString('description'),
        documentId: json.optString('documentId'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
        name: json.requireString('name', 'CreateNewLpRequest'),
      );

  /// Optional description of the loyalty program (max 256 chars).
  final String? description;

  /// Optional unique document ID.
  final String? documentId;

  /// Optional metadata related to the loyalty program.
  final Map<String, Object?>? meta;

  /// Required name of the loyalty program (max 128 chars).
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (description != null) 'description': description!,
        if (documentId != null) 'documentId': documentId!,
        if (meta != null) 'meta': meta!,
        'name': name,
      };
}

/// The request body of `PATCH /loyalty/config/programs/{pid}`.
final class PartiallyUpdateLoyaltyProgramRequest {
  /// Creates the request body.
  const PartiallyUpdateLoyaltyProgramRequest({
    this.description,
    this.meta,
    this.name,
  });

  /// Decodes a JSON object into a PartiallyUpdateLoyaltyProgramRequest. Optional fields absent from [json] stay null.
  factory PartiallyUpdateLoyaltyProgramRequest.fromJson(
          Map<String, Object?> json) =>
      PartiallyUpdateLoyaltyProgramRequest(
        description: json.optString('description'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
        name: json.optString('name'),
      );

  /// Loyalty Program description
  final String? description;

  /// Loyalty Program meta data
  final Map<String, Object?>? meta;

  /// Loyalty Program name
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (description != null) 'description': description!,
        if (meta != null) 'meta': meta!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `POST /loyalty/config/programs/{pid}/subscription-members`.
final class SubscribeMemberToASubscriptionRequest {
  /// Creates the request body.
  const SubscribeMemberToASubscriptionRequest({
    this.contactId,
    this.loyaltySubscriptionId,
    required this.memberContactIds,
  });

  /// Decodes a JSON object into a SubscribeMemberToASubscriptionRequest. Optional fields absent from [json] stay null.
  factory SubscribeMemberToASubscriptionRequest.fromJson(
          Map<String, Object?> json) =>
      SubscribeMemberToASubscriptionRequest(
        contactId: json.optInt('contactId'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        memberContactIds: json.containsKey('memberContactIds')
            ? json.optIntList('memberContactIds')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'SubscribeMemberToASubscriptionRequest',
                key: 'memberContactIds')),
      );

  /// Required if LoyaltySubscriptionId is not provided, must be greater than 0
  final int? contactId;

  /// Required if ContactId is not provided, max length 64
  final String? loyaltySubscriptionId;

  /// Required, each item must be greater than or equal to 1
  final List<int> memberContactIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contactId': contactId!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        'memberContactIds': memberContactIds,
      };
}

/// The request body of `POST /loyalty/config/programs/{pid}/subscriptions`.
final class SubscribeToLoyaltyProgramRequest {
  /// Creates the request body.
  const SubscribeToLoyaltyProgramRequest({
    required this.contactId,
    this.creationDate,
    this.loyaltySubscriptionId,
    this.meta,
  });

  /// Decodes a JSON object into a SubscribeToLoyaltyProgramRequest. Optional fields absent from [json] stay null.
  factory SubscribeToLoyaltyProgramRequest.fromJson(
          Map<String, Object?> json) =>
      SubscribeToLoyaltyProgramRequest(
        contactId:
            json.requireInt('contactId', 'SubscribeToLoyaltyProgramRequest'),
        creationDate: json.optString('creationDate'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
      );

  /// Required contact ID; must be greater than 0.
  final int contactId;

  /// Optional creation date in ISO 8601 format (YYYY-MM-DDThh:mm:ss.ffffff+HH:MM). Must be in the past.
  final String? creationDate;

  /// [creationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get creationDateDate =>
      creationDate == null ? null : DateTime.tryParse(creationDate!);

  /// Optional subscription ID (max length 64).
  final String? loyaltySubscriptionId;

  /// Optional metadata associated with the subscription.
  final Map<String, Object?>? meta;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'contactId': contactId,
        if (creationDate != null) 'creationDate': creationDate!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (meta != null) 'meta': meta!,
      };
}

/// The request body of `PUT /loyalty/config/programs/{pid}`.
final class UpdateLoyaltyProgramRequest {
  /// Creates the request body.
  const UpdateLoyaltyProgramRequest({
    this.description,
    this.meta,
    required this.name,
  });

  /// Decodes a JSON object into a UpdateLoyaltyProgramRequest. Optional fields absent from [json] stay null.
  factory UpdateLoyaltyProgramRequest.fromJson(Map<String, Object?> json) =>
      UpdateLoyaltyProgramRequest(
        description: json.optString('description'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
        name: json.requireString('name', 'UpdateLoyaltyProgramRequest'),
      );

  /// Loyalty Program description
  final String? description;

  /// Loyalty Program meta data
  final Map<String, Object?>? meta;

  /// Loyalty Program name
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (description != null) 'description': description!,
        if (meta != null) 'meta': meta!,
        'name': name,
      };
}
