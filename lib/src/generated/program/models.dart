// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /loyalty/config/programs` object of the Brevo API.
final class GetLpListResponse {
  /// Creates a GetLpListResponse from already-decoded fields.
  const GetLpListResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetLpListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetLpListResponse.fromJson(Map<String, Object?> json) =>
      GetLpListResponse(
        items: json.optObjectList('items', LoyaltyProgram.fromJson,
            objectName: 'GetLpListResponse'),
        raw: json,
      );

  /// Loyalty Program list
  final List<LoyaltyProgram> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// The `GET /loyalty/config/programs/{pid}/account-info` object of the Brevo API.
final class GetParameterSubscriptionInfoResponse {
  /// Creates a GetParameterSubscriptionInfoResponse from already-decoded fields.
  const GetParameterSubscriptionInfoResponse({
    this.balance,
    this.loyaltyProgramName,
    this.members = const [],
    this.membership,
    this.reward = const [],
    this.tier = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponse.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponse(
        balance: json.optNested(
            'balance', GetParameterSubscriptionInfoResponseBalance.fromJson),
        loyaltyProgramName: json.optString('loyaltyProgramName'),
        members: json.optObjectList(
            'members', GetParameterSubscriptionInfoResponseMembersItem.fromJson,
            objectName: 'GetParameterSubscriptionInfoResponse'),
        membership: json.optNested('membership',
            GetParameterSubscriptionInfoResponseMembership.fromJson),
        reward: json.optObjectList(
            'reward', GetParameterSubscriptionInfoResponseRewardItem.fromJson,
            objectName: 'GetParameterSubscriptionInfoResponse'),
        tier: json.optObjectList(
            'tier', GetParameterSubscriptionInfoResponseTierItem.fromJson,
            objectName: 'GetParameterSubscriptionInfoResponse'),
        raw: json,
      );

  /// Balance details for the subscription.
  final GetParameterSubscriptionInfoResponseBalance? balance;

  /// Name of the loyalty program.
  final String? loyaltyProgramName;

  /// List of members associated with the subscription.
  final List<GetParameterSubscriptionInfoResponseMembersItem> members;

  /// Membership details of the subscription. Returned when the subscription could be resolved from the provided `contactId` or `loyaltySubscriptionId`.
  final GetParameterSubscriptionInfoResponseMembership? membership;

  /// List of rewards associated with the subscription.
  final List<GetParameterSubscriptionInfoResponseRewardItem> reward;

  /// List of tier assignments for the subscription.
  final List<GetParameterSubscriptionInfoResponseTierItem> tier;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (balance != null) 'balance': balance!.toJson(),
        if (loyaltyProgramName != null)
          'loyaltyProgramName': loyaltyProgramName!,
        if (members.isNotEmpty)
          'members': [for (final item in members) item.toJson()],
        if (membership != null) 'membership': membership!.toJson(),
        if (reward.isNotEmpty)
          'reward': [for (final item in reward) item.toJson()],
        if (tier.isNotEmpty) 'tier': [for (final item in tier) item.toJson()],
      };
}

/// The `GetParameterSubscriptionInfoResponse.balance` object of the Brevo API.
final class GetParameterSubscriptionInfoResponseBalance {
  /// Creates a GetParameterSubscriptionInfoResponseBalance from already-decoded fields.
  const GetParameterSubscriptionInfoResponseBalance({
    this.balances = const [],
    this.contactId,
    this.loyaltyProgramId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseBalance. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseBalance.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseBalance(
        balances: json.optObjectList('balances',
            GetParameterSubscriptionInfoResponseBalanceBalancesItem.fromJson,
            objectName: 'GetParameterSubscriptionInfoResponseBalance'),
        contactId: json.optInt('contactId'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        raw: json,
      );

  /// List of balance details associated with the contact.
  final List<GetParameterSubscriptionInfoResponseBalanceBalancesItem> balances;

  /// Unique identifier of the contact.
  final int? contactId;

  /// Unique identifier of the loyalty program.
  final String? loyaltyProgramId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (balances.isNotEmpty)
          'balances': [for (final item in balances) item.toJson()],
        if (contactId != null) 'contactId': contactId!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
      };
}

/// The `GetParameterSubscriptionInfoResponseBalance.balancesItem` object of the Brevo API.
final class GetParameterSubscriptionInfoResponseBalanceBalancesItem {
  /// Creates a GetParameterSubscriptionInfoResponseBalanceBalancesItem from already-decoded fields.
  const GetParameterSubscriptionInfoResponseBalanceBalancesItem({
    this.balanceDefinitionId,
    this.balanceDefinitionName,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseBalanceBalancesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseBalanceBalancesItem.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseBalanceBalancesItem(
        balanceDefinitionId: json.optString('balanceDefinitionId'),
        balanceDefinitionName: json.optString('balanceDefinitionName'),
        valueValue: json.optDouble('value'),
        raw: json,
      );

  /// Unique identifier for the balance definition).
  final String? balanceDefinitionId;

  /// Name of the balance definition.
  final String? balanceDefinitionName;

  /// The amount of the balance.
  final double? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (balanceDefinitionId != null)
          'balanceDefinitionId': balanceDefinitionId!,
        if (balanceDefinitionName != null)
          'balanceDefinitionName': balanceDefinitionName!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `GetParameterSubscriptionInfoResponse.membersItem` object of the Brevo API.
final class GetParameterSubscriptionInfoResponseMembersItem {
  /// Creates a GetParameterSubscriptionInfoResponseMembersItem from already-decoded fields.
  const GetParameterSubscriptionInfoResponseMembersItem({
    this.createdAt,
    this.memberContactId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseMembersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseMembersItem.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseMembersItem(
        createdAt: json.optString('createdAt'),
        memberContactId: json.optInt('memberContactId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Timestamp when the member was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Unique identifier of the member.
  final int? memberContactId;

  /// Timestamp when the member was last updated.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (memberContactId != null) 'memberContactId': memberContactId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// Membership details of the subscription. Returned when the subscription could be resolved from the provided `contactId` or `loyaltySubscriptionId`.
final class GetParameterSubscriptionInfoResponseMembership {
  /// Creates a GetParameterSubscriptionInfoResponseMembership from already-decoded fields.
  const GetParameterSubscriptionInfoResponseMembership({
    this.createdAt,
    this.loyaltySubscriptionId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseMembership. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseMembership.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseMembership(
        createdAt: json.optString('createdAt'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Timestamp when the subscription was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Unique identifier of the loyalty subscription.
  final String? loyaltySubscriptionId;

  /// Timestamp when the subscription was last updated.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `GetParameterSubscriptionInfoResponse.rewardItem` object of the Brevo API.
final class GetParameterSubscriptionInfoResponseRewardItem {
  /// Creates a GetParameterSubscriptionInfoResponseRewardItem from already-decoded fields.
  const GetParameterSubscriptionInfoResponseRewardItem({
    this.code,
    this.contactId,
    this.createdAt,
    this.expirationDate,
    this.id,
    this.loyaltyProgramId,
    this.meta = const {},
    this.publicDescription,
    this.rewardId,
    this.rewardName,
    this.unit,
    this.updatedAt,
    this.validFrom,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseRewardItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseRewardItem.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseRewardItem(
        code: json.optString('code'),
        contactId: json.optInt('contactId'),
        createdAt: json.optString('createdAt'),
        expirationDate: json.optString('expirationDate'),
        id: json.optString('id'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        meta: json.optObjectMap('meta'),
        publicDescription: json.optString('publicDescription'),
        rewardId: json.optString('rewardId'),
        rewardName: json.optString('rewardName'),
        unit: json.optEnum('unit',
            GetParameterSubscriptionInfoResponseRewardItemUnit.fromWire),
        updatedAt: json.optString('updatedAt'),
        validFrom: json.optString('validFrom'),
        valueValue: json.optDouble('value'),
        raw: json,
      );

  /// Reward code assigned to the contact.
  final String? code;

  /// Unique identifier of the contact.
  final int? contactId;

  /// Timestamp when the reward was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Expiration date of the reward.
  final String? expirationDate;

  /// [expirationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get expirationDateDate =>
      expirationDate == null ? null : DateTime.tryParse(expirationDate!);

  /// Unique identifier of the reward.
  final String? id;

  /// Unique identifier of the loyalty program.
  final String? loyaltyProgramId;

  /// Additional metadata related to the reward.
  final Map<String, Object?> meta;

  /// Customer-facing description of the reward, as configured on the reward definition.
  final String? publicDescription;

  /// Unique identifier of the reward definition.
  final String? rewardId;

  /// Customer-facing name of the reward. Falls back to the internal reward name when no public name is set.
  final String? rewardName;

  /// Unit the reward's value is expressed in — one of the currency codes listed below, or PERCENT. Omitted when the reward has no unit (e.g. free-product rewards).
  final GetParameterSubscriptionInfoResponseRewardItemUnit? unit;

  /// Timestamp when the reward was last updated.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// Date from which the voucher becomes valid.
  final String? validFrom;

  /// [validFrom] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get validFromDate =>
      validFrom == null ? null : DateTime.tryParse(validFrom!);

  /// The value recorded when this reward was attributed to the contact — a snapshot, not necessarily the reward's current configured value. Omitted when not set.
  final double? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (code != null) 'code': code!,
        if (contactId != null) 'contactId': contactId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (expirationDate != null) 'expirationDate': expirationDate!,
        if (id != null) 'id': id!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (meta.isNotEmpty) 'meta': meta,
        if (publicDescription != null) 'publicDescription': publicDescription!,
        if (rewardId != null) 'rewardId': rewardId!,
        if (rewardName != null) 'rewardName': rewardName!,
        if (unit != null) 'unit': unit!.value,
        if (updatedAt != null) 'updatedAt': updatedAt!,
        if (validFrom != null) 'validFrom': validFrom!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `GetParameterSubscriptionInfoResponse.tierItem` object of the Brevo API.
final class GetParameterSubscriptionInfoResponseTierItem {
  /// Creates a GetParameterSubscriptionInfoResponseTierItem from already-decoded fields.
  const GetParameterSubscriptionInfoResponseTierItem({
    this.contactId,
    this.createdAt,
    this.groupId,
    this.groupName,
    this.loyaltyProgramId,
    this.meta = const {},
    this.tierId,
    this.tierName,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetParameterSubscriptionInfoResponseTierItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetParameterSubscriptionInfoResponseTierItem.fromJson(
          Map<String, Object?> json) =>
      GetParameterSubscriptionInfoResponseTierItem(
        contactId: json.optInt('contactId'),
        createdAt: json.optString('createdAt'),
        groupId: json.optString('groupId'),
        groupName: json.optString('groupName'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        meta: json.optObjectMap('meta'),
        tierId: json.optString('tierId'),
        tierName: json.optString('tierName'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Unique identifier of the contact.
  final int? contactId;

  /// Timestamp when the tier was assigned.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Unique identifier of the group associated with the tier.
  final String? groupId;

  /// Name of the group associated with the tier.
  final String? groupName;

  /// Unique identifier of the loyalty program.
  final String? loyaltyProgramId;

  /// Additional metadata related to the tier.
  final Map<String, Object?> meta;

  /// Unique identifier of the tier.
  final String? tierId;

  /// Name of the tier.
  final String? tierName;

  /// Timestamp when the tier was last updated
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contactId': contactId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (groupId != null) 'groupId': groupId!,
        if (groupName != null) 'groupName': groupName!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (meta.isNotEmpty) 'meta': meta,
        if (tierId != null) 'tierId': tierId!,
        if (tierName != null) 'tierName': tierName!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `components/schemas/loyaltyProgram` object of the Brevo API.
final class LoyaltyProgram {
  /// Creates a LoyaltyProgram from already-decoded fields.
  const LoyaltyProgram({
    this.birthdayAttribute,
    this.codeCount,
    this.createdAt,
    this.description,
    this.documentId,
    this.id,
    this.meta = const {},
    this.name,
    this.pattern,
    this.state,
    this.subscriptionGeneratorId,
    this.subscriptionPoolId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a LoyaltyProgram. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory LoyaltyProgram.fromJson(Map<String, Object?> json) => LoyaltyProgram(
        birthdayAttribute: json.optString('birthdayAttribute'),
        codeCount: json.optInt('codeCount'),
        createdAt: json.optString('createdAt'),
        description: json.optString('description'),
        documentId: json.optString('documentId'),
        id: json.optString('id'),
        meta: json.optObjectMap('meta'),
        name: json.optString('name'),
        pattern: json.optString('pattern'),
        state: json.optEnum('state', LoyaltyProgramState.fromWire),
        subscriptionGeneratorId: json.optString('subscriptionGeneratorId'),
        subscriptionPoolId: json.optString('subscriptionPoolId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Contact attribute name used for birthday tracking.
  final String? birthdayAttribute;

  /// Number of available subscription codes.
  final int? codeCount;

  /// Timestamp when the loyalty program was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Loyalty Program description.
  final String? description;

  /// Associated document identifier.
  final String? documentId;

  /// Loyalty Program unique identifier.
  final String? id;

  /// Loyalty Program metadata.
  final Map<String, Object?> meta;

  /// Loyalty Program name.
  final String? name;

  /// Subscription code pattern.
  final String? pattern;

  /// Current state of the loyalty program.
  final LoyaltyProgramState? state;

  /// Loyalty Program subscription generator ID.
  final String? subscriptionGeneratorId;

  /// Loyalty Program subscription pool ID.
  final String? subscriptionPoolId;

  /// Timestamp when the loyalty program was last modified.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (birthdayAttribute != null) 'birthdayAttribute': birthdayAttribute!,
        if (codeCount != null) 'codeCount': codeCount!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (description != null) 'description': description!,
        if (documentId != null) 'documentId': documentId!,
        if (id != null) 'id': id!,
        if (meta.isNotEmpty) 'meta': meta,
        if (name != null) 'name': name!,
        if (pattern != null) 'pattern': pattern!,
        if (state != null) 'state': state!.value,
        if (subscriptionGeneratorId != null)
          'subscriptionGeneratorId': subscriptionGeneratorId!,
        if (subscriptionPoolId != null)
          'subscriptionPoolId': subscriptionPoolId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `POST /loyalty/config/programs/{pid}/subscription-members` object of the Brevo API.
final class SubscribeMemberToASubscriptionResponse {
  /// Creates a SubscribeMemberToASubscriptionResponse from already-decoded fields.
  const SubscribeMemberToASubscriptionResponse({
    this.createdAt,
    this.memberContactIds = const [],
    this.organizationId,
    this.ownerContactId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SubscribeMemberToASubscriptionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SubscribeMemberToASubscriptionResponse.fromJson(
          Map<String, Object?> json) =>
      SubscribeMemberToASubscriptionResponse(
        createdAt: json.optString('createdAt'),
        memberContactIds: json.optIntList('memberContactIds'),
        organizationId: json.optInt('organizationId'),
        ownerContactId: json.optInt('ownerContactId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Timestamp when the subscription member was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// List of unique member contact IDs.
  final List<int> memberContactIds;

  /// Unique identifier of the organization.
  final int? organizationId;

  /// Unique identifier of the subscription owner.
  final int? ownerContactId;

  /// Timestamp when the subscription member was last updated.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (memberContactIds.isNotEmpty) 'memberContactIds': memberContactIds,
        if (organizationId != null) 'organizationId': organizationId!,
        if (ownerContactId != null) 'ownerContactId': ownerContactId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `POST /loyalty/config/programs/{pid}/subscriptions` object of the Brevo API.
final class SubscribeToLoyaltyProgramResponse {
  /// Creates a SubscribeToLoyaltyProgramResponse from already-decoded fields.
  const SubscribeToLoyaltyProgramResponse({
    this.contactId,
    this.createdAt,
    this.loyaltyProgramId,
    this.loyaltySubscriptionId,
    this.meta = const {},
    this.organizationId,
    this.updatedAt,
    this.versionId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SubscribeToLoyaltyProgramResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SubscribeToLoyaltyProgramResponse.fromJson(
          Map<String, Object?> json) =>
      SubscribeToLoyaltyProgramResponse(
        contactId: json.optInt('contactId'),
        createdAt: json.optString('createdAt'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        meta: json.optObjectMap('meta'),
        organizationId: json.optInt('organizationId'),
        updatedAt: json.optString('updatedAt'),
        versionId: json.optInt('versionId'),
        raw: json,
      );

  /// Unique identifier of the contact.
  final int? contactId;

  /// Timestamp when the subscription was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Unique identifier of the loyalty program.
  final String? loyaltyProgramId;

  /// Unique identifier of the subscription.
  final String? loyaltySubscriptionId;

  /// Metadata associated with the subscription.
  final Map<String, Object?> meta;

  /// Unique identifier of the organization.
  final int? organizationId;

  /// Timestamp when the subscription was last updated.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// Version number of the subscription.
  final int? versionId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contactId': contactId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (meta.isNotEmpty) 'meta': meta,
        if (organizationId != null) 'organizationId': organizationId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
        if (versionId != null) 'versionId': versionId!,
      };
}
