// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// File data that is uploaded
final class FileData {
  /// Creates a FileData from already-decoded fields.
  const FileData({
    this.authorId,
    this.companyId,
    this.contactId,
    this.createdAt,
    this.dealId,
    this.id,
    this.name,
    this.size,
    this.raw = const {},
  });

  /// Decodes a JSON object into a FileData. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory FileData.fromJson(Map<String, Object?> json) => FileData(
        authorId: json.optString('authorId'),
        companyId: json.optString('companyId'),
        contactId: json.optInt('contactId'),
        createdAt: json.optString('createdAt'),
        dealId: json.optString('dealId'),
        id: json.optString('id'),
        name: json.optString('name'),
        size: json.optInt('size'),
        raw: json,
      );

  /// Account id of user which created the file
  final String? authorId;

  /// Company id linked to a file
  final String? companyId;

  /// Contact id of contact on which file is uploaded
  final int? contactId;

  /// File created date/time
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Deal id linked to a file
  final String? dealId;

  /// Unique file id
  final String? id;

  /// Name of uploaded file
  final String? name;

  /// Size of file in bytes
  final int? size;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authorId != null) 'authorId': authorId!,
        if (companyId != null) 'companyId': companyId!,
        if (contactId != null) 'contactId': contactId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (dealId != null) 'dealId': dealId!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (size != null) 'size': size!,
      };
}

/// Downloadable file link
final class GetCrmFilesIdResponse {
  /// Creates a GetCrmFilesIdResponse from already-decoded fields.
  const GetCrmFilesIdResponse({
    this.fileUrl,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCrmFilesIdResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCrmFilesIdResponse.fromJson(Map<String, Object?> json) =>
      GetCrmFilesIdResponse(
        fileUrl: json.optString('fileUrl'),
        raw: json,
      );

  /// A unique link to download the requested file.
  final String? fileUrl;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (fileUrl != null) 'fileUrl': fileUrl!,
      };
}
