// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /feeds`.
final class CreateExternalFeedRequest {
  /// Creates the request body.
  const CreateExternalFeedRequest({
    this.authType,
    this.cache,
    this.headers,
    this.maxRetries,
    required this.name,
    this.password,
    this.token,
    required this.url,
    this.username,
  });

  /// Decodes a JSON object into a CreateExternalFeedRequest. Optional fields absent from [json] stay null.
  factory CreateExternalFeedRequest.fromJson(Map<String, Object?> json) =>
      CreateExternalFeedRequest(
        authType: json.optEnum(
            'authType', CreateExternalFeedRequestAuthType.fromWire),
        cache: json.optBool('cache'),
        headers: json.containsKey('headers')
            ? json.optObjectList(
                'headers', CreateExternalFeedRequestHeadersItem.fromJson,
                objectName: 'CreateExternalFeedRequest')
            : null,
        maxRetries: json.optInt('maxRetries'),
        name: json.requireString('name', 'CreateExternalFeedRequest'),
        password: json.optString('password'),
        token: json.optString('token'),
        url: json.requireString('url', 'CreateExternalFeedRequest'),
        username: json.optString('username'),
      );

  /// Authentication type for accessing the feed
  final CreateExternalFeedRequestAuthType? authType;

  /// Whether to cache the feed response
  final bool? cache;

  /// Custom HTTP headers for the feed request
  final List<CreateExternalFeedRequestHeadersItem>? headers;

  /// Maximum number of retry attempts for failed requests
  final int? maxRetries;

  /// Name of the feed
  final String name;

  /// Password for basic authentication (required if authType is 'basic')
  final String? password;

  /// Token for token-based authentication (required if authType is 'token')
  final String? token;

  /// URL of the external data source
  final String url;

  /// Username for basic authentication (required if authType is 'basic')
  final String? username;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (authType != null) 'authType': authType!.value,
        if (cache != null) 'cache': cache!,
        if (headers != null)
          'headers': [for (final item in headers!) item.toJson()],
        if (maxRetries != null) 'maxRetries': maxRetries!,
        'name': name,
        if (password != null) 'password': password!,
        if (token != null) 'token': token!,
        'url': url,
        if (username != null) 'username': username!,
      };
}

/// The request body of `CreateExternalFeedRequest.headersItem`.
final class CreateExternalFeedRequestHeadersItem {
  /// Creates the request body.
  const CreateExternalFeedRequestHeadersItem({
    required this.name,
    required this.valueValue,
  });

  /// Decodes a JSON object into a CreateExternalFeedRequestHeadersItem. Optional fields absent from [json] stay null.
  factory CreateExternalFeedRequestHeadersItem.fromJson(
          Map<String, Object?> json) =>
      CreateExternalFeedRequestHeadersItem(
        name:
            json.requireString('name', 'CreateExternalFeedRequestHeadersItem'),
        valueValue:
            json.requireString('value', 'CreateExternalFeedRequestHeadersItem'),
      );

  /// Header name
  final String name;

  /// Header value
  final String valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'name': name,
        'value': valueValue,
      };
}

/// The request body of `PUT /feeds/{uuid}`.
final class UpdateExternalFeedRequest {
  /// Creates the request body.
  const UpdateExternalFeedRequest({
    this.authType,
    this.cache,
    this.headers,
    this.maxRetries,
    this.name,
    this.password,
    this.token,
    this.url,
    this.username,
  });

  /// Decodes a JSON object into a UpdateExternalFeedRequest. Optional fields absent from [json] stay null.
  factory UpdateExternalFeedRequest.fromJson(Map<String, Object?> json) =>
      UpdateExternalFeedRequest(
        authType: json.optEnum(
            'authType', UpdateExternalFeedRequestAuthType.fromWire),
        cache: json.optBool('cache'),
        headers: json.containsKey('headers')
            ? json.optObjectList(
                'headers', UpdateExternalFeedRequestHeadersItem.fromJson,
                objectName: 'UpdateExternalFeedRequest')
            : null,
        maxRetries: json.optInt('maxRetries'),
        name: json.optString('name'),
        password: json.optString('password'),
        token: json.optString('token'),
        url: json.optString('url'),
        username: json.optString('username'),
      );

  /// Authentication type for accessing the feed
  final UpdateExternalFeedRequestAuthType? authType;

  /// Whether to cache the feed response
  final bool? cache;

  /// Custom HTTP headers for the feed request
  final List<UpdateExternalFeedRequestHeadersItem>? headers;

  /// Maximum number of retry attempts for failed requests
  final int? maxRetries;

  /// Name of the feed
  final String? name;

  /// Password for basic authentication
  final String? password;

  /// Token for token-based authentication
  final String? token;

  /// URL of the external data source
  final String? url;

  /// Username for basic authentication
  final String? username;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (authType != null) 'authType': authType!.value,
        if (cache != null) 'cache': cache!,
        if (headers != null)
          'headers': [for (final item in headers!) item.toJson()],
        if (maxRetries != null) 'maxRetries': maxRetries!,
        if (name != null) 'name': name!,
        if (password != null) 'password': password!,
        if (token != null) 'token': token!,
        if (url != null) 'url': url!,
        if (username != null) 'username': username!,
      };
}

/// The request body of `UpdateExternalFeedRequest.headersItem`.
final class UpdateExternalFeedRequestHeadersItem {
  /// Creates the request body.
  const UpdateExternalFeedRequestHeadersItem({
    required this.name,
    required this.valueValue,
  });

  /// Decodes a JSON object into a UpdateExternalFeedRequestHeadersItem. Optional fields absent from [json] stay null.
  factory UpdateExternalFeedRequestHeadersItem.fromJson(
          Map<String, Object?> json) =>
      UpdateExternalFeedRequestHeadersItem(
        name:
            json.requireString('name', 'UpdateExternalFeedRequestHeadersItem'),
        valueValue:
            json.requireString('value', 'UpdateExternalFeedRequestHeadersItem'),
      );

  /// Header name
  final String name;

  /// Header value
  final String valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'name': name,
        'value': valueValue,
      };
}
