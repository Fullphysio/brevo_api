// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `POST /feeds` object of the Brevo API.
final class CreateExternalFeedResponse {
  /// Creates a CreateExternalFeedResponse from already-decoded fields.
  const CreateExternalFeedResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateExternalFeedResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateExternalFeedResponse.fromJson(Map<String, Object?> json) =>
      CreateExternalFeedResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// UUID of the created feed
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `GET /feeds` object of the Brevo API.
final class GetAllExternalFeedsResponse {
  /// Creates a GetAllExternalFeedsResponse from already-decoded fields.
  const GetAllExternalFeedsResponse({
    this.count,
    this.feeds = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAllExternalFeedsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAllExternalFeedsResponse.fromJson(Map<String, Object?> json) =>
      GetAllExternalFeedsResponse(
        count: json.optInt('count'),
        feeds: json.optObjectList(
            'feeds', GetAllExternalFeedsResponseFeedsItem.fromJson,
            objectName: 'GetAllExternalFeedsResponse'),
        raw: json,
      );

  /// Total number of feeds
  final int? count;

  /// List of external feeds
  final List<GetAllExternalFeedsResponseFeedsItem> feeds;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (feeds.isNotEmpty)
          'feeds': [for (final item in feeds) item.toJson()],
      };
}

/// The `GetAllExternalFeedsResponse.feedsItem` object of the Brevo API.
final class GetAllExternalFeedsResponseFeedsItem {
  /// Creates a GetAllExternalFeedsResponseFeedsItem from already-decoded fields.
  const GetAllExternalFeedsResponseFeedsItem({
    this.authType,
    this.cache,
    this.createdAt,
    this.headers = const [],
    this.id,
    this.maxRetries,
    this.modifiedAt,
    this.name,
    this.password,
    this.token,
    this.url,
    this.username,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAllExternalFeedsResponseFeedsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAllExternalFeedsResponseFeedsItem.fromJson(
          Map<String, Object?> json) =>
      GetAllExternalFeedsResponseFeedsItem(
        authType: json.optEnum(
            'authType', GetAllExternalFeedsResponseFeedsItemAuthType.fromWire),
        cache: json.optBool('cache'),
        createdAt: json.optString('createdAt'),
        headers: json.optObjectList(
            'headers', GetAllExternalFeedsResponseFeedsItemHeadersItem.fromJson,
            objectName: 'GetAllExternalFeedsResponseFeedsItem'),
        id: json.optString('id'),
        maxRetries: json.optInt('maxRetries'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        password: json.optString('password'),
        token: json.optString('token'),
        url: json.optString('url'),
        username: json.optString('username'),
        raw: json,
      );

  /// Authentication type for accessing the feed
  final GetAllExternalFeedsResponseFeedsItemAuthType? authType;

  /// Whether to cache the feed response
  final bool? cache;

  /// Feed creation timestamp
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Custom HTTP headers for the feed request
  final List<GetAllExternalFeedsResponseFeedsItemHeadersItem> headers;

  /// Unique identifier of the feed
  final String? id;

  /// Maximum number of retry attempts for failed requests
  final int? maxRetries;

  /// Feed last modification timestamp
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the feed
  final String? name;

  /// Password for basic authentication. Only returned when authType is 'basic'. Excluded when authType is 'token'.
  final String? password;

  /// Token for token-based authentication. Only returned when authType is 'token'. Excluded when authType is 'basic' or 'noAuth'.
  final String? token;

  /// URL of the external data source
  final String? url;

  /// Username for basic authentication. Only returned when authType is 'basic'. Excluded when authType is 'token'.
  final String? username;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authType != null) 'authType': authType!.value,
        if (cache != null) 'cache': cache!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (headers.isNotEmpty)
          'headers': [for (final item in headers) item.toJson()],
        if (id != null) 'id': id!,
        if (maxRetries != null) 'maxRetries': maxRetries!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (password != null) 'password': password!,
        if (token != null) 'token': token!,
        if (url != null) 'url': url!,
        if (username != null) 'username': username!,
      };
}

/// The `GetAllExternalFeedsResponseFeedsItem.headersItem` object of the Brevo API.
final class GetAllExternalFeedsResponseFeedsItemHeadersItem {
  /// Creates a GetAllExternalFeedsResponseFeedsItemHeadersItem from already-decoded fields.
  const GetAllExternalFeedsResponseFeedsItemHeadersItem({
    this.name,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAllExternalFeedsResponseFeedsItemHeadersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAllExternalFeedsResponseFeedsItemHeadersItem.fromJson(
          Map<String, Object?> json) =>
      GetAllExternalFeedsResponseFeedsItemHeadersItem(
        name: json.optString('name'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// Header name
  final String? name;

  /// Header value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (name != null) 'name': name!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The `GET /feeds/{uuid}` object of the Brevo API.
final class GetExternalFeedByUuidResponse {
  /// Creates a GetExternalFeedByUuidResponse from already-decoded fields.
  const GetExternalFeedByUuidResponse({
    this.authType,
    this.cache,
    this.createdAt,
    this.headers = const [],
    this.id,
    this.maxRetries,
    this.modifiedAt,
    this.name,
    this.password,
    this.token,
    this.url,
    this.username,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetExternalFeedByUuidResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetExternalFeedByUuidResponse.fromJson(Map<String, Object?> json) =>
      GetExternalFeedByUuidResponse(
        authType: json.optEnum(
            'authType', GetExternalFeedByUuidResponseAuthType.fromWire),
        cache: json.optBool('cache'),
        createdAt: json.optString('createdAt'),
        headers: json.optObjectList(
            'headers', GetExternalFeedByUuidResponseHeadersItem.fromJson,
            objectName: 'GetExternalFeedByUuidResponse'),
        id: json.optString('id'),
        maxRetries: json.optInt('maxRetries'),
        modifiedAt: json.optString('modifiedAt'),
        name: json.optString('name'),
        password: json.optString('password'),
        token: json.optString('token'),
        url: json.optString('url'),
        username: json.optString('username'),
        raw: json,
      );

  /// Authentication type for accessing the feed
  final GetExternalFeedByUuidResponseAuthType? authType;

  /// Whether to cache the feed response
  final bool? cache;

  /// Feed creation timestamp
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Custom HTTP headers for the feed request
  final List<GetExternalFeedByUuidResponseHeadersItem> headers;

  /// Unique identifier of the feed
  final String? id;

  /// Maximum number of retry attempts for failed requests
  final int? maxRetries;

  /// Feed last modification timestamp
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Name of the feed
  final String? name;

  /// Password for basic authentication. Only returned when authType is 'basic'. Excluded from response when authType is 'token'.
  final String? password;

  /// Token for token-based authentication. Only returned when authType is 'token'. Excluded from response when authType is 'basic' or 'noAuth'.
  final String? token;

  /// URL of the external data source
  final String? url;

  /// Username for basic authentication. Only returned when authType is 'basic'. Excluded from response when authType is 'token'.
  final String? username;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authType != null) 'authType': authType!.value,
        if (cache != null) 'cache': cache!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (headers.isNotEmpty)
          'headers': [for (final item in headers) item.toJson()],
        if (id != null) 'id': id!,
        if (maxRetries != null) 'maxRetries': maxRetries!,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (name != null) 'name': name!,
        if (password != null) 'password': password!,
        if (token != null) 'token': token!,
        if (url != null) 'url': url!,
        if (username != null) 'username': username!,
      };
}

/// The `GetExternalFeedByUuidResponse.headersItem` object of the Brevo API.
final class GetExternalFeedByUuidResponseHeadersItem {
  /// Creates a GetExternalFeedByUuidResponseHeadersItem from already-decoded fields.
  const GetExternalFeedByUuidResponseHeadersItem({
    this.name,
    this.valueValue,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetExternalFeedByUuidResponseHeadersItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetExternalFeedByUuidResponseHeadersItem.fromJson(
          Map<String, Object?> json) =>
      GetExternalFeedByUuidResponseHeadersItem(
        name: json.optString('name'),
        valueValue: json.optString('value'),
        raw: json,
      );

  /// Header name
  final String? name;

  /// Header value
  final String? valueValue;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (name != null) 'name': name!,
        if (valueValue != null) 'value': valueValue!,
      };
}
