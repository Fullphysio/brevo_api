// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /whatsapp/statistics/events` object of the Brevo API.
final class GetWhatsappEventReportResponse {
  /// Creates a GetWhatsappEventReportResponse from already-decoded fields.
  const GetWhatsappEventReportResponse({
    this.events = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsappEventReportResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsappEventReportResponse.fromJson(Map<String, Object?> json) =>
      GetWhatsappEventReportResponse(
        events: json.optObjectList(
            'events', GetWhatsappEventReportResponseEventsItem.fromJson,
            objectName: 'GetWhatsappEventReportResponse'),
        raw: json,
      );

  /// The `events` field.
  final List<GetWhatsappEventReportResponseEventsItem> events;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
      };
}

/// The `GetWhatsappEventReportResponse.eventsItem` object of the Brevo API.
final class GetWhatsappEventReportResponseEventsItem {
  /// Creates a GetWhatsappEventReportResponseEventsItem from already-decoded fields.
  const GetWhatsappEventReportResponseEventsItem({
    this.body,
    this.contactNumber,
    this.date,
    this.event,
    this.mediaUrl,
    this.messageId,
    this.reason,
    this.senderNumber,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetWhatsappEventReportResponseEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetWhatsappEventReportResponseEventsItem.fromJson(
          Map<String, Object?> json) =>
      GetWhatsappEventReportResponseEventsItem(
        body: json.optString('body'),
        contactNumber: json.optString('contactNumber'),
        date: json.optString('date'),
        event: json.optEnum(
            'event', GetWhatsappEventReportResponseEventsItemEvent.fromWire),
        mediaUrl: json.optString('mediaUrl'),
        messageId: json.optString('messageId'),
        reason: json.optString('reason'),
        senderNumber: json.optString('senderNumber'),
        raw: json,
      );

  /// Text of the reply (will be there only in case of `reply` event with text)
  final String? body;

  /// WhatsApp Number with country code. Example, 85264318721
  final String? contactNumber;

  /// UTC date-time on which the event has been generated
  final String? date;

  /// Event which occurred
  final GetWhatsappEventReportResponseEventsItemEvent? event;

  /// Url of the media reply (will be there only in case of `reply` event with media)
  final String? mediaUrl;

  /// Message ID which generated the event
  final String? messageId;

  /// Reason for the event (will be there in case of `error` and `soft-bounce` events)
  final String? reason;

  /// WhatsApp Number with country code. Example, 85264318721
  final String? senderNumber;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (body != null) 'body': body!,
        if (contactNumber != null) 'contactNumber': contactNumber!,
        if (date != null) 'date': date!,
        if (event != null) 'event': event!.value,
        if (mediaUrl != null) 'mediaUrl': mediaUrl!,
        if (messageId != null) 'messageId': messageId!,
        if (reason != null) 'reason': reason!,
        if (senderNumber != null) 'senderNumber': senderNumber!,
      };
}

/// The `POST /whatsapp/sendMessage` object of the Brevo API.
final class SendWhatsappMessageResponse {
  /// Creates a SendWhatsappMessageResponse from already-decoded fields.
  const SendWhatsappMessageResponse({
    this.messageId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a SendWhatsappMessageResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory SendWhatsappMessageResponse.fromJson(Map<String, Object?> json) =>
      SendWhatsappMessageResponse(
        messageId: json.optString('messageId'),
        raw: json,
      );

  /// messageId of sent message
  final String? messageId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (messageId != null) 'messageId': messageId!,
      };
}
