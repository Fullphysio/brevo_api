// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// Note data to be saved
final class NoteData {
  /// Creates the request body.
  const NoteData({
    this.companyIds,
    this.contactIds,
    this.dealIds,
    required this.text,
  });

  /// Decodes a JSON object into a NoteData. Optional fields absent from [json] stay null.
  factory NoteData.fromJson(Map<String, Object?> json) => NoteData(
        companyIds: json.containsKey('companyIds')
            ? json.optStringList('companyIds')
            : null,
        contactIds: json.containsKey('contactIds')
            ? json.optIntList('contactIds')
            : null,
        dealIds:
            json.containsKey('dealIds') ? json.optStringList('dealIds') : null,
        text: json.requireString('text', 'NoteData'),
      );

  /// Company Ids linked to a note
  final List<String>? companyIds;

  /// Contact Ids linked to a note
  final List<int>? contactIds;

  /// Deal Ids linked to a note
  final List<String>? dealIds;

  /// Content of the note. Supports HTML for rich text formatting. Supported tags include: `&lt;p&gt;` (paragraph), `&lt;b&gt;` / `&lt;strong&gt;` (bold), `&lt;i&gt;` / `&lt;em&gt;` (italic), `&lt;u&gt;` (underline), `&lt;br&gt;` (line break), `&lt;a href="..."&gt;` (labelled hyperlink). Example labelled link: `&lt;a href="https://example.com"&gt;Link text&lt;/a&gt;`.
  final String text;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (companyIds != null) 'companyIds': companyIds!,
        if (contactIds != null) 'contactIds': contactIds!,
        if (dealIds != null) 'dealIds': dealIds!,
        'text': text,
      };
}
