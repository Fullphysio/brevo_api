// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// Note Details
final class Note {
  /// Creates a Note from already-decoded fields.
  const Note({
    this.authorId = const {},
    this.companyIds = const [],
    this.contactIds = const [],
    this.createdAt,
    this.dealIds = const [],
    this.id,
    this.text,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Note. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Note.fromJson(Map<String, Object?> json) => Note(
        authorId: json.optObjectMap('authorId'),
        companyIds: json.optStringList('companyIds'),
        contactIds: json.optIntList('contactIds'),
        createdAt: json.optString('createdAt'),
        dealIds: json.optStringList('dealIds'),
        id: json.optString('id'),
        text: json.optString('text'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Account details of user which created the note
  final Map<String, Object?> authorId;

  /// Company ids linked to a note
  final List<String> companyIds;

  /// Contact ids linked to a note
  final List<int> contactIds;

  /// Note created date/time
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Deal ids linked to a note
  final List<String> dealIds;

  /// Unique note Id
  final String? id;

  /// Content of the note. Supports HTML for rich text formatting. Supported tags include: `&lt;p&gt;` (paragraph), `&lt;b&gt;` / `&lt;strong&gt;` (bold), `&lt;i&gt;` / `&lt;em&gt;` (italic), `&lt;u&gt;` (underline), `&lt;br&gt;` (line break), `&lt;a href="..."&gt;` (labelled hyperlink). Example labelled link: `&lt;a href="https://example.com"&gt;Link text&lt;/a&gt;`.
  final String? text;

  /// Note updated date/time
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (authorId.isNotEmpty) 'authorId': authorId,
        if (companyIds.isNotEmpty) 'companyIds': companyIds,
        if (contactIds.isNotEmpty) 'contactIds': contactIds,
        if (createdAt != null) 'createdAt': createdAt!,
        if (dealIds.isNotEmpty) 'dealIds': dealIds,
        if (id != null) 'id': id!,
        if (text != null) 'text': text!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// Created Note ID
final class PostCrmNotesResponse {
  /// Creates a PostCrmNotesResponse from already-decoded fields.
  const PostCrmNotesResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostCrmNotesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostCrmNotesResponse.fromJson(Map<String, Object?> json) =>
      PostCrmNotesResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// Unique note Id
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}
