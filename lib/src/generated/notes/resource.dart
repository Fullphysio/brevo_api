// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import 'enums.dart';
import 'models.dart';
import 'requests.dart';

/// The `notes` resource of the Brevo API: `client.notes`.
final class BrevoNotes {
  /// Creates the resource backed by [_client].
  const BrevoNotes(this._client);

  final BrevoClient _client;

  /// `POST /crm/notes`
  ///
  /// Create a note
  ///
  /// Create a new CRM note and optionally associate it with contacts, companies, or deals. The note text content is required, and you can link the note to multiple entities simultaneously during creation.
  Future<PostCrmNotesResponse> createANote(NoteData request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'POST',
        path: '/crm/notes',
        body: request.toJson(),
        options: options);
    return PostCrmNotesResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'PostCrmNotesResponse'));
  }

  /// `DELETE /crm/notes/{id}`
  ///
  /// Delete a note
  ///
  /// Permanently delete a CRM note by its identifier. This removes the note and unlinks it from any associated contacts, companies, or deals.
  ///
  /// \[id\]: Note ID to delete
  Future<void> deleteANote(
      {required String id, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/crm/notes/${brevoPathSegment(id)}',
        options: options);
  }

  /// `GET /crm/notes/{id}`
  ///
  /// Get a note
  ///
  /// Retrieve the full details of a single CRM note by its identifier. The response includes the note''s text content, creation date, author, and any associated contacts, companies, or deals.
  ///
  /// \[id\]: Note ID to get
  Future<Note> getANote(
      {required String id, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/crm/notes/${brevoPathSegment(id)}',
        options: options);
    return Note.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'Note'));
  }

  /// `GET /crm/notes`
  ///
  /// Get all notes
  ///
  /// Retrieve a paginated list of CRM notes with optional filtering by entity type, entity IDs, and date range. Results are sorted by creation date in descending order by default, with a default limit of 50 notes per page.
  ///
  /// \[entity\]: Filter by note entity type
  ///
  /// \[entityIds\]: Filter by note entity IDs
  ///
  /// \[dateFrom\]: dateFrom to date range filter type (timestamp in milliseconds)
  ///
  /// \[dateTo\]: dateTo to date range filter type (timestamp in milliseconds)
  ///
  /// \[offset\]: Index of the first document of the page
  ///
  /// \[limit\]: Number of documents per page
  ///
  /// \[sort\]: Sort the results in the ascending/descending order. Default order is **descending** by creation if `sort` is not passed
  Future<List<Note>> getAllNotes(
      {GetCrmNotesRequestEntity? entity,
      String? entityIds,
      int? dateFrom,
      int? dateTo,
      int? offset,
      int? limit,
      GetCrmNotesRequestSort? sort,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/crm/notes',
        query: <String, Object?>{
          'entity': entity?.value,
          'entityIds': entityIds,
          'dateFrom': dateFrom,
          'dateTo': dateTo,
          'offset': offset,
          'limit': limit,
          'sort': sort?.value
        },
        options: options);
    final items = brevoRequireJsonList(json ?? const <Object?>[],
        objectName: 'getAllNotes');
    return [
      for (final item in items)
        Note.fromJson(brevoRequireJsonObject(item, objectName: 'Note'))
    ];
  }

  /// `PATCH /crm/notes/{id}`
  ///
  /// Update a note
  ///
  /// Update an existing CRM note''s text content and its associations with contacts, companies, or deals. You can modify the note text, change the pinned status, or update the linked entities.
  ///
  /// \[id\]: Note ID to update
  Future<void> updateANote(NoteData request,
      {required String id, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'PATCH',
        path: '/crm/notes/${brevoPathSegment(id)}',
        body: request.toJson(),
        options: options);
  }
}
