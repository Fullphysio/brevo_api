// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The request body of `POST /loyalty/offer/programs/{pid}/offers`.
final class CreateRewardRequest {
  /// Creates the request body.
  const CreateRewardRequest({
    required this.name,
    this.publicDescription,
    this.publicImage,
    this.publicName,
  });

  /// Decodes a JSON object into a CreateRewardRequest. Optional fields absent from [json] stay null.
  factory CreateRewardRequest.fromJson(Map<String, Object?> json) =>
      CreateRewardRequest(
        name: json.requireString('name', 'CreateRewardRequest'),
        publicDescription: json.optString('publicDescription'),
        publicImage: json.optString('publicImage'),
        publicName: json.optString('publicName'),
      );

  /// Internal name of the reward
  final String name;

  /// Public facing description of the reward
  final String? publicDescription;

  /// URL of the public image for the reward
  final String? publicImage;

  /// Public facing name of the reward
  final String? publicName;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'name': name,
        if (publicDescription != null) 'publicDescription': publicDescription!,
        if (publicImage != null) 'publicImage': publicImage!,
        if (publicName != null) 'publicName': publicName!,
      };
}

/// The request body of `POST /loyalty/offer/programs/{pid}/rewards/attribute`.
final class CreateVoucherRequest {
  /// Creates the request body.
  const CreateVoucherRequest({
    this.code,
    this.contactId,
    this.expirationDate,
    this.loyaltySubscriptionId,
    this.meta,
    required this.rewardId,
    this.validFrom,
    this.valueValue,
  });

  /// Decodes a JSON object into a CreateVoucherRequest. Optional fields absent from [json] stay null.
  factory CreateVoucherRequest.fromJson(Map<String, Object?> json) =>
      CreateVoucherRequest(
        code: json.optString('code'),
        contactId: json.optInt('contactId'),
        expirationDate: json.optString('expirationDate'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
        rewardId: json.requireString('rewardId', 'CreateVoucherRequest'),
        validFrom: json.optString('validFrom'),
        valueValue: json.optDouble('value'),
      );

  /// Code generated to attribute reward to a contact
  final String? code;

  /// Contact to attribute the reward
  final int? contactId;

  /// Reward expiration date
  final String? expirationDate;

  /// [expirationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get expirationDateDate =>
      expirationDate == null ? null : DateTime.tryParse(expirationDate!);

  /// One of contactId or loyaltySubscriptionId is required
  final String? loyaltySubscriptionId;

  /// Offer meta information (key/value object)
  final Map<String, Object?>? meta;

  /// Reward id
  final String rewardId;

  /// Date from which the voucher becomes valid. Accepts RFC 3339 or DD/MM/YYYY HH:MM AM/PM format. Converted to UTC using the organization's timezone.
  final String? validFrom;

  /// Value of the selected reward config
  final double? valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (code != null) 'code': code!,
        if (contactId != null) 'contactId': contactId!,
        if (expirationDate != null) 'expirationDate': expirationDate!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (meta != null) 'meta': meta!,
        'rewardId': rewardId,
        if (validFrom != null) 'validFrom': validFrom!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The request body of `POST /loyalty/offer/programs/{pid}/rewards/redeem`.
final class RedeemVoucherRequest {
  /// Creates the request body.
  const RedeemVoucherRequest({
    this.attributedRewardId,
    this.autoComplete,
    this.code,
    this.contactId,
    this.loyaltySubscriptionId,
    this.meta,
    this.order,
    this.rewardId,
    this.ttl,
  });

  /// Decodes a JSON object into a RedeemVoucherRequest. Optional fields absent from [json] stay null.
  factory RedeemVoucherRequest.fromJson(Map<String, Object?> json) =>
      RedeemVoucherRequest(
        attributedRewardId: json.optString('attributedRewardId'),
        autoComplete: json.optBool('autoComplete'),
        code: json.optString('code'),
        contactId: json.optInt('contactId'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        meta: json.containsKey('meta') ? json.optObjectMap('meta') : null,
        order: json.optNested('order', RedeemVoucherRequestOrder.fromJson),
        rewardId: json.optString('rewardId'),
        ttl: json.optInt('ttl'),
      );

  /// Unique identifier for the attributed reward
  final String? attributedRewardId;

  /// Whether the redemption should be automatically completed
  final bool? autoComplete;

  /// Redemption code for the reward
  final String? code;

  /// Unique identifier for the contact
  final int? contactId;

  /// Identifier for the loyalty subscription
  final String? loyaltySubscriptionId;

  /// Additional metadata associated with the redeem request
  final Map<String, Object?>? meta;

  /// Order details for the redemption
  final RedeemVoucherRequestOrder? order;

  /// Unique identifier for the reward
  final String? rewardId;

  /// Time to live in seconds for the redemption request
  final int? ttl;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributedRewardId != null)
          'attributedRewardId': attributedRewardId!,
        if (autoComplete != null) 'autoComplete': autoComplete!,
        if (code != null) 'code': code!,
        if (contactId != null) 'contactId': contactId!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (meta != null) 'meta': meta!,
        if (order != null) 'order': order!.toJson(),
        if (rewardId != null) 'rewardId': rewardId!,
        if (ttl != null) 'ttl': ttl!,
      };
}

/// The request body of `RedeemVoucherRequest.order`.
final class RedeemVoucherRequestOrder {
  /// Creates the request body.
  const RedeemVoucherRequestOrder({
    this.amount,
    this.billing,
    this.contactId,
    this.coupons,
    this.createdAt,
    this.email,
    this.id,
    this.identifiers,
    this.products,
    this.status,
    this.storeId,
    this.updatedAt,
  });

  /// Decodes a JSON object into a RedeemVoucherRequestOrder. Optional fields absent from [json] stay null.
  factory RedeemVoucherRequestOrder.fromJson(Map<String, Object?> json) =>
      RedeemVoucherRequestOrder(
        amount: json.optDouble('amount'),
        billing: json.optNested(
            'billing', RedeemVoucherRequestOrderBilling.fromJson),
        contactId: json.optInt('contact_id'),
        coupons:
            json.containsKey('coupons') ? json.optStringList('coupons') : null,
        createdAt: json.optString('createdAt'),
        email: json.optString('email'),
        id: json.optString('id'),
        identifiers: json.optNested(
            'identifiers', RedeemVoucherRequestOrderIdentifiers.fromJson),
        products: json.containsKey('products')
            ? json.optObjectList(
                'products', RedeemVoucherRequestOrderProductsItem.fromJson,
                objectName: 'RedeemVoucherRequestOrder')
            : null,
        status: json.optString('status'),
        storeId: json.optString('storeId'),
        updatedAt: json.optString('updatedAt'),
      );

  /// Total amount of the order
  final double? amount;

  /// Billing information for the order
  final RedeemVoucherRequestOrderBilling? billing;

  /// Unique identifier for the contact
  final int? contactId;

  /// List of coupon codes applied to the order
  final List<String>? coupons;

  /// Timestamp when the order was created
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Email address associated with the order
  final String? email;

  /// Unique identifier for the order
  final String? id;

  /// Additional identifiers for the order
  final RedeemVoucherRequestOrderIdentifiers? identifiers;

  /// List of products in the order
  final List<RedeemVoucherRequestOrderProductsItem>? products;

  /// Current status of the order
  final String? status;

  /// Identifier for the store where the order was placed
  final String? storeId;

  /// Timestamp when the order was last updated
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (amount != null) 'amount': amount!,
        if (billing != null) 'billing': billing!.toJson(),
        if (contactId != null) 'contact_id': contactId!,
        if (coupons != null) 'coupons': coupons!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
        if (products != null)
          'products': [for (final item in products!) item.toJson()],
        if (status != null) 'status': status!,
        if (storeId != null) 'storeId': storeId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The request body of `RedeemVoucherRequestOrder.billing`.
final class RedeemVoucherRequestOrderBilling {
  /// Creates the request body.
  const RedeemVoucherRequestOrderBilling({
    this.address,
    this.city,
    this.countryCode,
    this.paymentMethod,
    this.phone,
    this.postCode,
    this.region,
  });

  /// Decodes a JSON object into a RedeemVoucherRequestOrderBilling. Optional fields absent from [json] stay null.
  factory RedeemVoucherRequestOrderBilling.fromJson(
          Map<String, Object?> json) =>
      RedeemVoucherRequestOrderBilling(
        address: json.optString('address'),
        city: json.optString('city'),
        countryCode: json.optString('countryCode'),
        paymentMethod: json.optString('paymentMethod'),
        phone: json.optInt('phone'),
        postCode: json.optInt('postCode'),
        region: json.optString('region'),
      );

  /// Address of the user
  final String? address;

  /// City of the user
  final String? city;

  /// Country code of the user
  final String? countryCode;

  /// Payment method opted by the user
  final String? paymentMethod;

  /// Phone number of the user
  final int? phone;

  /// Postal Code of user's location
  final int? postCode;

  /// Region where user resides
  final String? region;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (address != null) 'address': address!,
        if (city != null) 'city': city!,
        if (countryCode != null) 'countryCode': countryCode!,
        if (paymentMethod != null) 'paymentMethod': paymentMethod!,
        if (phone != null) 'phone': phone!,
        if (postCode != null) 'postCode': postCode!,
        if (region != null) 'region': region!,
      };
}

/// The request body of `RedeemVoucherRequestOrder.identifiers`.
final class RedeemVoucherRequestOrderIdentifiers {
  /// Creates the request body.
  const RedeemVoucherRequestOrderIdentifiers({
    this.extId,
    this.loyaltySubscriptionId,
  });

  /// Decodes a JSON object into a RedeemVoucherRequestOrderIdentifiers. Optional fields absent from [json] stay null.
  factory RedeemVoucherRequestOrderIdentifiers.fromJson(
          Map<String, Object?> json) =>
      RedeemVoucherRequestOrderIdentifiers(
        extId: json.optString('ext_id'),
        loyaltySubscriptionId: json.optString('loyalty_subscription_id'),
      );

  /// External identifier for the order
  final String? extId;

  /// Identifier for the loyalty subscription
  final String? loyaltySubscriptionId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (loyaltySubscriptionId != null)
          'loyalty_subscription_id': loyaltySubscriptionId!,
      };
}

/// The request body of `RedeemVoucherRequestOrder.productsItem`.
final class RedeemVoucherRequestOrderProductsItem {
  /// Creates the request body.
  const RedeemVoucherRequestOrderProductsItem({
    this.category,
    this.price,
    this.productId,
    this.quantity,
    this.variantId,
  });

  /// Decodes a JSON object into a RedeemVoucherRequestOrderProductsItem. Optional fields absent from [json] stay null.
  factory RedeemVoucherRequestOrderProductsItem.fromJson(
          Map<String, Object?> json) =>
      RedeemVoucherRequestOrderProductsItem(
        category: json.containsKey('category')
            ? json.optStringList('category')
            : null,
        price: json.optDouble('price'),
        productId: json.optString('productId'),
        quantity: json.optDouble('quantity'),
        variantId: json.optString('variantId'),
      );

  /// List of categories the product belongs to
  final List<String>? category;

  /// Price of the product
  final double? price;

  /// Unique identifier for the product
  final String? productId;

  /// Quantity of the product
  final double? quantity;

  /// Identifier for the product variant
  final String? variantId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (category != null) 'category': category!,
        if (price != null) 'price': price!,
        if (productId != null) 'productId': productId!,
        if (quantity != null) 'quantity': quantity!,
        if (variantId != null) 'variantId': variantId!,
      };
}

/// The request body of `POST /loyalty/offer/programs/{pid}/rewards/validate`.
final class ValidateRewardRequest {
  /// Creates the request body.
  const ValidateRewardRequest({
    this.attributedRewardId,
    this.code,
    this.contactId,
    this.loyaltySubscriptionId,
    this.pointOfSellId,
    this.rewardId,
  });

  /// Decodes a JSON object into a ValidateRewardRequest. Optional fields absent from [json] stay null.
  factory ValidateRewardRequest.fromJson(Map<String, Object?> json) =>
      ValidateRewardRequest(
        attributedRewardId: json.optString('attributedRewardId'),
        code: json.optString('code'),
        contactId: json.optInt('contactId'),
        loyaltySubscriptionId: json.optString('loyaltySubscriptionId'),
        pointOfSellId: json.optString('pointOfSellId'),
        rewardId: json.optString('rewardId'),
      );

  /// Unique identifier for the attributed reward
  final String? attributedRewardId;

  /// Validation code for the reward
  final String? code;

  /// Unique identifier for the contact
  final int? contactId;

  /// Identifier for the loyalty subscription
  final String? loyaltySubscriptionId;

  /// Identifier for the point of sale
  final String? pointOfSellId;

  /// Unique identifier for the reward
  final String? rewardId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributedRewardId != null)
          'attributedRewardId': attributedRewardId!,
        if (code != null) 'code': code!,
        if (contactId != null) 'contactId': contactId!,
        if (loyaltySubscriptionId != null)
          'loyaltySubscriptionId': loyaltySubscriptionId!,
        if (pointOfSellId != null) 'pointOfSellId': pointOfSellId!,
        if (rewardId != null) 'rewardId': rewardId!,
      };
}
