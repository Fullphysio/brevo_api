// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/offset_page.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import '../shared/requests.dart';
import 'enums.dart';
import 'models.dart';
import 'requests.dart';

/// The `emailCampaigns` resource of the Brevo API: `client.emailCampaigns`.
final class BrevoEmailCampaigns {
  /// Creates the resource backed by [_client].
  const BrevoEmailCampaigns(this._client);

  final BrevoClient _client;

  /// `POST /emailCampaigns`
  ///
  /// Create an email campaign
  ///
  /// Create a new email campaign. The campaign requires at minimum a name and sender details, and is created in draft status by default. You must provide email content via one of three mutually exclusive options: htmlContent (inline HTML), htmlUrl (remote URL), or templateId (existing template); additionally, A/B testing can be enabled by setting abTesting to true with subjectA and subjectB, but this is incompatible with sendAtBestTime.
  Future<void> createEmailCampaign(CreateEmailCampaignRequest request,
      {BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/emailCampaigns',
        body: request.toJson(),
        options: options);
  }

  /// `DELETE /emailCampaigns/{campaignId}`
  ///
  /// Delete an email campaign
  ///
  /// Delete an email campaign by its campaign ID. Only campaigns that have not been scheduled can be deleted; attempting to delete a campaign that has already been scheduled will return a 403 permission denied error. Related data in templates, newsletter builder, and schedule collections is also cleaned up.
  ///
  /// \[campaignId\]: id of the campaign
  Future<void> deleteEmailCampaign(
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}',
        options: options);
  }

  /// `POST /emailCampaigns/{campaignId}/exportRecipients`
  ///
  /// Export the recipients of an email campaign
  ///
  /// Export the recipients of a sent email campaign as an asynchronous process, filtered by recipient type (e.g. openers, clickers, hardBounces). The recipientsType field is required and determines which subset of recipients to export. An optional notifyURL webhook will be called once the export is complete, and the response returns a processId to track the export status.
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> emailExportRecipients(EmailExportRecipientsRequest request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path:
            '/emailCampaigns/${brevoPathSegment(campaignId)}/exportRecipients',
        body: request.toJson(),
        options: options);
  }

  /// `GET /emailCampaigns/{campaignId}/abTestCampaignResult`
  ///
  /// Get an A/B test email campaign results
  ///
  /// Obtain winning version of an A/B test email campaign
  ///
  /// \[campaignId\]: Id of the A/B test campaign
  Future<GetAbTestCampaignResultResponse> getAbTestCampaignResult(
      {required int campaignId, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path:
            '/emailCampaigns/${brevoPathSegment(campaignId)}/abTestCampaignResult',
        options: options);
    return GetAbTestCampaignResultResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetAbTestCampaignResultResponse'));
  }

  /// `GET /emailCampaigns/{campaignId}`
  ///
  /// Get an email campaign report
  ///
  /// Retrieve detailed information about a specific email campaign by its ID, including recipients, statistics, and HTML content. Use the statistics query parameter to select which statistics to include (globalStats, linksStats, statsByDomain, statsByDevice, or statsByBrowser); statsByDevice and statsByBrowser are only available on this single-campaign endpoint. You can exclude HTML content from the response by setting excludeHtmlContent to true.
  ///
  /// \[campaignId\]: Id of the campaign
  ///
  /// \[statistics\]: Filter on the type of statistics required. Example: **globalStats** value will only fetch globalStats info of the campaign in the returned response. `statsByDevice` and `statsByBrowser` are only available when retrieving a single campaign (not in the list endpoint).
  ///
  /// \[excludeHtmlContent\]: Use this flag to exclude htmlContent from the response body. If set to **true**, htmlContent field will be returned as empty string in the response body
  Future<GetEmailCampaignResponse> getEmailCampaign(
      {required int campaignId,
      GetEmailCampaignRequestStatistics? statistics,
      bool? excludeHtmlContent,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}',
        query: <String, Object?>{
          'statistics': statistics?.value,
          'excludeHtmlContent': excludeHtmlContent
        },
        options: options);
    return GetEmailCampaignResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetEmailCampaignResponse'));
  }

  /// `GET /emailCampaigns`
  ///
  /// Return all your created email campaigns
  ///
  /// &lt;Note&gt;The response payload for this endpoint has changed
  /// You now need to specify which type of statistics you would like to retrieve. For more information visit \[this page\](https://developers.brevo.com/changelog/get-all-marketing-campaigns).&lt;/Note&gt;
  ///
  /// \[type\]: Filter on the type of the campaigns
  ///
  /// \[status\]: Filter on the status of the campaign
  ///
  /// \[statistics\]: Filter on the type of statistics required. Example: **globalStats** value will only fetch globalStats info of the campaign in the returned response. This option only returns data for events that occurred in the last 6 months. For older campaigns, it is advisable to use the **Get Campaign Report** endpoint.
  ///
  /// \[startDate\]: **Mandatory if endDate is used.** Starting (urlencoded) UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) to filter the sent email campaigns. **Prefer to pass your timezone in date-time format for accurate result.** Only available if `status` is not passed or is set to `sent`. The date range between `startDate` and `endDate` must not exceed 2 years. `startDate` must not be in the future.
  ///
  /// \[endDate\]: **Mandatory if startDate is used.** Ending (urlencoded) UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ) to filter the sent email campaigns. **Prefer to pass your timezone in date-time format for accurate result.** Only available if `status` is not passed or is set to `sent`. The date range between `startDate` and `endDate` must not exceed 2 years. `endDate` must not be in the future.
  ///
  /// \[limit\]: Number of documents per page
  ///
  /// \[offset\]: Index of the first document in the page
  ///
  /// \[sort\]: Sort the results in the ascending/descending order of record creation. Default order is **descending** if `sort` is not passed
  ///
  /// \[excludeHtmlContent\]: Use this flag to exclude htmlContent from the response body. If set to **true**, the htmlContent field will be returned as an empty string in the response body.
  ///
  /// \[excludePdfAttachment\]: Use this flag to filter out campaigns that have a PDF attachment. If set to **true**, only campaigns without a PDF attachment (or with no attachment at all) will be returned.
  Future<GetEmailCampaignsResponse> getEmailCampaigns(
      {GetEmailCampaignsRequestType? type,
      GetEmailCampaignsRequestStatus? status,
      GetEmailCampaignsRequestStatistics? statistics,
      String? startDate,
      String? endDate,
      int? limit,
      int? offset,
      GetEmailCampaignsRequestSort? sort,
      bool? excludeHtmlContent,
      bool? excludePdfAttachment,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/emailCampaigns',
        query: <String, Object?>{
          'type': type?.value,
          'status': status?.value,
          'statistics': statistics?.value,
          'startDate': startDate,
          'endDate': endDate,
          'limit': limit,
          'offset': offset,
          'sort': sort?.value,
          'excludeHtmlContent': excludeHtmlContent,
          'excludePdfAttachment': excludePdfAttachment
        },
        options: options);
    return GetEmailCampaignsResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetEmailCampaignsResponse'));
  }

  /// Calls \[getEmailCampaigns\] and wraps the result in a \[BrevoOffsetPage\], so the following pages can be fetched with `nextPage()` or every item walked with `autoPaging()`.
  Future<BrevoOffsetPage<GetEmailCampaignsResponseCampaignsItem>>
      getEmailCampaignsPaged(
          {int limit = 50,
          int offset = 0,
          GetEmailCampaignsRequestType? type,
          GetEmailCampaignsRequestStatus? status,
          GetEmailCampaignsRequestStatistics? statistics,
          String? startDate,
          String? endDate,
          GetEmailCampaignsRequestSort? sort,
          bool? excludeHtmlContent,
          bool? excludePdfAttachment,
          BrevoRequestOptions? options}) async {
    final page = await getEmailCampaigns(
        limit: limit,
        type: type,
        status: status,
        statistics: statistics,
        startDate: startDate,
        endDate: endDate,
        sort: sort,
        excludeHtmlContent: excludeHtmlContent,
        excludePdfAttachment: excludePdfAttachment,
        options: options,
        offset: offset);
    return BrevoOffsetPage<GetEmailCampaignsResponseCampaignsItem>(
      items: page.campaigns,
      limit: limit,
      offset: offset,
      count: page.count,
      fetchPage: (next) => getEmailCampaignsPaged(
          limit: limit,
          type: type,
          status: status,
          statistics: statistics,
          startDate: startDate,
          endDate: endDate,
          sort: sort,
          excludeHtmlContent: excludeHtmlContent,
          excludePdfAttachment: excludePdfAttachment,
          options: options,
          offset: next),
    );
  }

  /// `GET /emailCampaigns/{campaignId}/sharedUrl`
  ///
  /// Get a shared template url
  ///
  /// Get a unique URL to share & import an email template from one Brevo account to another.
  ///
  /// \[campaignId\]: Id of the campaign or template
  Future<GetSharedTemplateUrlResponse> getSharedTemplateUrl(
      {required int campaignId, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}/sharedUrl',
        options: options);
    return GetSharedTemplateUrlResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'GetSharedTemplateUrlResponse'));
  }

  /// `POST /emailCampaigns/{campaignId}/sendNow`
  ///
  /// Send an email campaign immediately, based on campaignId
  ///
  /// Send an existing email campaign immediately by scheduling it for the current time. The campaign must have valid recipients and content configured before sending. The system verifies your account''s send limit and credit balance before dispatching; if credits are insufficient, a 402 error is returned.
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> sendEmailCampaignNow(
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}/sendNow',
        options: options);
  }

  /// `POST /emailCampaigns/{campaignId}/sendReport`
  ///
  /// Send the report of a campaign
  ///
  /// A PDF will be sent to the specified email addresses
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> sendReport(SendReport request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}/sendReport',
        body: request.toJson(),
        options: options);
  }

  /// `POST /emailCampaigns/{campaignId}/sendTest`
  ///
  /// Send an email campaign to your test list
  ///
  /// Send a test version of an email campaign to specified email addresses or your entire test list. If the emailTo array is left empty, the test mail will be sent to all addresses in your test list. You can send a maximum of 50 test emails per day.
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> sendTestEmail(SendTestEmail request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}/sendTest',
        body: request.toJson(),
        options: options);
  }

  /// `PUT /emailCampaigns/{campaignId}/status`
  ///
  /// Update an email campaign status
  ///
  /// Update the status of an email campaign, such as suspending, archiving, or replicating it. Available status values include suspended, archive, darchive, sent, queued, replicate, replicateTemplate, cancel, and draft. Note that the replicateTemplate status is only available for template type campaigns.
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> updateCampaignStatus(UpdateCampaignStatus request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'PUT',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}/status',
        body: request.toJson(),
        options: options);
  }

  /// `PUT /emailCampaigns/{campaignId}`
  ///
  /// Update an email campaign
  ///
  /// Update an existing email campaign''s properties such as name, subject, content, sender, recipients, schedule, and A/B testing configuration. The campaign must exist and the request body must contain at least one valid field to update. Only draft or scheduled campaigns can be modified; if sendAtBestTime is enabled, IP warmup will be automatically disabled.
  ///
  /// \[campaignId\]: Id of the campaign
  Future<void> updateEmailCampaign(UpdateEmailCampaignRequest request,
      {required int campaignId, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'PUT',
        path: '/emailCampaigns/${brevoPathSegment(campaignId)}',
        body: request.toJson(),
        options: options);
  }

  /// `POST /emailCampaigns/images`
  ///
  /// Upload an image to your account's image gallery
  ///
  /// Upload an image to your account''s image gallery by providing an absolute URL to the image. The maximum allowed image size is 2MB and supported formats are jpeg, jpg, png, bmp, and gif; local file uploads are not supported.
  Future<UploadImageToGalleryResponse> uploadImageToGallery(
      UploadImageToGalleryRequest request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'POST',
        path: '/emailCampaigns/images',
        body: request.toJson(),
        options: options);
    return UploadImageToGalleryResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'UploadImageToGalleryResponse'));
  }
}
