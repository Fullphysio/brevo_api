// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /emailCampaigns`.
final class CreateEmailCampaignRequest {
  /// Creates the request body.
  const CreateEmailCampaignRequest({
    this.abTesting,
    this.attachmentUrl,
    this.emailExpirationDate,
    this.footer,
    this.header,
    this.htmlContent,
    this.htmlUrl,
    this.increaseRate,
    this.initialQuota,
    this.inlineImageActivation,
    this.ipWarmupEnable,
    this.mirrorActive,
    required this.name,
    this.params,
    this.previewText,
    this.recipients,
    this.replyTo,
    this.scheduledAt,
    this.sendAtBestTime,
    required this.sender,
    this.splitRule,
    this.subject,
    this.subjectA,
    this.subjectB,
    this.tag,
    this.templateId,
    this.toField,
    this.unsubscriptionPageId,
    this.updateFormId,
    this.utmCampaign,
    this.utmContent,
    this.utmTerm,
    this.winnerCriteria,
    this.winnerDelay,
  });

  /// Decodes a JSON object into a CreateEmailCampaignRequest. Optional fields absent from [json] stay null.
  factory CreateEmailCampaignRequest.fromJson(Map<String, Object?> json) =>
      CreateEmailCampaignRequest(
        abTesting: json.optBool('abTesting'),
        attachmentUrl: json.optString('attachmentUrl'),
        emailExpirationDate: json.optNested('emailExpirationDate',
            CreateEmailCampaignRequestEmailExpirationDate.fromJson),
        footer: json.optString('footer'),
        header: json.optString('header'),
        htmlContent: json.optString('htmlContent'),
        htmlUrl: json.optString('htmlUrl'),
        increaseRate: json.optInt('increaseRate'),
        initialQuota: json.optInt('initialQuota'),
        inlineImageActivation: json.optBool('inlineImageActivation'),
        ipWarmupEnable: json.optBool('ipWarmupEnable'),
        mirrorActive: json.optBool('mirrorActive'),
        name: json.requireString('name', 'CreateEmailCampaignRequest'),
        params: json.containsKey('params') ? json.optObjectMap('params') : null,
        previewText: json.optString('previewText'),
        recipients: json.optNested(
            'recipients', CreateEmailCampaignRequestRecipients.fromJson),
        replyTo: json.optString('replyTo'),
        scheduledAt: json.optString('scheduledAt'),
        sendAtBestTime: json.optBool('sendAtBestTime'),
        sender: json.optNested(
                'sender', CreateEmailCampaignRequestSender.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateEmailCampaignRequest', key: 'sender')),
        splitRule: json.optInt('splitRule'),
        subject: json.optString('subject'),
        subjectA: json.optString('subjectA'),
        subjectB: json.optString('subjectB'),
        tag: json.optString('tag'),
        templateId: json.optInt('templateId'),
        toField: json.optString('toField'),
        unsubscriptionPageId: json.optString('unsubscriptionPageId'),
        updateFormId: json.optString('updateFormId'),
        utmCampaign: json.optString('utmCampaign'),
        utmContent: json.optString('utmContent'),
        utmTerm: json.optString('utmTerm'),
        winnerCriteria: json.optEnum('winnerCriteria',
            CreateEmailCampaignRequestWinnerCriteria.fromWire),
        winnerDelay: json.optInt('winnerDelay'),
      );

  /// Status of A/B Test. abTesting = false means it is disabled & abTesting = true means it is enabled. **subjectA, subjectB, splitRule, winnerCriteria & winnerDelay** will be considered when abTesting is set to true. subjectA & subjectB are mandatory together & subject if passed is ignored. **Can be set to true only if sendAtBestTime is false**. You will be able to set up two subject lines for your campaign and send them to a random sample of your total recipients. Half of the test group will receive version A, and the other half will receive version B
  final bool? abTesting;

  /// Absolute url of the attachment (no local file). Extension allowed: #### xlsx, xls, ods, docx, docm, doc, csv, pdf, txt, gif, jpg, jpeg, png, tif, tiff, rtf, bmp, cgm, css, shtml, html, htm, zip, xml, ppt, pptx, tar, ez, ics, mobi, msg, pub and eps
  final String? attachmentUrl;

  /// To reduce your carbon footprint, set an expiration date for your email. If supported, it will be automatically deleted from the recipient’s inbox, saving storage space and energy. Learn more about setting an email expiration date. For reference , ``https://help.brevo.com/hc/en-us/articles/4413566705298-Create-an-email-campaign``
  final CreateEmailCampaignRequestEmailExpirationDate? emailExpirationDate;

  /// Footer of the email campaign
  final String? footer;

  /// Header of the email campaign
  final String? header;

  /// **Mandatory if htmlUrl and templateId are empty.** Body of the message (HTML). Must have more than 10 characters and be less than 1MB in size. Cannot be used together with `htmlUrl` or `templateId`.
  final String? htmlContent;

  /// **Mandatory if htmlContent and templateId are empty.** URL to the message (HTML). Cannot be used together with `htmlContent` or `templateId`. For example: **https://html.domain.com**
  final String? htmlUrl;

  /// **Mandatory if ipWarmupEnable is set to true**. Set a percentage increase rate for warming up your ip. We recommend you set the increase rate to 30% per day. If you want to send the same number of emails every day, set the daily increase value to 0%.
  final int? increaseRate;

  /// **Mandatory if ipWarmupEnable is set to true**. Set an initial quota greater than 1 for warming up your ip. We recommend you set a value of 3000.
  final int? initialQuota;

  /// Use true to embedded the images in your email. Final size of the email should be less than **4MB**. Campaigns with embedded images can _not be sent to more than 5000 contacts_
  final bool? inlineImageActivation;

  /// **Available for dedicated ip clients**. Set this to true if you wish to warm up your ip.
  final bool? ipWarmupEnable;

  /// Use true to enable the mirror link
  final bool? mirrorActive;

  /// Name of the campaign
  final String name;

  /// Pass the set of attributes to customize the type classic campaign. For example: **{"FNAME":"Joe", "LNAME":"Doe"}**. Only available if **type** is **classic**. It's considered only if campaign is in _New Template Language format_. The New Template Language is dependent on the values of **subject, htmlContent/htmlUrl, sender.name & toField**
  final Map<String, Object?>? params;

  /// Preview text or preheader of the email campaign
  final String? previewText;

  /// Segment ids and List ids to include/exclude from campaign
  final CreateEmailCampaignRequestRecipients? recipients;

  /// Email on which the campaign recipients will be able to reply to
  final String? replyTo;

  /// Sending UTC date-time (YYYY-MM-DDTHH:mm:ss.SSSZ). **Prefer to pass your timezone in date-time format for accurate result**. If sendAtBestTime is set to true, your campaign will be sent according to the date passed (ignoring the time part). For example: **2017-06-01T12:30:00+02:00**
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Set this to true if you want to send your campaign at best time.
  final bool? sendAtBestTime;

  /// Sender details including id or email and name (optional). Only one of either Sender’s email or Sender’s ID shall be passed in one request at a time. Passing both `email` and `id` will result in an error. For example: **{"name":"xyz", "email":"example@abc.com"}** or **{"name":"xyz", "id":123}**
  final CreateEmailCampaignRequestSender sender;

  /// Add the size of your test groups. **Mandatory if abTesting = true & 'recipients' is passed**. We'll send version A and B to a random sample of recipients, and then the winning version to everyone else
  final int? splitRule;

  /// Subject of the campaign. **Mandatory if abTesting is false**. Ignored if abTesting is true.
  final String? subject;

  /// Subject A of the campaign. **Mandatory if abTesting = true**. subjectA & subjectB should have unique value
  final String? subjectA;

  /// Subject B of the campaign. **Mandatory if abTesting = true**. subjectA & subjectB should have unique value
  final String? subjectB;

  /// Tag of the campaign
  final String? tag;

  /// **Mandatory if htmlContent and htmlUrl are empty.** Id of the transactional email template with status _active_. Used to copy only its content fetched from htmlContent/htmlUrl to an email campaign for RSS feature. Cannot be used together with `htmlContent` or `htmlUrl`.
  final int? templateId;

  /// To personalize the **To** Field. If you want to include the first name and last name of your recipient, add **{FNAME} {LNAME}**. These contact attributes must already exist in your Brevo account. If input parameter **params** used please use **{{contact.FNAME}} {{contact.LNAME}}** for personalization
  final String? toField;

  /// Enter an unsubscription page id. The page id is a 24 digit alphanumeric id that can be found in the URL when editing the page. If not entered, then the default unsubscription page will be used.
  final String? unsubscriptionPageId;

  /// **Mandatory if templateId is used containing the {{ update_profile }} tag**. Enter an update profile form id. The form id is a 24 digit alphanumeric id that can be found in the URL when editing the form. If not entered, then the default update profile form will be used.
  final String? updateFormId;

  /// Customize the utm_campaign value. If this field is empty, the campaign name will be used. Only alphanumeric characters and spaces are allowed
  final String? utmCampaign;

  /// Customize the utm_content value. Appears on outgoing tracking links alongside utm_campaign. Only alphanumeric characters and spaces are allowed
  final String? utmContent;

  /// Customize the utm_term value. Appears on outgoing tracking links alongside utm_campaign. Only alphanumeric characters and spaces are allowed
  final String? utmTerm;

  /// Choose the metrics that will determinate the winning version. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerCriteria` is ignored if passed
  final CreateEmailCampaignRequestWinnerCriteria? winnerCriteria;

  /// Choose the duration of the test in hours. Maximum is 7 days, pass 24*7 = 168 hours. The winning version will be sent at the end of the test. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerDelay` is ignored if passed
  final int? winnerDelay;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (abTesting != null) 'abTesting': abTesting!,
        if (attachmentUrl != null) 'attachmentUrl': attachmentUrl!,
        if (emailExpirationDate != null)
          'emailExpirationDate': emailExpirationDate!.toJson(),
        if (footer != null) 'footer': footer!,
        if (header != null) 'header': header!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (htmlUrl != null) 'htmlUrl': htmlUrl!,
        if (increaseRate != null) 'increaseRate': increaseRate!,
        if (initialQuota != null) 'initialQuota': initialQuota!,
        if (inlineImageActivation != null)
          'inlineImageActivation': inlineImageActivation!,
        if (ipWarmupEnable != null) 'ipWarmupEnable': ipWarmupEnable!,
        if (mirrorActive != null) 'mirrorActive': mirrorActive!,
        'name': name,
        if (params != null) 'params': params!,
        if (previewText != null) 'previewText': previewText!,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (replyTo != null) 'replyTo': replyTo!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sendAtBestTime != null) 'sendAtBestTime': sendAtBestTime!,
        'sender': sender.toJson(),
        if (splitRule != null) 'splitRule': splitRule!,
        if (subject != null) 'subject': subject!,
        if (subjectA != null) 'subjectA': subjectA!,
        if (subjectB != null) 'subjectB': subjectB!,
        if (tag != null) 'tag': tag!,
        if (templateId != null) 'templateId': templateId!,
        if (toField != null) 'toField': toField!,
        if (unsubscriptionPageId != null)
          'unsubscriptionPageId': unsubscriptionPageId!,
        if (updateFormId != null) 'updateFormId': updateFormId!,
        if (utmCampaign != null) 'utmCampaign': utmCampaign!,
        if (utmContent != null) 'utmContent': utmContent!,
        if (utmTerm != null) 'utmTerm': utmTerm!,
        if (winnerCriteria != null) 'winnerCriteria': winnerCriteria!.value,
        if (winnerDelay != null) 'winnerDelay': winnerDelay!,
      };
}

/// To reduce your carbon footprint, set an expiration date for your email. If supported, it will be automatically deleted from the recipient’s inbox, saving storage space and energy. Learn more about setting an email expiration date. For reference , ``https://help.brevo.com/hc/en-us/articles/4413566705298-Create-an-email-campaign``
final class CreateEmailCampaignRequestEmailExpirationDate {
  /// Creates the request body.
  const CreateEmailCampaignRequestEmailExpirationDate({
    this.duration,
    this.unit,
  });

  /// Decodes a JSON object into a CreateEmailCampaignRequestEmailExpirationDate. Optional fields absent from [json] stay null.
  factory CreateEmailCampaignRequestEmailExpirationDate.fromJson(
          Map<String, Object?> json) =>
      CreateEmailCampaignRequestEmailExpirationDate(
        duration: json.optInt('duration'),
        unit: json.optEnum(
            'unit', CreateEmailCampaignRequestEmailExpirationDateUnit.fromWire),
      );

  /// Duration of the email expiry. maximum duration can be 3600 days or 480 weeks or 120 months.
  final int? duration;

  /// unit of the duration
  final CreateEmailCampaignRequestEmailExpirationDateUnit? unit;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (duration != null) 'duration': duration!,
        if (unit != null) 'unit': unit!.value,
      };
}

/// Segment ids and List ids to include/exclude from campaign
final class CreateEmailCampaignRequestRecipients {
  /// Creates the request body.
  const CreateEmailCampaignRequestRecipients({
    this.exclusionListIds,
    this.exclusionSegmentIds,
    this.listIds,
    this.segmentIds,
  });

  /// Decodes a JSON object into a CreateEmailCampaignRequestRecipients. Optional fields absent from [json] stay null.
  factory CreateEmailCampaignRequestRecipients.fromJson(
          Map<String, Object?> json) =>
      CreateEmailCampaignRequestRecipients(
        exclusionListIds: json.containsKey('exclusionListIds')
            ? json.optIntList('exclusionListIds')
            : null,
        exclusionSegmentIds: json.containsKey('exclusionSegmentIds')
            ? json.optIntList('exclusionSegmentIds')
            : null,
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        segmentIds: json.containsKey('segmentIds')
            ? json.optIntList('segmentIds')
            : null,
      );

  /// List ids to exclude from the campaign
  final List<int>? exclusionListIds;

  /// Segment ids which have to be excluded from a campaign.
  final List<int>? exclusionSegmentIds;

  /// **Mandatory if scheduledAt is not empty**. List Ids to send the campaign to
  final List<int>? listIds;

  /// **Mandatory if listIds are not used**. Segment ids to send the campaign to.
  final List<int>? segmentIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (exclusionListIds != null) 'exclusionListIds': exclusionListIds!,
        if (exclusionSegmentIds != null)
          'exclusionSegmentIds': exclusionSegmentIds!,
        if (listIds != null) 'listIds': listIds!,
        if (segmentIds != null) 'segmentIds': segmentIds!,
      };
}

/// Sender details including id or email and name (optional). Only one of either Sender’s email or Sender’s ID shall be passed in one request at a time. Passing both `email` and `id` will result in an error. For example: **{"name":"xyz", "email":"example@abc.com"}** or **{"name":"xyz", "id":123}**
final class CreateEmailCampaignRequestSender {
  /// Creates the request body.
  const CreateEmailCampaignRequestSender({
    this.email,
    this.id,
    this.name,
  });

  /// Decodes a JSON object into a CreateEmailCampaignRequestSender. Optional fields absent from [json] stay null.
  factory CreateEmailCampaignRequestSender.fromJson(
          Map<String, Object?> json) =>
      CreateEmailCampaignRequestSender(
        email: json.optString('email'),
        id: json.optInt('id'),
        name: json.optString('name'),
      );

  /// Sender email. Must be a valid email address. Cannot be used together with `id` in the same request.
  final String? email;

  /// Select the sender for the campaign on the basis of sender id. Cannot be used together with `email` in the same request. _In order to select a sender with a specific pool of IPs, dedicated IP users shall pass id (instead of email)._
  final int? id;

  /// Sender Name
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `POST /emailCampaigns/{campaignId}/exportRecipients`.
final class EmailExportRecipientsRequest {
  /// Creates the request body.
  const EmailExportRecipientsRequest({
    this.notifyUrl,
    required this.recipientsType,
  });

  /// Decodes a JSON object into a EmailExportRecipientsRequest. Optional fields absent from [json] stay null.
  factory EmailExportRecipientsRequest.fromJson(Map<String, Object?> json) =>
      EmailExportRecipientsRequest(
        notifyUrl: json.optString('notifyURL'),
        recipientsType: json.optEnum('recipientsType',
                EmailExportRecipientsRequestRecipientsType.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'EmailExportRecipientsRequest',
                key: 'recipientsType')),
      );

  /// Webhook called once the export process is finished. For reference, https://help.brevo.com/hc/en-us/articles/360007666479
  final String? notifyUrl;

  /// Type of recipients to export for a campaign
  final EmailExportRecipientsRequestRecipientsType recipientsType;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (notifyUrl != null) 'notifyURL': notifyUrl!,
        'recipientsType': recipientsType.value,
      };
}

/// The request body of `PUT /emailCampaigns/{campaignId}`.
final class UpdateEmailCampaignRequest {
  /// Creates the request body.
  const UpdateEmailCampaignRequest({
    this.abTesting,
    this.attachmentUrl,
    this.emailExpirationDate,
    this.footer,
    this.header,
    this.htmlContent,
    this.htmlUrl,
    this.increaseRate,
    this.initialQuota,
    this.inlineImageActivation,
    this.ipWarmupEnable,
    this.mirrorActive,
    this.name,
    this.params,
    this.previewText,
    this.recipients,
    this.recurring,
    this.replyTo,
    this.scheduledAt,
    this.sendAtBestTime,
    this.sender,
    this.splitRule,
    this.subject,
    this.subjectA,
    this.subjectB,
    this.tag,
    this.toField,
    this.unsubscriptionPageId,
    this.updateFormId,
    this.utmCampaign,
    this.utmContent,
    this.utmTerm,
    this.winnerCriteria,
    this.winnerDelay,
  });

  /// Decodes a JSON object into a UpdateEmailCampaignRequest. Optional fields absent from [json] stay null.
  factory UpdateEmailCampaignRequest.fromJson(Map<String, Object?> json) =>
      UpdateEmailCampaignRequest(
        abTesting: json.optBool('abTesting'),
        attachmentUrl: json.optString('attachmentUrl'),
        emailExpirationDate: json.optNested('emailExpirationDate',
            UpdateEmailCampaignRequestEmailExpirationDate.fromJson),
        footer: json.optString('footer'),
        header: json.optString('header'),
        htmlContent: json.optString('htmlContent'),
        htmlUrl: json.optString('htmlUrl'),
        increaseRate: json.optInt('increaseRate'),
        initialQuota: json.optInt('initialQuota'),
        inlineImageActivation: json.optBool('inlineImageActivation'),
        ipWarmupEnable: json.optBool('ipWarmupEnable'),
        mirrorActive: json.optBool('mirrorActive'),
        name: json.optString('name'),
        params: json.containsKey('params') ? json.optObjectMap('params') : null,
        previewText: json.optString('previewText'),
        recipients: json.optNested(
            'recipients', UpdateEmailCampaignRequestRecipients.fromJson),
        recurring: json.optBool('recurring'),
        replyTo: json.optString('replyTo'),
        scheduledAt: json.optString('scheduledAt'),
        sendAtBestTime: json.optBool('sendAtBestTime'),
        sender:
            json.optNested('sender', UpdateEmailCampaignRequestSender.fromJson),
        splitRule: json.optInt('splitRule'),
        subject: json.optString('subject'),
        subjectA: json.optString('subjectA'),
        subjectB: json.optString('subjectB'),
        tag: json.optString('tag'),
        toField: json.optString('toField'),
        unsubscriptionPageId: json.optString('unsubscriptionPageId'),
        updateFormId: json.optString('updateFormId'),
        utmCampaign: json.optString('utmCampaign'),
        utmContent: json.optString('utmContent'),
        utmTerm: json.optString('utmTerm'),
        winnerCriteria: json.optEnum('winnerCriteria',
            UpdateEmailCampaignRequestWinnerCriteria.fromWire),
        winnerDelay: json.optInt('winnerDelay'),
      );

  /// Status of A/B Test. abTesting = false means it is disabled & abTesting = true means it is enabled. **subjectA, subjectB, splitRule, winnerCriteria & winnerDelay** will be considered when abTesting is set to true. subjectA & subjectB are mandatory together & subject if passed is ignored. **Can be set to true only if sendAtBestTime is false**. You will be able to set up two subject lines for your campaign and send them to a random sample of your total recipients. Half of the test group will receive version A, and the other half will receive version B
  final bool? abTesting;

  /// Absolute url of the attachment (no local file). Extension allowed: #### xlsx, xls, ods, docx, docm, doc, csv, pdf, txt, gif, jpg, jpeg, png, tif, tiff, rtf, bmp, cgm, css, shtml, html, htm, zip, xml, ppt, pptx, tar, ez, ics, mobi, msg, pub and eps'
  final String? attachmentUrl;

  /// To reduce your carbon footprint, set an expiration date for your email. If supported, it will be automatically deleted from the recipient’s inbox, saving storage space and energy.
  final UpdateEmailCampaignRequestEmailExpirationDate? emailExpirationDate;

  /// Footer of the email campaign
  final String? footer;

  /// Header of the email campaign
  final String? header;

  /// Body of the message (HTML version). If the campaign is designed using Drag & Drop editor via HTML content, then the design page will not have Drag & Drop editor access for that campaign. **REQUIRED if htmlUrl is empty**
  final String? htmlContent;

  /// Url which contents the body of the email message. **REQUIRED if htmlContent is empty**
  final String? htmlUrl;

  /// Set a percentage increase rate for warming up your ip. We recommend you set the increase rate to 30% per day. If you want to send the same number of emails every day, set the daily increase value to 0%.
  final int? increaseRate;

  /// Set an initial quota greater than 1 for warming up your ip. We recommend you set a value of 3000.
  final int? initialQuota;

  /// Status of inline image. inlineImageActivation = false means image can’t be embedded, & inlineImageActivation = true means image can be embedded, in the email. You cannot send a campaign of more than **4MB** with images embedded in the email. Campaigns with the images embedded in the email _must be sent to less than 5000 contacts_.
  final bool? inlineImageActivation;

  /// **Available for dedicated ip clients**. Set this to true if you wish to warm up your ip.
  final bool? ipWarmupEnable;

  /// Status of mirror links in campaign. mirrorActive = false means mirror links are deactivated, & mirrorActive = true means mirror links are activated, in the campaign
  final bool? mirrorActive;

  /// Name of the campaign
  final String? name;

  /// Pass the set of attributes to customize the type classic campaign. For example: **{"FNAME":"Joe", "LNAME":"Doe"}**. Only available if **type** is **classic**. It's considered only if campaign is in _New Template Language format_. The New Template Language is dependent on the values of **subject, htmlContent/htmlUrl, sender.name & toField**
  final Map<String, Object?>? params;

  /// Preview text or preheader of the email campaign
  final String? previewText;

  /// Segment ids and List ids to include/exclude from campaign
  final UpdateEmailCampaignRequestRecipients? recipients;

  /// **FOR TRIGGER ONLY !** Type of trigger campaign.recurring = false means contact can receive the same Trigger campaign only once, & recurring = true means contact can receive the same Trigger campaign several times
  final bool? recurring;

  /// Email on which campaign recipients will be able to reply to
  final String? replyTo;

  /// UTC date-time on which the campaign has to run (YYYY-MM-DDTHH:mm:ss.SSSZ). **Prefer to pass your timezone in date-time format for accurate result.** If sendAtBestTime is set to true, your campaign will be sent according to the date passed (ignoring the time part).
  final String? scheduledAt;

  /// [scheduledAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledAtDate =>
      scheduledAt == null ? null : DateTime.tryParse(scheduledAt!);

  /// Set this to true if you want to send your campaign at best time. Note:- **if true, warmup ip will be disabled.**
  final bool? sendAtBestTime;

  /// Sender details including id or email and name (optional). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
  final UpdateEmailCampaignRequestSender? sender;

  /// Add the size of your test groups. **Mandatory if abTesting = true & 'recipients' is passed**. We'll send version A and B to a random sample of recipients, and then the winning version to everyone else
  final int? splitRule;

  /// Subject of the campaign
  final String? subject;

  /// Subject A of the campaign. **Mandatory if abTesting = true**. subjectA & subjectB should have unique value
  final String? subjectA;

  /// Subject B of the campaign. **Mandatory if abTesting = true**. subjectA & subjectB should have unique value
  final String? subjectB;

  /// Tag of the campaign
  final String? tag;

  /// To personalize the **To** Field. If you want to include the first name and last name of your recipient, add **{FNAME} {LNAME}**. These contact attributes must already exist in your Brevo account. If input parameter **params** used please use **{{contact.FNAME}} {{contact.LNAME}}** for personalization
  final String? toField;

  /// Enter an unsubscription page id. The page id is a 24 digit alphanumeric id that can be found in the URL when editing the page.
  final String? unsubscriptionPageId;

  /// **Mandatory if templateId is used containing the {{ update_profile }} tag**. Enter an update profile form id. The form id is a 24 digit alphanumeric id that can be found in the URL when editing the form.
  final String? updateFormId;

  /// Customize the utm_campaign value. If this field is empty, the campaign name will be used. Only alphanumeric characters and spaces are allowed
  final String? utmCampaign;

  /// Customize the utm_content value. Appears on outgoing tracking links alongside utm_campaign. Only alphanumeric characters and spaces are allowed
  final String? utmContent;

  /// Customize the utm_term value. Appears on outgoing tracking links alongside utm_campaign. Only alphanumeric characters and spaces are allowed
  final String? utmTerm;

  /// Choose the metrics that will determinate the winning version. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerCriteria` is ignored if passed
  final UpdateEmailCampaignRequestWinnerCriteria? winnerCriteria;

  /// Choose the duration of the test in hours. Maximum is 7 days, pass 24*7 = 168 hours. The winning version will be sent at the end of the test. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerDelay` is ignored if passed
  final int? winnerDelay;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (abTesting != null) 'abTesting': abTesting!,
        if (attachmentUrl != null) 'attachmentUrl': attachmentUrl!,
        if (emailExpirationDate != null)
          'emailExpirationDate': emailExpirationDate!.toJson(),
        if (footer != null) 'footer': footer!,
        if (header != null) 'header': header!,
        if (htmlContent != null) 'htmlContent': htmlContent!,
        if (htmlUrl != null) 'htmlUrl': htmlUrl!,
        if (increaseRate != null) 'increaseRate': increaseRate!,
        if (initialQuota != null) 'initialQuota': initialQuota!,
        if (inlineImageActivation != null)
          'inlineImageActivation': inlineImageActivation!,
        if (ipWarmupEnable != null) 'ipWarmupEnable': ipWarmupEnable!,
        if (mirrorActive != null) 'mirrorActive': mirrorActive!,
        if (name != null) 'name': name!,
        if (params != null) 'params': params!,
        if (previewText != null) 'previewText': previewText!,
        if (recipients != null) 'recipients': recipients!.toJson(),
        if (recurring != null) 'recurring': recurring!,
        if (replyTo != null) 'replyTo': replyTo!,
        if (scheduledAt != null) 'scheduledAt': scheduledAt!,
        if (sendAtBestTime != null) 'sendAtBestTime': sendAtBestTime!,
        if (sender != null) 'sender': sender!.toJson(),
        if (splitRule != null) 'splitRule': splitRule!,
        if (subject != null) 'subject': subject!,
        if (subjectA != null) 'subjectA': subjectA!,
        if (subjectB != null) 'subjectB': subjectB!,
        if (tag != null) 'tag': tag!,
        if (toField != null) 'toField': toField!,
        if (unsubscriptionPageId != null)
          'unsubscriptionPageId': unsubscriptionPageId!,
        if (updateFormId != null) 'updateFormId': updateFormId!,
        if (utmCampaign != null) 'utmCampaign': utmCampaign!,
        if (utmContent != null) 'utmContent': utmContent!,
        if (utmTerm != null) 'utmTerm': utmTerm!,
        if (winnerCriteria != null) 'winnerCriteria': winnerCriteria!.value,
        if (winnerDelay != null) 'winnerDelay': winnerDelay!,
      };
}

/// To reduce your carbon footprint, set an expiration date for your email. If supported, it will be automatically deleted from the recipient’s inbox, saving storage space and energy.
final class UpdateEmailCampaignRequestEmailExpirationDate {
  /// Creates the request body.
  const UpdateEmailCampaignRequestEmailExpirationDate({
    this.duration,
    this.unit,
  });

  /// Decodes a JSON object into a UpdateEmailCampaignRequestEmailExpirationDate. Optional fields absent from [json] stay null.
  factory UpdateEmailCampaignRequestEmailExpirationDate.fromJson(
          Map<String, Object?> json) =>
      UpdateEmailCampaignRequestEmailExpirationDate(
        duration: json.optInt('duration'),
        unit: json.optEnum(
            'unit', UpdateEmailCampaignRequestEmailExpirationDateUnit.fromWire),
      );

  /// Duration of the email expiry. maximum duration can be 3600 days or 480 weeks or 120 months.
  final int? duration;

  /// unit of the duration
  final UpdateEmailCampaignRequestEmailExpirationDateUnit? unit;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (duration != null) 'duration': duration!,
        if (unit != null) 'unit': unit!.value,
      };
}

/// Segment ids and List ids to include/exclude from campaign
final class UpdateEmailCampaignRequestRecipients {
  /// Creates the request body.
  const UpdateEmailCampaignRequestRecipients({
    this.exclusionListIds,
    this.exclusionSegmentIds,
    this.listIds,
    this.segmentIds,
  });

  /// Decodes a JSON object into a UpdateEmailCampaignRequestRecipients. Optional fields absent from [json] stay null.
  factory UpdateEmailCampaignRequestRecipients.fromJson(
          Map<String, Object?> json) =>
      UpdateEmailCampaignRequestRecipients(
        exclusionListIds: json.containsKey('exclusionListIds')
            ? json.optIntList('exclusionListIds')
            : null,
        exclusionSegmentIds: json.containsKey('exclusionSegmentIds')
            ? json.optIntList('exclusionSegmentIds')
            : null,
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        segmentIds: json.containsKey('segmentIds')
            ? json.optIntList('segmentIds')
            : null,
      );

  /// List ids which have to be excluded from a campaign
  final List<int>? exclusionListIds;

  /// Segment ids which have to be excluded from a campaign.
  final List<int>? exclusionSegmentIds;

  /// Lists Ids to send the campaign to. **Campaign should only be updated with listIds if listIds were used to create it. REQUIRED if already not present in campaign and scheduledAt is not empty**
  final List<int>? listIds;

  /// **Mandatory if listIds are not used. Campaign should only be updated with segmentIds if segmentIds were used to create it.** Segment ids to send the campaign to.
  final List<int>? segmentIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (exclusionListIds != null) 'exclusionListIds': exclusionListIds!,
        if (exclusionSegmentIds != null)
          'exclusionSegmentIds': exclusionSegmentIds!,
        if (listIds != null) 'listIds': listIds!,
        if (segmentIds != null) 'segmentIds': segmentIds!,
      };
}

/// Sender details including id or email and name (optional). Only one of either Sender's email or Sender's ID shall be passed in one request at a time. For example: **{"name":"xyz", "email":"example@abc.com"}** **{"name":"xyz", "id":123}**
final class UpdateEmailCampaignRequestSender {
  /// Creates the request body.
  const UpdateEmailCampaignRequestSender({
    this.email,
    this.id,
    this.name,
  });

  /// Decodes a JSON object into a UpdateEmailCampaignRequestSender. Optional fields absent from [json] stay null.
  factory UpdateEmailCampaignRequestSender.fromJson(
          Map<String, Object?> json) =>
      UpdateEmailCampaignRequestSender(
        email: json.optString('email'),
        id: json.optInt('id'),
        name: json.optString('name'),
      );

  /// Sender email from which the campaign emails are sent
  final String? email;

  /// Select the sender for the campaign on the basis of sender id. **In order to select a sender with specific pool of IP’s, dedicated ip users shall pass id (instead of email)**.
  final int? id;

  /// Sender Name from which the campaign emails are sent
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}

/// The request body of `POST /emailCampaigns/images`.
final class UploadImageToGalleryRequest {
  /// Creates the request body.
  const UploadImageToGalleryRequest({
    required this.imageUrl,
    this.name,
  });

  /// Decodes a JSON object into a UploadImageToGalleryRequest. Optional fields absent from [json] stay null.
  factory UploadImageToGalleryRequest.fromJson(Map<String, Object?> json) =>
      UploadImageToGalleryRequest(
        imageUrl: json.requireString('imageUrl', 'UploadImageToGalleryRequest'),
        name: json.optString('name'),
      );

  /// The absolute url of the image (**no local file**). Maximum allowed size for image is **2MB**. Allowed extensions for images are: #### jpeg, jpg, png, bmp, gif.
  final String imageUrl;

  /// Name shown in your Brevo image gallery. Include the file extension, e.g. product-banner.png
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'imageUrl': imageUrl,
        if (name != null) 'name': name!,
      };
}
