// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/open_enum.dart';

/// unit of the duration
final class CreateEmailCampaignRequestEmailExpirationDateUnit
    extends BrevoOpenEnum {
  const CreateEmailCampaignRequestEmailExpirationDateUnit._(super.value);

  /// Wraps a value this package does not know.
  const CreateEmailCampaignRequestEmailExpirationDateUnit.unknown(super.value);

  /// The `days` value.
  static const days =
      CreateEmailCampaignRequestEmailExpirationDateUnit._('days');

  /// The `weeks` value.
  static const weeks =
      CreateEmailCampaignRequestEmailExpirationDateUnit._('weeks');

  /// The `months` value.
  static const months =
      CreateEmailCampaignRequestEmailExpirationDateUnit._('months');

  /// The values known when this package was generated.
  static const values = [days, weeks, months];

  /// Decodes a wire value, preserving one this package does not know.
  factory CreateEmailCampaignRequestEmailExpirationDateUnit.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            CreateEmailCampaignRequestEmailExpirationDateUnit.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Choose the metrics that will determinate the winning version. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerCriteria` is ignored if passed
final class CreateEmailCampaignRequestWinnerCriteria extends BrevoOpenEnum {
  const CreateEmailCampaignRequestWinnerCriteria._(super.value);

  /// Wraps a value this package does not know.
  const CreateEmailCampaignRequestWinnerCriteria.unknown(super.value);

  /// The `open` value.
  static const open = CreateEmailCampaignRequestWinnerCriteria._('open');

  /// The `click` value.
  static const click = CreateEmailCampaignRequestWinnerCriteria._('click');

  /// The values known when this package was generated.
  static const values = [open, click];

  /// Decodes a wire value, preserving one this package does not know.
  factory CreateEmailCampaignRequestWinnerCriteria.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => CreateEmailCampaignRequestWinnerCriteria.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Type of recipients to export for a campaign
final class EmailExportRecipientsRequestRecipientsType extends BrevoOpenEnum {
  const EmailExportRecipientsRequestRecipientsType._(super.value);

  /// Wraps a value this package does not know.
  const EmailExportRecipientsRequestRecipientsType.unknown(super.value);

  /// The `all` value.
  static const all = EmailExportRecipientsRequestRecipientsType._('all');

  /// The `nonClickers` value.
  static const nonClickers =
      EmailExportRecipientsRequestRecipientsType._('nonClickers');

  /// The `nonOpeners` value.
  static const nonOpeners =
      EmailExportRecipientsRequestRecipientsType._('nonOpeners');

  /// The `clickers` value.
  static const clickers =
      EmailExportRecipientsRequestRecipientsType._('clickers');

  /// The `openers` value.
  static const openers =
      EmailExportRecipientsRequestRecipientsType._('openers');

  /// The `softBounces` value.
  static const softBounces =
      EmailExportRecipientsRequestRecipientsType._('softBounces');

  /// The `hardBounces` value.
  static const hardBounces =
      EmailExportRecipientsRequestRecipientsType._('hardBounces');

  /// The `unsubscribed` value.
  static const unsubscribed =
      EmailExportRecipientsRequestRecipientsType._('unsubscribed');

  /// The values known when this package was generated.
  static const values = [
    all,
    nonClickers,
    nonOpeners,
    clickers,
    openers,
    softBounces,
    hardBounces,
    unsubscribed
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory EmailExportRecipientsRequestRecipientsType.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => EmailExportRecipientsRequestRecipientsType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Criteria chosen for winning version (Open/Click)
final class GetAbTestCampaignResultResponseWinningCriteria
    extends BrevoOpenEnum {
  const GetAbTestCampaignResultResponseWinningCriteria._(super.value);

  /// Wraps a value this package does not know.
  const GetAbTestCampaignResultResponseWinningCriteria.unknown(super.value);

  /// The `Open` value.
  static const open = GetAbTestCampaignResultResponseWinningCriteria._('Open');

  /// The `Click` value.
  static const click =
      GetAbTestCampaignResultResponseWinningCriteria._('Click');

  /// The values known when this package was generated.
  static const values = [open, click];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetAbTestCampaignResultResponseWinningCriteria.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetAbTestCampaignResultResponseWinningCriteria.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Winning Campaign Info. pending = Campaign has been picked for sending and winning version is yet to be decided, tie = A tie happened between both the versions, notAvailable = Campaign has not yet been picked for sending.
final class GetAbTestCampaignResultResponseWinningVersion
    extends BrevoOpenEnum {
  const GetAbTestCampaignResultResponseWinningVersion._(super.value);

  /// Wraps a value this package does not know.
  const GetAbTestCampaignResultResponseWinningVersion.unknown(super.value);

  /// The `notAvailable` value.
  static const notAvailable =
      GetAbTestCampaignResultResponseWinningVersion._('notAvailable');

  /// The `pending` value.
  static const pending =
      GetAbTestCampaignResultResponseWinningVersion._('pending');

  /// The `tie` value.
  static const tie = GetAbTestCampaignResultResponseWinningVersion._('tie');

  /// The `A` value.
  static const a = GetAbTestCampaignResultResponseWinningVersion._('A');

  /// The `B` value.
  static const b = GetAbTestCampaignResultResponseWinningVersion._('B');

  /// The values known when this package was generated.
  static const values = [notAvailable, pending, tie, a, b];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetAbTestCampaignResultResponseWinningVersion.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetAbTestCampaignResultResponseWinningVersion.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetEmailCampaignRequestStatistics` values of the Brevo API.
final class GetEmailCampaignRequestStatistics extends BrevoOpenEnum {
  const GetEmailCampaignRequestStatistics._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignRequestStatistics.unknown(super.value);

  /// The `globalStats` value.
  static const globalStats = GetEmailCampaignRequestStatistics._('globalStats');

  /// The `linksStats` value.
  static const linksStats = GetEmailCampaignRequestStatistics._('linksStats');

  /// The `statsByDomain` value.
  static const statsByDomain =
      GetEmailCampaignRequestStatistics._('statsByDomain');

  /// The `statsByDevice` value.
  static const statsByDevice =
      GetEmailCampaignRequestStatistics._('statsByDevice');

  /// The `statsByBrowser` value.
  static const statsByBrowser =
      GetEmailCampaignRequestStatistics._('statsByBrowser');

  /// The values known when this package was generated.
  static const values = [
    globalStats,
    linksStats,
    statsByDomain,
    statsByDevice,
    statsByBrowser
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignRequestStatistics.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignRequestStatistics.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Unit of the duration
final class GetEmailCampaignResponseEmailExpirationDateUnit
    extends BrevoOpenEnum {
  const GetEmailCampaignResponseEmailExpirationDateUnit._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignResponseEmailExpirationDateUnit.unknown(super.value);

  /// The `days` value.
  static const days = GetEmailCampaignResponseEmailExpirationDateUnit._('days');

  /// The `weeks` value.
  static const weeks =
      GetEmailCampaignResponseEmailExpirationDateUnit._('weeks');

  /// The `months` value.
  static const months =
      GetEmailCampaignResponseEmailExpirationDateUnit._('months');

  /// The values known when this package was generated.
  static const values = [days, weeks, months];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignResponseEmailExpirationDateUnit.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetEmailCampaignResponseEmailExpirationDateUnit.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Status of the campaign
final class GetEmailCampaignResponseStatus extends BrevoOpenEnum {
  const GetEmailCampaignResponseStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignResponseStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetEmailCampaignResponseStatus._('draft');

  /// The `sent` value.
  static const sent = GetEmailCampaignResponseStatus._('sent');

  /// The `archive` value.
  static const archive = GetEmailCampaignResponseStatus._('archive');

  /// The `queued` value.
  static const queued = GetEmailCampaignResponseStatus._('queued');

  /// The `suspended` value.
  static const suspended = GetEmailCampaignResponseStatus._('suspended');

  /// The `in_process` value.
  static const inProcess = GetEmailCampaignResponseStatus._('in_process');

  /// The `in_review` value.
  static const inReview = GetEmailCampaignResponseStatus._('in_review');

  /// The `cancelling` value.
  static const cancelling = GetEmailCampaignResponseStatus._('cancelling');

  /// The `cancelled` value.
  static const cancelled = GetEmailCampaignResponseStatus._('cancelled');

  /// The values known when this package was generated.
  static const values = [
    draft,
    sent,
    archive,
    queued,
    suspended,
    inProcess,
    inReview,
    cancelling,
    cancelled
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignResponseStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignResponseStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Type of campaign
final class GetEmailCampaignResponseType extends BrevoOpenEnum {
  const GetEmailCampaignResponseType._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignResponseType.unknown(super.value);

  /// The `classic` value.
  static const classic = GetEmailCampaignResponseType._('classic');

  /// The `trigger` value.
  static const trigger = GetEmailCampaignResponseType._('trigger');

  /// The values known when this package was generated.
  static const values = [classic, trigger];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignResponseType.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignResponseType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetEmailCampaignsRequestSort` values of the Brevo API.
final class GetEmailCampaignsRequestSort extends BrevoOpenEnum {
  const GetEmailCampaignsRequestSort._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsRequestSort.unknown(super.value);

  /// The `asc` value.
  static const asc = GetEmailCampaignsRequestSort._('asc');

  /// The `desc` value.
  static const desc = GetEmailCampaignsRequestSort._('desc');

  /// The values known when this package was generated.
  static const values = [asc, desc];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsRequestSort.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignsRequestSort.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetEmailCampaignsRequestStatistics` values of the Brevo API.
final class GetEmailCampaignsRequestStatistics extends BrevoOpenEnum {
  const GetEmailCampaignsRequestStatistics._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsRequestStatistics.unknown(super.value);

  /// The `globalStats` value.
  static const globalStats =
      GetEmailCampaignsRequestStatistics._('globalStats');

  /// The `linksStats` value.
  static const linksStats = GetEmailCampaignsRequestStatistics._('linksStats');

  /// The `statsByDomain` value.
  static const statsByDomain =
      GetEmailCampaignsRequestStatistics._('statsByDomain');

  /// The values known when this package was generated.
  static const values = [globalStats, linksStats, statsByDomain];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsRequestStatistics.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignsRequestStatistics.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetEmailCampaignsRequestStatus` values of the Brevo API.
final class GetEmailCampaignsRequestStatus extends BrevoOpenEnum {
  const GetEmailCampaignsRequestStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsRequestStatus.unknown(super.value);

  /// The `suspended` value.
  static const suspended = GetEmailCampaignsRequestStatus._('suspended');

  /// The `archive` value.
  static const archive = GetEmailCampaignsRequestStatus._('archive');

  /// The `sent` value.
  static const sent = GetEmailCampaignsRequestStatus._('sent');

  /// The `queued` value.
  static const queued = GetEmailCampaignsRequestStatus._('queued');

  /// The `draft` value.
  static const draft = GetEmailCampaignsRequestStatus._('draft');

  /// The `inProcess` value.
  static const inProcess = GetEmailCampaignsRequestStatus._('inProcess');

  /// The `inReview` value.
  static const inReview = GetEmailCampaignsRequestStatus._('inReview');

  /// The `cancelling` value.
  static const cancelling = GetEmailCampaignsRequestStatus._('cancelling');

  /// The `cancelled` value.
  static const cancelled = GetEmailCampaignsRequestStatus._('cancelled');

  /// The values known when this package was generated.
  static const values = [
    suspended,
    archive,
    sent,
    queued,
    draft,
    inProcess,
    inReview,
    cancelling,
    cancelled
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsRequestStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignsRequestStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetEmailCampaignsRequestType` values of the Brevo API.
final class GetEmailCampaignsRequestType extends BrevoOpenEnum {
  const GetEmailCampaignsRequestType._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsRequestType.unknown(super.value);

  /// The `classic` value.
  static const classic = GetEmailCampaignsRequestType._('classic');

  /// The `trigger` value.
  static const trigger = GetEmailCampaignsRequestType._('trigger');

  /// The values known when this package was generated.
  static const values = [classic, trigger];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsRequestType.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignsRequestType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Unit of the duration
final class GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit
    extends BrevoOpenEnum {
  const GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit._(
      super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit.unknown(
      super.value);

  /// The `days` value.
  static const days =
      GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit._('days');

  /// The `weeks` value.
  static const weeks =
      GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit._('weeks');

  /// The `months` value.
  static const months =
      GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit._('months');

  /// The values known when this package was generated.
  static const values = [days, weeks, months];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetEmailCampaignsResponseCampaignsItemEmailExpirationDateUnit
                .unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Status of the campaign
final class GetEmailCampaignsResponseCampaignsItemStatus extends BrevoOpenEnum {
  const GetEmailCampaignsResponseCampaignsItemStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsResponseCampaignsItemStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetEmailCampaignsResponseCampaignsItemStatus._('draft');

  /// The `sent` value.
  static const sent = GetEmailCampaignsResponseCampaignsItemStatus._('sent');

  /// The `archive` value.
  static const archive =
      GetEmailCampaignsResponseCampaignsItemStatus._('archive');

  /// The `queued` value.
  static const queued =
      GetEmailCampaignsResponseCampaignsItemStatus._('queued');

  /// The `suspended` value.
  static const suspended =
      GetEmailCampaignsResponseCampaignsItemStatus._('suspended');

  /// The `in_process` value.
  static const inProcess =
      GetEmailCampaignsResponseCampaignsItemStatus._('in_process');

  /// The `in_review` value.
  static const inReview =
      GetEmailCampaignsResponseCampaignsItemStatus._('in_review');

  /// The `cancelling` value.
  static const cancelling =
      GetEmailCampaignsResponseCampaignsItemStatus._('cancelling');

  /// The `cancelled` value.
  static const cancelled =
      GetEmailCampaignsResponseCampaignsItemStatus._('cancelled');

  /// The values known when this package was generated.
  static const values = [
    draft,
    sent,
    archive,
    queued,
    suspended,
    inProcess,
    inReview,
    cancelling,
    cancelled
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsResponseCampaignsItemStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetEmailCampaignsResponseCampaignsItemStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Type of campaign
final class GetEmailCampaignsResponseCampaignsItemType extends BrevoOpenEnum {
  const GetEmailCampaignsResponseCampaignsItemType._(super.value);

  /// Wraps a value this package does not know.
  const GetEmailCampaignsResponseCampaignsItemType.unknown(super.value);

  /// The `classic` value.
  static const classic =
      GetEmailCampaignsResponseCampaignsItemType._('classic');

  /// The `trigger` value.
  static const trigger =
      GetEmailCampaignsResponseCampaignsItemType._('trigger');

  /// The values known when this package was generated.
  static const values = [classic, trigger];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetEmailCampaignsResponseCampaignsItemType.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetEmailCampaignsResponseCampaignsItemType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Unit of the duration
final class GetExtendedCampaignOverviewEmailExpirationDateUnit
    extends BrevoOpenEnum {
  const GetExtendedCampaignOverviewEmailExpirationDateUnit._(super.value);

  /// Wraps a value this package does not know.
  const GetExtendedCampaignOverviewEmailExpirationDateUnit.unknown(super.value);

  /// The `days` value.
  static const days =
      GetExtendedCampaignOverviewEmailExpirationDateUnit._('days');

  /// The `weeks` value.
  static const weeks =
      GetExtendedCampaignOverviewEmailExpirationDateUnit._('weeks');

  /// The `months` value.
  static const months =
      GetExtendedCampaignOverviewEmailExpirationDateUnit._('months');

  /// The values known when this package was generated.
  static const values = [days, weeks, months];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetExtendedCampaignOverviewEmailExpirationDateUnit.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            GetExtendedCampaignOverviewEmailExpirationDateUnit.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Status of the campaign
final class GetExtendedCampaignOverviewStatus extends BrevoOpenEnum {
  const GetExtendedCampaignOverviewStatus._(super.value);

  /// Wraps a value this package does not know.
  const GetExtendedCampaignOverviewStatus.unknown(super.value);

  /// The `draft` value.
  static const draft = GetExtendedCampaignOverviewStatus._('draft');

  /// The `sent` value.
  static const sent = GetExtendedCampaignOverviewStatus._('sent');

  /// The `archive` value.
  static const archive = GetExtendedCampaignOverviewStatus._('archive');

  /// The `queued` value.
  static const queued = GetExtendedCampaignOverviewStatus._('queued');

  /// The `suspended` value.
  static const suspended = GetExtendedCampaignOverviewStatus._('suspended');

  /// The `in_process` value.
  static const inProcess = GetExtendedCampaignOverviewStatus._('in_process');

  /// The `in_review` value.
  static const inReview = GetExtendedCampaignOverviewStatus._('in_review');

  /// The `cancelling` value.
  static const cancelling = GetExtendedCampaignOverviewStatus._('cancelling');

  /// The `cancelled` value.
  static const cancelled = GetExtendedCampaignOverviewStatus._('cancelled');

  /// The values known when this package was generated.
  static const values = [
    draft,
    sent,
    archive,
    queued,
    suspended,
    inProcess,
    inReview,
    cancelling,
    cancelled
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetExtendedCampaignOverviewStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetExtendedCampaignOverviewStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Type of campaign
final class GetExtendedCampaignOverviewType extends BrevoOpenEnum {
  const GetExtendedCampaignOverviewType._(super.value);

  /// Wraps a value this package does not know.
  const GetExtendedCampaignOverviewType.unknown(super.value);

  /// The `classic` value.
  static const classic = GetExtendedCampaignOverviewType._('classic');

  /// The `trigger` value.
  static const trigger = GetExtendedCampaignOverviewType._('trigger');

  /// The values known when this package was generated.
  static const values = [classic, trigger];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetExtendedCampaignOverviewType.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetExtendedCampaignOverviewType.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// unit of the duration
final class UpdateEmailCampaignRequestEmailExpirationDateUnit
    extends BrevoOpenEnum {
  const UpdateEmailCampaignRequestEmailExpirationDateUnit._(super.value);

  /// Wraps a value this package does not know.
  const UpdateEmailCampaignRequestEmailExpirationDateUnit.unknown(super.value);

  /// The `days` value.
  static const days =
      UpdateEmailCampaignRequestEmailExpirationDateUnit._('days');

  /// The `weeks` value.
  static const weeks =
      UpdateEmailCampaignRequestEmailExpirationDateUnit._('weeks');

  /// The `months` value.
  static const months =
      UpdateEmailCampaignRequestEmailExpirationDateUnit._('months');

  /// The values known when this package was generated.
  static const values = [days, weeks, months];

  /// Decodes a wire value, preserving one this package does not know.
  factory UpdateEmailCampaignRequestEmailExpirationDateUnit.fromWire(
          String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () =>
            UpdateEmailCampaignRequestEmailExpirationDateUnit.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Choose the metrics that will determinate the winning version. **Mandatory if _splitRule_ &gt;= 1 and &lt; 50**. If splitRule = 50, `winnerCriteria` is ignored if passed
final class UpdateEmailCampaignRequestWinnerCriteria extends BrevoOpenEnum {
  const UpdateEmailCampaignRequestWinnerCriteria._(super.value);

  /// Wraps a value this package does not know.
  const UpdateEmailCampaignRequestWinnerCriteria.unknown(super.value);

  /// The `open` value.
  static const open = UpdateEmailCampaignRequestWinnerCriteria._('open');

  /// The `click` value.
  static const click = UpdateEmailCampaignRequestWinnerCriteria._('click');

  /// The values known when this package was generated.
  static const values = [open, click];

  /// Decodes a wire value, preserving one this package does not know.
  factory UpdateEmailCampaignRequestWinnerCriteria.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => UpdateEmailCampaignRequestWinnerCriteria.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}
