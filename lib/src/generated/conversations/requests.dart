// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The request body of `POST /conversations/agentOnlinePing`.
final class PostConversationsAgentOnlinePingRequest {
  /// Creates the request body.
  const PostConversationsAgentOnlinePingRequest({
    this.agentEmail,
    this.agentId,
    this.agentName,
    this.receivedFrom,
  });

  /// Decodes a JSON object into a PostConversationsAgentOnlinePingRequest. Optional fields absent from [json] stay null.
  factory PostConversationsAgentOnlinePingRequest.fromJson(
          Map<String, Object?> json) =>
      PostConversationsAgentOnlinePingRequest(
        agentEmail: json.optString('agentEmail'),
        agentId: json.optString('agentId'),
        agentName: json.optString('agentName'),
        receivedFrom: json.optString('receivedFrom'),
      );

  /// Agent's email address. When sending online pings from a standalone system, it's hard to maintain a 1-to-1 relationship between the users of both systems. In this case, an agent can be specified by their email address. If there's no agent with the specified email address in your Brevo organization, a dummy agent will be created automatically.
  final String? agentEmail;

  /// Agent ID. It can be found on the agent's page or received &lt;a href="https://developers.brevo.com/docs/conversations-webhooks"&gt;from a webhook&lt;/a&gt;. Alternatively, you can use `agentEmail` + `agentName` + `receivedFrom` instead (all 3 fields required).
  final String? agentId;

  /// Agent's name.
  final String? agentName;

  /// Mark your messages to distinguish messages created by you from the others.
  final String? receivedFrom;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (agentEmail != null) 'agentEmail': agentEmail!,
        if (agentId != null) 'agentId': agentId!,
        if (agentName != null) 'agentName': agentName!,
        if (receivedFrom != null) 'receivedFrom': receivedFrom!,
      };
}

/// The request body of `POST /conversations/messages`.
final class PostConversationsMessagesRequest {
  /// Creates the request body.
  const PostConversationsMessagesRequest({
    this.agentEmail,
    this.agentId,
    this.agentName,
    this.receivedFrom,
    required this.text,
    required this.visitorId,
  });

  /// Decodes a JSON object into a PostConversationsMessagesRequest. Optional fields absent from [json] stay null.
  factory PostConversationsMessagesRequest.fromJson(
          Map<String, Object?> json) =>
      PostConversationsMessagesRequest(
        agentEmail: json.optString('agentEmail'),
        agentId: json.optString('agentId'),
        agentName: json.optString('agentName'),
        receivedFrom: json.optString('receivedFrom'),
        text: json.requireString('text', 'PostConversationsMessagesRequest'),
        visitorId:
            json.requireString('visitorId', 'PostConversationsMessagesRequest'),
      );

  /// Agent's email address. When sending messages from a standalone system, it's hard to maintain a 1-to-1 relationship between the users of both systems. In this case, an agent can be specified by their email address.
  final String? agentEmail;

  /// Agent ID. It can be found on the agent's page or received &lt;a href="https://developers.brevo.com/docs/conversations-webhooks"&gt;from a webhook&lt;/a&gt;. Alternatively, you can use `agentEmail` + `agentName` + `receivedFrom` instead (all 3 fields required).
  final String? agentId;

  /// Agent's name.
  final String? agentName;

  /// Mark your messages to distinguish messages created by you from the others.
  final String? receivedFrom;

  /// Message text.
  final String text;

  /// Visitor's ID received &lt;a href="https://developers.brevo.com/docs/conversations-webhooks"&gt;from a webhook&lt;/a&gt; or generated by you to &lt;a href="https://developers.brevo.com/docs/customize-the-widget#identifying-existing-users"&gt;bind an existing user account to Conversations&lt;/a&gt;.
  final String visitorId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (agentEmail != null) 'agentEmail': agentEmail!,
        if (agentId != null) 'agentId': agentId!,
        if (agentName != null) 'agentName': agentName!,
        if (receivedFrom != null) 'receivedFrom': receivedFrom!,
        'text': text,
        'visitorId': visitorId,
      };
}

/// The request body of `POST /conversations/pushedMessages`.
final class PostConversationsPushedMessagesRequest {
  /// Creates the request body.
  const PostConversationsPushedMessagesRequest({
    this.agentId,
    this.groupId,
    required this.text,
    required this.visitorId,
  });

  /// Decodes a JSON object into a PostConversationsPushedMessagesRequest. Optional fields absent from [json] stay null.
  factory PostConversationsPushedMessagesRequest.fromJson(
          Map<String, Object?> json) =>
      PostConversationsPushedMessagesRequest(
        agentId: json.optString('agentId'),
        groupId: json.optString('groupId'),
        text: json.requireString(
            'text', 'PostConversationsPushedMessagesRequest'),
        visitorId: json.requireString(
            'visitorId', 'PostConversationsPushedMessagesRequest'),
      );

  /// Agent ID. It can be found on the agent's page or received &lt;a href="https://developers.brevo.com/docs/conversations-webhooks"&gt;from a webhook&lt;/a&gt;.
  final String? agentId;

  /// Group ID. It can be found on the group's page.
  final String? groupId;

  /// Message text.
  final String text;

  /// Visitor's ID received &lt;a href="https://developers.brevo.com/docs/conversations-webhooks"&gt;from a webhook&lt;/a&gt; or generated by you to &lt;a href="https://developers.brevo.com/docs/customize-the-widget#identifying-existing-users"&gt;bind an existing user account to Conversations&lt;/a&gt;.
  final String visitorId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (agentId != null) 'agentId': agentId!,
        if (groupId != null) 'groupId': groupId!,
        'text': text,
        'visitorId': visitorId,
      };
}

/// The request body of `PUT /conversations/messages/{id}`.
final class PutConversationsMessagesIdRequest {
  /// Creates the request body.
  const PutConversationsMessagesIdRequest({
    required this.text,
  });

  /// Decodes a JSON object into a PutConversationsMessagesIdRequest. Optional fields absent from [json] stay null.
  factory PutConversationsMessagesIdRequest.fromJson(
          Map<String, Object?> json) =>
      PutConversationsMessagesIdRequest(
        text: json.requireString('text', 'PutConversationsMessagesIdRequest'),
      );

  /// The new message text.
  final String text;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'text': text,
      };
}

/// The request body of `PUT /conversations/pushedMessages/{id}`.
final class PutConversationsPushedMessagesIdRequest {
  /// Creates the request body.
  const PutConversationsPushedMessagesIdRequest({
    required this.text,
  });

  /// Decodes a JSON object into a PutConversationsPushedMessagesIdRequest. Optional fields absent from [json] stay null.
  factory PutConversationsPushedMessagesIdRequest.fromJson(
          Map<String, Object?> json) =>
      PutConversationsPushedMessagesIdRequest(
        text: json.requireString(
            'text', 'PutConversationsPushedMessagesIdRequest'),
      );

  /// The new message text.
  final String text;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'text': text,
      };
}

/// The request body of `PUT /conversations/visitorGroup`.
final class PutConversationsVisitorGroupRequest {
  /// Creates the request body.
  const PutConversationsVisitorGroupRequest({
    required this.groupId,
  });

  /// Decodes a JSON object into a PutConversationsVisitorGroupRequest. Optional fields absent from [json] stay null.
  factory PutConversationsVisitorGroupRequest.fromJson(
          Map<String, Object?> json) =>
      PutConversationsVisitorGroupRequest(
        groupId: json['groupId'],
      );

  /// Group ID. It can be found on group’s page.
  final Object? groupId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (groupId != null) 'groupId': groupId,
      };
}
