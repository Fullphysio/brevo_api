// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// a Conversations message
final class ConversationsMessage {
  /// Creates a ConversationsMessage from already-decoded fields.
  const ConversationsMessage({
    this.agentId,
    this.agentName,
    this.agentUserpic,
    this.attachments = const [],
    this.bcc = const [],
    this.cc = const [],
    this.createdAt,
    this.file,
    this.forwardedToSourceStatus,
    this.from,
    this.html,
    this.id,
    this.integrations = const {},
    this.isBot,
    this.isForward,
    this.isMissed,
    this.isMissedByVisitor,
    this.isPushed,
    this.isSentViaJsApi,
    this.isTrigger,
    this.messageType,
    this.rawUnsafeHtml,
    this.receivedFrom,
    this.replyTo,
    this.source = const {},
    this.sourceMessageId,
    this.subject,
    this.text,
    this.to = const [],
    this.type,
    this.visitorId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessage. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessage.fromJson(Map<String, Object?> json) =>
      ConversationsMessage(
        agentId: json.optString('agentId'),
        agentName: json.optString('agentName'),
        agentUserpic: json.optString('agentUserpic'),
        attachments: json.optObjectList(
            'attachments', ConversationsMessageAttachmentsItem.fromJson,
            objectName: 'ConversationsMessage'),
        bcc: json.optObjectList('bcc', ConversationsMessageBccItem.fromJson,
            objectName: 'ConversationsMessage'),
        cc: json.optObjectList('cc', ConversationsMessageCcItem.fromJson,
            objectName: 'ConversationsMessage'),
        createdAt: json.optInt('createdAt'),
        file: json.optNested('file', ConversationsMessageFile.fromJson),
        forwardedToSourceStatus: json.optNested('forwardedToSourceStatus',
            ConversationsMessageForwardedToSourceStatus.fromJson),
        from: json.optNested('from', ConversationsMessageFrom.fromJson),
        html: json.optString('html'),
        id: json.optString('id'),
        integrations: json.optObjectMap('integrations'),
        isBot: json.optBool('isBot'),
        isForward: json.optBool('isForward'),
        isMissed: json.optBool('isMissed'),
        isMissedByVisitor: json.optBool('isMissedByVisitor'),
        isPushed: json.optBool('isPushed'),
        isSentViaJsApi: json.optBool('isSentViaJsApi'),
        isTrigger: json.optBool('isTrigger'),
        messageType: json.optString('messageType'),
        rawUnsafeHtml: json.optString('rawUnsafeHtml'),
        receivedFrom: json.optString('receivedFrom'),
        replyTo:
            json.optNested('replyTo', ConversationsMessageReplyTo.fromJson),
        source: json.optObjectMap('source'),
        sourceMessageId: json.optString('sourceMessageId'),
        subject: json.optString('subject'),
        text: json.optString('text'),
        to: json.optObjectList('to', ConversationsMessageToItem.fromJson,
            objectName: 'ConversationsMessage'),
        type: json.optEnum('type', ConversationsMessageType.fromWire),
        visitorId: json.optString('visitorId'),
        raw: json,
      );

  /// ID of the agent on whose behalf the message was sent (only in messages sent by an agent).
  final String? agentId;

  /// Agent’s name as displayed to the visitor. Only in the messages sent by an agent.
  final String? agentName;

  /// Only set if the agent has uploaded a profile picture.
  final String? agentUserpic;

  /// An array of file attachments.
  final List<ConversationsMessageAttachmentsItem> attachments;

  /// An array containing details of the blind carbon copy (BCC) recipients (applicable only to messages in email threads).
  final List<ConversationsMessageBccItem> bcc;

  /// An array containing details of the carbon copy (CC) recipients (applicable only to messages in email threads).
  final List<ConversationsMessageCcItem> cc;

  /// Timestamp in milliseconds.
  final int? createdAt;

  /// The `file` field.
  final ConversationsMessageFile? file;

  /// Status of the message forwarding to the source.
  final ConversationsMessageForwardedToSourceStatus? forwardedToSourceStatus;

  /// An object containing details about the email sender (applicable only to messages in email threads).
  final ConversationsMessageFrom? from;

  /// The HTML content of the message.
  final String? html;

  /// Message ID. It can be used for further manipulations with the message.
  final String? id;

  /// Integration details.
  final Map<String, Object?> integrations;

  /// `true` for automated messages generated by an AI bot.
  final bool? isBot;

  /// ‘`true` if the message is a forwarded message.’
  final bool? isForward;

  /// `true` for missed and offline messages.
  final bool? isMissed;

  /// `true` for unread agent’s messages in finished chats.
  final bool? isMissedByVisitor;

  /// `true` for pushed messages
  final bool? isPushed;

  /// ‘`true` if the message was sent via JavaScript API.’
  final bool? isSentViaJsApi;

  /// `true` for automatic messages from “Targeted chats & triggers” and API (https://developers.brevo.com/docs/javascript-api-reference#sendautomessage)
  final bool? isTrigger;

  /// The type of message content (e.g. for integration-specific message types).
  final String? messageType;

  /// Unescaped HTML content of the message (may include unsafe HTML).
  final String? rawUnsafeHtml;

  /// In two-way integrations, messages sent via REST API can be marked with receivedFrom property and then filtered out when received in a webhook to avoid infinite loop.
  final String? receivedFrom;

  /// An object containing details of the reply-to email address (applicable only to messages in email threads).
  final ConversationsMessageReplyTo? replyTo;

  /// Source information for the message.
  final Map<String, Object?> source;

  /// The ID of the message assigned by the integration source.
  final String? sourceMessageId;

  /// The subject line of the email message (only for messages sent to email threads).
  final String? subject;

  /// Message text or name of the attached file
  final String? text;

  /// An array containing details of the recipients (applicable only to messages in email threads).
  final List<ConversationsMessageToItem> to;

  /// `"agent"` for agents’ messages, `"visitor"` for visitors’ messages.
  final ConversationsMessageType? type;

  /// visitor’s ID
  final String? visitorId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (agentId != null) 'agentId': agentId!,
        if (agentName != null) 'agentName': agentName!,
        if (agentUserpic != null) 'agentUserpic': agentUserpic!,
        if (attachments.isNotEmpty)
          'attachments': [for (final item in attachments) item.toJson()],
        if (bcc.isNotEmpty) 'bcc': [for (final item in bcc) item.toJson()],
        if (cc.isNotEmpty) 'cc': [for (final item in cc) item.toJson()],
        if (createdAt != null) 'createdAt': createdAt!,
        if (file != null) 'file': file!.toJson(),
        if (forwardedToSourceStatus != null)
          'forwardedToSourceStatus': forwardedToSourceStatus!.toJson(),
        if (from != null) 'from': from!.toJson(),
        if (html != null) 'html': html!,
        if (id != null) 'id': id!,
        if (integrations.isNotEmpty) 'integrations': integrations,
        if (isBot != null) 'isBot': isBot!,
        if (isForward != null) 'isForward': isForward!,
        if (isMissed != null) 'isMissed': isMissed!,
        if (isMissedByVisitor != null) 'isMissedByVisitor': isMissedByVisitor!,
        if (isPushed != null) 'isPushed': isPushed!,
        if (isSentViaJsApi != null) 'isSentViaJsApi': isSentViaJsApi!,
        if (isTrigger != null) 'isTrigger': isTrigger!,
        if (messageType != null) 'messageType': messageType!,
        if (rawUnsafeHtml != null) 'rawUnsafeHtml': rawUnsafeHtml!,
        if (receivedFrom != null) 'receivedFrom': receivedFrom!,
        if (replyTo != null) 'replyTo': replyTo!.toJson(),
        if (source.isNotEmpty) 'source': source,
        if (sourceMessageId != null) 'sourceMessageId': sourceMessageId!,
        if (subject != null) 'subject': subject!,
        if (text != null) 'text': text!,
        if (to.isNotEmpty) 'to': [for (final item in to) item.toJson()],
        if (type != null) 'type': type!.value,
        if (visitorId != null) 'visitorId': visitorId!,
      };
}

/// The `ConversationsMessage.attachmentsItem` object of the Brevo API.
final class ConversationsMessageAttachmentsItem {
  /// Creates a ConversationsMessageAttachmentsItem from already-decoded fields.
  const ConversationsMessageAttachmentsItem({
    this.imageInfo,
    this.inlineId,
    this.isAllowedFileType,
    this.isImage,
    this.isInline,
    this.isSticker,
    this.link,
    this.mimeType,
    this.name,
    this.size,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageAttachmentsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageAttachmentsItem.fromJson(
          Map<String, Object?> json) =>
      ConversationsMessageAttachmentsItem(
        imageInfo: json.optNested(
            'imageInfo', ConversationsMessageAttachmentsItemImageInfo.fromJson),
        inlineId: json.optString('inlineId'),
        isAllowedFileType: json.optBool('isAllowedFileType'),
        isImage: json.optBool('isImage'),
        isInline: json.optBool('isInline'),
        isSticker: json.optBool('isSticker'),
        link: json.optString('link'),
        mimeType: json.optString('mimeType'),
        name: json.optString('name'),
        size: json.optInt('size'),
        raw: json,
      );

  /// Image information (only present for image files).
  final ConversationsMessageAttachmentsItemImageInfo? imageInfo;

  /// The ID of the inline file.
  final String? inlineId;

  /// Whether the file extension is allowed for download.
  final bool? isAllowedFileType;

  /// `true` for images.
  final bool? isImage;

  /// `true` for inline files.
  final bool? isInline;

  /// `true` for sticker files.
  final bool? isSticker;

  /// The URL of the file.
  final String? link;

  /// The MIME type of the file.
  final String? mimeType;

  /// The name of the file.
  final String? name;

  /// The size of the file in bytes.
  final int? size;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (imageInfo != null) 'imageInfo': imageInfo!.toJson(),
        if (inlineId != null) 'inlineId': inlineId!,
        if (isAllowedFileType != null) 'isAllowedFileType': isAllowedFileType!,
        if (isImage != null) 'isImage': isImage!,
        if (isInline != null) 'isInline': isInline!,
        if (isSticker != null) 'isSticker': isSticker!,
        if (link != null) 'link': link!,
        if (mimeType != null) 'mimeType': mimeType!,
        if (name != null) 'name': name!,
        if (size != null) 'size': size!,
      };
}

/// Image information (only present for image files).
final class ConversationsMessageAttachmentsItemImageInfo {
  /// Creates a ConversationsMessageAttachmentsItemImageInfo from already-decoded fields.
  const ConversationsMessageAttachmentsItemImageInfo({
    this.height,
    this.previewLink,
    this.width,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageAttachmentsItemImageInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageAttachmentsItemImageInfo.fromJson(
          Map<String, Object?> json) =>
      ConversationsMessageAttachmentsItemImageInfo(
        height: json.optInt('height'),
        previewLink: json.optString('previewLink'),
        width: json.optInt('width'),
        raw: json,
      );

  /// Height of the image in pixels.
  final int? height;

  /// URL of the image preview.
  final String? previewLink;

  /// Width of the image in pixels.
  final int? width;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (height != null) 'height': height!,
        if (previewLink != null) 'previewLink': previewLink!,
        if (width != null) 'width': width!,
      };
}

/// The `ConversationsMessage.bccItem` object of the Brevo API.
final class ConversationsMessageBccItem {
  /// Creates a ConversationsMessageBccItem from already-decoded fields.
  const ConversationsMessageBccItem({
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageBccItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageBccItem.fromJson(Map<String, Object?> json) =>
      ConversationsMessageBccItem(
        email: json.optString('email'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `email` field.
  final String? email;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (name != null) 'name': name!,
      };
}

/// The `ConversationsMessage.ccItem` object of the Brevo API.
final class ConversationsMessageCcItem {
  /// Creates a ConversationsMessageCcItem from already-decoded fields.
  const ConversationsMessageCcItem({
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageCcItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageCcItem.fromJson(Map<String, Object?> json) =>
      ConversationsMessageCcItem(
        email: json.optString('email'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `email` field.
  final String? email;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (name != null) 'name': name!,
      };
}

/// The `ConversationsMessage.file` object of the Brevo API.
final class ConversationsMessageFile {
  /// Creates a ConversationsMessageFile from already-decoded fields.
  const ConversationsMessageFile({
    this.imageInfo,
    this.inlineId,
    this.isAllowedFileType,
    this.isImage,
    this.isInline,
    this.isSticker,
    this.link,
    this.mimeType,
    this.name,
    this.size,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageFile. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageFile.fromJson(Map<String, Object?> json) =>
      ConversationsMessageFile(
        imageInfo: json.optNested(
            'imageInfo', ConversationsMessageFileImageInfo.fromJson),
        inlineId: json.optString('inlineId'),
        isAllowedFileType: json.optBool('isAllowedFileType'),
        isImage: json.optBool('isImage'),
        isInline: json.optBool('isInline'),
        isSticker: json.optBool('isSticker'),
        link: json.optString('link'),
        mimeType: json.optString('mimeType'),
        name: json.optString('name'),
        size: json.optInt('size'),
        raw: json,
      );

  /// Image info, present when the file is an image.
  final ConversationsMessageFileImageInfo? imageInfo;

  /// The ID of the inline file.
  final String? inlineId;

  /// Whether the file extension is allowed for download.
  final bool? isAllowedFileType;

  /// Whether the file is an image.
  final bool? isImage;

  /// Whether the file is inline.
  final bool? isInline;

  /// Whether the file is a sticker.
  final bool? isSticker;

  /// URL of the file.
  final String? link;

  /// MIME type of the file.
  final String? mimeType;

  /// Name of the file.
  final String? name;

  /// Size in bytes.
  final int? size;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (imageInfo != null) 'imageInfo': imageInfo!.toJson(),
        if (inlineId != null) 'inlineId': inlineId!,
        if (isAllowedFileType != null) 'isAllowedFileType': isAllowedFileType!,
        if (isImage != null) 'isImage': isImage!,
        if (isInline != null) 'isInline': isInline!,
        if (isSticker != null) 'isSticker': isSticker!,
        if (link != null) 'link': link!,
        if (mimeType != null) 'mimeType': mimeType!,
        if (name != null) 'name': name!,
        if (size != null) 'size': size!,
      };
}

/// Image info, present when the file is an image.
final class ConversationsMessageFileImageInfo {
  /// Creates a ConversationsMessageFileImageInfo from already-decoded fields.
  const ConversationsMessageFileImageInfo({
    this.height,
    this.previewLink,
    this.width,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageFileImageInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageFileImageInfo.fromJson(
          Map<String, Object?> json) =>
      ConversationsMessageFileImageInfo(
        height: json.optInt('height'),
        previewLink: json.optString('previewLink'),
        width: json.optInt('width'),
        raw: json,
      );

  /// Height of the image in pixels.
  final int? height;

  /// URL of the image preview.
  final String? previewLink;

  /// Width of the image in pixels.
  final int? width;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (height != null) 'height': height!,
        if (previewLink != null) 'previewLink': previewLink!,
        if (width != null) 'width': width!,
      };
}

/// Status of the message forwarding to the source.
final class ConversationsMessageForwardedToSourceStatus {
  /// Creates a ConversationsMessageForwardedToSourceStatus from already-decoded fields.
  const ConversationsMessageForwardedToSourceStatus({
    this.error,
    this.isSuccess,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageForwardedToSourceStatus. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageForwardedToSourceStatus.fromJson(
          Map<String, Object?> json) =>
      ConversationsMessageForwardedToSourceStatus(
        error: json.optString('error'),
        isSuccess: json.optBool('isSuccess'),
        raw: json,
      );

  /// The `error` field.
  final String? error;

  /// The `isSuccess` field.
  final bool? isSuccess;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (error != null) 'error': error!,
        if (isSuccess != null) 'isSuccess': isSuccess!,
      };
}

/// An object containing details about the email sender (applicable only to messages in email threads).
final class ConversationsMessageFrom {
  /// Creates a ConversationsMessageFrom from already-decoded fields.
  const ConversationsMessageFrom({
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageFrom. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageFrom.fromJson(Map<String, Object?> json) =>
      ConversationsMessageFrom(
        email: json.optString('email'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `email` field.
  final String? email;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (name != null) 'name': name!,
      };
}

/// An object containing details of the reply-to email address (applicable only to messages in email threads).
final class ConversationsMessageReplyTo {
  /// Creates a ConversationsMessageReplyTo from already-decoded fields.
  const ConversationsMessageReplyTo({
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageReplyTo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageReplyTo.fromJson(Map<String, Object?> json) =>
      ConversationsMessageReplyTo(
        email: json.optString('email'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `email` field.
  final String? email;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (name != null) 'name': name!,
      };
}

/// The `ConversationsMessage.toItem` object of the Brevo API.
final class ConversationsMessageToItem {
  /// Creates a ConversationsMessageToItem from already-decoded fields.
  const ConversationsMessageToItem({
    this.email,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConversationsMessageToItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConversationsMessageToItem.fromJson(Map<String, Object?> json) =>
      ConversationsMessageToItem(
        email: json.optString('email'),
        name: json.optString('name'),
        raw: json,
      );

  /// The `email` field.
  final String? email;

  /// The `name` field.
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (email != null) 'email': email!,
        if (name != null) 'name': name!,
      };
}

/// The `PUT /conversations/visitorGroup` object of the Brevo API.
final class PutConversationsVisitorGroupResponse {
  /// Creates a PutConversationsVisitorGroupResponse from already-decoded fields.
  const PutConversationsVisitorGroupResponse({
    this.groupId,
    this.visitorId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PutConversationsVisitorGroupResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PutConversationsVisitorGroupResponse.fromJson(
          Map<String, Object?> json) =>
      PutConversationsVisitorGroupResponse(
        groupId: json['groupId'],
        visitorId: json['visitorId'],
        raw: json,
      );

  /// group ID the visitor was assigned to
  final Object? groupId;

  /// visitor ID
  final Object? visitorId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (groupId != null) 'groupId': groupId,
        if (visitorId != null) 'visitorId': visitorId,
      };
}
