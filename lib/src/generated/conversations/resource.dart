// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import 'models.dart';
import 'requests.dart';

/// The `conversations` resource of the Brevo API: `client.conversations`.
final class BrevoConversations {
  /// Creates the resource backed by [_client].
  const BrevoConversations(this._client);

  final BrevoClient _client;

  /// `DELETE /conversations/messages/{id}`
  ///
  /// Delete a message sent by an agent
  ///
  /// Delete a message sent by an agent. Only messages of type `agent` can be deleted.
  ///
  /// \[id\]: ID of the message
  Future<void> deleteAMessageSentByAnAgent(
      {required String id, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/conversations/messages/${brevoPathSegment(id)}',
        options: options);
  }

  /// `DELETE /conversations/pushedMessages/{id}`
  ///
  /// Delete an automated message
  ///
  /// Delete an automated (pushed) message by its ID.
  ///
  /// \[id\]: ID of the message
  Future<void> deleteAnAutomatedMessage(
      {required String id, BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'DELETE',
        path: '/conversations/pushedMessages/${brevoPathSegment(id)}',
        options: options);
  }

  /// `GET /conversations/messages/{id}`
  ///
  /// Get a message
  ///
  /// Retrieve a single message by its ID.
  ///
  /// \[id\]: ID of the message
  Future<ConversationsMessage> getAMessage(
      {required String id, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/conversations/messages/${brevoPathSegment(id)}',
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }

  /// `GET /conversations/pushedMessages/{id}`
  ///
  /// Get an automated message
  ///
  /// Retrieve a single automated (pushed) message by its ID.
  ///
  /// \[id\]: ID of the message sent previously
  Future<ConversationsMessage> getAnAutomatedMessage(
      {required String id, BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path: '/conversations/pushedMessages/${brevoPathSegment(id)}',
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }

  /// `POST /conversations/messages`
  ///
  /// Send a message as an agent
  ///
  /// Send a message as an agent to an existing visitor's conversation. You must provide either `agentId` alone, or all three of `agentEmail` + `agentName` + `receivedFrom` to identify the agent.
  Future<ConversationsMessage> sendAMessageAsAnAgent(
      PostConversationsMessagesRequest request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'POST',
        path: '/conversations/messages',
        body: request.toJson(),
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }

  /// `POST /conversations/pushedMessages`
  ///
  /// Send an automated message to a visitor
  ///
  /// Send an automated (pushed) message to a visitor on behalf of an agent. Example use cases: order status updates, announcing new features in your web app, etc.
  Future<ConversationsMessage> sendAnAutomatedMessageToAVisitor(
      PostConversationsPushedMessagesRequest request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'POST',
        path: '/conversations/pushedMessages',
        body: request.toJson(),
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }

  /// `PUT /conversations/visitorGroup`
  ///
  /// Set visitor group assignment
  ///
  /// Assigns a visitor to a specific agent group or removes them from their current group.
  Future<PutConversationsVisitorGroupResponse> setVisitorGroupAssignment(
      PutConversationsVisitorGroupRequest request,
      {BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'PUT',
        path: '/conversations/visitorGroup',
        body: request.toJson(),
        options: options);
    return PutConversationsVisitorGroupResponse.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'PutConversationsVisitorGroupResponse'));
  }

  /// `POST /conversations/agentOnlinePing`
  ///
  /// Sets agent's status to online for 2-3 minutes
  ///
  /// Sets the agent's status to online for 2-3 minutes. We recommend pinging this endpoint every minute for as long as the agent has to be considered online. You must provide either `agentId` alone, or all three of `agentEmail` + `agentName` + `receivedFrom`.
  Future<void> setsAgentsStatusToOnlineFor23Minutes(
      PostConversationsAgentOnlinePingRequest request,
      {BrevoRequestOptions? options}) async {
    await _client.requestVoid(
        method: 'POST',
        path: '/conversations/agentOnlinePing',
        body: request.toJson(),
        options: options);
  }

  /// `PUT /conversations/messages/{id}`
  ///
  /// Update a message sent by an agent
  ///
  /// Update the text of a message sent by an agent. Only messages of type `agent` can be edited. The `text` and `html` fields of the message will be updated.
  ///
  /// \[id\]: ID of the message
  Future<ConversationsMessage> updateAMessageSentByAnAgent(
      PutConversationsMessagesIdRequest request,
      {required String id,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'PUT',
        path: '/conversations/messages/${brevoPathSegment(id)}',
        body: request.toJson(),
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }

  /// `PUT /conversations/pushedMessages/{id}`
  ///
  /// Update an automated message
  ///
  /// Update the text of an automated (pushed) message. The `text` and `html` fields of the message will be updated.
  ///
  /// \[id\]: ID of the message
  Future<ConversationsMessage> updateAnAutomatedMessage(
      PutConversationsPushedMessagesIdRequest request,
      {required String id,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'PUT',
        path: '/conversations/pushedMessages/${brevoPathSegment(id)}',
        body: request.toJson(),
        options: options);
    return ConversationsMessage.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'ConversationsMessage'));
  }
}
