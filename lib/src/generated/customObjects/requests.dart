// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /objects/{object_type}/batch/delete`.
final class BatchDeleteObjectRecordsRequest {
  /// Creates the request body.
  const BatchDeleteObjectRecordsRequest({
    this.identifiers,
  });

  /// Decodes a JSON object into a BatchDeleteObjectRecordsRequest. Optional fields absent from [json] stay null.
  factory BatchDeleteObjectRecordsRequest.fromJson(Map<String, Object?> json) =>
      BatchDeleteObjectRecordsRequest(
        identifiers: json.optNested(
            'identifiers', BatchDeleteObjectRecordsRequestIdentifiers.fromJson),
      );

  /// Either `ids` or `ext_ids` must be provided, but not both in the same request.
  final BatchDeleteObjectRecordsRequestIdentifiers? identifiers;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
      };
}

/// Either `ids` or `ext_ids` must be provided, but not both in the same request.
final class BatchDeleteObjectRecordsRequestIdentifiers {
  /// Creates the request body.
  const BatchDeleteObjectRecordsRequestIdentifiers({
    this.ids,
    this.extIds,
  });

  /// Decodes a JSON object into a BatchDeleteObjectRecordsRequestIdentifiers. Optional fields absent from [json] stay null.
  factory BatchDeleteObjectRecordsRequestIdentifiers.fromJson(
          Map<String, Object?> json) =>
      BatchDeleteObjectRecordsRequestIdentifiers(
        ids: json.containsKey('ids') ? json.optIntList('ids') : null,
        extIds:
            json.containsKey('ext_ids') ? json.optStringList('ext_ids') : null,
      );

  /// List of object record IDs
  final List<int>? ids;

  /// List of external record identifiers
  final List<String>? extIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (ids != null) 'ids': ids!,
        if (extIds != null) 'ext_ids': extIds!,
      };
}

/// The request body of `POST /objects/{object_type}/batch/upsert`.
final class UpsertrecordsRequest {
  /// Creates the request body.
  const UpsertrecordsRequest({
    required this.records,
  });

  /// Decodes a JSON object into a UpsertrecordsRequest. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequest.fromJson(Map<String, Object?> json) =>
      UpsertrecordsRequest(
        records: json.containsKey('records')
            ? json.optObjectList(
                'records', UpsertrecordsRequestRecordsItem.fromJson,
                objectName: 'UpsertrecordsRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpsertrecordsRequest', key: 'records')),
      );

  /// List of object records to be upsert. Each record can have attributes, identifiers, and associations.
  final List<UpsertrecordsRequestRecordsItem> records;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'records': [for (final item in records) item.toJson()],
      };
}

/// The request body of `UpsertrecordsRequest.recordsItem`.
final class UpsertrecordsRequestRecordsItem {
  /// Creates the request body.
  const UpsertrecordsRequestRecordsItem({
    this.associations,
    this.attributes,
    this.identifiers,
  });

  /// Decodes a JSON object into a UpsertrecordsRequestRecordsItem. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequestRecordsItem.fromJson(Map<String, Object?> json) =>
      UpsertrecordsRequestRecordsItem(
        associations: json.containsKey('associations')
            ? json.optObjectList('associations',
                UpsertrecordsRequestRecordsItemAssociationsItem.fromJson,
                objectName: 'UpsertrecordsRequestRecordsItem')
            : null,
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        identifiers: json.optNested(
            'identifiers', UpsertrecordsRequestRecordsItemIdentifiers.fromJson),
      );

  /// List of associations for the object record. Maximum 20 total entries allowed, with a maximum of 10 link actions and 10 unlink actions. Each entry specifies an object_type and an action (link or unlink). Unlink is idempotent — removing a non-existing association does not return an error. Both associated records must already exist before creating a link.
  final List<UpsertrecordsRequestRecordsItemAssociationsItem>? associations;

  /// Key-value pairs of attribute data for the object record. Each key must be the attribute **key** (e.g., `company_name`, `engine_type`), not the attribute label (e.g., "Company Name", "Engine Type"). Only attributes that already exist in the object schema will be accepted — unknown keys are silently ignored. Minimum 1 attribute is required.
  ///
  /// For `category` or `multiple_category` attributes, set the value to the option **key** (not the option label or option ID). You can find available option keys in the `options` array returned by the GET attributes endpoint, or in the frontend under each attribute's settings.
  ///
  /// For example, use the option key "hybrid" not the label "Hybrid" — using the label will be silently ignored.
  ///
  /// For `multiple_choice` type, pass an **array of option keys** (not display labels). For example, if options have keys `"sunroof"` and `"leather_seats"`, pass `\["sunroof", "leather_seats"\]`.
  final Map<String, Object?>? attributes;

  /// Identifiers for the object record. Supports `id` (singular, not `ids`) or `ext_id`.
  /// - `ext_id`: Your external system's identifier for this record.
  /// - `id`: Internal Brevo record ID — can only be used for **updating** existing records, not for creation.
  /// NOTE:
  /// - This is an optional field. If no identifier is provided, Brevo auto-generates an internal ID that you can use for future operations.
  /// - If both `ext_id` and `id` are provided, `id` takes precedence and the operation is treated as an update. It is recommended to provide only one identifier per record.
  final UpsertrecordsRequestRecordsItemIdentifiers? identifiers;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (associations != null)
          'associations': [for (final item in associations!) item.toJson()],
        if (attributes != null) 'attributes': attributes!,
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
      };
}

/// The request body of `UpsertrecordsRequestRecordsItem.associationsItem`.
final class UpsertrecordsRequestRecordsItemAssociationsItem {
  /// Creates the request body.
  const UpsertrecordsRequestRecordsItemAssociationsItem({
    this.action,
    required this.objectType,
    required this.records,
  });

  /// Decodes a JSON object into a UpsertrecordsRequestRecordsItemAssociationsItem. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequestRecordsItemAssociationsItem.fromJson(
          Map<String, Object?> json) =>
      UpsertrecordsRequestRecordsItemAssociationsItem(
        action: json.optEnum('action',
            UpsertrecordsRequestRecordsItemAssociationsItemAction.fromWire),
        objectType: json.requireString(
            'object_type', 'UpsertrecordsRequestRecordsItemAssociationsItem'),
        records: json.containsKey('records')
            ? json.optObjectList(
                'records',
                UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem
                    .fromJson,
                objectName: 'UpsertrecordsRequestRecordsItemAssociationsItem')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpsertrecordsRequestRecordsItemAssociationsItem',
                key: 'records')),
      );

  /// Action to perform on the association. - `link`: Create association between records (default if not provided) - `unlink`: Remove association between records. Idempotent — unlinking a non-existing association is a no-op success.
  final UpsertrecordsRequestRecordsItemAssociationsItemAction? action;

  /// Type of the associated object
  final String objectType;

  /// The `records` field.
  final List<UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem>
      records;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (action != null) 'action': action!.value,
        'object_type': objectType,
        'records': [for (final item in records) item.toJson()],
      };
}

/// The request body of `UpsertrecordsRequestRecordsItemAssociationsItem.recordsItem`.
final class UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem {
  /// Creates the request body.
  const UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem({
    this.identifiers,
  });

  /// Decodes a JSON object into a UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem.fromJson(
          Map<String, Object?> json) =>
      UpsertrecordsRequestRecordsItemAssociationsItemRecordsItem(
        identifiers: json.optNested(
            'identifiers',
            UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers
                .fromJson),
      );

  /// Identifiers for the associated object record. Provide either `ext_id` or `id`.
  final UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers?
      identifiers;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
      };
}

/// Identifiers for the associated object record. Provide either `ext_id` or `id`.
final class UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers {
  /// Creates the request body.
  const UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers({
    this.extId,
    this.id,
  });

  /// Decodes a JSON object into a UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers.fromJson(
          Map<String, Object?> json) =>
      UpsertrecordsRequestRecordsItemAssociationsItemRecordsItemIdentifiers(
        extId: json.optString('ext_id'),
        id: json.optInt('id'),
      );

  /// External ID of the associated record in your system.
  final String? extId;

  /// Internal ID of the associated object record generated by Brevo.
  final int? id;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
      };
}

/// Identifiers for the object record. Supports `id` (singular, not `ids`) or `ext_id`.
/// - `ext_id`: Your external system's identifier for this record.
/// - `id`: Internal Brevo record ID — can only be used for **updating** existing records, not for creation.
/// NOTE:
/// - This is an optional field. If no identifier is provided, Brevo auto-generates an internal ID that you can use for future operations.
/// - If both `ext_id` and `id` are provided, `id` takes precedence and the operation is treated as an update. It is recommended to provide only one identifier per record.
final class UpsertrecordsRequestRecordsItemIdentifiers {
  /// Creates the request body.
  const UpsertrecordsRequestRecordsItemIdentifiers({
    this.extId,
    this.id,
  });

  /// Decodes a JSON object into a UpsertrecordsRequestRecordsItemIdentifiers. Optional fields absent from [json] stay null.
  factory UpsertrecordsRequestRecordsItemIdentifiers.fromJson(
          Map<String, Object?> json) =>
      UpsertrecordsRequestRecordsItemIdentifiers(
        extId: json.optString('ext_id'),
        id: json.optInt('id'),
      );

  /// ext_id is ID of record in the external system that client wants to store in the object system.
  final String? extId;

  /// Internal ID of the object record generated by Brevo
  final int? id;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
      };
}
