// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `POST /objects/{object_type}/batch/delete` object of the Brevo API.
final class BatchDeleteObjectRecordsResponse {
  /// Creates a BatchDeleteObjectRecordsResponse from already-decoded fields.
  const BatchDeleteObjectRecordsResponse({
    this.message,
    this.processId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a BatchDeleteObjectRecordsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory BatchDeleteObjectRecordsResponse.fromJson(
          Map<String, Object?> json) =>
      BatchDeleteObjectRecordsResponse(
        message: json.optString('message'),
        processId: json.optInt('processId'),
        raw: json,
      );

  /// The `message` field.
  final String? message;

  /// Identifier for batch process tracking
  final int? processId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (message != null) 'message': message!,
        if (processId != null) 'processId': processId!,
      };
}

/// The `GET /objects/{object_type}/associated-records` object of the Brevo API.
final class GetAssociatedRecordsResponse {
  /// Creates a GetAssociatedRecordsResponse from already-decoded fields.
  const GetAssociatedRecordsResponse({
    this.hasMore,
    this.items = const [],
    this.offset,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAssociatedRecordsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAssociatedRecordsResponse.fromJson(Map<String, Object?> json) =>
      GetAssociatedRecordsResponse(
        hasMore: json.optBool('has_more'),
        items: json.optObjectList(
            'items', GetAssociatedRecordsResponseItemsItem.fromJson,
            objectName: 'GetAssociatedRecordsResponse'),
        offset: json.optInt('offset'),
        raw: json,
      );

  /// `true` when at least one more associated record exists after this page.
  final bool? hasMore;

  /// Associated records for this page, up to 20 per page. Fewer when the last page is reached, empty when the source record has no matching associations.
  final List<GetAssociatedRecordsResponseItemsItem> items;

  /// The offset that was requested.
  final int? offset;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (hasMore != null) 'has_more': hasMore!,
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
        if (offset != null) 'offset': offset!,
      };
}

/// The `GetAssociatedRecordsResponse.itemsItem` object of the Brevo API.
final class GetAssociatedRecordsResponseItemsItem {
  /// Creates a GetAssociatedRecordsResponseItemsItem from already-decoded fields.
  const GetAssociatedRecordsResponseItemsItem({
    this.object,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAssociatedRecordsResponseItemsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAssociatedRecordsResponseItemsItem.fromJson(
          Map<String, Object?> json) =>
      GetAssociatedRecordsResponseItemsItem(
        object: json.optNested(
            'object', GetAssociatedRecordsResponseItemsItemObject.fromJson),
        type: json.optString('type'),
        raw: json,
      );

  /// The associated record. `ext_id`, `attributes`, `created_at` and `updated_at` are omitted when they are not available for the record.
  final GetAssociatedRecordsResponseItemsItemObject? object;

  /// Object type of the associated record.
  final String? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (object != null) 'object': object!.toJson(),
        if (type != null) 'type': type!,
      };
}

/// The associated record. `ext_id`, `attributes`, `created_at` and `updated_at` are omitted when they are not available for the record.
final class GetAssociatedRecordsResponseItemsItemObject {
  /// Creates a GetAssociatedRecordsResponseItemsItemObject from already-decoded fields.
  const GetAssociatedRecordsResponseItemsItemObject({
    this.attributes = const {},
    this.createdAt,
    this.extId,
    this.id,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAssociatedRecordsResponseItemsItemObject. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAssociatedRecordsResponseItemsItemObject.fromJson(
          Map<String, Object?> json) =>
      GetAssociatedRecordsResponseItemsItemObject(
        attributes: json.optObjectMap('attributes'),
        createdAt: json.optString('created_at'),
        extId: json.optString('ext_id'),
        id: json.optInt('id'),
        updatedAt: json.optString('updated_at'),
        raw: json,
      );

  /// Key-value pairs of attribute data for the associated record. Each key is the attribute **key** (e.g., `company_name`, `engine_type`), not the attribute label (e.g., "Company Name", "Engine Type").
  /// For `category` or `multiple_category` attributes, the value is the option **key** (not the option label or option ID).
  /// For contacts, attribute keys are returned in lowercase, for example `email`, `first_name`, `last_name`, `sms` and `ext_id`.
  final Map<String, Object?> attributes;

  /// Timestamp when the associated record was created. Not returned for contacts.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// External ID of the associated record in your system. Not returned for contacts, whose external ID is available as `attributes.ext_id`.
  final String? extId;

  /// Internal ID of the associated record generated by Brevo.
  final int? id;

  /// Timestamp when the associated record was last updated. Not returned for contacts.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (createdAt != null) 'created_at': createdAt!,
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
        if (updatedAt != null) 'updated_at': updatedAt!,
      };
}

/// The `GET /objects/{object_type}/records` object of the Brevo API.
final class GetrecordsResponse {
  /// Creates a GetrecordsResponse from already-decoded fields.
  const GetrecordsResponse({
    this.count,
    this.records = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponse.fromJson(Map<String, Object?> json) =>
      GetrecordsResponse(
        count: json.optInt('count'),
        records: json.optObjectList(
            'records', GetrecordsResponseRecordsItem.fromJson,
            objectName: 'GetrecordsResponse'),
        raw: json,
      );

  /// Total number of object records for an object type.
  final int? count;

  /// The `records` field.
  final List<GetrecordsResponseRecordsItem> records;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (records.isNotEmpty)
          'records': [for (final item in records) item.toJson()],
      };
}

/// The `GetrecordsResponse.recordsItem` object of the Brevo API.
final class GetrecordsResponseRecordsItem {
  /// Creates a GetrecordsResponseRecordsItem from already-decoded fields.
  const GetrecordsResponseRecordsItem({
    this.associations = const [],
    this.attributes = const {},
    this.createdAt,
    this.identifiers,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponseRecordsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponseRecordsItem.fromJson(Map<String, Object?> json) =>
      GetrecordsResponseRecordsItem(
        associations: json.optObjectList('associations',
            GetrecordsResponseRecordsItemAssociationsItem.fromJson,
            objectName: 'GetrecordsResponseRecordsItem'),
        attributes: json.optObjectMap('attributes'),
        createdAt: json.optString('createdAt'),
        identifiers: json.optNested(
            'identifiers', GetrecordsResponseRecordsItemIdentifiers.fromJson),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// List of associations for the object record. If association query param is true it will return 5 associated records per association.
  final List<GetrecordsResponseRecordsItemAssociationsItem> associations;

  /// Key-value pairs of attribute data for the object record. Each key is the attribute **key** (e.g., `company_name`, `engine_type`), not the attribute label (e.g., "Company Name", "Engine Type"). Only attributes that already exist in the object schema will be present.
  /// For `category` or `multiple_category` attributes, the value is the option **key** (not the option label or option ID).
  final Map<String, Object?> attributes;

  /// Timestamp when the object record was created
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Identifiers for the object record. Supports `id` (singular, not `ids`) or `ext_id`.
  /// - `ext_id`: Your external system's identifier for this record.
  /// - `id`: Internal Brevo record ID.
  final GetrecordsResponseRecordsItemIdentifiers? identifiers;

  /// Timestamp when the object record was last updated
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (associations.isNotEmpty)
          'associations': [for (final item in associations) item.toJson()],
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (createdAt != null) 'createdAt': createdAt!,
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `GetrecordsResponseRecordsItem.associationsItem` object of the Brevo API.
final class GetrecordsResponseRecordsItemAssociationsItem {
  /// Creates a GetrecordsResponseRecordsItemAssociationsItem from already-decoded fields.
  const GetrecordsResponseRecordsItemAssociationsItem({
    this.objectType,
    this.records = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponseRecordsItemAssociationsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponseRecordsItemAssociationsItem.fromJson(
          Map<String, Object?> json) =>
      GetrecordsResponseRecordsItemAssociationsItem(
        objectType: json.optString('object_type'),
        records: json.optObjectList('records',
            GetrecordsResponseRecordsItemAssociationsItemRecordsItem.fromJson,
            objectName: 'GetrecordsResponseRecordsItemAssociationsItem'),
        raw: json,
      );

  /// Type of the associated object
  final String? objectType;

  /// The `records` field.
  final List<GetrecordsResponseRecordsItemAssociationsItemRecordsItem> records;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (objectType != null) 'object_type': objectType!,
        if (records.isNotEmpty)
          'records': [for (final item in records) item.toJson()],
      };
}

/// The `GetrecordsResponseRecordsItemAssociationsItem.recordsItem` object of the Brevo API.
final class GetrecordsResponseRecordsItemAssociationsItemRecordsItem {
  /// Creates a GetrecordsResponseRecordsItemAssociationsItemRecordsItem from already-decoded fields.
  const GetrecordsResponseRecordsItemAssociationsItemRecordsItem({
    this.identifiers,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponseRecordsItemAssociationsItemRecordsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponseRecordsItemAssociationsItemRecordsItem.fromJson(
          Map<String, Object?> json) =>
      GetrecordsResponseRecordsItemAssociationsItemRecordsItem(
        identifiers: json.optNested(
            'identifiers',
            GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers
                .fromJson),
        raw: json,
      );

  /// Identifiers attached with the associated object record. Only includes the internal ID.
  final GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers?
      identifiers;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
      };
}

/// Identifiers attached with the associated object record. Only includes the internal ID.
final class GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers {
  /// Creates a GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers from already-decoded fields.
  const GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers.fromJson(
          Map<String, Object?> json) =>
      GetrecordsResponseRecordsItemAssociationsItemRecordsItemIdentifiers(
        id: json.optInt('id'),
        raw: json,
      );

  /// Internal ID of the insurance object record generated by Brevo
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// Identifiers for the object record. Supports `id` (singular, not `ids`) or `ext_id`.
/// - `ext_id`: Your external system's identifier for this record.
/// - `id`: Internal Brevo record ID.
final class GetrecordsResponseRecordsItemIdentifiers {
  /// Creates a GetrecordsResponseRecordsItemIdentifiers from already-decoded fields.
  const GetrecordsResponseRecordsItemIdentifiers({
    this.extId,
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetrecordsResponseRecordsItemIdentifiers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetrecordsResponseRecordsItemIdentifiers.fromJson(
          Map<String, Object?> json) =>
      GetrecordsResponseRecordsItemIdentifiers(
        extId: json.optString('ext_id'),
        id: json.optInt('id'),
        raw: json,
      );

  /// External ID of the record in your system.
  final String? extId;

  /// Internal ID of the object record generated by Brevo.
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
      };
}

/// The `POST /objects/{object_type}/batch/upsert` object of the Brevo API.
final class UpsertrecordsResponse {
  /// Creates a UpsertrecordsResponse from already-decoded fields.
  const UpsertrecordsResponse({
    this.message,
    this.processId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a UpsertrecordsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory UpsertrecordsResponse.fromJson(Map<String, Object?> json) =>
      UpsertrecordsResponse(
        message: json.optString('message'),
        processId: json.optInt('processId'),
        raw: json,
      );

  /// The `message` field.
  final String? message;

  /// Unique Id for the batch process used to track the status of the batch. **How to use this processId:**   Refer to the \[Get process status API\](https://developers.brevo.com/reference/get-process) to check the execution status of this batch using the returned `processId`.
  final int? processId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (message != null) 'message': message!,
        if (processId != null) 'processId': processId!,
      };
}
