// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import '../shared/models.dart';
import 'enums.dart';

/// A contact's membership status in a single consent group.
final class ConsentGroupItem {
  /// Creates a ConsentGroupItem from already-decoded fields.
  const ConsentGroupItem({
    this.id,
    this.status,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConsentGroupItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConsentGroupItem.fromJson(Map<String, Object?> json) =>
      ConsentGroupItem(
        id: json.optInt('id'),
        status: json.optEnum('status', ConsentGroupItemStatus.fromWire),
        raw: json,
      );

  /// ID of the consent group
  final int? id;

  /// The contact's subscription status within this consent group
  final ConsentGroupItemStatus? status;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (status != null) 'status': status!.value,
      };
}

/// The `components/schemas/contactErrorModel` object of the Brevo API.
final class ContactErrorModel {
  /// Creates a ContactErrorModel from already-decoded fields.
  const ContactErrorModel({
    this.code,
    this.message,
    this.metadata = const {},
    this.raw = const {},
  });

  /// Decodes a JSON object into a ContactErrorModel. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ContactErrorModel.fromJson(Map<String, Object?> json) =>
      ContactErrorModel(
        code: json.optEnum('code', ContactErrorModelCode.fromWire),
        message: json.optString('message'),
        metadata: json.optObjectMap('metadata'),
        raw: json,
      );

  /// Error code displayed in case of a failure
  final ContactErrorModelCode? code;

  /// Readable message associated to the failure
  final String? message;

  /// Additional information about the error
  final Map<String, Object?> metadata;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (code != null) 'code': code!.value,
        if (message != null) 'message': message!,
        if (metadata.isNotEmpty) 'metadata': metadata,
      };
}

/// The `POST /contacts` object of the Brevo API.
final class CreateContactResponseUndefined {
  /// Creates a CreateContactResponseUndefined from already-decoded fields.
  const CreateContactResponseUndefined({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateContactResponseUndefined. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateContactResponseUndefined.fromJson(Map<String, Object?> json) =>
      CreateContactResponseUndefined(
        id: json.optInt('id'),
        raw: json,
      );

  /// ID of the contact when a new contact is created
  final int? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `GET /contacts/attributes` object of the Brevo API.
final class GetAttributesResponse {
  /// Creates a GetAttributesResponse from already-decoded fields.
  const GetAttributesResponse({
    this.attributes = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAttributesResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAttributesResponse.fromJson(Map<String, Object?> json) =>
      GetAttributesResponse(
        attributes: json.optObjectList(
            'attributes', GetAttributesResponseAttributesItem.fromJson,
            objectName: 'GetAttributesResponse'),
        raw: json,
      );

  /// Listing of available contact attributes in your account
  final List<GetAttributesResponseAttributesItem> attributes;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty)
          'attributes': [for (final item in attributes) item.toJson()],
      };
}

/// The `GetAttributesResponse.attributesItem` object of the Brevo API.
final class GetAttributesResponseAttributesItem {
  /// Creates a GetAttributesResponseAttributesItem from already-decoded fields.
  const GetAttributesResponseAttributesItem({
    this.calculatedValue,
    this.category,
    this.enumeration = const [],
    this.multiCategoryOptions = const [],
    this.name,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAttributesResponseAttributesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAttributesResponseAttributesItem.fromJson(
          Map<String, Object?> json) =>
      GetAttributesResponseAttributesItem(
        calculatedValue: json.optString('calculatedValue'),
        category: json.optEnum(
            'category', GetAttributesResponseAttributesItemCategory.fromWire),
        enumeration: json.optObjectList('enumeration',
            GetAttributesResponseAttributesItemEnumerationItem.fromJson,
            objectName: 'GetAttributesResponseAttributesItem'),
        multiCategoryOptions: json.optStringList('multiCategoryOptions'),
        name: json.optString('name'),
        type: json.optEnum(
            'type', GetAttributesResponseAttributesItemType.fromWire),
        raw: json,
      );

  /// Calculated value formula
  final String? calculatedValue;

  /// Category of the attribute
  final GetAttributesResponseAttributesItemCategory? category;

  /// Parameter only available for "category" type attributes.
  final List<GetAttributesResponseAttributesItemEnumerationItem> enumeration;

  /// Parameter only available for "multiple-choice" type attributes.
  final List<String> multiCategoryOptions;

  /// Name of the attribute
  final String? name;

  /// Type of the attribute
  final GetAttributesResponseAttributesItemType? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (calculatedValue != null) 'calculatedValue': calculatedValue!,
        if (category != null) 'category': category!.value,
        if (enumeration.isNotEmpty)
          'enumeration': [for (final item in enumeration) item.toJson()],
        if (multiCategoryOptions.isNotEmpty)
          'multiCategoryOptions': multiCategoryOptions,
        if (name != null) 'name': name!,
        if (type != null) 'type': type!.value,
      };
}

/// The `GetAttributesResponseAttributesItem.enumerationItem` object of the Brevo API.
final class GetAttributesResponseAttributesItemEnumerationItem {
  /// Creates a GetAttributesResponseAttributesItemEnumerationItem from already-decoded fields.
  const GetAttributesResponseAttributesItemEnumerationItem({
    this.label,
    this.valueValue,
    this.valueStr,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAttributesResponseAttributesItemEnumerationItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAttributesResponseAttributesItemEnumerationItem.fromJson(
          Map<String, Object?> json) =>
      GetAttributesResponseAttributesItemEnumerationItem(
        label: json.optString('label'),
        valueValue: json.optInt('value'),
        valueStr: json.optString('valueStr'),
        raw: json,
      );

  /// Label of the "category" type attribute
  final String? label;

  /// Numeric ID of the "category" type attribute value. Set to 0 when the raw value cannot be converted to an integer (for example non-numeric values such as "en" or "fr"). Refer to `valueStr` for the original string representation.
  final int? valueValue;

  /// String representation of the "category" type attribute value. Always contains the original value as stored. Use this field when the attribute value is non-numeric (e.g. "en", "fr") or when you need the exact string form alongside the numeric `value`.
  final String? valueStr;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (label != null) 'label': label!,
        if (valueValue != null) 'value': valueValue!,
        if (valueStr != null) 'valueStr': valueStr!,
      };
}

/// The `components/schemas/getContactDetails` object of the Brevo API.
final class GetContactDetails {
  /// Creates a GetContactDetails from already-decoded fields.
  const GetContactDetails({
    this.attributes = const {},
    this.consentGroups = const [],
    this.createdAt,
    this.email,
    this.emailBlacklisted,
    this.id,
    this.listIds = const [],
    this.listUnsubscribed = const [],
    this.modifiedAt,
    this.smsBlacklisted,
    this.whatsappBlacklisted,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactDetails. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactDetails.fromJson(Map<String, Object?> json) =>
      GetContactDetails(
        attributes: json.optObjectMap('attributes'),
        consentGroups: json.optObjectList(
            'consentGroups', ConsentGroupItem.fromJson,
            objectName: 'GetContactDetails'),
        createdAt: json.optString('createdAt'),
        email: json.optString('email'),
        emailBlacklisted: json.optBool('emailBlacklisted'),
        id: json.optInt('id'),
        listIds: json.optIntList('listIds'),
        listUnsubscribed: json.optIntList('listUnsubscribed'),
        modifiedAt: json.optString('modifiedAt'),
        smsBlacklisted: json.optBool('smsBlacklisted'),
        whatsappBlacklisted: json.optBool('whatsappBlacklisted'),
        raw: json,
      );

  /// Set of attributes of the contact
  final Map<String, Object?> attributes;

  /// Consent groups the contact belongs to, with their subscription status. Only present when the Consent Groups feature is enabled for your account.
  final List<ConsentGroupItem> consentGroups;

  /// Creation UTC date-time of the contact (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Email address of the contact for which you requested the details
  final String? email;

  /// Blacklist status for email campaigns (true=blacklisted, false=not blacklisted)
  final bool? emailBlacklisted;

  /// ID of the contact for which you requested the details
  final int? id;

  /// The `listIds` field.
  final List<int> listIds;

  /// The `listUnsubscribed` field.
  final List<int> listUnsubscribed;

  /// Last modification UTC date-time of the contact (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Blacklist status for SMS campaigns (true=blacklisted, false=not blacklisted)
  final bool? smsBlacklisted;

  /// Blacklist status for WhatsApp campaigns (true=blacklisted, false=not blacklisted)
  final bool? whatsappBlacklisted;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (consentGroups.isNotEmpty)
          'consentGroups': [for (final item in consentGroups) item.toJson()],
        if (createdAt != null) 'createdAt': createdAt!,
        if (email != null) 'email': email!,
        if (emailBlacklisted != null) 'emailBlacklisted': emailBlacklisted!,
        if (id != null) 'id': id!,
        if (listIds.isNotEmpty) 'listIds': listIds,
        if (listUnsubscribed.isNotEmpty) 'listUnsubscribed': listUnsubscribed,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (smsBlacklisted != null) 'smsBlacklisted': smsBlacklisted!,
        if (whatsappBlacklisted != null)
          'whatsappBlacklisted': whatsappBlacklisted!,
      };
}

/// The `GET /contacts/{identifier}` object of the Brevo API.
final class GetContactInfoResponse {
  /// Creates a GetContactInfoResponse from already-decoded fields.
  const GetContactInfoResponse({
    this.attributes = const {},
    this.consentGroups = const [],
    this.createdAt,
    this.email,
    this.emailBlacklisted,
    this.id,
    this.listIds = const [],
    this.listUnsubscribed = const [],
    this.modifiedAt,
    this.smsBlacklisted,
    this.whatsappBlacklisted,
    this.statistics,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponse.fromJson(Map<String, Object?> json) =>
      GetContactInfoResponse(
        attributes: json.optObjectMap('attributes'),
        consentGroups: json.optObjectList(
            'consentGroups', ConsentGroupItem.fromJson,
            objectName: 'GetContactInfoResponse'),
        createdAt: json.optString('createdAt'),
        email: json.optString('email'),
        emailBlacklisted: json.optBool('emailBlacklisted'),
        id: json.optInt('id'),
        listIds: json.optIntList('listIds'),
        listUnsubscribed: json.optIntList('listUnsubscribed'),
        modifiedAt: json.optString('modifiedAt'),
        smsBlacklisted: json.optBool('smsBlacklisted'),
        whatsappBlacklisted: json.optBool('whatsappBlacklisted'),
        statistics: json.optNested(
            'statistics', GetContactInfoResponseStatistics.fromJson),
        raw: json,
      );

  /// Set of attributes of the contact
  final Map<String, Object?> attributes;

  /// Consent groups the contact belongs to, with their subscription status. Only present when the Consent Groups feature is enabled for your account.
  final List<ConsentGroupItem> consentGroups;

  /// Creation UTC date-time of the contact (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Email address of the contact for which you requested the details
  final String? email;

  /// Blacklist status for email campaigns (true=blacklisted, false=not blacklisted)
  final bool? emailBlacklisted;

  /// ID of the contact for which you requested the details
  final int? id;

  /// The `listIds` field.
  final List<int> listIds;

  /// The `listUnsubscribed` field.
  final List<int> listUnsubscribed;

  /// Last modification UTC date-time of the contact (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? modifiedAt;

  /// [modifiedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get modifiedAtDate =>
      modifiedAt == null ? null : DateTime.tryParse(modifiedAt!);

  /// Blacklist status for SMS campaigns (true=blacklisted, false=not blacklisted)
  final bool? smsBlacklisted;

  /// Blacklist status for WhatsApp campaigns (true=blacklisted, false=not blacklisted)
  final bool? whatsappBlacklisted;

  /// Campaign statistics of the contact
  final GetContactInfoResponseStatistics? statistics;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attributes.isNotEmpty) 'attributes': attributes,
        if (consentGroups.isNotEmpty)
          'consentGroups': [for (final item in consentGroups) item.toJson()],
        if (createdAt != null) 'createdAt': createdAt!,
        if (email != null) 'email': email!,
        if (emailBlacklisted != null) 'emailBlacklisted': emailBlacklisted!,
        if (id != null) 'id': id!,
        if (listIds.isNotEmpty) 'listIds': listIds,
        if (listUnsubscribed.isNotEmpty) 'listUnsubscribed': listUnsubscribed,
        if (modifiedAt != null) 'modifiedAt': modifiedAt!,
        if (smsBlacklisted != null) 'smsBlacklisted': smsBlacklisted!,
        if (whatsappBlacklisted != null)
          'whatsappBlacklisted': whatsappBlacklisted!,
        if (statistics != null) 'statistics': statistics!.toJson(),
      };
}

/// Campaign statistics of the contact
final class GetContactInfoResponseStatistics {
  /// Creates a GetContactInfoResponseStatistics from already-decoded fields.
  const GetContactInfoResponseStatistics({
    this.clicked = const [],
    this.complaints = const [],
    this.delivered = const [],
    this.hardBounces = const [],
    this.messagesSent = const [],
    this.opened = const [],
    this.softBounces = const [],
    this.transacAttributes = const [],
    this.unsubscriptions,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatistics. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatistics.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatistics(
        clicked: json.optObjectList(
            'clicked', GetContactInfoResponseStatisticsClickedItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        complaints: json.optObjectList('complaints',
            GetContactInfoResponseStatisticsComplaintsItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        delivered: json.optObjectList(
            'delivered', GetContactInfoResponseStatisticsDeliveredItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        hardBounces: json.optObjectList('hardBounces',
            GetContactInfoResponseStatisticsHardBouncesItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        messagesSent: json.optObjectList('messagesSent',
            GetContactInfoResponseStatisticsMessagesSentItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        opened: json.optObjectList(
            'opened', GetContactInfoResponseStatisticsOpenedItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        softBounces: json.optObjectList('softBounces',
            GetContactInfoResponseStatisticsSoftBouncesItem.fromJson,
            objectName: 'GetContactInfoResponseStatistics'),
        transacAttributes: json.optList(
            'transacAttributes',
            (element) => element is Map<String, Object?>
                ? element
                : <String, Object?>{}),
        unsubscriptions: json.optNested('unsubscriptions',
            GetContactInfoResponseStatisticsUnsubscriptions.fromJson),
        raw: json,
      );

  /// Listing of the clicks generated by the contact
  final List<GetContactInfoResponseStatisticsClickedItem> clicked;

  /// Listing of the complaints generated by the contact
  final List<GetContactInfoResponseStatisticsComplaintsItem> complaints;

  /// Listing of the delivered campaign for the contact
  final List<GetContactInfoResponseStatisticsDeliveredItem> delivered;

  /// Listing of the hard bounces generated by the contact
  final List<GetContactInfoResponseStatisticsHardBouncesItem> hardBounces;

  /// Listing of the sent campaign for the contact
  final List<GetContactInfoResponseStatisticsMessagesSentItem> messagesSent;

  /// Listing of the openings generated by the contact
  final List<GetContactInfoResponseStatisticsOpenedItem> opened;

  /// Listing of the soft bounces generated by the contact
  final List<GetContactInfoResponseStatisticsSoftBouncesItem> softBounces;

  /// Listing of the transactional attributes for the contact
  final List<Map<String, Object?>> transacAttributes;

  /// Listing of the unsubscription for the contact
  final GetContactInfoResponseStatisticsUnsubscriptions? unsubscriptions;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (clicked.isNotEmpty)
          'clicked': [for (final item in clicked) item.toJson()],
        if (complaints.isNotEmpty)
          'complaints': [for (final item in complaints) item.toJson()],
        if (delivered.isNotEmpty)
          'delivered': [for (final item in delivered) item.toJson()],
        if (hardBounces.isNotEmpty)
          'hardBounces': [for (final item in hardBounces) item.toJson()],
        if (messagesSent.isNotEmpty)
          'messagesSent': [for (final item in messagesSent) item.toJson()],
        if (opened.isNotEmpty)
          'opened': [for (final item in opened) item.toJson()],
        if (softBounces.isNotEmpty)
          'softBounces': [for (final item in softBounces) item.toJson()],
        if (transacAttributes.isNotEmpty)
          'transacAttributes': transacAttributes,
        if (unsubscriptions != null)
          'unsubscriptions': unsubscriptions!.toJson(),
      };
}

/// The `GetContactInfoResponseStatistics.clickedItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsClickedItem {
  /// Creates a GetContactInfoResponseStatisticsClickedItem from already-decoded fields.
  const GetContactInfoResponseStatisticsClickedItem({
    this.campaignId,
    this.links = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsClickedItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsClickedItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsClickedItem(
        campaignId: json.optInt('campaignId'),
        links: json.optObjectList('links',
            GetContactInfoResponseStatisticsClickedItemLinksItem.fromJson,
            objectName: 'GetContactInfoResponseStatisticsClickedItem'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// Listing of the clicked links for the campaign
  final List<GetContactInfoResponseStatisticsClickedItemLinksItem> links;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (links.isNotEmpty)
          'links': [for (final item in links) item.toJson()],
      };
}

/// The `GetContactInfoResponseStatisticsClickedItem.linksItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsClickedItemLinksItem {
  /// Creates a GetContactInfoResponseStatisticsClickedItemLinksItem from already-decoded fields.
  const GetContactInfoResponseStatisticsClickedItemLinksItem({
    this.count,
    this.eventTime,
    this.ip,
    this.url,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsClickedItemLinksItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsClickedItemLinksItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsClickedItemLinksItem(
        count: json.optInt('count'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        url: json.optString('url'),
        raw: json,
      );

  /// Number of clicks on this link for the campaign
  final int? count;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has clicked on the link
  final String? ip;

  /// URL of the clicked link
  final String? url;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
        if (url != null) 'url': url!,
      };
}

/// The `GetContactInfoResponseStatistics.complaintsItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsComplaintsItem {
  /// Creates a GetContactInfoResponseStatisticsComplaintsItem from already-decoded fields.
  const GetContactInfoResponseStatisticsComplaintsItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsComplaintsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsComplaintsItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsComplaintsItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactInfoResponseStatistics.deliveredItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsDeliveredItem {
  /// Creates a GetContactInfoResponseStatisticsDeliveredItem from already-decoded fields.
  const GetContactInfoResponseStatisticsDeliveredItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsDeliveredItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsDeliveredItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsDeliveredItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactInfoResponseStatistics.hardBouncesItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsHardBouncesItem {
  /// Creates a GetContactInfoResponseStatisticsHardBouncesItem from already-decoded fields.
  const GetContactInfoResponseStatisticsHardBouncesItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsHardBouncesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsHardBouncesItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsHardBouncesItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactInfoResponseStatistics.messagesSentItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsMessagesSentItem {
  /// Creates a GetContactInfoResponseStatisticsMessagesSentItem from already-decoded fields.
  const GetContactInfoResponseStatisticsMessagesSentItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsMessagesSentItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsMessagesSentItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsMessagesSentItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactInfoResponseStatistics.openedItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsOpenedItem {
  /// Creates a GetContactInfoResponseStatisticsOpenedItem from already-decoded fields.
  const GetContactInfoResponseStatisticsOpenedItem({
    this.campaignId,
    this.count,
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsOpenedItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsOpenedItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsOpenedItem(
        campaignId: json.optInt('campaignId'),
        count: json.optInt('count'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// Number of openings for the campaign
  final int? count;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has opened the email
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (count != null) 'count': count!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `GetContactInfoResponseStatistics.softBouncesItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsSoftBouncesItem {
  /// Creates a GetContactInfoResponseStatisticsSoftBouncesItem from already-decoded fields.
  const GetContactInfoResponseStatisticsSoftBouncesItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsSoftBouncesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsSoftBouncesItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsSoftBouncesItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// Listing of the unsubscription for the contact
final class GetContactInfoResponseStatisticsUnsubscriptions {
  /// Creates a GetContactInfoResponseStatisticsUnsubscriptions from already-decoded fields.
  const GetContactInfoResponseStatisticsUnsubscriptions({
    this.adminUnsubscription = const [],
    this.userUnsubscription = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsUnsubscriptions. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsUnsubscriptions.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsUnsubscriptions(
        adminUnsubscription: json.optObjectList(
            'adminUnsubscription',
            GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem
                .fromJson,
            objectName: 'GetContactInfoResponseStatisticsUnsubscriptions'),
        userUnsubscription: json.optObjectList(
            'userUnsubscription',
            GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem
                .fromJson,
            objectName: 'GetContactInfoResponseStatisticsUnsubscriptions'),
        raw: json,
      );

  /// Contact has been unsubscribed from the administrator
  final List<
          GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem>
      adminUnsubscription;

  /// Contact unsubscribe via unsubscription link in a campaign
  final List<
          GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem>
      userUnsubscription;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (adminUnsubscription.isNotEmpty)
          'adminUnsubscription': [
            for (final item in adminUnsubscription) item.toJson()
          ],
        if (userUnsubscription.isNotEmpty)
          'userUnsubscription': [
            for (final item in userUnsubscription) item.toJson()
          ],
      };
}

/// The `GetContactInfoResponseStatisticsUnsubscriptions.adminUnsubscriptionItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem {
  /// Creates a GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem from already-decoded fields.
  const GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem({
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsUnsubscriptionsAdminUnsubscriptionItem(
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has been unsubscribed
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `GetContactInfoResponseStatisticsUnsubscriptions.userUnsubscriptionItem` object of the Brevo API.
final class GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem {
  /// Creates a GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem from already-decoded fields.
  const GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem({
    this.campaignId,
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem.fromJson(
          Map<String, Object?> json) =>
      GetContactInfoResponseStatisticsUnsubscriptionsUserUnsubscriptionItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has unsubscribed
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// Campaign Statistics for the contact
final class GetContactStatsResponse {
  /// Creates a GetContactStatsResponse from already-decoded fields.
  const GetContactStatsResponse({
    this.clicked = const [],
    this.complaints = const [],
    this.delivered = const [],
    this.hardBounces = const [],
    this.messagesSent = const [],
    this.opened = const [],
    this.softBounces = const [],
    this.transacAttributes = const [],
    this.unsubscriptions,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponse.fromJson(Map<String, Object?> json) =>
      GetContactStatsResponse(
        clicked: json.optObjectList(
            'clicked', GetContactStatsResponseClickedItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        complaints: json.optObjectList(
            'complaints', GetContactStatsResponseComplaintsItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        delivered: json.optObjectList(
            'delivered', GetContactStatsResponseDeliveredItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        hardBounces: json.optObjectList(
            'hardBounces', GetContactStatsResponseHardBouncesItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        messagesSent: json.optObjectList(
            'messagesSent', GetContactStatsResponseMessagesSentItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        opened: json.optObjectList(
            'opened', GetContactStatsResponseOpenedItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        softBounces: json.optObjectList(
            'softBounces', GetContactStatsResponseSoftBouncesItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        transacAttributes: json.optObjectList('transacAttributes',
            GetContactStatsResponseTransacAttributesItem.fromJson,
            objectName: 'GetContactStatsResponse'),
        unsubscriptions: json.optNested(
            'unsubscriptions', GetContactStatsResponseUnsubscriptions.fromJson),
        raw: json,
      );

  /// The `clicked` field.
  final List<GetContactStatsResponseClickedItem> clicked;

  /// The `complaints` field.
  final List<GetContactStatsResponseComplaintsItem> complaints;

  /// The `delivered` field.
  final List<GetContactStatsResponseDeliveredItem> delivered;

  /// The `hardBounces` field.
  final List<GetContactStatsResponseHardBouncesItem> hardBounces;

  /// The `messagesSent` field.
  final List<GetContactStatsResponseMessagesSentItem> messagesSent;

  /// The `opened` field.
  final List<GetContactStatsResponseOpenedItem> opened;

  /// The `softBounces` field.
  final List<GetContactStatsResponseSoftBouncesItem> softBounces;

  /// The `transacAttributes` field.
  final List<GetContactStatsResponseTransacAttributesItem> transacAttributes;

  /// The `unsubscriptions` field.
  final GetContactStatsResponseUnsubscriptions? unsubscriptions;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (clicked.isNotEmpty)
          'clicked': [for (final item in clicked) item.toJson()],
        if (complaints.isNotEmpty)
          'complaints': [for (final item in complaints) item.toJson()],
        if (delivered.isNotEmpty)
          'delivered': [for (final item in delivered) item.toJson()],
        if (hardBounces.isNotEmpty)
          'hardBounces': [for (final item in hardBounces) item.toJson()],
        if (messagesSent.isNotEmpty)
          'messagesSent': [for (final item in messagesSent) item.toJson()],
        if (opened.isNotEmpty)
          'opened': [for (final item in opened) item.toJson()],
        if (softBounces.isNotEmpty)
          'softBounces': [for (final item in softBounces) item.toJson()],
        if (transacAttributes.isNotEmpty)
          'transacAttributes': [
            for (final item in transacAttributes) item.toJson()
          ],
        if (unsubscriptions != null)
          'unsubscriptions': unsubscriptions!.toJson(),
      };
}

/// The `GetContactStatsResponse.clickedItem` object of the Brevo API.
final class GetContactStatsResponseClickedItem {
  /// Creates a GetContactStatsResponseClickedItem from already-decoded fields.
  const GetContactStatsResponseClickedItem({
    this.campaignId,
    this.links = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseClickedItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseClickedItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseClickedItem(
        campaignId: json.optInt('campaignId'),
        links: json.optObjectList(
            'links', GetContactStatsResponseClickedItemLinksItem.fromJson,
            objectName: 'GetContactStatsResponseClickedItem'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// The `links` field.
  final List<GetContactStatsResponseClickedItemLinksItem> links;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (links.isNotEmpty)
          'links': [for (final item in links) item.toJson()],
      };
}

/// The `GetContactStatsResponseClickedItem.linksItem` object of the Brevo API.
final class GetContactStatsResponseClickedItemLinksItem {
  /// Creates a GetContactStatsResponseClickedItemLinksItem from already-decoded fields.
  const GetContactStatsResponseClickedItemLinksItem({
    this.count,
    this.eventTime,
    this.ip,
    this.url,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseClickedItemLinksItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseClickedItemLinksItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseClickedItemLinksItem(
        count: json.optInt('count'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        url: json.optString('url'),
        raw: json,
      );

  /// Number of clicks on this link for the campaign
  final int? count;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has clicked on the link
  final String? ip;

  /// URL of the clicked link
  final String? url;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
        if (url != null) 'url': url!,
      };
}

/// The `GetContactStatsResponse.complaintsItem` object of the Brevo API.
final class GetContactStatsResponseComplaintsItem {
  /// Creates a GetContactStatsResponseComplaintsItem from already-decoded fields.
  const GetContactStatsResponseComplaintsItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseComplaintsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseComplaintsItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseComplaintsItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactStatsResponse.deliveredItem` object of the Brevo API.
final class GetContactStatsResponseDeliveredItem {
  /// Creates a GetContactStatsResponseDeliveredItem from already-decoded fields.
  const GetContactStatsResponseDeliveredItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseDeliveredItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseDeliveredItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseDeliveredItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactStatsResponse.hardBouncesItem` object of the Brevo API.
final class GetContactStatsResponseHardBouncesItem {
  /// Creates a GetContactStatsResponseHardBouncesItem from already-decoded fields.
  const GetContactStatsResponseHardBouncesItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseHardBouncesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseHardBouncesItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseHardBouncesItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactStatsResponse.messagesSentItem` object of the Brevo API.
final class GetContactStatsResponseMessagesSentItem {
  /// Creates a GetContactStatsResponseMessagesSentItem from already-decoded fields.
  const GetContactStatsResponseMessagesSentItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseMessagesSentItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseMessagesSentItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseMessagesSentItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactStatsResponse.openedItem` object of the Brevo API.
final class GetContactStatsResponseOpenedItem {
  /// Creates a GetContactStatsResponseOpenedItem from already-decoded fields.
  const GetContactStatsResponseOpenedItem({
    this.campaignId,
    this.count,
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseOpenedItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseOpenedItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseOpenedItem(
        campaignId: json.optInt('campaignId'),
        count: json.optInt('count'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// Number of openings of the campaign
  final int? count;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has opened the campaign
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (count != null) 'count': count!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `GetContactStatsResponse.softBouncesItem` object of the Brevo API.
final class GetContactStatsResponseSoftBouncesItem {
  /// Creates a GetContactStatsResponseSoftBouncesItem from already-decoded fields.
  const GetContactStatsResponseSoftBouncesItem({
    this.campaignId,
    this.eventTime,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseSoftBouncesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseSoftBouncesItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseSoftBouncesItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
      };
}

/// The `GetContactStatsResponse.transacAttributesItem` object of the Brevo API.
final class GetContactStatsResponseTransacAttributesItem {
  /// Creates a GetContactStatsResponseTransacAttributesItem from already-decoded fields.
  const GetContactStatsResponseTransacAttributesItem({
    this.orderDate,
    this.orderId,
    this.orderPrice,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseTransacAttributesItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseTransacAttributesItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseTransacAttributesItem(
        orderDate: json.optString('orderDate'),
        orderId: json.optInt('orderId'),
        orderPrice: json.optDouble('orderPrice'),
        raw: json,
      );

  /// Date of the order
  final String? orderDate;

  /// [orderDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get orderDateDate =>
      orderDate == null ? null : DateTime.tryParse(orderDate!);

  /// ID of the order
  final int? orderId;

  /// Price of the order
  final double? orderPrice;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (orderDate != null) 'orderDate': orderDate!,
        if (orderId != null) 'orderId': orderId!,
        if (orderPrice != null) 'orderPrice': orderPrice!,
      };
}

/// The `GetContactStatsResponse.unsubscriptions` object of the Brevo API.
final class GetContactStatsResponseUnsubscriptions {
  /// Creates a GetContactStatsResponseUnsubscriptions from already-decoded fields.
  const GetContactStatsResponseUnsubscriptions({
    this.adminUnsubscription = const [],
    this.userUnsubscription = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseUnsubscriptions. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseUnsubscriptions.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseUnsubscriptions(
        adminUnsubscription: json.optObjectList(
            'adminUnsubscription',
            GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem
                .fromJson,
            objectName: 'GetContactStatsResponseUnsubscriptions'),
        userUnsubscription: json.optObjectList(
            'userUnsubscription',
            GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem
                .fromJson,
            objectName: 'GetContactStatsResponseUnsubscriptions'),
        raw: json,
      );

  /// Contact has been unsubscribed from the administrator
  final List<GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem>
      adminUnsubscription;

  /// Contact has unsubscribed via the unsubscription link in the email
  final List<GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem>
      userUnsubscription;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (adminUnsubscription.isNotEmpty)
          'adminUnsubscription': [
            for (final item in adminUnsubscription) item.toJson()
          ],
        if (userUnsubscription.isNotEmpty)
          'userUnsubscription': [
            for (final item in userUnsubscription) item.toJson()
          ],
      };
}

/// The `GetContactStatsResponseUnsubscriptions.adminUnsubscriptionItem` object of the Brevo API.
final class GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem {
  /// Creates a GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem from already-decoded fields.
  const GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem({
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseUnsubscriptionsAdminUnsubscriptionItem(
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has been unsubscribed
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `GetContactStatsResponseUnsubscriptions.userUnsubscriptionItem` object of the Brevo API.
final class GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem {
  /// Creates a GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem from already-decoded fields.
  const GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem({
    this.campaignId,
    this.eventTime,
    this.ip,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem.fromJson(
          Map<String, Object?> json) =>
      GetContactStatsResponseUnsubscriptionsUserUnsubscriptionItem(
        campaignId: json.optInt('campaignId'),
        eventTime: json.optString('eventTime'),
        ip: json.optString('ip'),
        raw: json,
      );

  /// ID of the campaign which generated the event
  final int? campaignId;

  /// UTC date-time of the event
  final String? eventTime;

  /// IP from which the user has unsubscribed
  final String? ip;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (eventTime != null) 'eventTime': eventTime!,
        if (ip != null) 'ip': ip!,
      };
}

/// The `components/schemas/getContacts` object of the Brevo API.
final class GetContacts {
  /// Creates a GetContacts from already-decoded fields.
  const GetContacts({
    this.contacts = const [],
    this.count,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetContacts. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetContacts.fromJson(Map<String, Object?> json) => GetContacts(
        contacts: json.optObjectList('contacts', GetContactDetails.fromJson,
            objectName: 'GetContacts'),
        count: json.optInt('count'),
        raw: json,
      );

  /// The `contacts` field.
  final List<GetContactDetails> contacts;

  /// Number of contacts
  final int? count;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contacts.isNotEmpty)
          'contacts': [for (final item in contacts) item.toJson()],
        if (count != null) 'count': count!,
      };
}

/// The `components/schemas/getFolder` object of the Brevo API.
final class GetFolder {
  /// Creates a GetFolder from already-decoded fields.
  const GetFolder({
    this.id,
    this.name,
    this.totalBlacklisted,
    this.totalSubscribers,
    this.uniqueSubscribers,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetFolder. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetFolder.fromJson(Map<String, Object?> json) => GetFolder(
        id: json.optInt('id'),
        name: json.optString('name'),
        totalBlacklisted: json.optInt('totalBlacklisted'),
        totalSubscribers: json.optInt('totalSubscribers'),
        uniqueSubscribers: json.optInt('uniqueSubscribers'),
        raw: json,
      );

  /// ID of the folder
  final int? id;

  /// Name of the folder
  final String? name;

  /// Number of blacklisted contacts in the folder
  final int? totalBlacklisted;

  /// Number of contacts in the folder
  final int? totalSubscribers;

  /// Number of unique contacts in the folder
  final int? uniqueSubscribers;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (totalBlacklisted != null) 'totalBlacklisted': totalBlacklisted!,
        if (totalSubscribers != null) 'totalSubscribers': totalSubscribers!,
        if (uniqueSubscribers != null) 'uniqueSubscribers': uniqueSubscribers!,
      };
}

/// The `GET /contacts/folders/{folderId}/lists` object of the Brevo API.
final class GetFolderListsResponse {
  /// Creates a GetFolderListsResponse from already-decoded fields.
  const GetFolderListsResponse({
    this.count,
    this.lists = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetFolderListsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetFolderListsResponse.fromJson(Map<String, Object?> json) =>
      GetFolderListsResponse(
        count: json.optInt('count'),
        lists: json.optObjectList('lists', GetList.fromJson,
            objectName: 'GetFolderListsResponse'),
        raw: json,
      );

  /// Number of lists in the folder
  final int? count;

  /// The `lists` field.
  final List<GetList> lists;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (lists.isNotEmpty)
          'lists': [for (final item in lists) item.toJson()],
      };
}

/// The `GET /contacts/folders` object of the Brevo API.
final class GetFoldersResponse {
  /// Creates a GetFoldersResponse from already-decoded fields.
  const GetFoldersResponse({
    this.count,
    this.folders = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetFoldersResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetFoldersResponse.fromJson(Map<String, Object?> json) =>
      GetFoldersResponse(
        count: json.optInt('count'),
        folders: json.optObjectList('folders', GetFolder.fromJson,
            objectName: 'GetFoldersResponse'),
        raw: json,
      );

  /// Number of folders available in your account
  final int? count;

  /// The `folders` field.
  final List<GetFolder> folders;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (folders.isNotEmpty)
          'folders': [for (final item in folders) item.toJson()],
      };
}

/// The `components/schemas/getList` object of the Brevo API.
final class GetList {
  /// Creates a GetList from already-decoded fields.
  const GetList({
    this.id,
    this.name,
    this.totalBlacklisted,
    this.totalSubscribers,
    this.uniqueSubscribers,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetList. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetList.fromJson(Map<String, Object?> json) => GetList(
        id: json.optInt('id'),
        name: json.optString('name'),
        totalBlacklisted: json.optInt('totalBlacklisted'),
        totalSubscribers: json.optInt('totalSubscribers'),
        uniqueSubscribers: json.optInt('uniqueSubscribers'),
        raw: json,
      );

  /// ID of the list
  final int? id;

  /// Name of the list
  final String? name;

  /// Number of blacklisted contacts in the list
  final int? totalBlacklisted;

  /// Number of contacts in the list
  final int? totalSubscribers;

  /// Number of unique contacts in the list
  final int? uniqueSubscribers;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (totalBlacklisted != null) 'totalBlacklisted': totalBlacklisted!,
        if (totalSubscribers != null) 'totalSubscribers': totalSubscribers!,
        if (uniqueSubscribers != null) 'uniqueSubscribers': uniqueSubscribers!,
      };
}

/// The `GET /contacts/lists/{listId}` object of the Brevo API.
final class GetListResponse {
  /// Creates a GetListResponse from already-decoded fields.
  const GetListResponse({
    this.id,
    this.name,
    this.totalBlacklisted,
    this.totalSubscribers,
    this.uniqueSubscribers,
    this.campaignStats = const [],
    this.createdAt,
    this.dynamicList,
    this.folderId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetListResponse.fromJson(Map<String, Object?> json) =>
      GetListResponse(
        id: json.optInt('id'),
        name: json.optString('name'),
        totalBlacklisted: json.optInt('totalBlacklisted'),
        totalSubscribers: json.optInt('totalSubscribers'),
        uniqueSubscribers: json.optInt('uniqueSubscribers'),
        campaignStats: json.optObjectList(
            'campaignStats', GetListResponseCampaignStatsItem.fromJson,
            objectName: 'GetListResponse'),
        createdAt: json.optString('createdAt'),
        dynamicList: json.optBool('dynamicList'),
        folderId: json.optInt('folderId'),
        raw: json,
      );

  /// ID of the list
  final int? id;

  /// Name of the list
  final String? name;

  /// Number of blacklisted contacts in the list
  final int? totalBlacklisted;

  /// Number of contacts in the list
  final int? totalSubscribers;

  /// Number of unique contacts in the list
  final int? uniqueSubscribers;

  /// The `campaignStats` field.
  final List<GetListResponseCampaignStatsItem> campaignStats;

  /// Creation UTC date-time of the list (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Status telling if the list is dynamic or not (true=dynamic, false=not dynamic)
  final bool? dynamicList;

  /// ID of the folder
  final int? folderId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (totalBlacklisted != null) 'totalBlacklisted': totalBlacklisted!,
        if (totalSubscribers != null) 'totalSubscribers': totalSubscribers!,
        if (uniqueSubscribers != null) 'uniqueSubscribers': uniqueSubscribers!,
        if (campaignStats.isNotEmpty)
          'campaignStats': [for (final item in campaignStats) item.toJson()],
        if (createdAt != null) 'createdAt': createdAt!,
        if (dynamicList != null) 'dynamicList': dynamicList!,
        if (folderId != null) 'folderId': folderId!,
      };
}

/// The `GetListResponse.campaignStatsItem` object of the Brevo API.
final class GetListResponseCampaignStatsItem {
  /// Creates a GetListResponseCampaignStatsItem from already-decoded fields.
  const GetListResponseCampaignStatsItem({
    this.campaignId,
    this.stats,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetListResponseCampaignStatsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetListResponseCampaignStatsItem.fromJson(
          Map<String, Object?> json) =>
      GetListResponseCampaignStatsItem(
        campaignId: json.optInt('campaignId'),
        stats: json.optNested('stats', GetCampaignStats.fromJson),
        raw: json,
      );

  /// ID of the campaign
  final int? campaignId;

  /// The `stats` field.
  final GetCampaignStats? stats;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (campaignId != null) 'campaignId': campaignId!,
        if (stats != null) 'stats': stats!.toJson(),
      };
}

/// The `GET /contacts/lists` object of the Brevo API.
final class GetListsResponse {
  /// Creates a GetListsResponse from already-decoded fields.
  const GetListsResponse({
    this.count,
    this.lists = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetListsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetListsResponse.fromJson(Map<String, Object?> json) =>
      GetListsResponse(
        count: json.optInt('count'),
        lists: json.optObjectList('lists', GetListsResponseListsItem.fromJson,
            objectName: 'GetListsResponse'),
        raw: json,
      );

  /// Number of lists in your account
  final int? count;

  /// Listing of all the lists available in your account
  final List<GetListsResponseListsItem> lists;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (lists.isNotEmpty)
          'lists': [for (final item in lists) item.toJson()],
      };
}

/// The `GetListsResponse.listsItem` object of the Brevo API.
final class GetListsResponseListsItem {
  /// Creates a GetListsResponseListsItem from already-decoded fields.
  const GetListsResponseListsItem({
    this.id,
    this.name,
    this.totalBlacklisted,
    this.totalSubscribers,
    this.uniqueSubscribers,
    this.folderId,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetListsResponseListsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetListsResponseListsItem.fromJson(Map<String, Object?> json) =>
      GetListsResponseListsItem(
        id: json.optInt('id'),
        name: json.optString('name'),
        totalBlacklisted: json.optInt('totalBlacklisted'),
        totalSubscribers: json.optInt('totalSubscribers'),
        uniqueSubscribers: json.optInt('uniqueSubscribers'),
        folderId: json.optInt('folderId'),
        raw: json,
      );

  /// ID of the list
  final int? id;

  /// Name of the list
  final String? name;

  /// Number of blacklisted contacts in the list
  final int? totalBlacklisted;

  /// Number of contacts in the list
  final int? totalSubscribers;

  /// Number of unique contacts in the list
  final int? uniqueSubscribers;

  /// ID of the folder
  final int? folderId;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (totalBlacklisted != null) 'totalBlacklisted': totalBlacklisted!,
        if (totalSubscribers != null) 'totalSubscribers': totalSubscribers!,
        if (uniqueSubscribers != null) 'uniqueSubscribers': uniqueSubscribers!,
        if (folderId != null) 'folderId': folderId!,
      };
}

/// The `GET /contacts/segments` object of the Brevo API.
final class GetSegmentsResponse {
  /// Creates a GetSegmentsResponse from already-decoded fields.
  const GetSegmentsResponse({
    this.count,
    this.segments = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSegmentsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSegmentsResponse.fromJson(Map<String, Object?> json) =>
      GetSegmentsResponse(
        count: json.optInt('count'),
        segments: json.optObjectList(
            'segments', GetSegmentsResponseSegmentsItem.fromJson,
            objectName: 'GetSegmentsResponse'),
        raw: json,
      );

  /// Number of Segments in your account
  final int? count;

  /// Listing of all the segments available in your account
  final List<GetSegmentsResponseSegmentsItem> segments;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (segments.isNotEmpty)
          'segments': [for (final item in segments) item.toJson()],
      };
}

/// The `GetSegmentsResponse.segmentsItem` object of the Brevo API.
final class GetSegmentsResponseSegmentsItem {
  /// Creates a GetSegmentsResponseSegmentsItem from already-decoded fields.
  const GetSegmentsResponseSegmentsItem({
    this.categoryName,
    this.id,
    this.segmentName,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetSegmentsResponseSegmentsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetSegmentsResponseSegmentsItem.fromJson(Map<String, Object?> json) =>
      GetSegmentsResponseSegmentsItem(
        categoryName: json.optString('categoryName'),
        id: json.optInt('id'),
        segmentName: json.optString('segmentName'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Name of the Segment Category
  final String? categoryName;

  /// ID of the segment
  final int? id;

  /// Name of the Segment
  final String? segmentName;

  /// Last update UTC date-time of the segment (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (categoryName != null) 'categoryName': categoryName!,
        if (id != null) 'id': id!,
        if (segmentName != null) 'segmentName': segmentName!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `components/schemas/postContactInfo` object of the Brevo API.
final class PostContactInfo {
  /// Creates a PostContactInfo from already-decoded fields.
  const PostContactInfo({
    this.contacts,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostContactInfo. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostContactInfo.fromJson(Map<String, Object?> json) =>
      PostContactInfo(
        contacts: json.optNested('contacts', PostContactInfoContacts.fromJson),
        raw: json,
      );

  /// The `contacts` field.
  final PostContactInfoContacts? contacts;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contacts != null) 'contacts': contacts!.toJson(),
      };
}

/// The `PostContactInfo.contacts` object of the Brevo API.
final class PostContactInfoContacts {
  /// Creates a PostContactInfoContacts from already-decoded fields.
  const PostContactInfoContacts({
    this.failure = const [],
    this.processId,
    this.success = const [],
    this.total,
    this.raw = const {},
  });

  /// Decodes a JSON object into a PostContactInfoContacts. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory PostContactInfoContacts.fromJson(Map<String, Object?> json) =>
      PostContactInfoContacts(
        failure: json.optList('failure', (element) => element),
        processId: json.optInt('processId'),
        success: json.optList('success', (element) => element),
        total: json.optInt('total'),
        raw: json,
      );

  /// The `failure` field.
  final List<Object?> failure;

  /// Id of the process created to remove contacts from list when user opts for "all" option.
  final int? processId;

  /// The `success` field.
  final List<Object?> success;

  /// Displays the count of total number of contacts removed from list when user opts for "all" option.
  final int? total;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (failure.isNotEmpty) 'failure': failure,
        if (processId != null) 'processId': processId!,
        if (success.isNotEmpty) 'success': success,
        if (total != null) 'total': total!,
      };
}
