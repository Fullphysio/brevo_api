// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /contacts/lists/{listId}/contacts/add`.
final class AddContactToListRequest {
  /// Creates the request body.
  const AddContactToListRequest({
    this.emails,
    this.ids,
    this.extIds,
  });

  /// Decodes a JSON object into a AddContactToListRequest. Optional fields absent from [json] stay null.
  factory AddContactToListRequest.fromJson(Map<String, Object?> json) =>
      AddContactToListRequest(
        emails:
            json.containsKey('emails') ? json.optStringList('emails') : null,
        ids: json.containsKey('ids') ? json.optIntList('ids') : null,
        extIds:
            json.containsKey('extIds') ? json.optStringList('extIds') : null,
      );

  /// Emails to add to a list. You can pass a **maximum of 150 emails** for addition in one request. **_If you need to add the emails in bulk, please prefer /contacts/import api._**
  final List<String>? emails;

  /// IDs to add to a list. You can pass a **maximum of 150 IDs** for addition in one request. **_If you need to add the emails in bulk, please prefer /contacts/import api._**
  final List<int>? ids;

  /// EXT_ID attributes to add to a list. You can pass a **maximum of 150 EXT_ID attributes** for addition in one request. **_If you need to add the emails in bulk, please prefer /contacts/import api._**
  final List<String>? extIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (emails != null) 'emails': emails!,
        if (ids != null) 'ids': ids!,
        if (extIds != null) 'extIds': extIds!,
      };
}

/// The request body of `POST /contacts/attributes/{attributeCategory}/{attributeName}`.
final class CreateAttributeRequest {
  /// Creates the request body.
  const CreateAttributeRequest({
    this.enumeration,
    this.isRecurring,
    this.multiCategoryOptions,
    this.type,
    this.valueValue,
  });

  /// Decodes a JSON object into a CreateAttributeRequest. Optional fields absent from [json] stay null.
  factory CreateAttributeRequest.fromJson(Map<String, Object?> json) =>
      CreateAttributeRequest(
        enumeration: json.containsKey('enumeration')
            ? json.optObjectList(
                'enumeration', CreateAttributeRequestEnumerationItem.fromJson,
                objectName: 'CreateAttributeRequest')
            : null,
        isRecurring: json.optBool('isRecurring'),
        multiCategoryOptions: json.containsKey('multiCategoryOptions')
            ? json.optStringList('multiCategoryOptions')
            : null,
        type: json.optEnum('type', CreateAttributeRequestType.fromWire),
        valueValue: json.optString('value'),
      );

  /// List of values and labels that the attribute can take. **Use only if the attribute's category is "category"**. None of the category options can exceed max 200 characters. For example: **\[{"value":1, "label":"male"}, {"value":2, "label":"female"}\]**
  final List<CreateAttributeRequestEnumerationItem>? enumeration;

  /// Type of the attribute. **Use only if the attribute's category is 'calculated' or 'global'**
  final bool? isRecurring;

  /// List of options you want to add for multiple-choice attribute. **Use only if the attribute's category is "normal" and attribute's type is "multiple-choice". None of the multicategory options can exceed max 200 characters.** For example: **\["USA","INDIA"\]**
  final List<String>? multiCategoryOptions;

  /// Type of the attribute. **Use only if the attribute's category is 'normal', 'category' or 'transactional'** Type **user and multiple-choice** is only available if the category is **normal** attribute Type **id** is only available if the category is **transactional** attribute Type **category** is only available if the category is **category** attribute
  final CreateAttributeRequestType? type;

  /// Value of the attribute. **Use only if the attribute's category is 'calculated' or 'global'**
  final String? valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (enumeration != null)
          'enumeration': [for (final item in enumeration!) item.toJson()],
        if (isRecurring != null) 'isRecurring': isRecurring!,
        if (multiCategoryOptions != null)
          'multiCategoryOptions': multiCategoryOptions!,
        if (type != null) 'type': type!.value,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The request body of `CreateAttributeRequest.enumerationItem`.
final class CreateAttributeRequestEnumerationItem {
  /// Creates the request body.
  const CreateAttributeRequestEnumerationItem({
    required this.label,
    required this.valueValue,
  });

  /// Decodes a JSON object into a CreateAttributeRequestEnumerationItem. Optional fields absent from [json] stay null.
  factory CreateAttributeRequestEnumerationItem.fromJson(
          Map<String, Object?> json) =>
      CreateAttributeRequestEnumerationItem(
        label: json.requireString(
            'label', 'CreateAttributeRequestEnumerationItem'),
        valueValue:
            json.requireInt('value', 'CreateAttributeRequestEnumerationItem'),
      );

  /// Label of the value
  final String label;

  /// Id of the value
  final int valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'label': label,
        'value': valueValue,
      };
}

/// The request body of `POST /contacts`.
final class CreateContactRequest {
  /// Creates the request body.
  const CreateContactRequest({
    this.attributes,
    this.email,
    this.emailBlacklisted,
    this.extId,
    this.forceMerge,
    this.getId,
    this.listIds,
    this.smsBlacklisted,
    this.smtpBlacklistSender,
    this.updateEnabled,
  });

  /// Decodes a JSON object into a CreateContactRequest. Optional fields absent from [json] stay null.
  factory CreateContactRequest.fromJson(Map<String, Object?> json) =>
      CreateContactRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        email: json.optString('email'),
        emailBlacklisted: json.optBool('emailBlacklisted'),
        extId: json.optString('ext_id'),
        forceMerge: json.optBool('forceMerge'),
        getId: json.optBool('getId'),
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        smsBlacklisted: json.optBool('smsBlacklisted'),
        smtpBlacklistSender: json.containsKey('smtpBlacklistSender')
            ? json.optStringList('smtpBlacklistSender')
            : null,
        updateEnabled: json.optBool('updateEnabled'),
      );

  /// Pass the set of attributes and their values. The attribute's parameter should be passed in capital letter while creating a contact. Values that don't match the attribute type (e.g. text or string in a date attribute) will be ignored. **These attributes must be present in your Brevo account**. For eg: **{"FNAME":"Elly", "LNAME":"Roger", "COUNTRIES": \["India","China"\]}**
  final Map<String, Object?>? attributes;

  /// Email address of the user. **Mandatory if "ext_id"  & "SMS" field is not passed.**
  final String? email;

  /// Set this field to blacklist the contact for emails (emailBlacklisted = true)
  final bool? emailBlacklisted;

  /// Pass your own Id to create a contact.
  final String? extId;

  /// When true, if the contact being created shares an identifier (email, SMS, ext_id, whatsapp, landline) with an existing contact, the two contacts are force-merged. The contact with the most recent `last_modified` timestamp is retained; the other is deleted. When false (default), a 4xx error is returned on identifier conflict.
  final bool? forceMerge;

  /// When true, the response returns the `id` of the surviving contact after merge.
  final bool? getId;

  /// Ids of the lists to add the contact to
  final List<int>? listIds;

  /// Set this field to blacklist the contact for SMS (smsBlacklisted = true)
  final bool? smsBlacklisted;

  /// transactional email forbidden sender for contact. Use only for email Contact ( only available if updateEnabled = true )
  final List<String>? smtpBlacklistSender;

  /// Facilitate to update the existing contact in the same request (updateEnabled = true)
  final bool? updateEnabled;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (email != null) 'email': email!,
        if (emailBlacklisted != null) 'emailBlacklisted': emailBlacklisted!,
        if (extId != null) 'ext_id': extId!,
        if (forceMerge != null) 'forceMerge': forceMerge!,
        if (getId != null) 'getId': getId!,
        if (listIds != null) 'listIds': listIds!,
        if (smsBlacklisted != null) 'smsBlacklisted': smsBlacklisted!,
        if (smtpBlacklistSender != null)
          'smtpBlacklistSender': smtpBlacklistSender!,
        if (updateEnabled != null) 'updateEnabled': updateEnabled!,
      };
}

/// The request body of `POST /contacts/doubleOptinConfirmation`.
final class CreateDoiContactRequest {
  /// Creates the request body.
  const CreateDoiContactRequest({
    this.attributes,
    required this.email,
    this.excludeListIds,
    required this.includeListIds,
    required this.redirectionUrl,
    required this.templateId,
  });

  /// Decodes a JSON object into a CreateDoiContactRequest. Optional fields absent from [json] stay null.
  factory CreateDoiContactRequest.fromJson(Map<String, Object?> json) =>
      CreateDoiContactRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        email: json.requireString('email', 'CreateDoiContactRequest'),
        excludeListIds: json.containsKey('excludeListIds')
            ? json.optIntList('excludeListIds')
            : null,
        includeListIds: json.containsKey('includeListIds')
            ? json.optIntList('includeListIds')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateDoiContactRequest', key: 'includeListIds')),
        redirectionUrl:
            json.requireString('redirectionUrl', 'CreateDoiContactRequest'),
        templateId: json.requireInt('templateId', 'CreateDoiContactRequest'),
      );

  /// Pass the set of attributes and their values. **These attributes must be present in your Brevo account**. For eg. **{'FNAME':'Elly', 'LNAME':'Roger', 'COUNTRIES': \['India','China'\]}**
  final Map<String, Object?>? attributes;

  /// Email address where the confirmation email will be sent. This email address will be the identifier for all other contact attributes.
  final String email;

  /// Lists under user account where contact should not be added
  final List<int>? excludeListIds;

  /// Lists under user account where contact should be added
  final List<int> includeListIds;

  /// URL of the web page that user will be redirected to after clicking on the double opt in URL. When editing your DOI template you can reference this URL by using the tag **{{ params.DOIurl }}**.
  final String redirectionUrl;

  /// Id of the Double opt-in (DOI) template
  final int templateId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        'email': email,
        if (excludeListIds != null) 'excludeListIds': excludeListIds!,
        'includeListIds': includeListIds,
        'redirectionUrl': redirectionUrl,
        'templateId': templateId,
      };
}

/// The request body of `POST /contacts/lists`.
final class CreateListRequest {
  /// Creates the request body.
  const CreateListRequest({
    required this.folderId,
    required this.name,
  });

  /// Decodes a JSON object into a CreateListRequest. Optional fields absent from [json] stay null.
  factory CreateListRequest.fromJson(Map<String, Object?> json) =>
      CreateListRequest(
        folderId: json.requireInt('folderId', 'CreateListRequest'),
        name: json.requireString('name', 'CreateListRequest'),
      );

  /// Id of the parent folder in which this list is to be created
  final int folderId;

  /// Name of the list
  final String name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'folderId': folderId,
        'name': name,
      };
}

/// The request body of `components/schemas/createUpdateFolder`.
final class CreateUpdateFolder {
  /// Creates the request body.
  const CreateUpdateFolder({
    this.name,
  });

  /// Decodes a JSON object into a CreateUpdateFolder. Optional fields absent from [json] stay null.
  factory CreateUpdateFolder.fromJson(Map<String, Object?> json) =>
      CreateUpdateFolder(
        name: json.optString('name'),
      );

  /// Name of the folder
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (name != null) 'name': name!,
      };
}

/// The request body of `POST /contacts/import`.
final class ImportContactsRequest {
  /// Creates the request body.
  const ImportContactsRequest({
    this.consentGroupIds,
    this.disableNotification,
    this.emailBlacklist,
    this.emptyContactsAttributes,
    this.fileBody,
    this.fileUrl,
    this.jsonBody,
    this.listIds,
    this.newList,
    this.notifyUrl,
    this.smsBlacklist,
    this.updateExistingContacts,
  });

  /// Decodes a JSON object into a ImportContactsRequest. Optional fields absent from [json] stay null.
  factory ImportContactsRequest.fromJson(Map<String, Object?> json) =>
      ImportContactsRequest(
        consentGroupIds: json.containsKey('consentGroupIds')
            ? json.optIntList('consentGroupIds')
            : null,
        disableNotification: json.optBool('disableNotification'),
        emailBlacklist: json.optBool('emailBlacklist'),
        emptyContactsAttributes: json.optBool('emptyContactsAttributes'),
        fileBody: json.optString('fileBody'),
        fileUrl: json.optString('fileUrl'),
        jsonBody: json.containsKey('jsonBody')
            ? json.optObjectList(
                'jsonBody', ImportContactsRequestJsonBodyItem.fromJson,
                objectName: 'ImportContactsRequest')
            : null,
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        newList:
            json.optNested('newList', ImportContactsRequestNewList.fromJson),
        notifyUrl: json.optString('notifyUrl'),
        smsBlacklist: json.optBool('smsBlacklist'),
        updateExistingContacts: json.optBool('updateExistingContacts'),
      );

  /// **Optional.** Ids of the consent groups to which all imported contacts will be added. Requires consent groups to be enabled for the organisation. For example, **\[1, 3\]**.
  final List<int>? consentGroupIds;

  /// To disable email notification
  final bool? disableNotification;

  /// To blacklist all the contacts for email
  final bool? emailBlacklist;

  /// To facilitate the choice to erase any attribute of the existing contacts with empty value. emptyContactsAttributes = true means the empty fields in your import will erase any attribute that currently contain data in Brevo, & emptyContactsAttributes = false means the empty fields will not affect your existing data ( **only available if `updateExistingContacts` set to true **)
  final bool? emptyContactsAttributes;

  /// **Mandatory if fileUrl and jsonBody is not defined.** CSV content to be imported. Use semicolon to separate multiple attributes. **Maximum allowed file body size is 10MB** . However we recommend a safe limit of around 8 MB to avoid the issues caused due to increase of file body size while parsing. Please use fileUrl instead to import bigger files.
  final String? fileBody;

  /// **Mandatory if fileBody and jsonBody is not defined.** URL of the file to be imported (**no local file**). Possible file formats: #### .txt, .csv, .json
  final String? fileUrl;

  /// **Mandatory if fileUrl and fileBody is not defined.** JSON content to be imported. **Maximum allowed json body size is 10MB** . However we recommend a safe limit of around 8 MB to avoid the issues caused due to increase of json body size while parsing. Please use fileUrl instead to import bigger files.
  final List<ImportContactsRequestJsonBodyItem>? jsonBody;

  /// **Mandatory if newList is not defined.** Ids of the lists in which the contacts shall be imported. For example, **\[2, 4, 7\]**.
  final List<int>? listIds;

  /// To create a new list and import the contacts into it, pass the listName and an optional folderId.
  final ImportContactsRequestNewList? newList;

  /// URL that will be called once the import process is finished. For reference, https://help.brevo.com/hc/en-us/articles/360007666479
  final String? notifyUrl;

  /// To blacklist all the contacts for sms
  final bool? smsBlacklist;

  /// To facilitate the choice to update the existing contacts
  final bool? updateExistingContacts;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (consentGroupIds != null) 'consentGroupIds': consentGroupIds!,
        if (disableNotification != null)
          'disableNotification': disableNotification!,
        if (emailBlacklist != null) 'emailBlacklist': emailBlacklist!,
        if (emptyContactsAttributes != null)
          'emptyContactsAttributes': emptyContactsAttributes!,
        if (fileBody != null) 'fileBody': fileBody!,
        if (fileUrl != null) 'fileUrl': fileUrl!,
        if (jsonBody != null)
          'jsonBody': [for (final item in jsonBody!) item.toJson()],
        if (listIds != null) 'listIds': listIds!,
        if (newList != null) 'newList': newList!.toJson(),
        if (notifyUrl != null) 'notifyUrl': notifyUrl!,
        if (smsBlacklist != null) 'smsBlacklist': smsBlacklist!,
        if (updateExistingContacts != null)
          'updateExistingContacts': updateExistingContacts!,
      };
}

/// The request body of `ImportContactsRequest.jsonBodyItem`.
final class ImportContactsRequestJsonBodyItem {
  /// Creates the request body.
  const ImportContactsRequestJsonBodyItem({
    this.attributes,
    this.email,
  });

  /// Decodes a JSON object into a ImportContactsRequestJsonBodyItem. Optional fields absent from [json] stay null.
  factory ImportContactsRequestJsonBodyItem.fromJson(
          Map<String, Object?> json) =>
      ImportContactsRequestJsonBodyItem(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        email: json.optString('email'),
      );

  /// List of attributes to be imported
  final Map<String, Object?>? attributes;

  /// The `email` field.
  final String? email;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (email != null) 'email': email!,
      };
}

/// To create a new list and import the contacts into it, pass the listName and an optional folderId.
final class ImportContactsRequestNewList {
  /// Creates the request body.
  const ImportContactsRequestNewList({
    this.folderId,
    this.listName,
  });

  /// Decodes a JSON object into a ImportContactsRequestNewList. Optional fields absent from [json] stay null.
  factory ImportContactsRequestNewList.fromJson(Map<String, Object?> json) =>
      ImportContactsRequestNewList(
        folderId: json.optInt('folderId'),
        listName: json.optString('listName'),
      );

  /// Id of the folder where this new list shall be created. **Mandatory if listName is not empty**
  final int? folderId;

  /// List with listName will be created first and users will be imported in it. **Mandatory if listIds is empty**.
  final String? listName;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (folderId != null) 'folderId': folderId!,
        if (listName != null) 'listName': listName!,
      };
}

/// The request body of `POST /contacts/lists/{listId}/contacts/remove`.
final class RemoveContactFromListRequest {
  /// Creates the request body.
  const RemoveContactFromListRequest({
    this.emails,
    this.ids,
    this.all,
    this.extIds,
  });

  /// Decodes a JSON object into a RemoveContactFromListRequest. Optional fields absent from [json] stay null.
  factory RemoveContactFromListRequest.fromJson(Map<String, Object?> json) =>
      RemoveContactFromListRequest(
        emails:
            json.containsKey('emails') ? json.optStringList('emails') : null,
        ids: json.containsKey('ids') ? json.optIntList('ids') : null,
        all: json.optBool('all'),
        extIds:
            json.containsKey('extIds') ? json.optStringList('extIds') : null,
      );

  /// **Required if 'all' is false and 'ids', 'extIds' are empty.** Emails to remove from a list. You can pass a **maximum of 150 emails** for removal in one request.
  final List<String>? emails;

  /// **Required if 'all' is false and 'emails', 'extIds' are empty.** IDs to remove from a list. You can pass a **maximum of 150 IDs** for removal in one request.
  final List<int>? ids;

  /// **Required if 'emails', 'extIds' and 'ids' are empty.** Remove all existing contacts from a list. A process will be created in this scenario. You can fetch the process details to know about the progress
  final bool? all;

  /// **Required if 'all' is false, 'ids' and 'emails' are empty.** EXT_ID attributes to remove from a list. You can pass a **maximum of 150 EXT_ID attributes** for removal in one request.
  final List<String>? extIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (emails != null) 'emails': emails!,
        if (ids != null) 'ids': ids!,
        if (all != null) 'all': all!,
        if (extIds != null) 'extIds': extIds!,
      };
}

/// The request body of `POST /contacts/export`.
final class RequestContactExportRequest {
  /// Creates the request body.
  const RequestContactExportRequest({
    required this.customContactFilter,
    this.disableNotification,
    this.exportAttributes,
    this.exportDateInUtc,
    this.exportMandatoryAttributes,
    this.exportMetadata,
    this.exportSubscriptionStatus,
    this.notifyUrl,
  });

  /// Decodes a JSON object into a RequestContactExportRequest. Optional fields absent from [json] stay null.
  factory RequestContactExportRequest.fromJson(Map<String, Object?> json) =>
      RequestContactExportRequest(
        customContactFilter: json.optNested('customContactFilter',
                RequestContactExportRequestCustomContactFilter.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'RequestContactExportRequest',
                key: 'customContactFilter')),
        disableNotification: json.optBool('disableNotification'),
        exportAttributes: json.containsKey('exportAttributes')
            ? json.optStringList('exportAttributes')
            : null,
        exportDateInUtc: json.optBool('exportDateInUTC'),
        exportMandatoryAttributes: json.optBool('exportMandatoryAttributes'),
        exportMetadata: json.containsKey('exportMetadata')
            ? json.optStringList('exportMetadata')
            : null,
        exportSubscriptionStatus: json.containsKey('exportSubscriptionStatus')
            ? json.optStringList('exportSubscriptionStatus')
            : null,
        notifyUrl: json.optString('notifyUrl'),
      );

  /// Set the filter for the contacts to be exported.
  final RequestContactExportRequestCustomContactFilter customContactFilter;

  /// To avoid generating the email notification upon contact export, pass **true**
  final bool? disableNotification;

  /// List of all the attributes that you want to export. **These attributes must be present in your contact database. It is required if exportMandatoryAttributes is set false. ** For example: **\['fname', 'lname', 'email'\]**
  final List<String>? exportAttributes;

  /// Specifies whether the date fields createdAt, modifiedAt in the exported data should be returned in UTC format.
  final bool? exportDateInUtc;

  /// To export mandatory attributes like EMAIL, ADDED_TIME, MODIFIED_TIME
  final bool? exportMandatoryAttributes;

  /// Export metadata of contacts such as _listIds, ADDED_TIME, MODIFIED_TIME.
  final List<String>? exportMetadata;

  /// Export subscription status of contacts for email & sms marketting. Pass email_marketing to obtain the marketing email subscription status & sms_marketing to retrieve the marketing SMS status of the contact.
  final List<String>? exportSubscriptionStatus;

  /// Webhook that will be called once the export process is finished. For reference, https://help.brevo.com/hc/en-us/articles/360007666479
  final String? notifyUrl;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'customContactFilter': customContactFilter.toJson(),
        if (disableNotification != null)
          'disableNotification': disableNotification!,
        if (exportAttributes != null) 'exportAttributes': exportAttributes!,
        if (exportDateInUtc != null) 'exportDateInUTC': exportDateInUtc!,
        if (exportMandatoryAttributes != null)
          'exportMandatoryAttributes': exportMandatoryAttributes!,
        if (exportMetadata != null) 'exportMetadata': exportMetadata!,
        if (exportSubscriptionStatus != null)
          'exportSubscriptionStatus': exportSubscriptionStatus!,
        if (notifyUrl != null) 'notifyUrl': notifyUrl!,
      };
}

/// Set the filter for the contacts to be exported.
final class RequestContactExportRequestCustomContactFilter {
  /// Creates the request body.
  const RequestContactExportRequestCustomContactFilter({
    this.actionForContacts,
    this.actionForEmailCampaigns,
    this.actionForSmsCampaigns,
    this.emailCampaignId,
    this.listId,
    this.segmentId,
    this.smsCampaignId,
  });

  /// Decodes a JSON object into a RequestContactExportRequestCustomContactFilter. Optional fields absent from [json] stay null.
  factory RequestContactExportRequestCustomContactFilter.fromJson(
          Map<String, Object?> json) =>
      RequestContactExportRequestCustomContactFilter(
        actionForContacts: json.optEnum(
            'actionForContacts',
            RequestContactExportRequestCustomContactFilterActionForContacts
                .fromWire),
        actionForEmailCampaigns: json.optEnum(
            'actionForEmailCampaigns',
            RequestContactExportRequestCustomContactFilterActionForEmailCampaigns
                .fromWire),
        actionForSmsCampaigns: json.optEnum(
            'actionForSmsCampaigns',
            RequestContactExportRequestCustomContactFilterActionForSmsCampaigns
                .fromWire),
        emailCampaignId: json.optInt('emailCampaignId'),
        listId: json.optInt('listId'),
        segmentId: json.optInt('segmentId'),
        smsCampaignId: json.optInt('smsCampaignId'),
      );

  /// **Mandatory if neither actionForEmailCampaigns nor actionForSmsCampaigns is passed.** This will export the contacts on the basis of provided action applied on contacts as per the list id. * **allContacts** - Fetch the list of all contacts for a particular list. * **subscribed & unsubscribed** - Fetch the list of subscribed / unsubscribed (blacklisted via any means) contacts for a particular list. * **unsubscribedPerList** - Fetch the list of contacts that are unsubscribed from a particular list only.
  final RequestContactExportRequestCustomContactFilterActionForContacts?
      actionForContacts;

  /// **Mandatory if neither actionForContacts nor actionForSmsCampaigns is passed.** This will export the contacts on the basis of provided action applied on email campaigns. * **openers & nonOpeners** - emailCampaignId is mandatory. Fetch the list of readers / non-readers for a particular email campaign. * **clickers & nonClickers** - emailCampaignId is mandatory. Fetch the list of clickers / non-clickers for a particular email campaign. * **unsubscribed** - emailCampaignId is mandatory. Fetch the list of all unsubscribed (blacklisted via any means) contacts for a particular email campaign. * **hardBounces & softBounces** - emailCampaignId is optional. Fetch the list of hard bounces / soft bounces for a particular / all email campaign(s).
  final RequestContactExportRequestCustomContactFilterActionForEmailCampaigns?
      actionForEmailCampaigns;

  /// **Mandatory if neither actionForContacts nor actionForEmailCampaigns is passed.** This will export the contacts on the basis of provided action applied on sms campaigns. * **unsubscribed** - Fetch the list of all unsubscribed (blacklisted via any means) contacts for all / particular sms campaigns. * **hardBounces & softBounces** - Fetch the list of hard bounces / soft bounces for all / particular sms campaigns.
  final RequestContactExportRequestCustomContactFilterActionForSmsCampaigns?
      actionForSmsCampaigns;

  /// Considered only if **actionForEmailCampaigns** is passed, ignored otherwise. **Mandatory if action is one of the following - openers, nonOpeners, clickers, nonClickers, unsubscribed.** The id of the email campaign for which the corresponding action shall be applied in the filter.
  final int? emailCampaignId;

  /// ID of the list. This is mandatory if actionForContacts is specified and segmentId is not provided. Either segmentId or listId must be included.
  final int? listId;

  /// ID of the segment. This is mandatory if actionForContacts is specified and listId is not provided. Either segmentId or listId must be included.
  final int? segmentId;

  /// Considered only if **actionForSmsCampaigns** is passed, ignored otherwise. The id of sms campaign for which the corresponding action shall be applied in the filter.
  final int? smsCampaignId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (actionForContacts != null)
          'actionForContacts': actionForContacts!.value,
        if (actionForEmailCampaigns != null)
          'actionForEmailCampaigns': actionForEmailCampaigns!.value,
        if (actionForSmsCampaigns != null)
          'actionForSmsCampaigns': actionForSmsCampaigns!.value,
        if (emailCampaignId != null) 'emailCampaignId': emailCampaignId!,
        if (listId != null) 'listId': listId!,
        if (segmentId != null) 'segmentId': segmentId!,
        if (smsCampaignId != null) 'smsCampaignId': smsCampaignId!,
      };
}

/// The request body of `PUT /contacts/attributes/{attributeCategory}/{attributeName}`.
final class UpdateAttributeRequest {
  /// Creates the request body.
  const UpdateAttributeRequest({
    this.enumeration,
    this.multiCategoryOptions,
    this.valueValue,
  });

  /// Decodes a JSON object into a UpdateAttributeRequest. Optional fields absent from [json] stay null.
  factory UpdateAttributeRequest.fromJson(Map<String, Object?> json) =>
      UpdateAttributeRequest(
        enumeration: json.containsKey('enumeration')
            ? json.optObjectList(
                'enumeration', UpdateAttributeRequestEnumerationItem.fromJson,
                objectName: 'UpdateAttributeRequest')
            : null,
        multiCategoryOptions: json.containsKey('multiCategoryOptions')
            ? json.optStringList('multiCategoryOptions')
            : null,
        valueValue: json.optString('value'),
      );

  /// List of the values and labels that the attribute can take. **Use only if the attribute's category is "category"** None of the category options can exceed max 200 characters. For example, **\[{"value":1, "label":"male"}, {"value":2, "label":"female"}\]**
  final List<UpdateAttributeRequestEnumerationItem>? enumeration;

  /// Use this option to add multiple-choice attributes options only if the attribute's category is "normal". **This option is specifically designed for updating multiple-choice attributes. None of the multicategory options can exceed max 200 characters.**. For example: **\["USA","INDIA"\]**
  final List<String>? multiCategoryOptions;

  /// Value of the attribute to update. **Use only if the attribute's category is 'calculated' or 'global'**
  final String? valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (enumeration != null)
          'enumeration': [for (final item in enumeration!) item.toJson()],
        if (multiCategoryOptions != null)
          'multiCategoryOptions': multiCategoryOptions!,
        if (valueValue != null) 'value': valueValue!,
      };
}

/// The request body of `UpdateAttributeRequest.enumerationItem`.
final class UpdateAttributeRequestEnumerationItem {
  /// Creates the request body.
  const UpdateAttributeRequestEnumerationItem({
    required this.label,
    required this.valueValue,
  });

  /// Decodes a JSON object into a UpdateAttributeRequestEnumerationItem. Optional fields absent from [json] stay null.
  factory UpdateAttributeRequestEnumerationItem.fromJson(
          Map<String, Object?> json) =>
      UpdateAttributeRequestEnumerationItem(
        label: json.requireString(
            'label', 'UpdateAttributeRequestEnumerationItem'),
        valueValue:
            json.requireInt('value', 'UpdateAttributeRequestEnumerationItem'),
      );

  /// Label of the value
  final String label;

  /// Id of the value
  final int valueValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'label': label,
        'value': valueValue,
      };
}

/// The request body of `POST /contacts/batch`.
final class UpdateBatchContactsRequest {
  /// Creates the request body.
  const UpdateBatchContactsRequest({
    this.contacts,
  });

  /// Decodes a JSON object into a UpdateBatchContactsRequest. Optional fields absent from [json] stay null.
  factory UpdateBatchContactsRequest.fromJson(Map<String, Object?> json) =>
      UpdateBatchContactsRequest(
        contacts: json.containsKey('contacts')
            ? json.optObjectList(
                'contacts', UpdateBatchContactsRequestContactsItem.fromJson,
                objectName: 'UpdateBatchContactsRequest')
            : null,
      );

  /// List of contacts to be updated
  final List<UpdateBatchContactsRequestContactsItem>? contacts;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contacts != null)
          'contacts': [for (final item in contacts!) item.toJson()],
      };
}

/// The request body of `UpdateBatchContactsRequest.contactsItem`.
final class UpdateBatchContactsRequestContactsItem {
  /// Creates the request body.
  const UpdateBatchContactsRequestContactsItem({
    this.attributes,
    this.email,
    this.emailBlacklisted,
    this.extId,
    this.id,
    this.listIds,
    this.sms,
    this.smsBlacklisted,
    this.smtpBlacklistSender,
    this.unlinkListIds,
  });

  /// Decodes a JSON object into a UpdateBatchContactsRequestContactsItem. Optional fields absent from [json] stay null.
  factory UpdateBatchContactsRequestContactsItem.fromJson(
          Map<String, Object?> json) =>
      UpdateBatchContactsRequestContactsItem(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        email: json.optString('email'),
        emailBlacklisted: json.optBool('emailBlacklisted'),
        extId: json.optString('ext_id'),
        id: json.optInt('id'),
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        sms: json.optString('sms'),
        smsBlacklisted: json.optBool('smsBlacklisted'),
        smtpBlacklistSender: json.containsKey('smtpBlacklistSender')
            ? json.optStringList('smtpBlacklistSender')
            : null,
        unlinkListIds: json.containsKey('unlinkListIds')
            ? json.optIntList('unlinkListIds')
            : null,
      );

  /// Pass the set of attributes to be updated. **These attributes must be present in your account**. To update existing email address of a contact with the new one please pass EMAIL in attribtes. For example, **{ "EMAIL":"newemail@domain.com", "FNAME":"Ellie", "LNAME":"Roger"}**. Keep in mind transactional attributes can be updated the same way as normal attributes. Mobile Number in **SMS** field should be passed with proper country code. For example: **{"SMS":"+91xxxxxxxxxx"} or {"SMS":"0091xxxxxxxxxx"}**
  final Map<String, Object?>? attributes;

  /// Email address of the user to be updated (For each operation only pass one of the supported contact identifiers. Email, id or sms)
  final String? email;

  /// Set/unset this field to blacklist/allow the contact for emails (emailBlacklisted = true)
  final bool? emailBlacklisted;

  /// Pass your own Id to update ext_id of a contact.
  final String? extId;

  /// id of the user to be updated (For each operation only pass one of the supported contact identifiers. Email, id or sms)
  final int? id;

  /// Ids of the lists to add the contact to
  final List<int>? listIds;

  /// SMS of the user to be updated (For each operation only pass one of the supported contact identifiers. Email, id or sms)
  final String? sms;

  /// Set/unset this field to blacklist/allow the contact for SMS (smsBlacklisted = true)
  final bool? smsBlacklisted;

  /// transactional email forbidden sender for contact. Use only for email Contact
  final List<String>? smtpBlacklistSender;

  /// Ids of the lists to remove the contact from
  final List<int>? unlinkListIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (email != null) 'email': email!,
        if (emailBlacklisted != null) 'emailBlacklisted': emailBlacklisted!,
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
        if (listIds != null) 'listIds': listIds!,
        if (sms != null) 'sms': sms!,
        if (smsBlacklisted != null) 'smsBlacklisted': smsBlacklisted!,
        if (smtpBlacklistSender != null)
          'smtpBlacklistSender': smtpBlacklistSender!,
        if (unlinkListIds != null) 'unlinkListIds': unlinkListIds!,
      };
}

/// The request body of `PUT /contacts/{identifier}`.
final class UpdateContactRequest {
  /// Creates the request body.
  const UpdateContactRequest({
    this.attributes,
    this.emailBlacklisted,
    this.extId,
    this.forceMerge,
    this.listIds,
    this.smsBlacklisted,
    this.smtpBlacklistSender,
    this.unlinkListIds,
  });

  /// Decodes a JSON object into a UpdateContactRequest. Optional fields absent from [json] stay null.
  factory UpdateContactRequest.fromJson(Map<String, Object?> json) =>
      UpdateContactRequest(
        attributes: json.containsKey('attributes')
            ? json.optObjectMap('attributes')
            : null,
        emailBlacklisted: json.optBool('emailBlacklisted'),
        extId: json.optString('ext_id'),
        forceMerge: json.optBool('forceMerge'),
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        smsBlacklisted: json.optBool('smsBlacklisted'),
        smtpBlacklistSender: json.containsKey('smtpBlacklistSender')
            ? json.optStringList('smtpBlacklistSender')
            : null,
        unlinkListIds: json.containsKey('unlinkListIds')
            ? json.optIntList('unlinkListIds')
            : null,
      );

  /// Pass the set of attributes to be updated. **These attributes must be present in your account**. To update existing email address of a contact with the new one please pass EMAIL in attributes. For example, **{ "EMAIL":"newemail@domain.com", "FNAME":"Ellie", "LNAME":"Roger", "COUNTRIES":\["India","China"\]}**. The attribute's parameter should be passed in capital letter while updating a contact. Values that don't match the attribute type (e.g. text or string in a date attribute) will be ignored .Keep in mind transactional attributes can be updated the same way as normal attributes. Mobile Number in **SMS** field should be passed with proper country code. For example: **{"SMS":"+91xxxxxxxxxx"} or {"SMS":"0091xxxxxxxxxx"}**
  final Map<String, Object?>? attributes;

  /// Set/unset this field to blacklist/allow the contact for emails (emailBlacklisted = true)
  final bool? emailBlacklisted;

  /// Pass your own Id to update ext_id of a contact.
  final String? extId;

  /// When true, if the contact being updated shares an identifier (email, SMS, ext_id, whatsapp, landline) with an existing contact, the two contacts are force-merged. The contact with the most recent `last_modified` timestamp is retained; the other is deleted. When false (default), a 4xx error is returned on identifier conflict.
  final bool? forceMerge;

  /// Ids of the lists to add the contact to
  final List<int>? listIds;

  /// Set/unset this field to blacklist/allow the contact for SMS (smsBlacklisted = true)
  final bool? smsBlacklisted;

  /// transactional email forbidden sender for contact. Use only for email Contact
  final List<String>? smtpBlacklistSender;

  /// Ids of the lists to remove the contact from
  final List<int>? unlinkListIds;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (attributes != null) 'attributes': attributes!,
        if (emailBlacklisted != null) 'emailBlacklisted': emailBlacklisted!,
        if (extId != null) 'ext_id': extId!,
        if (forceMerge != null) 'forceMerge': forceMerge!,
        if (listIds != null) 'listIds': listIds!,
        if (smsBlacklisted != null) 'smsBlacklisted': smsBlacklisted!,
        if (smtpBlacklistSender != null)
          'smtpBlacklistSender': smtpBlacklistSender!,
        if (unlinkListIds != null) 'unlinkListIds': unlinkListIds!,
      };
}

/// The request body of `PUT /contacts/lists/{listId}`.
final class UpdateListRequest {
  /// Creates the request body.
  const UpdateListRequest({
    this.folderId,
    this.name,
  });

  /// Decodes a JSON object into a UpdateListRequest. Optional fields absent from [json] stay null.
  factory UpdateListRequest.fromJson(Map<String, Object?> json) =>
      UpdateListRequest(
        folderId: json.optInt('folderId'),
        name: json.optString('name'),
      );

  /// Id of the folder in which the list is to be moved. Either of the two parameters (name, folderId) can be updated at a time.
  final int? folderId;

  /// Name of the list. Either of the two parameters (name, folderId) can be updated at a time.
  final String? name;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (folderId != null) 'folderId': folderId!,
        if (name != null) 'name': name!,
      };
}
