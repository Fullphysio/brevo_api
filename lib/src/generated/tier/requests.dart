// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `POST /loyalty/tier/programs/{pid}/tier-groups/{gid}/tiers`.
final class CreateTierForTierGroupRequest {
  /// Creates the request body.
  const CreateTierForTierGroupRequest({
    required this.accessConditions,
    this.imageRef,
    required this.name,
    this.tierRewards,
  });

  /// Decodes a JSON object into a CreateTierForTierGroupRequest. Optional fields absent from [json] stay null.
  factory CreateTierForTierGroupRequest.fromJson(Map<String, Object?> json) =>
      CreateTierForTierGroupRequest(
        accessConditions: json.containsKey('accessConditions')
            ? json.optObjectList('accessConditions',
                CreateTierForTierGroupRequestAccessConditionsItem.fromJson,
                objectName: 'CreateTierForTierGroupRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateTierForTierGroupRequest',
                key: 'accessConditions')),
        imageRef: json.optString('imageRef'),
        name: json.requireString('name', 'CreateTierForTierGroupRequest'),
        tierRewards: json.containsKey('tierRewards')
            ? json.optObjectList('tierRewards',
                CreateTierForTierGroupRequestTierRewardsItem.fromJson,
                objectName: 'CreateTierForTierGroupRequest')
            : null,
      );

  /// The `accessConditions` field.
  final List<CreateTierForTierGroupRequestAccessConditionsItem>
      accessConditions;

  /// Image of the tier
  final String? imageRef;

  /// Name of the tier to be created
  final String name;

  /// The `tierRewards` field.
  final List<CreateTierForTierGroupRequestTierRewardsItem>? tierRewards;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'accessConditions': [
          for (final item in accessConditions) item.toJson()
        ],
        if (imageRef != null) 'imageRef': imageRef!,
        'name': name,
        if (tierRewards != null)
          'tierRewards': [for (final item in tierRewards!) item.toJson()],
      };
}

/// The request body of `CreateTierForTierGroupRequest.accessConditionsItem`.
final class CreateTierForTierGroupRequestAccessConditionsItem {
  /// Creates the request body.
  const CreateTierForTierGroupRequestAccessConditionsItem({
    this.balanceDefinitionId,
    this.minimumValue,
  });

  /// Decodes a JSON object into a CreateTierForTierGroupRequestAccessConditionsItem. Optional fields absent from [json] stay null.
  factory CreateTierForTierGroupRequestAccessConditionsItem.fromJson(
          Map<String, Object?> json) =>
      CreateTierForTierGroupRequestAccessConditionsItem(
        balanceDefinitionId: json.optString('balanceDefinitionId'),
        minimumValue: json.optInt('minimumValue'),
      );

  /// Balance definition identifier for accessing the tier
  final String? balanceDefinitionId;

  /// Minimum value required to access the tier
  final int? minimumValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (balanceDefinitionId != null)
          'balanceDefinitionId': balanceDefinitionId!,
        if (minimumValue != null) 'minimumValue': minimumValue!,
      };
}

/// The request body of `CreateTierForTierGroupRequest.tierRewardsItem`.
final class CreateTierForTierGroupRequestTierRewardsItem {
  /// Creates the request body.
  const CreateTierForTierGroupRequestTierRewardsItem({
    this.rewardId,
  });

  /// Decodes a JSON object into a CreateTierForTierGroupRequestTierRewardsItem. Optional fields absent from [json] stay null.
  factory CreateTierForTierGroupRequestTierRewardsItem.fromJson(
          Map<String, Object?> json) =>
      CreateTierForTierGroupRequestTierRewardsItem(
        rewardId: json.optString('rewardId'),
      );

  /// Reward unique identifier
  final String? rewardId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (rewardId != null) 'rewardId': rewardId!,
      };
}

/// The request body of `POST /loyalty/tier/programs/{pid}/tier-groups`.
final class CreateTierGroupRequest {
  /// Creates the request body.
  const CreateTierGroupRequest({
    this.downgradeSchedule,
    this.downgradeStrategy,
    this.meta,
    required this.name,
    this.tierOrder,
    this.upgradeSchedule,
    this.upgradeStrategy,
  });

  /// Decodes a JSON object into a CreateTierGroupRequest. Optional fields absent from [json] stay null.
  factory CreateTierGroupRequest.fromJson(Map<String, Object?> json) =>
      CreateTierGroupRequest(
        downgradeSchedule: json.optNested('downgradeSchedule',
            CreateTierGroupRequestDowngradeSchedule.fromJson),
        downgradeStrategy: json.optEnum('downgradeStrategy',
            CreateTierGroupRequestDowngradeStrategy.fromWire),
        meta: json.optNested('meta', CreateTierGroupRequestMeta.fromJson),
        name: json.requireString('name', 'CreateTierGroupRequest'),
        tierOrder: json.containsKey('tierOrder')
            ? json.optStringList('tierOrder')
            : null,
        upgradeSchedule: json.optNested(
            'upgradeSchedule', CreateTierGroupRequestUpgradeSchedule.fromJson),
        upgradeStrategy: json.optEnum(
            'upgradeStrategy', CreateTierGroupRequestUpgradeStrategy.fromWire),
      );

  /// Schedule configuration for tier downgrades. Required when downgradeStrategy is set to a schedule-based strategy.
  final CreateTierGroupRequestDowngradeSchedule? downgradeSchedule;

  /// Select real_time to downgrade tier on real time balance updates. Select membership_anniversary to downgrade tier on subscription anniversary. Select tier_anniversary to downgrade tier on tier anniversary.
  final CreateTierGroupRequestDowngradeStrategy? downgradeStrategy;

  /// Additional metadata for the tier group.
  final CreateTierGroupRequestMeta? meta;

  /// Name of the tier group
  final String name;

  /// Order of the tiers in the group in ascending order
  final List<String>? tierOrder;

  /// Schedule configuration for tier upgrades. Required when upgradeStrategy is set to a schedule-based strategy.
  final CreateTierGroupRequestUpgradeSchedule? upgradeSchedule;

  /// Select real_time to upgrade tier on real time balance updates. Select membership_anniversary to upgrade tier on subscription anniversary. Select tier_anniversary to upgrade tier on tier anniversary.
  final CreateTierGroupRequestUpgradeStrategy? upgradeStrategy;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (downgradeSchedule != null)
          'downgradeSchedule': downgradeSchedule!.toJson(),
        if (downgradeStrategy != null)
          'downgradeStrategy': downgradeStrategy!.value,
        if (meta != null) 'meta': meta!.toJson(),
        'name': name,
        if (tierOrder != null) 'tierOrder': tierOrder!,
        if (upgradeSchedule != null)
          'upgradeSchedule': upgradeSchedule!.toJson(),
        if (upgradeStrategy != null) 'upgradeStrategy': upgradeStrategy!.value,
      };
}

/// Schedule configuration for tier downgrades. Required when downgradeStrategy is set to a schedule-based strategy.
final class CreateTierGroupRequestDowngradeSchedule {
  /// Creates the request body.
  const CreateTierGroupRequestDowngradeSchedule({
    this.durationModifier,
    this.durationUnit,
    this.durationValue,
    this.scheduledDate,
  });

  /// Decodes a JSON object into a CreateTierGroupRequestDowngradeSchedule. Optional fields absent from [json] stay null.
  factory CreateTierGroupRequestDowngradeSchedule.fromJson(
          Map<String, Object?> json) =>
      CreateTierGroupRequestDowngradeSchedule(
        durationModifier: json.optEnum('durationModifier',
            CreateTierGroupRequestDowngradeScheduleDurationModifier.fromWire),
        durationUnit: json.optEnum('durationUnit',
            CreateTierGroupRequestDowngradeScheduleDurationUnit.fromWire),
        durationValue: json.optInt('durationValue'),
        scheduledDate: json.optString('scheduledDate'),
      );

  /// Modifier for the duration.
  final CreateTierGroupRequestDowngradeScheduleDurationModifier?
      durationModifier;

  /// Duration unit for the schedule.
  final CreateTierGroupRequestDowngradeScheduleDurationUnit? durationUnit;

  /// Duration value for the schedule.
  final int? durationValue;

  /// Scheduled date in DD/MM format.
  final String? scheduledDate;

  /// [scheduledDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledDateDate =>
      scheduledDate == null ? null : DateTime.tryParse(scheduledDate!);

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (durationModifier != null)
          'durationModifier': durationModifier!.value,
        if (durationUnit != null) 'durationUnit': durationUnit!.value,
        if (durationValue != null) 'durationValue': durationValue!,
        if (scheduledDate != null) 'scheduledDate': scheduledDate!,
      };
}

/// Additional metadata for the tier group.
final class CreateTierGroupRequestMeta {
  /// Creates the request body.
  const CreateTierGroupRequestMeta({
    this.isInternal,
  });

  /// Decodes a JSON object into a CreateTierGroupRequestMeta. Optional fields absent from [json] stay null.
  factory CreateTierGroupRequestMeta.fromJson(Map<String, Object?> json) =>
      CreateTierGroupRequestMeta(
        isInternal: json.optBool('isInternal'),
      );

  /// Indicates whether the tier group is internal.
  final bool? isInternal;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (isInternal != null) 'isInternal': isInternal!,
      };
}

/// Schedule configuration for tier upgrades. Required when upgradeStrategy is set to a schedule-based strategy.
final class CreateTierGroupRequestUpgradeSchedule {
  /// Creates the request body.
  const CreateTierGroupRequestUpgradeSchedule({
    this.durationModifier,
    this.durationUnit,
    this.durationValue,
    this.scheduledDate,
  });

  /// Decodes a JSON object into a CreateTierGroupRequestUpgradeSchedule. Optional fields absent from [json] stay null.
  factory CreateTierGroupRequestUpgradeSchedule.fromJson(
          Map<String, Object?> json) =>
      CreateTierGroupRequestUpgradeSchedule(
        durationModifier: json.optEnum('durationModifier',
            CreateTierGroupRequestUpgradeScheduleDurationModifier.fromWire),
        durationUnit: json.optEnum('durationUnit',
            CreateTierGroupRequestUpgradeScheduleDurationUnit.fromWire),
        durationValue: json.optInt('durationValue'),
        scheduledDate: json.optString('scheduledDate'),
      );

  /// Modifier for the duration.
  final CreateTierGroupRequestUpgradeScheduleDurationModifier? durationModifier;

  /// Duration unit for the schedule.
  final CreateTierGroupRequestUpgradeScheduleDurationUnit? durationUnit;

  /// Duration value for the schedule.
  final int? durationValue;

  /// Scheduled date in DD/MM format.
  final String? scheduledDate;

  /// [scheduledDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledDateDate =>
      scheduledDate == null ? null : DateTime.tryParse(scheduledDate!);

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (durationModifier != null)
          'durationModifier': durationModifier!.value,
        if (durationUnit != null) 'durationUnit': durationUnit!.value,
        if (durationValue != null) 'durationValue': durationValue!,
        if (scheduledDate != null) 'scheduledDate': scheduledDate!,
      };
}

/// The request body of `PUT /loyalty/tier/programs/{pid}/tier-groups/{gid}`.
final class UpdateTierGroupRequest {
  /// Creates the request body.
  const UpdateTierGroupRequest({
    required this.downgradeStrategy,
    this.meta,
    required this.name,
    required this.tierOrder,
    required this.upgradeStrategy,
  });

  /// Decodes a JSON object into a UpdateTierGroupRequest. Optional fields absent from [json] stay null.
  factory UpdateTierGroupRequest.fromJson(Map<String, Object?> json) =>
      UpdateTierGroupRequest(
        downgradeStrategy: json.optEnum('downgradeStrategy',
                UpdateTierGroupRequestDowngradeStrategy.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpdateTierGroupRequest',
                key: 'downgradeStrategy')),
        meta: json.optNested('meta', UpdateTierGroupRequestMeta.fromJson),
        name: json.requireString('name', 'UpdateTierGroupRequest'),
        tierOrder: json.containsKey('tierOrder')
            ? json.optStringList('tierOrder')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpdateTierGroupRequest', key: 'tierOrder')),
        upgradeStrategy: json.optEnum('upgradeStrategy',
                UpdateTierGroupRequestUpgradeStrategy.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpdateTierGroupRequest', key: 'upgradeStrategy')),
      );

  /// Select real_time to downgrade tier on real time balance updates. Select membership_anniversary to downgrade tier on subscription anniversary. Select tier_anniversary to downgrade tier on tier anniversary.
  final UpdateTierGroupRequestDowngradeStrategy downgradeStrategy;

  /// Additional metadata for the tier group.
  final UpdateTierGroupRequestMeta? meta;

  /// Name of the tier group
  final String name;

  /// Order of the tiers in the group in ascending order
  final List<String> tierOrder;

  /// Select real_time to upgrade tier on real time balance updates. Select membership_anniversary to upgrade tier on subscription anniversary. Select tier_anniversary to upgrade tier on tier anniversary.
  final UpdateTierGroupRequestUpgradeStrategy upgradeStrategy;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'downgradeStrategy': downgradeStrategy.value,
        if (meta != null) 'meta': meta!.toJson(),
        'name': name,
        'tierOrder': tierOrder,
        'upgradeStrategy': upgradeStrategy.value,
      };
}

/// Additional metadata for the tier group.
final class UpdateTierGroupRequestMeta {
  /// Creates the request body.
  const UpdateTierGroupRequestMeta({
    this.isInternal,
  });

  /// Decodes a JSON object into a UpdateTierGroupRequestMeta. Optional fields absent from [json] stay null.
  factory UpdateTierGroupRequestMeta.fromJson(Map<String, Object?> json) =>
      UpdateTierGroupRequestMeta(
        isInternal: json.optBool('isInternal'),
      );

  /// Indicates whether the tier group is internal.
  final bool? isInternal;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (isInternal != null) 'isInternal': isInternal!,
      };
}

/// The request body of `PUT /loyalty/tier/programs/{pid}/tiers/{tid}`.
final class UpdateTierRequest {
  /// Creates the request body.
  const UpdateTierRequest({
    required this.accessConditions,
    this.imageRef,
    required this.name,
    required this.tierRewards,
  });

  /// Decodes a JSON object into a UpdateTierRequest. Optional fields absent from [json] stay null.
  factory UpdateTierRequest.fromJson(Map<String, Object?> json) =>
      UpdateTierRequest(
        accessConditions: json.containsKey('accessConditions')
            ? json.optObjectList('accessConditions',
                UpdateTierRequestAccessConditionsItem.fromJson,
                objectName: 'UpdateTierRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpdateTierRequest', key: 'accessConditions')),
        imageRef: json.optString('imageRef'),
        name: json.requireString('name', 'UpdateTierRequest'),
        tierRewards: json.containsKey('tierRewards')
            ? json.optObjectList(
                'tierRewards', UpdateTierRequestTierRewardsItem.fromJson,
                objectName: 'UpdateTierRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'UpdateTierRequest', key: 'tierRewards')),
      );

  /// The `accessConditions` field.
  final List<UpdateTierRequestAccessConditionsItem> accessConditions;

  /// Image of the tier
  final String? imageRef;

  /// Name of the tier to be created
  final String name;

  /// The `tierRewards` field.
  final List<UpdateTierRequestTierRewardsItem> tierRewards;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'accessConditions': [
          for (final item in accessConditions) item.toJson()
        ],
        if (imageRef != null) 'imageRef': imageRef!,
        'name': name,
        'tierRewards': [for (final item in tierRewards) item.toJson()],
      };
}

/// The request body of `UpdateTierRequest.accessConditionsItem`.
final class UpdateTierRequestAccessConditionsItem {
  /// Creates the request body.
  const UpdateTierRequestAccessConditionsItem({
    this.balanceDefinitionId,
    this.minimumValue,
  });

  /// Decodes a JSON object into a UpdateTierRequestAccessConditionsItem. Optional fields absent from [json] stay null.
  factory UpdateTierRequestAccessConditionsItem.fromJson(
          Map<String, Object?> json) =>
      UpdateTierRequestAccessConditionsItem(
        balanceDefinitionId: json.optString('balanceDefinitionId'),
        minimumValue: json.optInt('minimumValue'),
      );

  /// Balance definition identifier for accessing the tier
  final String? balanceDefinitionId;

  /// Minimum value required to access the tier
  final int? minimumValue;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (balanceDefinitionId != null)
          'balanceDefinitionId': balanceDefinitionId!,
        if (minimumValue != null) 'minimumValue': minimumValue!,
      };
}

/// The request body of `UpdateTierRequest.tierRewardsItem`.
final class UpdateTierRequestTierRewardsItem {
  /// Creates the request body.
  const UpdateTierRequestTierRewardsItem({
    this.rewardId,
  });

  /// Decodes a JSON object into a UpdateTierRequestTierRewardsItem. Optional fields absent from [json] stay null.
  factory UpdateTierRequestTierRewardsItem.fromJson(
          Map<String, Object?> json) =>
      UpdateTierRequestTierRewardsItem(
        rewardId: json.optString('rewardId'),
      );

  /// Reward unique identifier
  final String? rewardId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (rewardId != null) 'rewardId': rewardId!,
      };
}
