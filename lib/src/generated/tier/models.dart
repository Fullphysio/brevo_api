// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `POST /loyalty/tier/programs/{pid}/contacts/{cid}/tiers/{tid}` object of the Brevo API.
final class AddSubscriptionToTierResponse {
  /// Creates a AddSubscriptionToTierResponse from already-decoded fields.
  const AddSubscriptionToTierResponse({
    this.contactId,
    this.createdAt,
    this.groupId,
    this.id,
    this.loyaltyProgramId,
    this.meta = const {},
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a AddSubscriptionToTierResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory AddSubscriptionToTierResponse.fromJson(Map<String, Object?> json) =>
      AddSubscriptionToTierResponse(
        contactId: json.optInt('contactId'),
        createdAt: json.optString('createdAt'),
        groupId: json.optString('groupId'),
        id: json.optString('id'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        meta: json.optObjectMap('meta'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Contact to which the tier is assigned
  final int? contactId;

  /// The `createdAt` field.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Group Id to which the tier is associated
  final String? groupId;

  /// Unique identifier for the assigned tier
  final String? id;

  /// Associated loyalty program Id
  final String? loyaltyProgramId;

  /// object
  final Map<String, Object?> meta;

  /// The `updatedAt` field.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contactId': contactId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (groupId != null) 'groupId': groupId!,
        if (id != null) 'id': id!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (meta.isNotEmpty) 'meta': meta,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `GET /loyalty/tier/programs/{pid}/tier-groups` object of the Brevo API.
final class GetListOfTierGroupsResponse {
  /// Creates a GetListOfTierGroupsResponse from already-decoded fields.
  const GetListOfTierGroupsResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetListOfTierGroupsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetListOfTierGroupsResponse.fromJson(Map<String, Object?> json) =>
      GetListOfTierGroupsResponse(
        items: json.optObjectList('items', TierGroup.fromJson,
            objectName: 'GetListOfTierGroupsResponse'),
        raw: json,
      );

  /// The `items` field.
  final List<TierGroup> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// The `GET /loyalty/tier/programs/{pid}/tiers` object of the Brevo API.
final class GetLoyaltyProgramTierResponse {
  /// Creates a GetLoyaltyProgramTierResponse from already-decoded fields.
  const GetLoyaltyProgramTierResponse({
    this.items = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetLoyaltyProgramTierResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetLoyaltyProgramTierResponse.fromJson(Map<String, Object?> json) =>
      GetLoyaltyProgramTierResponse(
        items: json.optObjectList('items', Tier.fromJson,
            objectName: 'GetLoyaltyProgramTierResponse'),
        raw: json,
      );

  /// The `items` field.
  final List<Tier> items;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (items.isNotEmpty)
          'items': [for (final item in items) item.toJson()],
      };
}

/// The `components/schemas/tier` object of the Brevo API.
final class Tier {
  /// Creates a Tier from already-decoded fields.
  const Tier({
    this.accessConditions = const [],
    this.createdAt,
    this.groupId,
    this.imageRef,
    this.loyaltyProgramId,
    this.name,
    this.tierId,
    this.tierRewards = const [],
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a Tier. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory Tier.fromJson(Map<String, Object?> json) => Tier(
        accessConditions: json.optObjectList(
            'accessConditions', TierAccessConditionsItem.fromJson,
            objectName: 'Tier'),
        createdAt: json.optString('createdAt'),
        groupId: json.optString('groupId'),
        imageRef: json.optString('imageRef'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        name: json.optString('name'),
        tierId: json.optString('tierId'),
        tierRewards: json.optObjectList(
            'tierRewards', TierTierRewardsItem.fromJson,
            objectName: 'Tier'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Conditions required to access this tier
  final List<TierAccessConditionsItem> accessConditions;

  /// The `createdAt` field.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Associated group Id
  final String? groupId;

  /// Tier image reference
  final String? imageRef;

  /// Associated loyalty program Id
  final String? loyaltyProgramId;

  /// Tier name
  final String? name;

  /// Tier id
  final String? tierId;

  /// Rewards associated with this tier
  final List<TierTierRewardsItem> tierRewards;

  /// The `updatedAt` field.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (accessConditions.isNotEmpty)
          'accessConditions': [
            for (final item in accessConditions) item.toJson()
          ],
        if (createdAt != null) 'createdAt': createdAt!,
        if (groupId != null) 'groupId': groupId!,
        if (imageRef != null) 'imageRef': imageRef!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (name != null) 'name': name!,
        if (tierId != null) 'tierId': tierId!,
        if (tierRewards.isNotEmpty)
          'tierRewards': [for (final item in tierRewards) item.toJson()],
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `Tier.accessConditionsItem` object of the Brevo API.
final class TierAccessConditionsItem {
  /// Creates a TierAccessConditionsItem from already-decoded fields.
  const TierAccessConditionsItem({
    this.balanceDefinitionId,
    this.createdAt,
    this.minimumValue,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TierAccessConditionsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TierAccessConditionsItem.fromJson(Map<String, Object?> json) =>
      TierAccessConditionsItem(
        balanceDefinitionId: json.optString('balanceDefinitionId'),
        createdAt: json.optString('createdAt'),
        minimumValue: json.optInt('minimumValue'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Balance definition identifier
  final String? balanceDefinitionId;

  /// The `createdAt` field.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Minimum value required to access this tier
  final int? minimumValue;

  /// The `updatedAt` field.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (balanceDefinitionId != null)
          'balanceDefinitionId': balanceDefinitionId!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (minimumValue != null) 'minimumValue': minimumValue!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// The `components/schemas/tierGroup` object of the Brevo API.
final class TierGroup {
  /// Creates a TierGroup from already-decoded fields.
  const TierGroup({
    this.createdAt,
    this.downgradeSchedule,
    this.downgradeStrategy,
    this.id,
    this.loyaltyProgramId,
    this.name,
    this.tierOrder = const [],
    this.updatedAt,
    this.upgradeSchedule,
    this.upgradeStrategy,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TierGroup. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TierGroup.fromJson(Map<String, Object?> json) => TierGroup(
        createdAt: json.optString('createdAt'),
        downgradeSchedule: json.optNested(
            'downgradeSchedule', TierGroupDowngradeSchedule.fromJson),
        downgradeStrategy: json.optEnum(
            'downgradeStrategy', TierGroupDowngradeStrategy.fromWire),
        id: json.optString('id'),
        loyaltyProgramId: json.optString('loyaltyProgramId'),
        name: json.optString('name'),
        tierOrder: json.optStringList('tierOrder'),
        updatedAt: json.optString('updatedAt'),
        upgradeSchedule: json.optNested(
            'upgradeSchedule', TierGroupUpgradeSchedule.fromJson),
        upgradeStrategy:
            json.optEnum('upgradeStrategy', TierGroupUpgradeStrategy.fromWire),
        raw: json,
      );

  /// Timestamp when the tier group was created
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Schedule configuration for tier downgrades.
  final TierGroupDowngradeSchedule? downgradeSchedule;

  /// Select real_time to downgrade tier on real time balance updates. Select membership_anniversary to downgrade tier on subscription anniversary. Select tier_anniversary to downgrade tier on tier anniversary.
  final TierGroupDowngradeStrategy? downgradeStrategy;

  /// Tier group unique identifier
  final String? id;

  /// Associated loyalty program Id
  final String? loyaltyProgramId;

  /// Tier group name
  final String? name;

  /// Order of the tiers in the group in ascending order
  final List<String> tierOrder;

  /// Timestamp when the tier group was last updated
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// Schedule configuration for tier upgrades.
  final TierGroupUpgradeSchedule? upgradeSchedule;

  /// Select real_time to upgrade tier on real time balance updates. Select membership_anniversary to upgrade tier on subscription anniversary. Select tier_anniversary to upgrade tier on tier anniversary.
  final TierGroupUpgradeStrategy? upgradeStrategy;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (downgradeSchedule != null)
          'downgradeSchedule': downgradeSchedule!.toJson(),
        if (downgradeStrategy != null)
          'downgradeStrategy': downgradeStrategy!.value,
        if (id != null) 'id': id!,
        if (loyaltyProgramId != null) 'loyaltyProgramId': loyaltyProgramId!,
        if (name != null) 'name': name!,
        if (tierOrder.isNotEmpty) 'tierOrder': tierOrder,
        if (updatedAt != null) 'updatedAt': updatedAt!,
        if (upgradeSchedule != null)
          'upgradeSchedule': upgradeSchedule!.toJson(),
        if (upgradeStrategy != null) 'upgradeStrategy': upgradeStrategy!.value,
      };
}

/// Schedule configuration for tier downgrades.
final class TierGroupDowngradeSchedule {
  /// Creates a TierGroupDowngradeSchedule from already-decoded fields.
  const TierGroupDowngradeSchedule({
    this.durationModifier,
    this.durationUnit,
    this.durationValue,
    this.scheduledDate,
    this.strategy,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TierGroupDowngradeSchedule. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TierGroupDowngradeSchedule.fromJson(Map<String, Object?> json) =>
      TierGroupDowngradeSchedule(
        durationModifier: json.optEnum('durationModifier',
            TierGroupDowngradeScheduleDurationModifier.fromWire),
        durationUnit: json.optEnum(
            'durationUnit', TierGroupDowngradeScheduleDurationUnit.fromWire),
        durationValue: json.optInt('durationValue'),
        scheduledDate: json.optString('scheduledDate'),
        strategy: json.optString('strategy'),
        raw: json,
      );

  /// Modifier for the duration.
  final TierGroupDowngradeScheduleDurationModifier? durationModifier;

  /// Duration unit for the schedule.
  final TierGroupDowngradeScheduleDurationUnit? durationUnit;

  /// Duration value for the schedule.
  final int? durationValue;

  /// Scheduled date in DD/MM format.
  final String? scheduledDate;

  /// [scheduledDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledDateDate =>
      scheduledDate == null ? null : DateTime.tryParse(scheduledDate!);

  /// Schedule strategy type.
  final String? strategy;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (durationModifier != null)
          'durationModifier': durationModifier!.value,
        if (durationUnit != null) 'durationUnit': durationUnit!.value,
        if (durationValue != null) 'durationValue': durationValue!,
        if (scheduledDate != null) 'scheduledDate': scheduledDate!,
        if (strategy != null) 'strategy': strategy!,
      };
}

/// Schedule configuration for tier upgrades.
final class TierGroupUpgradeSchedule {
  /// Creates a TierGroupUpgradeSchedule from already-decoded fields.
  const TierGroupUpgradeSchedule({
    this.durationModifier,
    this.durationUnit,
    this.durationValue,
    this.scheduledDate,
    this.strategy,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TierGroupUpgradeSchedule. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TierGroupUpgradeSchedule.fromJson(Map<String, Object?> json) =>
      TierGroupUpgradeSchedule(
        durationModifier: json.optEnum('durationModifier',
            TierGroupUpgradeScheduleDurationModifier.fromWire),
        durationUnit: json.optEnum(
            'durationUnit', TierGroupUpgradeScheduleDurationUnit.fromWire),
        durationValue: json.optInt('durationValue'),
        scheduledDate: json.optString('scheduledDate'),
        strategy: json.optString('strategy'),
        raw: json,
      );

  /// Modifier for the duration.
  final TierGroupUpgradeScheduleDurationModifier? durationModifier;

  /// Duration unit for the schedule.
  final TierGroupUpgradeScheduleDurationUnit? durationUnit;

  /// Duration value for the schedule.
  final int? durationValue;

  /// Scheduled date in DD/MM format.
  final String? scheduledDate;

  /// [scheduledDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get scheduledDateDate =>
      scheduledDate == null ? null : DateTime.tryParse(scheduledDate!);

  /// Schedule strategy type.
  final String? strategy;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (durationModifier != null)
          'durationModifier': durationModifier!.value,
        if (durationUnit != null) 'durationUnit': durationUnit!.value,
        if (durationValue != null) 'durationValue': durationValue!,
        if (scheduledDate != null) 'scheduledDate': scheduledDate!,
        if (strategy != null) 'strategy': strategy!,
      };
}

/// The `Tier.tierRewardsItem` object of the Brevo API.
final class TierTierRewardsItem {
  /// Creates a TierTierRewardsItem from already-decoded fields.
  const TierTierRewardsItem({
    this.createdAt,
    this.rewardId,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a TierTierRewardsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory TierTierRewardsItem.fromJson(Map<String, Object?> json) =>
      TierTierRewardsItem(
        createdAt: json.optString('createdAt'),
        rewardId: json.optString('rewardId'),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// The `createdAt` field.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Reward to be attributed on tier assignment
  final String? rewardId;

  /// The `updatedAt` field.
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (rewardId != null) 'rewardId': rewardId!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}
