// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `components/schemas/walletPassInstallUrl` object of the Brevo API.
final class WalletPassInstallUrl {
  /// Creates a WalletPassInstallUrl from already-decoded fields.
  const WalletPassInstallUrl({
    this.url,
    this.raw = const {},
  });

  /// Decodes a JSON object into a WalletPassInstallUrl. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory WalletPassInstallUrl.fromJson(Map<String, Object?> json) =>
      WalletPassInstallUrl(
        url: json.optString('url'),
        raw: json,
      );

  /// The wallet installation URL for the contact. Opening it lets the contact add the pass to their Apple Wallet or Google Wallet.
  final String? url;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (url != null) 'url': url!,
      };
}
