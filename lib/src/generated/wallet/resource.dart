// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/client.dart';
import '../../core/json_reading.dart';
import '../../core/path_encoding.dart';
import '../../core/request_options.dart';
import 'models.dart';

/// The `wallet` resource of the Brevo API: `client.wallet`.
final class BrevoWallet {
  /// Creates the resource backed by [_client].
  const BrevoWallet(this._client);

  final BrevoClient _client;

  /// `GET /wallet/passes/{passId}/installUrl/{contactId}`
  ///
  /// Get a pass installation URL for a contact
  ///
  /// Generate a wallet installation URL for a specific contact. The returned URL points to the pass installation page and encodes the pass, contact and organization identifiers as an encrypted token, so it can be shared with the contact (email, SMS, QR code, ...) to add the pass to their Apple Wallet or Google Wallet.
  ///
  /// \[passId\]: Pass ID. The unique identifier of the wallet pass for which to generate an installation URL.
  ///
  /// \[contactId\]: The Brevo contact ID the installation URL is generated for.
  Future<WalletPassInstallUrl> getWalletPassInstallUrl(
      {required String passId,
      required int contactId,
      BrevoRequestOptions? options}) async {
    final json = await _client.requestJson(
        method: 'GET',
        path:
            '/wallet/passes/${brevoPathSegment(passId)}/installUrl/${brevoPathSegment(contactId)}',
        options: options);
    return WalletPassInstallUrl.fromJson(brevoRequireJsonObject(
        json ?? const <String, Object?>{},
        objectName: 'WalletPassInstallUrl'));
  }
}
