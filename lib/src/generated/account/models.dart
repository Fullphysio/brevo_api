// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /organization/activities` object of the Brevo API.
final class GetAccountActivityResponse {
  /// Creates a GetAccountActivityResponse from already-decoded fields.
  const GetAccountActivityResponse({
    this.logs = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountActivityResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountActivityResponse.fromJson(Map<String, Object?> json) =>
      GetAccountActivityResponse(
        logs: json.optObjectList(
            'logs', GetAccountActivityResponseLogsItem.fromJson,
            objectName: 'GetAccountActivityResponse'),
        raw: json,
      );

  /// Get user activity logs
  final List<GetAccountActivityResponseLogsItem> logs;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (logs.isNotEmpty) 'logs': [for (final item in logs) item.toJson()],
      };
}

/// The `GetAccountActivityResponse.logsItem` object of the Brevo API.
final class GetAccountActivityResponseLogsItem {
  /// Creates a GetAccountActivityResponseLogsItem from already-decoded fields.
  const GetAccountActivityResponseLogsItem({
    this.action,
    this.date,
    this.userAgent,
    this.userEmail,
    this.userIp,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountActivityResponseLogsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountActivityResponseLogsItem.fromJson(
          Map<String, Object?> json) =>
      GetAccountActivityResponseLogsItem(
        action: json.optString('action'),
        date: json.optString('date'),
        userAgent: json.optString('user_agent'),
        userEmail: json.optString('user_email'),
        userIp: json.optString('user_ip'),
        raw: json,
      );

  /// Type of activity in the account.
  final String? action;

  /// Time of the activity.
  final String? date;

  /// Browser details of the user who performed the activity.
  final String? userAgent;

  /// Email address of the user who performed activity in the
  /// account.
  final String? userEmail;

  /// IP address of the user who performed activity in the
  /// account.
  final String? userIp;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (action != null) 'action': action!,
        if (date != null) 'date': date!,
        if (userAgent != null) 'user_agent': userAgent!,
        if (userEmail != null) 'user_email': userEmail!,
        if (userIp != null) 'user_ip': userIp!,
      };
}

/// The `GET /account` object of the Brevo API.
final class GetAccountResponse {
  /// Creates a GetAccountResponse from already-decoded fields.
  const GetAccountResponse({
    this.companyName,
    this.email,
    this.enterprise,
    this.firstName,
    this.lastName,
    this.organizationId,
    this.userId,
    this.address,
    this.marketingAutomation,
    this.plan = const [],
    this.planVerticals = const [],
    this.relay,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponse.fromJson(Map<String, Object?> json) =>
      GetAccountResponse(
        companyName: json.optString('companyName'),
        email: json.optString('email'),
        enterprise: json.optBool('enterprise'),
        firstName: json.optString('firstName'),
        lastName: json.optString('lastName'),
        organizationId: json.optString('organization_id'),
        userId: json.optInt('user_id'),
        address: json.optNested('address', GetAccountResponseAddress.fromJson),
        marketingAutomation: json.optNested('marketingAutomation',
            GetAccountResponseMarketingAutomation.fromJson),
        plan: json.optObjectList('plan', GetAccountResponsePlanItem.fromJson,
            objectName: 'GetAccountResponse'),
        planVerticals: json.optObjectList(
            'planVerticals', GetAccountResponsePlanVerticalsItem.fromJson,
            objectName: 'GetAccountResponse'),
        relay: json.optNested('relay', GetAccountResponseRelay.fromJson),
        raw: json,
      );

  /// Name of the company
  final String? companyName;

  /// Login Email
  final String? email;

  /// Indicates if the account has enterprise features
  final bool? enterprise;

  /// First Name
  final String? firstName;

  /// Last Name
  final String? lastName;

  /// Unique identifier for the organization
  final String? organizationId;

  /// Unique identifier for the user
  final int? userId;

  /// Address informations
  final GetAccountResponseAddress? address;

  /// The `marketingAutomation` field.
  final GetAccountResponseMarketingAutomation? marketingAutomation;

  /// Information about your plans and credits
  final List<GetAccountResponsePlanItem> plan;

  /// Detailed information about different plan categories
  final List<GetAccountResponsePlanVerticalsItem> planVerticals;

  /// Information about your transactional email account
  final GetAccountResponseRelay? relay;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (companyName != null) 'companyName': companyName!,
        if (email != null) 'email': email!,
        if (enterprise != null) 'enterprise': enterprise!,
        if (firstName != null) 'firstName': firstName!,
        if (lastName != null) 'lastName': lastName!,
        if (organizationId != null) 'organization_id': organizationId!,
        if (userId != null) 'user_id': userId!,
        if (address != null) 'address': address!.toJson(),
        if (marketingAutomation != null)
          'marketingAutomation': marketingAutomation!.toJson(),
        if (plan.isNotEmpty) 'plan': [for (final item in plan) item.toJson()],
        if (planVerticals.isNotEmpty)
          'planVerticals': [for (final item in planVerticals) item.toJson()],
        if (relay != null) 'relay': relay!.toJson(),
      };
}

/// Address informations
final class GetAccountResponseAddress {
  /// Creates a GetAccountResponseAddress from already-decoded fields.
  const GetAccountResponseAddress({
    this.city,
    this.country,
    this.street,
    this.zipCode,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponseAddress. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponseAddress.fromJson(Map<String, Object?> json) =>
      GetAccountResponseAddress(
        city: json.optString('city'),
        country: json.optString('country'),
        street: json.optString('street'),
        zipCode: json.optString('zipCode'),
        raw: json,
      );

  /// City information
  final String? city;

  /// Country information
  final String? country;

  /// Street information
  final String? street;

  /// Zip Code information
  final String? zipCode;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (city != null) 'city': city!,
        if (country != null) 'country': country!,
        if (street != null) 'street': street!,
        if (zipCode != null) 'zipCode': zipCode!,
      };
}

/// The `GetAccountResponse.marketingAutomation` object of the Brevo API.
final class GetAccountResponseMarketingAutomation {
  /// Creates a GetAccountResponseMarketingAutomation from already-decoded fields.
  const GetAccountResponseMarketingAutomation({
    this.enabled,
    this.key,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponseMarketingAutomation. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponseMarketingAutomation.fromJson(
          Map<String, Object?> json) =>
      GetAccountResponseMarketingAutomation(
        enabled: json.optBool('enabled'),
        key: json.optString('key'),
        raw: json,
      );

  /// Status of Marketing Automation Plateform activation
  /// for your account (true=enabled, false=disabled)
  final bool? enabled;

  /// Marketing Automation Tracker ID
  final String? key;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (enabled != null) 'enabled': enabled!,
        if (key != null) 'key': key!,
      };
}

/// The `GetAccountResponse.planItem` object of the Brevo API.
final class GetAccountResponsePlanItem {
  /// Creates a GetAccountResponsePlanItem from already-decoded fields.
  const GetAccountResponsePlanItem({
    this.credits,
    this.creditsType,
    this.endDate,
    this.startDate,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponsePlanItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponsePlanItem.fromJson(Map<String, Object?> json) =>
      GetAccountResponsePlanItem(
        credits: json.optDouble('credits'),
        creditsType: json.optString('creditsType'),
        endDate: json.optString('endDate'),
        startDate: json.optString('startDate'),
        type: json.optEnum('type', GetAccountResponsePlanItemType.fromWire),
        raw: json,
      );

  /// Remaining credits of the user
  final double? credits;

  /// This is the type of the credit, "Send Limit" is one
  /// of the possible types of credit of a user. "Send
  /// Limit" implies the total number of emails you can
  /// send to the subscribers in your account.
  final String? creditsType;

  /// Date of the period from which the plan will end
  /// (Unix timestamp format)
  final String? endDate;

  /// [endDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get endDateDate =>
      endDate == null ? null : DateTime.tryParse(endDate!);

  /// Date of the period from which the plan will start
  /// (Unix timestamp format)
  final String? startDate;

  /// [startDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get startDateDate =>
      startDate == null ? null : DateTime.tryParse(startDate!);

  /// Displays the plan type of the user
  final GetAccountResponsePlanItemType? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (credits != null) 'credits': credits!,
        if (creditsType != null) 'creditsType': creditsType!,
        if (endDate != null) 'endDate': endDate!,
        if (startDate != null) 'startDate': startDate!,
        if (type != null) 'type': type!.value,
      };
}

/// The `GetAccountResponse.planVerticalsItem` object of the Brevo API.
final class GetAccountResponsePlanVerticalsItem {
  /// Creates a GetAccountResponsePlanVerticalsItem from already-decoded fields.
  const GetAccountResponsePlanVerticalsItem({
    this.credits,
    this.endDate,
    this.name,
    this.planCategory,
    this.planType,
    this.startDate,
    this.status,
    this.users,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponsePlanVerticalsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponsePlanVerticalsItem.fromJson(
          Map<String, Object?> json) =>
      GetAccountResponsePlanVerticalsItem(
        credits: json.optString('credits'),
        endDate: json.optString('endDate'),
        name: json.optString('name'),
        planCategory: json.optEnum('planCategory',
            GetAccountResponsePlanVerticalsItemPlanCategory.fromWire),
        planType: json.optString('planType'),
        startDate: json.optString('startDate'),
        status: json.optEnum(
            'status', GetAccountResponsePlanVerticalsItemStatus.fromWire),
        users: json.optNested(
            'users', GetAccountResponsePlanVerticalsItemUsers.fromJson),
        raw: json,
      );

  /// Credits allocated for this plan category (optional, may not be present for all plan categories)
  final String? credits;

  /// Plan end date (Unix timestamp)
  final String? endDate;

  /// [endDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get endDateDate =>
      endDate == null ? null : DateTime.tryParse(endDate!);

  /// Name of the plan
  final String? name;

  /// Category of the plan
  final GetAccountResponsePlanVerticalsItemPlanCategory? planCategory;

  /// Type of plan within the category
  final String? planType;

  /// Plan start date (Unix timestamp)
  final String? startDate;

  /// [startDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get startDateDate =>
      startDate == null ? null : DateTime.tryParse(startDate!);

  /// Current status of the plan
  final GetAccountResponsePlanVerticalsItemStatus? status;

  /// User seat information for the plan
  final GetAccountResponsePlanVerticalsItemUsers? users;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (credits != null) 'credits': credits!,
        if (endDate != null) 'endDate': endDate!,
        if (name != null) 'name': name!,
        if (planCategory != null) 'planCategory': planCategory!.value,
        if (planType != null) 'planType': planType!,
        if (startDate != null) 'startDate': startDate!,
        if (status != null) 'status': status!.value,
        if (users != null) 'users': users!.toJson(),
      };
}

/// User seat information for the plan
final class GetAccountResponsePlanVerticalsItemUsers {
  /// Creates a GetAccountResponsePlanVerticalsItemUsers from already-decoded fields.
  const GetAccountResponsePlanVerticalsItemUsers({
    this.purchasedSeats,
    this.usedSeats,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponsePlanVerticalsItemUsers. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponsePlanVerticalsItemUsers.fromJson(
          Map<String, Object?> json) =>
      GetAccountResponsePlanVerticalsItemUsers(
        purchasedSeats: json.optString('purchasedSeats'),
        usedSeats: json.optString('usedSeats'),
        raw: json,
      );

  /// Number of purchased user seats
  final String? purchasedSeats;

  /// Number of used user seats
  final String? usedSeats;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (purchasedSeats != null) 'purchasedSeats': purchasedSeats!,
        if (usedSeats != null) 'usedSeats': usedSeats!,
      };
}

/// Information about your transactional email account
final class GetAccountResponseRelay {
  /// Creates a GetAccountResponseRelay from already-decoded fields.
  const GetAccountResponseRelay({
    this.data,
    this.enabled,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponseRelay. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponseRelay.fromJson(Map<String, Object?> json) =>
      GetAccountResponseRelay(
        data: json.optNested('data', GetAccountResponseRelayData.fromJson),
        enabled: json.optBool('enabled'),
        raw: json,
      );

  /// Data regarding the transactional email account
  final GetAccountResponseRelayData? data;

  /// Status of your transactional email Account
  /// (true=Enabled, false=Disabled)
  final bool? enabled;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (data != null) 'data': data!.toJson(),
        if (enabled != null) 'enabled': enabled!,
      };
}

/// Data regarding the transactional email account
final class GetAccountResponseRelayData {
  /// Creates a GetAccountResponseRelayData from already-decoded fields.
  const GetAccountResponseRelayData({
    this.port,
    this.relay,
    this.userName,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetAccountResponseRelayData. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetAccountResponseRelayData.fromJson(Map<String, Object?> json) =>
      GetAccountResponseRelayData(
        port: json.optInt('port'),
        relay: json.optString('relay'),
        userName: json.optString('userName'),
        raw: json,
      );

  /// Port used for SMTP Relay
  final int? port;

  /// URL of the SMTP Relay
  final String? relay;

  /// Email to use as login on transactional platform
  final String? userName;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (port != null) 'port': port!,
        if (relay != null) 'relay': relay!,
        if (userName != null) 'userName': userName!,
      };
}
