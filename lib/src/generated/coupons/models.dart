// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// The `POST /couponCollections` object of the Brevo API.
final class CreateCouponCollectionResponse {
  /// Creates a CreateCouponCollectionResponse from already-decoded fields.
  const CreateCouponCollectionResponse({
    this.id,
    this.raw = const {},
  });

  /// Decodes a JSON object into a CreateCouponCollectionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory CreateCouponCollectionResponse.fromJson(Map<String, Object?> json) =>
      CreateCouponCollectionResponse(
        id: json.optString('id'),
        raw: json,
      );

  /// The id of the created collection
  final String? id;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (id != null) 'id': id!,
      };
}

/// The `components/schemas/getCouponCollection` object of the Brevo API.
final class GetCouponCollection {
  /// Creates a GetCouponCollection from already-decoded fields.
  const GetCouponCollection({
    this.createdAt,
    this.defaultCoupon,
    this.expirationDate,
    this.id,
    this.name,
    this.remainingCoupons,
    this.remainingCouponsAlert,
    this.remainingDaysAlert,
    this.totalCoupons,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetCouponCollection. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetCouponCollection.fromJson(Map<String, Object?> json) =>
      GetCouponCollection(
        createdAt: json.optString('createdAt'),
        defaultCoupon: json.optString('defaultCoupon'),
        expirationDate: json.optString('expirationDate'),
        id: json.optString('id'),
        name: json.optString('name'),
        remainingCoupons: json.optInt('remainingCoupons'),
        remainingCouponsAlert: json.optInt('remainingCouponsAlert'),
        remainingDaysAlert: json.optInt('remainingDaysAlert'),
        totalCoupons: json.optInt('totalCoupons'),
        raw: json,
      );

  /// Datetime on which the collection was created.
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// The default coupon of the collection.
  final String? defaultCoupon;

  /// Expiration date for the coupon collection in RFC3339 format.
  final String? expirationDate;

  /// [expirationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get expirationDateDate =>
      expirationDate == null ? null : DateTime.tryParse(expirationDate!);

  /// The id of the collection.
  final String? id;

  /// The name of the collection.
  final String? name;

  /// Number of coupons that have not been sent yet.
  final int? remainingCoupons;

  /// If present, an email notification is going to be sent when the total number of available coupons falls below the defined threshold.
  final int? remainingCouponsAlert;

  /// If present, an email notification is going to be sent the defined amount of days before to the expiration date.
  final int? remainingDaysAlert;

  /// Total number of coupons in the collection.
  final int? totalCoupons;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (createdAt != null) 'createdAt': createdAt!,
        if (defaultCoupon != null) 'defaultCoupon': defaultCoupon!,
        if (expirationDate != null) 'expirationDate': expirationDate!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
        if (remainingCoupons != null) 'remainingCoupons': remainingCoupons!,
        if (remainingCouponsAlert != null)
          'remainingCouponsAlert': remainingCouponsAlert!,
        if (remainingDaysAlert != null)
          'remainingDaysAlert': remainingDaysAlert!,
        if (totalCoupons != null) 'totalCoupons': totalCoupons!,
      };
}

/// The `PATCH /couponCollections/{id}` object of the Brevo API.
final class UpdateCouponCollectionResponse {
  /// Creates a UpdateCouponCollectionResponse from already-decoded fields.
  const UpdateCouponCollectionResponse({
    this.defaultCoupon,
    this.id,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a UpdateCouponCollectionResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory UpdateCouponCollectionResponse.fromJson(Map<String, Object?> json) =>
      UpdateCouponCollectionResponse(
        defaultCoupon: json.optString('defaultCoupon'),
        id: json.optString('id'),
        name: json.optString('name'),
        raw: json,
      );

  /// The default coupon of the collection
  final String? defaultCoupon;

  /// The id of the collection
  final String? id;

  /// The name of the collection
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (defaultCoupon != null) 'defaultCoupon': defaultCoupon!,
        if (id != null) 'id': id!,
        if (name != null) 'name': name!,
      };
}
