// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';

/// The request body of `POST /couponCollections`.
final class CreateCouponCollectionRequest {
  /// Creates the request body.
  const CreateCouponCollectionRequest({
    required this.defaultCoupon,
    this.expirationDate,
    required this.name,
    this.remainingCouponsAlert,
    this.remainingDaysAlert,
  });

  /// Decodes a JSON object into a CreateCouponCollectionRequest. Optional fields absent from [json] stay null.
  factory CreateCouponCollectionRequest.fromJson(Map<String, Object?> json) =>
      CreateCouponCollectionRequest(
        defaultCoupon: json.requireString(
            'defaultCoupon', 'CreateCouponCollectionRequest'),
        expirationDate: json.optString('expirationDate'),
        name: json.requireString('name', 'CreateCouponCollectionRequest'),
        remainingCouponsAlert: json.optInt('remainingCouponsAlert'),
        remainingDaysAlert: json.optInt('remainingDaysAlert'),
      );

  /// Default coupons collection name
  final String defaultCoupon;

  /// Specify an expiration date for the coupon collection in RFC3339 format. Use null to remove the expiration date.
  final String? expirationDate;

  /// [expirationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get expirationDateDate =>
      expirationDate == null ? null : DateTime.tryParse(expirationDate!);

  /// Name of the coupons collection
  final String name;

  /// Send a notification alert (email) when the remaining coupons count is equal or fall bellow this number. Use null to disable alerts.
  final int? remainingCouponsAlert;

  /// Send a notification alert (email) when the remaining days until the expiration date are equal or fall bellow this number. Use null to disable alerts.
  final int? remainingDaysAlert;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'defaultCoupon': defaultCoupon,
        if (expirationDate != null) 'expirationDate': expirationDate!,
        'name': name,
        if (remainingCouponsAlert != null)
          'remainingCouponsAlert': remainingCouponsAlert!,
        if (remainingDaysAlert != null)
          'remainingDaysAlert': remainingDaysAlert!,
      };
}

/// The request body of `POST /coupons`.
final class CreateCouponsRequest {
  /// Creates the request body.
  const CreateCouponsRequest({
    required this.collectionId,
    required this.coupons,
  });

  /// Decodes a JSON object into a CreateCouponsRequest. Optional fields absent from [json] stay null.
  factory CreateCouponsRequest.fromJson(Map<String, Object?> json) =>
      CreateCouponsRequest(
        collectionId:
            json.requireString('collectionId', 'CreateCouponsRequest'),
        coupons: json.containsKey('coupons')
            ? json.optStringList('coupons')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateCouponsRequest', key: 'coupons')),
      );

  /// The id of the coupon collection for which the coupons will be created
  final String collectionId;

  /// The `coupons` field.
  final List<String> coupons;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'collectionId': collectionId,
        'coupons': coupons,
      };
}

/// The request body of `PATCH /couponCollections/{id}`.
final class UpdateCouponCollectionRequest {
  /// Creates the request body.
  const UpdateCouponCollectionRequest({
    this.defaultCoupon,
    this.expirationDate,
    this.remainingCouponsAlert,
    this.remainingDaysAlert,
  });

  /// Decodes a JSON object into a UpdateCouponCollectionRequest. Optional fields absent from [json] stay null.
  factory UpdateCouponCollectionRequest.fromJson(Map<String, Object?> json) =>
      UpdateCouponCollectionRequest(
        defaultCoupon: json.optString('defaultCoupon'),
        expirationDate: json.optString('expirationDate'),
        remainingCouponsAlert: json.optInt('remainingCouponsAlert'),
        remainingDaysAlert: json.optInt('remainingDaysAlert'),
      );

  /// A default coupon to be used in case there are no coupons left
  final String? defaultCoupon;

  /// Specify an expiration date for the coupon collection in RFC3339 format. Use null to remove the expiration date.
  final String? expirationDate;

  /// [expirationDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get expirationDateDate =>
      expirationDate == null ? null : DateTime.tryParse(expirationDate!);

  /// Send a notification alert (email) when the remaining coupons count is equal or fall bellow this number. Use null to disable alerts.
  final int? remainingCouponsAlert;

  /// Send a notification alert (email) when the remaining days until the expiration date are equal or fall bellow this number. Use null to disable alerts.
  final int? remainingDaysAlert;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (defaultCoupon != null) 'defaultCoupon': defaultCoupon!,
        if (expirationDate != null) 'expirationDate': expirationDate!,
        if (remainingCouponsAlert != null)
          'remainingCouponsAlert': remainingCouponsAlert!,
        if (remainingDaysAlert != null)
          'remainingDaysAlert': remainingDaysAlert!,
      };
}
