// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// The request body of `components/schemas/sendReport`.
final class SendReport {
  /// Creates the request body.
  const SendReport({
    required this.email,
    this.language,
  });

  /// Decodes a JSON object into a SendReport. Optional fields absent from [json] stay null.
  factory SendReport.fromJson(Map<String, Object?> json) => SendReport(
        email: json.optNested('email', SendReportEmail.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'SendReport', key: 'email')),
        language: json.optEnum('language', SendReportLanguage.fromWire),
      );

  /// Custom attributes for the report email.
  final SendReportEmail email;

  /// Language of email content for campaign report sending.
  final SendReportLanguage? language;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'email': email.toJson(),
        if (language != null) 'language': language!.value,
      };
}

/// Custom attributes for the report email.
final class SendReportEmail {
  /// Creates the request body.
  const SendReportEmail({
    required this.body,
    required this.to,
  });

  /// Decodes a JSON object into a SendReportEmail. Optional fields absent from [json] stay null.
  factory SendReportEmail.fromJson(Map<String, Object?> json) =>
      SendReportEmail(
        body: json.requireString('body', 'SendReportEmail'),
        to: json.containsKey('to')
            ? json.optStringList('to')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'SendReportEmail', key: 'to')),
      );

  /// Custom text message to be presented in the report email.
  final String body;

  /// Email addresses of the recipients
  final List<String> to;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'body': body,
        'to': to,
      };
}

/// The request body of `components/schemas/sendTestEmail`.
final class SendTestEmail {
  /// Creates the request body.
  const SendTestEmail({
    this.emailTo,
  });

  /// Decodes a JSON object into a SendTestEmail. Optional fields absent from [json] stay null.
  factory SendTestEmail.fromJson(Map<String, Object?> json) => SendTestEmail(
        emailTo:
            json.containsKey('emailTo') ? json.optStringList('emailTo') : null,
      );

  /// List of the email addresses of the recipients whom you wish to send the test mail. _If left empty, the test mail will be sent to your entire test list. You can not send more than 50 test emails per day_.
  final List<String>? emailTo;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (emailTo != null) 'emailTo': emailTo!,
      };
}

/// Status of the campaign
final class UpdateCampaignStatus {
  /// Creates the request body.
  const UpdateCampaignStatus({
    this.status,
  });

  /// Decodes a JSON object into a UpdateCampaignStatus. Optional fields absent from [json] stay null.
  factory UpdateCampaignStatus.fromJson(Map<String, Object?> json) =>
      UpdateCampaignStatus(
        status: json.optEnum('status', UpdateCampaignStatusStatus.fromWire),
      );

  /// Note:- **replicateTemplate** status will be available **only for template type campaigns.**
  final UpdateCampaignStatusStatus? status;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (status != null) 'status': status!.value,
      };
}
