// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/open_enum.dart';

/// Language of email content for campaign report sending.
final class SendReportLanguage extends BrevoOpenEnum {
  const SendReportLanguage._(super.value);

  /// Wraps a value this package does not know.
  const SendReportLanguage.unknown(super.value);

  /// The `fr` value.
  static const fr = SendReportLanguage._('fr');

  /// The `es` value.
  static const es = SendReportLanguage._('es');

  /// The `pt` value.
  static const pt = SendReportLanguage._('pt');

  /// The `it` value.
  static const it = SendReportLanguage._('it');

  /// The `de` value.
  static const de = SendReportLanguage._('de');

  /// The `en` value.
  static const en = SendReportLanguage._('en');

  /// The values known when this package was generated.
  static const values = [fr, es, pt, it, de, en];

  /// Decodes a wire value, preserving one this package does not know.
  factory SendReportLanguage.fromWire(String value) => values.firstWhere(
        (known) => known.value == value,
        orElse: () => SendReportLanguage.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Note:- **replicateTemplate** status will be available **only for template type campaigns.**
final class UpdateCampaignStatusStatus extends BrevoOpenEnum {
  const UpdateCampaignStatusStatus._(super.value);

  /// Wraps a value this package does not know.
  const UpdateCampaignStatusStatus.unknown(super.value);

  /// The `suspended` value.
  static const suspended = UpdateCampaignStatusStatus._('suspended');

  /// The `archive` value.
  static const archive = UpdateCampaignStatusStatus._('archive');

  /// The `darchive` value.
  static const darchive = UpdateCampaignStatusStatus._('darchive');

  /// The `sent` value.
  static const sent = UpdateCampaignStatusStatus._('sent');

  /// The `queued` value.
  static const queued = UpdateCampaignStatusStatus._('queued');

  /// The `replicate` value.
  static const replicate = UpdateCampaignStatusStatus._('replicate');

  /// The `replicateTemplate` value.
  static const replicateTemplate =
      UpdateCampaignStatusStatus._('replicateTemplate');

  /// The `cancel` value.
  static const cancel = UpdateCampaignStatusStatus._('cancel');

  /// The `draft` value.
  static const draft = UpdateCampaignStatusStatus._('draft');

  /// The values known when this package was generated.
  static const values = [
    suspended,
    archive,
    darchive,
    sent,
    queued,
    replicate,
    replicateTemplate,
    cancel,
    draft
  ];

  /// Decodes a wire value, preserving one this package does not know.
  factory UpdateCampaignStatusStatus.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => UpdateCampaignStatusStatus.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}
