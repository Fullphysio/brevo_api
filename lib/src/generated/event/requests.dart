// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';

/// The request body of `POST /events/batch`.
final class CreateBatchEventsRequest {
  /// Creates the request body.
  const CreateBatchEventsRequest({
    required this.events,
  });

  /// Decodes a JSON object into a CreateBatchEventsRequest. Optional fields absent from [json] stay null.
  factory CreateBatchEventsRequest.fromJson(Map<String, Object?> json) =>
      CreateBatchEventsRequest(
        events: json.containsKey('events')
            ? json.optObjectList(
                'events', CreateBatchEventsRequestEventsItem.fromJson,
                objectName: 'CreateBatchEventsRequest')
            : (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateBatchEventsRequest', key: 'events')),
      );

  /// The `events` field.
  final List<CreateBatchEventsRequestEventsItem> events;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        'events': [for (final item in events) item.toJson()],
      };
}

/// The request body of `CreateBatchEventsRequest.eventsItem`.
final class CreateBatchEventsRequestEventsItem {
  /// Creates the request body.
  const CreateBatchEventsRequestEventsItem({
    this.contactProperties,
    this.eventDate,
    required this.eventName,
    this.eventProperties,
    required this.identifiers,
    this.object,
  });

  /// Decodes a JSON object into a CreateBatchEventsRequestEventsItem. Optional fields absent from [json] stay null.
  factory CreateBatchEventsRequestEventsItem.fromJson(
          Map<String, Object?> json) =>
      CreateBatchEventsRequestEventsItem(
        contactProperties: json.containsKey('contact_properties')
            ? json.optObjectMap('contact_properties')
            : null,
        eventDate: json.optString('event_date'),
        eventName: json.requireString(
            'event_name', 'CreateBatchEventsRequestEventsItem'),
        eventProperties: json.containsKey('event_properties')
            ? json.optObjectMap('event_properties')
            : null,
        identifiers: json.optNested('identifiers',
                CreateBatchEventsRequestEventsItemIdentifiers.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateBatchEventsRequestEventsItem',
                key: 'identifiers')),
        object: json.optNested(
            'object', CreateBatchEventsRequestEventsItemObject.fromJson),
      );

  /// Properties defining the state of the contact associated to this event. Useful to update contact attributes defined in your contacts database while passing the event. For example: **"FIRSTNAME": "Jane" , "AGE": 37**
  final Map<String, Object?>? contactProperties;

  /// Timestamp of when the event occurred (e.g. "2024-01-24T17:39:57+01:00"). If no value is passed, the timestamp of the event creation is used.
  final String? eventDate;

  /// The name of the event that occurred. This is how you will find your event in Brevo. Limited to 255 characters, alphanumerical characters and - _ only.
  final String eventName;

  /// Properties of the event. Top level properties and nested properties can be used to better segment contacts and personalise workflow conditions. The following field type are supported: string, number, boolean (true/false), date (Timestamp e.g. "2024-01-24T17:39:57+01:00"). Keys are limited to 255 characters, alphanumerical characters and - _ only. Size is limited to 50Kb.
  final Map<String, Object?>? eventProperties;

  /// Identifies the contact associated with the event. At least one identifier is required.
  final CreateBatchEventsRequestEventsItemIdentifiers identifiers;

  /// Identifiers of the object record associated with this event. Ignored if the object type or identifier for this record does not exist on the account.
  final CreateBatchEventsRequestEventsItemObject? object;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactProperties != null) 'contact_properties': contactProperties!,
        if (eventDate != null) 'event_date': eventDate!,
        'event_name': eventName,
        if (eventProperties != null) 'event_properties': eventProperties!,
        'identifiers': identifiers.toJson(),
        if (object != null) 'object': object!.toJson(),
      };
}

/// Identifies the contact associated with the event. At least one identifier is required.
final class CreateBatchEventsRequestEventsItemIdentifiers {
  /// Creates the request body.
  const CreateBatchEventsRequestEventsItemIdentifiers({
    this.contactId,
    this.emailId,
    this.extId,
    this.landlineNumberId,
    this.phoneId,
    this.whatsappId,
  });

  /// Decodes a JSON object into a CreateBatchEventsRequestEventsItemIdentifiers. Optional fields absent from [json] stay null.
  factory CreateBatchEventsRequestEventsItemIdentifiers.fromJson(
          Map<String, Object?> json) =>
      CreateBatchEventsRequestEventsItemIdentifiers(
        contactId: json.optInt('contact_id'),
        emailId: json.optString('email_id'),
        extId: json.optString('ext_id'),
        landlineNumberId: json.optString('landline_number_id'),
        phoneId: json.optString('phone_id'),
        whatsappId: json.optString('whatsapp_id'),
      );

  /// Internal unique contact ID. When present, this takes priority over all other identifiers for event attribution and contact resolution.
  final int? contactId;

  /// Email Id associated with the event
  final String? emailId;

  /// ext_id associated with the event
  final String? extId;

  /// landline_number associated with the event
  final String? landlineNumberId;

  /// SMS associated with the event
  final String? phoneId;

  /// whatsapp associated with the event
  final String? whatsappId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contact_id': contactId!,
        if (emailId != null) 'email_id': emailId!,
        if (extId != null) 'ext_id': extId!,
        if (landlineNumberId != null) 'landline_number_id': landlineNumberId!,
        if (phoneId != null) 'phone_id': phoneId!,
        if (whatsappId != null) 'whatsapp_id': whatsappId!,
      };
}

/// Identifiers of the object record associated with this event. Ignored if the object type or identifier for this record does not exist on the account.
final class CreateBatchEventsRequestEventsItemObject {
  /// Creates the request body.
  const CreateBatchEventsRequestEventsItemObject({
    this.identifiers,
    this.type,
  });

  /// Decodes a JSON object into a CreateBatchEventsRequestEventsItemObject. Optional fields absent from [json] stay null.
  factory CreateBatchEventsRequestEventsItemObject.fromJson(
          Map<String, Object?> json) =>
      CreateBatchEventsRequestEventsItemObject(
        identifiers: json.optNested('identifiers',
            CreateBatchEventsRequestEventsItemObjectIdentifiers.fromJson),
        type: json.optString('type'),
      );

  /// Identifiers for the object.
  final CreateBatchEventsRequestEventsItemObjectIdentifiers? identifiers;

  /// Type of object (e.g., subscription, vehicle, etc.)
  final String? type;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
        if (type != null) 'type': type!,
      };
}

/// Identifiers for the object.
final class CreateBatchEventsRequestEventsItemObjectIdentifiers {
  /// Creates the request body.
  const CreateBatchEventsRequestEventsItemObjectIdentifiers({
    this.extId,
    this.id,
  });

  /// Decodes a JSON object into a CreateBatchEventsRequestEventsItemObjectIdentifiers. Optional fields absent from [json] stay null.
  factory CreateBatchEventsRequestEventsItemObjectIdentifiers.fromJson(
          Map<String, Object?> json) =>
      CreateBatchEventsRequestEventsItemObjectIdentifiers(
        extId: json.optString('ext_id'),
        id: json.optString('id'),
      );

  /// External object ID
  final String? extId;

  /// Internal object ID
  final String? id;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
      };
}

/// The request body of `POST /events`.
final class CreateEventRequest {
  /// Creates the request body.
  const CreateEventRequest({
    this.contactProperties,
    this.eventDate,
    required this.eventName,
    this.eventProperties,
    required this.identifiers,
    this.object,
  });

  /// Decodes a JSON object into a CreateEventRequest. Optional fields absent from [json] stay null.
  factory CreateEventRequest.fromJson(Map<String, Object?> json) =>
      CreateEventRequest(
        contactProperties: json.containsKey('contact_properties')
            ? json.optObjectMap('contact_properties')
            : null,
        eventDate: json.optString('event_date'),
        eventName: json.requireString('event_name', 'CreateEventRequest'),
        eventProperties: json.containsKey('event_properties')
            ? json.optObjectMap('event_properties')
            : null,
        identifiers: json.optNested(
                'identifiers', CreateEventRequestIdentifiers.fromJson) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateEventRequest', key: 'identifiers')),
        object: json.optNested('object', CreateEventRequestObject.fromJson),
      );

  /// Properties defining the state of the contact associated to this event. Useful to update contact attributes defined in your contacts database while passing the event. For example: **"FIRSTNAME": "Jane" , "AGE": 37**
  final Map<String, Object?>? contactProperties;

  /// ISO 8601 timestamp of when the event occurred (e.g. "2024-01-24T17:39:57+01:00"). If no value is passed, the timestamp of the event creation is used.
  final String? eventDate;

  /// [eventDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get eventDateDate =>
      eventDate == null ? null : DateTime.tryParse(eventDate!);

  /// The name of the event that occurred. This is how you will find your event in Brevo. Limited to 255 characters; only alphanumeric characters, hyphens (-), and underscores (_) are allowed.
  final String eventName;

  /// Properties of the event. Top level properties and nested properties can be used to better segment contacts and personalise workflow conditions. The following field types are supported: string, number, boolean (true/false), date (Timestamp e.g. "2024-01-24T17:39:57+01:00"). Keys are limited to 255 characters, alphanumerical characters and - _ only. Size is limited to 50KB.
  final Map<String, Object?>? eventProperties;

  /// Identifies the contact associated with the event. At least one identifier is required.
  final CreateEventRequestIdentifiers identifiers;

  /// Identifiers of the object record associated with this event. Ignored if the object type or identifier for this record does not exist on the account.
  final CreateEventRequestObject? object;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactProperties != null) 'contact_properties': contactProperties!,
        if (eventDate != null) 'event_date': eventDate!,
        'event_name': eventName,
        if (eventProperties != null) 'event_properties': eventProperties!,
        'identifiers': identifiers.toJson(),
        if (object != null) 'object': object!.toJson(),
      };
}

/// Identifies the contact associated with the event. At least one identifier is required.
final class CreateEventRequestIdentifiers {
  /// Creates the request body.
  const CreateEventRequestIdentifiers({
    this.contactId,
    this.emailId,
    this.extId,
    this.landlineNumberId,
    this.phoneId,
    this.whatsappId,
  });

  /// Decodes a JSON object into a CreateEventRequestIdentifiers. Optional fields absent from [json] stay null.
  factory CreateEventRequestIdentifiers.fromJson(Map<String, Object?> json) =>
      CreateEventRequestIdentifiers(
        contactId: json.optInt('contact_id'),
        emailId: json.optString('email_id'),
        extId: json.optString('ext_id'),
        landlineNumberId: json.optString('landline_number_id'),
        phoneId: json.optString('phone_id'),
        whatsappId: json.optString('whatsapp_id'),
      );

  /// Internal unique contact ID. When present, this takes priority over all other identifiers for event attribution and contact resolution.
  final int? contactId;

  /// Email Id associated with the event
  final String? emailId;

  /// ext_id associated with the event
  final String? extId;

  /// landline_number associated with the event
  final String? landlineNumberId;

  /// SMS associated with the event
  final String? phoneId;

  /// whatsapp associated with the event
  final String? whatsappId;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contact_id': contactId!,
        if (emailId != null) 'email_id': emailId!,
        if (extId != null) 'ext_id': extId!,
        if (landlineNumberId != null) 'landline_number_id': landlineNumberId!,
        if (phoneId != null) 'phone_id': phoneId!,
        if (whatsappId != null) 'whatsapp_id': whatsappId!,
      };
}

/// Identifiers of the object record associated with this event. Ignored if the object type or identifier for this record does not exist on the account.
final class CreateEventRequestObject {
  /// Creates the request body.
  const CreateEventRequestObject({
    this.identifiers,
    this.type,
  });

  /// Decodes a JSON object into a CreateEventRequestObject. Optional fields absent from [json] stay null.
  factory CreateEventRequestObject.fromJson(Map<String, Object?> json) =>
      CreateEventRequestObject(
        identifiers: json.optNested(
            'identifiers', CreateEventRequestObjectIdentifiers.fromJson),
        type: json.optString('type'),
      );

  /// Identifiers for the object.
  final CreateEventRequestObjectIdentifiers? identifiers;

  /// Type of object (e.g., subscription, vehicle, etc.)
  final String? type;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (identifiers != null) 'identifiers': identifiers!.toJson(),
        if (type != null) 'type': type!,
      };
}

/// Identifiers for the object.
final class CreateEventRequestObjectIdentifiers {
  /// Creates the request body.
  const CreateEventRequestObjectIdentifiers({
    this.extId,
    this.id,
  });

  /// Decodes a JSON object into a CreateEventRequestObjectIdentifiers. Optional fields absent from [json] stay null.
  factory CreateEventRequestObjectIdentifiers.fromJson(
          Map<String, Object?> json) =>
      CreateEventRequestObjectIdentifiers(
        extId: json.optString('ext_id'),
        id: json.optString('id'),
      );

  /// External object ID
  final String? extId;

  /// Internal object ID
  final String? id;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (extId != null) 'ext_id': extId!,
        if (id != null) 'id': id!,
      };
}
