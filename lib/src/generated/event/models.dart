// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';

/// Response containing a list of events and the total count for pagination
final class GetEventsList {
  /// Creates a GetEventsList from already-decoded fields.
  const GetEventsList({
    this.count,
    this.events = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetEventsList. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetEventsList.fromJson(Map<String, Object?> json) => GetEventsList(
        count: json.optInt('count'),
        events: json.optObjectList('events', GetEventsListEventsItem.fromJson,
            objectName: 'GetEventsList'),
        raw: json,
      );

  /// Total count of events matching the filters (use for pagination)
  final int? count;

  /// List of matching events ordered by event_date descending
  final List<GetEventsListEventsItem> events;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (count != null) 'count': count!,
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
      };
}

/// The `GetEventsList.eventsItem` object of the Brevo API.
final class GetEventsListEventsItem {
  /// Creates a GetEventsListEventsItem from already-decoded fields.
  const GetEventsListEventsItem({
    this.contactId,
    this.contactProperties = const {},
    this.eventDate,
    this.eventFilterId,
    this.eventName,
    this.eventProperties = const {},
    this.objectType,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetEventsListEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetEventsListEventsItem.fromJson(Map<String, Object?> json) =>
      GetEventsListEventsItem(
        contactId: json.optInt('contact_id'),
        contactProperties: json.optObjectMap('contact_properties'),
        eventDate: json.optString('event_date'),
        eventFilterId: json.optString('event_filter_id'),
        eventName: json.optString('event_name'),
        eventProperties: json.optObjectMap('event_properties'),
        objectType: json.optString('object_type'),
        raw: json,
      );

  /// Contact ID associated with the event
  final int? contactId;

  /// Contact-level properties at time of event
  final Map<String, Object?> contactProperties;

  /// Date and time of the event
  final String? eventDate;

  /// [eventDate] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get eventDateDate =>
      eventDate == null ? null : DateTime.tryParse(eventDate!);

  /// Filter ID of the event
  final String? eventFilterId;

  /// Name of the event
  final String? eventName;

  /// Event-level custom properties
  final Map<String, Object?> eventProperties;

  /// Object type associated with the event
  final String? objectType;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contactId != null) 'contact_id': contactId!,
        if (contactProperties.isNotEmpty)
          'contact_properties': contactProperties,
        if (eventDate != null) 'event_date': eventDate!,
        if (eventFilterId != null) 'event_filter_id': eventFilterId!,
        if (eventName != null) 'event_name': eventName!,
        if (eventProperties.isNotEmpty) 'event_properties': eventProperties,
        if (objectType != null) 'object_type': objectType!,
      };
}
