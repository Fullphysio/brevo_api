// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// The `GET /inbound/events/{uuid}` object of the Brevo API.
final class GetInboundEmailEventsByUuidResponse {
  /// Creates a GetInboundEmailEventsByUuidResponse from already-decoded fields.
  const GetInboundEmailEventsByUuidResponse({
    this.attachments = const [],
    this.deliveredAt,
    this.logs = const [],
    this.messageId,
    this.receivedAt,
    this.recipient,
    this.sender,
    this.subject,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInboundEmailEventsByUuidResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInboundEmailEventsByUuidResponse.fromJson(
          Map<String, Object?> json) =>
      GetInboundEmailEventsByUuidResponse(
        attachments: json.optObjectList('attachments',
            GetInboundEmailEventsByUuidResponseAttachmentsItem.fromJson,
            objectName: 'GetInboundEmailEventsByUuidResponse'),
        deliveredAt: json.optString('deliveredAt'),
        logs: json.optObjectList(
            'logs', GetInboundEmailEventsByUuidResponseLogsItem.fromJson,
            objectName: 'GetInboundEmailEventsByUuidResponse'),
        messageId: json.optString('messageId'),
        receivedAt: json.optString('receivedAt'),
        recipient: json.optString('recipient'),
        sender: json.optString('sender'),
        subject: json.optString('subject'),
        raw: json,
      );

  /// List of attachments of the email. This will be present only after the processing is done.
  final List<GetInboundEmailEventsByUuidResponseAttachmentsItem> attachments;

  /// Date when email was delivered successfully to the client’s webhook
  final String? deliveredAt;

  /// [deliveredAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get deliveredAtDate =>
      deliveredAt == null ? null : DateTime.tryParse(deliveredAt!);

  /// List of events/logs that describe the lifecycle of the email on the Brevo platform
  final List<GetInboundEmailEventsByUuidResponseLogsItem> logs;

  /// Value of the Message-ID header. This will be present only after the processing is done.
  final String? messageId;

  /// Date when email was received on SMTP relay
  final String? receivedAt;

  /// [receivedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get receivedAtDate =>
      receivedAt == null ? null : DateTime.tryParse(receivedAt!);

  /// Recipient’s email address
  final String? recipient;

  /// Sender’s email address
  final String? sender;

  /// Value of the Subject header. This will be present only after the processing is done.
  final String? subject;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (attachments.isNotEmpty)
          'attachments': [for (final item in attachments) item.toJson()],
        if (deliveredAt != null) 'deliveredAt': deliveredAt!,
        if (logs.isNotEmpty) 'logs': [for (final item in logs) item.toJson()],
        if (messageId != null) 'messageId': messageId!,
        if (receivedAt != null) 'receivedAt': receivedAt!,
        if (recipient != null) 'recipient': recipient!,
        if (sender != null) 'sender': sender!,
        if (subject != null) 'subject': subject!,
      };
}

/// The `GetInboundEmailEventsByUuidResponse.attachmentsItem` object of the Brevo API.
final class GetInboundEmailEventsByUuidResponseAttachmentsItem {
  /// Creates a GetInboundEmailEventsByUuidResponseAttachmentsItem from already-decoded fields.
  const GetInboundEmailEventsByUuidResponseAttachmentsItem({
    this.contentId,
    this.contentLength,
    this.contentType,
    this.name,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInboundEmailEventsByUuidResponseAttachmentsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInboundEmailEventsByUuidResponseAttachmentsItem.fromJson(
          Map<String, Object?> json) =>
      GetInboundEmailEventsByUuidResponseAttachmentsItem(
        contentId: json.optString('contentId'),
        contentLength: json.optInt('contentLength'),
        contentType: json.optString('contentType'),
        name: json.optString('name'),
        raw: json,
      );

  /// value of the Content-ID header of the attachment.
  final String? contentId;

  /// size of the attachment in bytes
  final int? contentLength;

  /// value of the Content-Type header of the attachment
  final String? contentType;

  /// filename specified in the Content-Disposition header of the attachment
  final String? name;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contentId != null) 'contentId': contentId!,
        if (contentLength != null) 'contentLength': contentLength!,
        if (contentType != null) 'contentType': contentType!,
        if (name != null) 'name': name!,
      };
}

/// The `GetInboundEmailEventsByUuidResponse.logsItem` object of the Brevo API.
final class GetInboundEmailEventsByUuidResponseLogsItem {
  /// Creates a GetInboundEmailEventsByUuidResponseLogsItem from already-decoded fields.
  const GetInboundEmailEventsByUuidResponseLogsItem({
    this.date,
    this.type,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInboundEmailEventsByUuidResponseLogsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInboundEmailEventsByUuidResponseLogsItem.fromJson(
          Map<String, Object?> json) =>
      GetInboundEmailEventsByUuidResponseLogsItem(
        date: json.optString('date'),
        type: json.optEnum(
            'type', GetInboundEmailEventsByUuidResponseLogsItemType.fromWire),
        raw: json,
      );

  /// Date of the event
  final String? date;

  /// [date] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get dateDate => date == null ? null : DateTime.tryParse(date!);

  /// Type of the event
  final GetInboundEmailEventsByUuidResponseLogsItemType? type;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (date != null) 'date': date!,
        if (type != null) 'type': type!.value,
      };
}

/// The `GET /inbound/events` object of the Brevo API.
final class GetInboundEmailEventsResponse {
  /// Creates a GetInboundEmailEventsResponse from already-decoded fields.
  const GetInboundEmailEventsResponse({
    this.events = const [],
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInboundEmailEventsResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInboundEmailEventsResponse.fromJson(Map<String, Object?> json) =>
      GetInboundEmailEventsResponse(
        events: json.optObjectList(
            'events', GetInboundEmailEventsResponseEventsItem.fromJson,
            objectName: 'GetInboundEmailEventsResponse'),
        raw: json,
      );

  /// The `events` field.
  final List<GetInboundEmailEventsResponseEventsItem> events;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (events.isNotEmpty)
          'events': [for (final item in events) item.toJson()],
      };
}

/// The `GetInboundEmailEventsResponse.eventsItem` object of the Brevo API.
final class GetInboundEmailEventsResponseEventsItem {
  /// Creates a GetInboundEmailEventsResponseEventsItem from already-decoded fields.
  const GetInboundEmailEventsResponseEventsItem({
    this.date,
    this.recipient,
    this.sender,
    this.uuid,
    this.raw = const {},
  });

  /// Decodes a JSON object into a GetInboundEmailEventsResponseEventsItem. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory GetInboundEmailEventsResponseEventsItem.fromJson(
          Map<String, Object?> json) =>
      GetInboundEmailEventsResponseEventsItem(
        date: json.optString('date'),
        recipient: json.optString('recipient'),
        sender: json.optString('sender'),
        uuid: json.optString('uuid'),
        raw: json,
      );

  /// Date when email was received on SMTP relay
  final String? date;

  /// [date] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get dateDate => date == null ? null : DateTime.tryParse(date!);

  /// Recipient’s email address
  final String? recipient;

  /// Sender’s email address
  final String? sender;

  /// UUID that can be used to fetch additional data
  final String? uuid;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (date != null) 'date': date!,
        if (recipient != null) 'recipient': recipient!,
        if (sender != null) 'sender': sender!,
        if (uuid != null) 'uuid': uuid!,
      };
}
