// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/open_enum.dart';

/// Controls how contacts are added to the group. `manual` — contacts are added explicitly via the API. `automatic` — contacts are added automatically at signup.
final class ConsentGroupSignupMode extends BrevoOpenEnum {
  const ConsentGroupSignupMode._(super.value);

  /// Wraps a value this package does not know.
  const ConsentGroupSignupMode.unknown(super.value);

  /// The `manual` value.
  static const manual = ConsentGroupSignupMode._('manual');

  /// The `automatic` value.
  static const automatic = ConsentGroupSignupMode._('automatic');

  /// The values known when this package was generated.
  static const values = [manual, automatic];

  /// Decodes a wire value, preserving one this package does not know.
  factory ConsentGroupSignupMode.fromWire(String value) => values.firstWhere(
        (known) => known.value == value,
        orElse: () => ConsentGroupSignupMode.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// Controls how contacts are added to the group. `manual` — contacts are added explicitly via the API. `automatic` — contacts are added automatically at signup.
final class CreateConsentGroupRequestSignupMode extends BrevoOpenEnum {
  const CreateConsentGroupRequestSignupMode._(super.value);

  /// Wraps a value this package does not know.
  const CreateConsentGroupRequestSignupMode.unknown(super.value);

  /// The `manual` value.
  static const manual = CreateConsentGroupRequestSignupMode._('manual');

  /// The `automatic` value.
  static const automatic = CreateConsentGroupRequestSignupMode._('automatic');

  /// The values known when this package was generated.
  static const values = [manual, automatic];

  /// Decodes a wire value, preserving one this package does not know.
  factory CreateConsentGroupRequestSignupMode.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => CreateConsentGroupRequestSignupMode.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// The `GetConsentGroupsRequestSignupMode` values of the Brevo API.
final class GetConsentGroupsRequestSignupMode extends BrevoOpenEnum {
  const GetConsentGroupsRequestSignupMode._(super.value);

  /// Wraps a value this package does not know.
  const GetConsentGroupsRequestSignupMode.unknown(super.value);

  /// The `manual` value.
  static const manual = GetConsentGroupsRequestSignupMode._('manual');

  /// The `automatic` value.
  static const automatic = GetConsentGroupsRequestSignupMode._('automatic');

  /// The values known when this package was generated.
  static const values = [manual, automatic];

  /// Decodes a wire value, preserving one this package does not know.
  factory GetConsentGroupsRequestSignupMode.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => GetConsentGroupsRequestSignupMode.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}

/// New signup mode
final class UpdateConsentGroupRequestSignupMode extends BrevoOpenEnum {
  const UpdateConsentGroupRequestSignupMode._(super.value);

  /// Wraps a value this package does not know.
  const UpdateConsentGroupRequestSignupMode.unknown(super.value);

  /// The `manual` value.
  static const manual = UpdateConsentGroupRequestSignupMode._('manual');

  /// The `automatic` value.
  static const automatic = UpdateConsentGroupRequestSignupMode._('automatic');

  /// The values known when this package was generated.
  static const values = [manual, automatic];

  /// Decodes a wire value, preserving one this package does not know.
  factory UpdateConsentGroupRequestSignupMode.fromWire(String value) =>
      values.firstWhere(
        (known) => known.value == value,
        orElse: () => UpdateConsentGroupRequestSignupMode.unknown(value),
      );

  @override
  bool get isKnown => values.contains(this);
}
