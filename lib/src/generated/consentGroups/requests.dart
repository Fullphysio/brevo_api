// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/decode_exception.dart';
import '../../core/json_reading.dart';
import 'enums.dart';

/// Request body for creating a consent group.
final class CreateConsentGroupRequest {
  /// Creates the request body.
  const CreateConsentGroupRequest({
    this.description,
    this.listIds,
    required this.name,
    required this.signupMode,
  });

  /// Decodes a JSON object into a CreateConsentGroupRequest. Optional fields absent from [json] stay null.
  factory CreateConsentGroupRequest.fromJson(Map<String, Object?> json) =>
      CreateConsentGroupRequest(
        description: json.optString('description'),
        listIds:
            json.containsKey('listIds') ? json.optIntList('listIds') : null,
        name: json.requireString('name', 'CreateConsentGroupRequest'),
        signupMode: json.optEnum(
                'signupMode', CreateConsentGroupRequestSignupMode.fromWire) ??
            (throw BrevoDecodeException.missingRequiredKey(
                objectName: 'CreateConsentGroupRequest', key: 'signupMode')),
      );

  /// Optional description (max 500 characters)
  final String? description;

  /// Optional list of contact list IDs. Contacts from these lists will be copied once into this consent group at creation time.
  final List<int>? listIds;

  /// Unique name for the consent group (max 255 characters)
  final String name;

  /// Controls how contacts are added to the group. `manual` — contacts are added explicitly via the API. `automatic` — contacts are added automatically at signup.
  final CreateConsentGroupRequestSignupMode signupMode;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (description != null) 'description': description!,
        if (listIds != null) 'listIds': listIds!,
        'name': name,
        'signupMode': signupMode.value,
      };
}

/// Request body for updating a consent group. At least one field must be provided.
final class UpdateConsentGroupRequest {
  /// Creates the request body.
  const UpdateConsentGroupRequest({
    this.description,
    this.name,
    this.signupMode,
  });

  /// Decodes a JSON object into a UpdateConsentGroupRequest. Optional fields absent from [json] stay null.
  factory UpdateConsentGroupRequest.fromJson(Map<String, Object?> json) =>
      UpdateConsentGroupRequest(
        description: json.optString('description'),
        name: json.optString('name'),
        signupMode: json.optEnum(
            'signupMode', UpdateConsentGroupRequestSignupMode.fromWire),
      );

  /// New description (max 500 characters)
  final String? description;

  /// New name for the consent group (max 255 characters)
  final String? name;

  /// New signup mode
  final UpdateConsentGroupRequestSignupMode? signupMode;

  /// Encodes the request body as JSON, omitting absent fields — a Dart `null` is never sent, exactly as `@getbrevo/brevo` drops `undefined`.
  Map<String, Object?> toJson() => {
        if (description != null) 'description': description!,
        if (name != null) 'name': name!,
        if (signupMode != null) 'signupMode': signupMode!.value,
      };
}
