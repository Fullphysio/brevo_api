// GENERATED by tool/generate.dart from tool/spec/brevo-openapi.json — do not edit by hand.

import '../../core/json_reading.dart';
import 'enums.dart';

/// A consent group representing a category of contact opt-in/opt-out preferences.
final class ConsentGroup {
  /// Creates a ConsentGroup from already-decoded fields.
  const ConsentGroup({
    this.contactsCount,
    this.createdAt,
    this.description,
    this.id,
    this.isDefault,
    this.name,
    this.signupMode,
    this.updatedAt,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConsentGroup. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConsentGroup.fromJson(Map<String, Object?> json) => ConsentGroup(
        contactsCount: json.optInt('contactsCount'),
        createdAt: json.optString('createdAt'),
        description: json.optString('description'),
        id: json.optInt('id'),
        isDefault: json.optBool('isDefault'),
        name: json.optString('name'),
        signupMode: json.optEnum('signupMode', ConsentGroupSignupMode.fromWire),
        updatedAt: json.optString('updatedAt'),
        raw: json,
      );

  /// Number of contacts currently in this consent group
  final int? contactsCount;

  /// UTC date-time when the consent group was created (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? createdAt;

  /// [createdAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get createdAtDate =>
      createdAt == null ? null : DateTime.tryParse(createdAt!);

  /// Optional description of the consent group
  final String? description;

  /// Unique identifier for the consent group
  final int? id;

  /// Whether this is a system-created default consent group
  final bool? isDefault;

  /// Display name of the consent group
  final String? name;

  /// Controls how contacts are added to the group. `manual` — contacts are added explicitly via the API. `automatic` — contacts are added automatically at signup.
  final ConsentGroupSignupMode? signupMode;

  /// UTC date-time when the consent group was last updated (YYYY-MM-DDTHH:mm:ss.SSSZ)
  final String? updatedAt;

  /// [updatedAt] parsed as an instant, or `null` when absent or unparsable.
  DateTime? get updatedAtDate =>
      updatedAt == null ? null : DateTime.tryParse(updatedAt!);

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (contactsCount != null) 'contactsCount': contactsCount!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (description != null) 'description': description!,
        if (id != null) 'id': id!,
        if (isDefault != null) 'isDefault': isDefault!,
        if (name != null) 'name': name!,
        if (signupMode != null) 'signupMode': signupMode!.value,
        if (updatedAt != null) 'updatedAt': updatedAt!,
      };
}

/// Paginated list of consent groups.
final class ConsentGroupsListResponse {
  /// Creates a ConsentGroupsListResponse from already-decoded fields.
  const ConsentGroupsListResponse({
    this.consentGroups = const [],
    this.limit,
    this.offset,
    this.total,
    this.raw = const {},
  });

  /// Decodes a JSON object into a ConsentGroupsListResponse. Every field is read tolerantly: one that is absent or has an unexpected type decodes to `null` (or an empty collection).
  factory ConsentGroupsListResponse.fromJson(Map<String, Object?> json) =>
      ConsentGroupsListResponse(
        consentGroups: json.optObjectList(
            'consentGroups', ConsentGroup.fromJson,
            objectName: 'ConsentGroupsListResponse'),
        limit: json.optInt('limit'),
        offset: json.optInt('offset'),
        total: json.optInt('total'),
        raw: json,
      );

  /// List of consent groups matching the filter
  final List<ConsentGroup> consentGroups;

  /// Maximum number of results returned
  final int? limit;

  /// Number of results skipped
  final int? offset;

  /// Total number of consent groups matching the filter
  final int? total;

  /// The whole decoded object, for fields not promoted to a property.
  final Map<String, Object?> raw;

  /// Encodes the object as JSON, omitting absent fields.
  Map<String, Object?> toJson() => {
        if (consentGroups.isNotEmpty)
          'consentGroups': [for (final item in consentGroups) item.toJson()],
        if (limit != null) 'limit': limit!,
        if (offset != null) 'offset': offset!,
        if (total != null) 'total': total!,
      };
}
