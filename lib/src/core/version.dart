/// The version of this package, as declared in `pubspec.yaml` and sent in
/// the `User-Agent` header of every request.
///
/// A test keeps this constant and the pubspec in step.
const String brevoApiVersion = '0.1.0';
